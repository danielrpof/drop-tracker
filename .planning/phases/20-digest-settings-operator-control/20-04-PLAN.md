---
phase: "20"
plan: "04"
type: execute
wave: 3
depends_on: ["20-03"]
files_modified:
  - web/app/components/ui/select.tsx
  - web/app/components/system/DigestSettings.tsx
  - web/app/components/system/DigestSettings.test.tsx
  - web/vitest.setup.ts
  - internal/webassets/build/client
autonomous: true
requirements: [DGST-02, DGST-16]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An operator picks Daily or Weekly from the cadence control on /system, the choice is written immediately with no submit step, and a reload reports the chosen cadence (DGST-02, ROADMAP success criterion 1)."
    - "The cadence control stays visible, focusable and populated with the stored value when digest mode is off — it is dimmed, never hidden and never reset to a placeholder, so the on/off-to-cadence relationship stays visible and the page never reflows when the switch flips (D-04). [UI-SPEC UI Considerations: partial | digest_enabled === false with a digest_cadence already set]"
    - "Both controls render the stored values on load: the switch reflects digest_enabled and the cadence control reflects digest_cadence, rendered in title case (Daily / Weekly) rather than the lowercase wire values. [UI-SPEC UI Considerations: populated]"
    - "A cadence change sends both fields, because the route is full-object PUT semantics — changing cadence never clears digest_enabled as a side effect (DGST-02)."
    - "With digest_last_sent_at null the last-sent row renders the literal 'Never sent yet' — never blank and never the em dash this view uses elsewhere for unknown-or-error, because a never-sent instance is an honest expected state rather than a missing value (DGST-16). [UI-SPEC UI Considerations: empty | Last digest sent]"
    - "With digest_last_sent_at populated the row renders a <time> element carrying the ISO value in dateTime, the full local timestamp in title, and the shared absolute-time formatting as text — the exact formatters the source panels already use for finished_at, reused rather than reimplemented (DGST-16)."
    - "The cadence option set is a closed two-value enum fixed by the requirement rather than by data, so no zero-option and no many-option state exists to design for. [UI-SPEC UI Considerations: zero-one-many — n/a, reason recorded]"
    - statement: "Every string in the digest card is a short, locked, fixed-vocabulary literal — no user-supplied or server-composed free text ever reaches it, since the timestamp is formatted rather than raw — so no future copy change can silently break the card's layout."
      verification: backstop
    - "The committed embedded bundle under internal/webassets/build/client is rebuilt from this phase's SPA source in the phase's final commit, so a Go binary built from a clone actually serves the digest card."
  prohibitions:
    - "The cadence control is never hidden, never unmounted, and never reset to a placeholder when digest mode is off — disabled only (D-04)."
    - "The lowercase wire values are never rendered to the operator; the visible option labels are title case."
    - "No em dash fallback for a null last-sent value — the locked explicit copy overrides format.ts's own convention for this one field."
    - "No edit to web/app/lib/format.ts to special-case this field — the null check belongs in the component, before the formatter is called."
    - "No new npm dependency and no third-party registry: the only registry action is a first-party shadcn component vendored as source."
    - "No new theme token; the run-health palette reserved by 19-UI-SPEC.md is not extended to this card."
    - "No Save button and no batched submit — the cadence control applies immediately, exactly like the switch (D-02)."
  artifacts:
    - web/app/components/ui/select.tsx
    - web/app/components/system/DigestSettings.tsx
    - web/app/components/system/DigestSettings.test.tsx
    - internal/webassets/build/client
  key_links:
    - "digest_enabled === false -> cadence control disabled but still rendering the stored value (D-04), never unmounted"
    - "cadence change -> updateDigestSettings({digestEnabled: current, digestCadence: next}) -> full-object PUT -> onSaved -> rendered value"
    - "digest_last_sent_at === null -> literal copy branch taken before formatAbsoluteTime is ever called -> em-dash path unreachable for this field"
    - "make web -> internal/webassets/build/client committed -> the Go binary embeds this phase's SPA"
---

<objective>
Expand the digest card out from plan 20-03's proven slice: the daily/weekly cadence control, the
last-sent row, and the embedded-bundle rebuild that makes the whole phase real in a built
binary.

Purpose: 20-03 proved the card, the fetch composition and the save machinery. Everything here
rides that spine — the cadence control reuses the same save path, and the last-sent row is pure
presentation over a column nothing writes yet.

Output: the complete Phase 20 operator surface — mode, cadence, last-sent — with the embedded
SPA bundle refreshed and the full Definition of Done green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-digest-settings-operator-control/20-CONTEXT.md
@.planning/phases/20-digest-settings-operator-control/20-PATTERNS.md
@.planning/phases/20-digest-settings-operator-control/20-UI-SPEC.md
@.planning/phases/20-digest-settings-operator-control/20-03-SUMMARY.md
</context>

## Artifacts this phase produces

See the inventory table in `20-01-PLAN.md`. This plan adds the vendored `Select` primitives, the
cadence and last-sent rows on `DigestSettings`, and the rebuilt embedded bundle. Nothing else in
phase 20 remains after it.

<tasks>

<task type="auto">
  <name>Task 1: Vendor the shadcn select primitive</name>
  <files>web/app/components/ui/select.tsx</files>
  <precondition>Network egress to `ui.shadcn.com` is available for the registry fetch; if it is not, take the documented offline fallback in the action rather than stalling.</precondition>
  <read_first>
    - `.planning/phases/20-digest-settings-operator-control/20-UI-SPEC.md` — the "To vendor this phase" row (the exact command, the registry file path, the warning that the raw registry JSON references an icon import path this repo does not have) and the Registry Safety table.
    - `web/components.json` — the shadcn config governing the add command: `base-maia` style, `zinc` base color, `rsc: false` (the client directive is stripped at add time), `registries: {}` (no third-party registry is in play).
    - `web/app/components/ui/switch.tsx` — the vendored-component house style: a thin wrapper over a `@base-ui/react/*` primitive, `data-slot` attributes, `cn(...)` class composition, a named export block at the end.
    - `web/app/components/ui/table.tsx` — the file Phase 19 vendored through the same command, and the precedent for hand-writing the component when the CLI mis-resolves a dependency (19-03 added a bogus `cn` package to the manifest and had to be reverted).
    - `web/package.json` — the manifest that must be byte-identical afterwards.
  </read_first>
  <action>
Vendor the shadcn `select` component into `web/app/components/ui/select.tsx` by running the
project's usual add command from `web/` (`npx shadcn@latest add select`), then verify the
result before trusting it.

Provenance is already established and recorded, so no legitimacy checkpoint is inserted: the
`20-UI-SPEC.md` Component Inventory records a direct registry fetch of the `base-maia` `select`
definition on 2026-09-11 showing a first-party shadcn registry item whose only dependency is
`cn` — already vendored here — backed by `@base-ui/react/select`, the same primitive family the
already-vendored `Switch` uses and a subpath of a package this project already depends on. The
Registry Safety table declares no third-party registry for this phase.

Two known hazards to check for explicitly, both drawn from what happened in 19-03:

1. The command may add a package to `web/package.json` and `web/pnpm-lock.yaml` by
   mis-resolving a component-internal import as an installable package. Both files must be
   byte-identical to their pre-task state when you are done. If the command dirties either
   one, revert both files and the lockfile, and hand-write the component instead.
2. The raw registry definition references a project-specific icon import path that does not
   exist in this repo. Trust the CLI's own vendoring transform; if it leaves an unresolved
   import behind, swap it for the equivalent `lucide-react` chevron, which is already a
   dependency.

The offline/hand-write fallback, if the registry is unreachable or the output is unusable: write
`web/app/components/ui/select.tsx` by hand in `switch.tsx`'s exact house style, wrapping the
`@base-ui/react/select` parts already present in `node_modules` (root, trigger, value, icon,
portal, positioner, popup, list, item, item-text, item-indicator) and exporting `Select`,
`SelectTrigger`, `SelectValue`, `SelectContent` and `SelectItem`. Export names are what the rest
of the phase imports — confirm them against the vendored file itself rather than against the
registry JSON, whose shape is not authoritative.

Whichever path produced the file, confirm it typechecks and that the project's coverage config
already excludes `app/components/ui/**`, so this file changes no coverage axis.
  </action>
  <acceptance_criteria>
    - `web/app/components/ui/select.tsx` exists and exports `Select`, `SelectTrigger`, `SelectValue`, `SelectContent` and `SelectItem`.
    - `grep -c "@base-ui/react/select" web/app/components/ui/select.tsx` prints at least `1` — it wraps the installed primitive rather than reimplementing one.
    - `git diff --name-only -- web/package.json web/pnpm-lock.yaml` prints nothing — the vendoring added no dependency.
    - `grep -rn "IconPlaceholder" web/app/components/ui/select.tsx | wc -l` prints `0` — no unresolved registry import survived.
    - `corepack pnpm --dir web exec prettier --check "**/*.{ts,tsx}"` passes.
    - `corepack pnpm --dir web test` exits 0 with all four coverage axes at or above 70 (the vendored file is excluded from the coverage denominator).
  </acceptance_criteria>
  <verify>
    <automated>git diff --name-only -- web/package.json web/pnpm-lock.yaml | wc -l</automated>
    <fails_when>prints anything other than 0 — the add command dirtied the manifest or lockfile, which means it resolved something as an installable package</fails_when>
    <automated>corepack pnpm --dir web exec prettier --write "**/*.{ts,tsx}" &amp;&amp; corepack pnpm --dir web exec prettier --check "**/*.{ts,tsx}"</automated>
    <fails_when>non-zero exit, or the output lists any file under "Code style issues found"</fails_when>
    <automated>corepack pnpm --dir web test</automated>
    <fails_when>non-zero exit, or the output contains a line beginning "ERROR: Coverage for", or the summary reports a non-zero "failed" count</fails_when>
  </verify>
  <reversibility rating="reversible">A vendored source file with no manifest change; deleting it is a clean revert.</reversibility>
  <done>`select.tsx` is vendored as ordinary reviewable source, the dependency graph is unchanged, and the suite is green. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cadence control and last-sent row</name>
  <files>web/app/components/system/DigestSettings.tsx, web/app/components/system/DigestSettings.test.tsx, web/vitest.setup.ts</files>
  <read_first>
    - `web/app/components/system/DigestSettings.tsx` — the file being extended: the definition-list layout, the rendered-value source (overlay or prop), the shared save helper, the `savingRef` guard and the shared status region, all as plan 20-03 committed them.
    - `web/app/components/ui/select.tsx` — the file task 1 vendored; take the actual export names and prop shape from it, not from the registry JSON.
    - `web/app/components/system/SourcePanel.tsx` — the existing `<time dateTime … title={formatIsoTitle(iso)}>{formatAbsoluteTime(iso)}</time>` usage for a run's finish time; this row copies it exactly.
    - `web/app/lib/format.ts` lines 88-122 — `formatAbsoluteTime` and `formatIsoTitle`, including the em-dash degradation this one field deliberately bypasses (the reason the null check must come first).
    - `.planning/phases/20-digest-settings-operator-control/20-UI-SPEC.md` — the Rows table (locked labels and value states, including the explicit em-dash override), the Visual Hierarchy row order, and the UI Considerations rows for `partial`, `empty` and `populated`.
    - `web/app/components/system/DigestSettings.test.tsx` — the file being extended; reuse `makeSettings()` and the existing partial mock.
    - `web/vitest.setup.ts` and `web/vitest.config.ts` — the single shared setup hook (where a jsdom polyfill would go, if one proves necessary) and the `TZ=UTC` pin that makes a hardcoded timestamp expectation stable.
  </read_first>
  <behavior>
    - With `{digest_enabled:true, digest_cadence:"daily"}` the card renders a `Cadence` row whose control displays `Daily`, and choosing `Weekly` calls `updateDigestSettings` exactly once with `{digestEnabled:true, digestCadence:"weekly"}` — both fields, not just the changed one.
    - With `{digest_enabled:false, digest_cadence:"weekly"}` the cadence control is present in the document, reports itself disabled, and still displays `Weekly` — it is neither hidden nor reset.
    - Flipping digest mode on does not change the displayed cadence, and flipping it off does not clear it.
    - With `digest_last_sent_at: null` the card renders the exact text `Never sent yet` and renders no `<time>` element for that row.
    - With `digest_last_sent_at` set to a fixed ISO instant, the row renders a `<time>` whose `dateTime` is that exact string and whose visible text is what the shared absolute-time formatter returns for it.
    - A failed cadence save leaves the previously displayed cadence on screen and renders the same locked failure copy the switch's failure path uses — one status region serves both controls.
  </behavior>
  <action>
Extend `web/app/components/system/DigestSettings.tsx` with the two remaining rows from the
UI-SPEC's Visual Hierarchy, inside the existing definition list, in this order after the
`Digest mode` row.

**Cadence row.** A `<dt className="text-label text-muted-foreground">` carrying the locked label
`Cadence`, and a `<dd>` holding the vendored select with exactly two items whose values are the
lowercase wire literals and whose visible labels are the title-case strings `Daily` and
`Weekly`. Bind its value to the same rendered-value source the switch uses (the optimistic
overlay when present, the prop otherwise) so both controls always agree about what is currently
stored. Set its disabled state from the rendered `digest_enabled` value — D-04 is explicit that
this control stays visible, focusable-in-the-DOM and populated when digest mode is off, dimmed
by the component's own disabled treatment; it is never conditionally rendered away and never
falls back to a placeholder. On change, route through the exact same save helper the switch
uses, passing the current enabled value alongside the new cadence: the route is full-object PUT
semantics, so sending only the changed field would clear the other one. Disable it during an
in-flight save, exactly as the switch is disabled.

**Last digest sent row.** A `<dt>` carrying the locked label `Last digest sent` and a `<dd>`
that branches on the raw value being null: when it is, render the locked literal `Never sent
yet` in `text-foreground`; otherwise render a `<time>` element whose `dateTime` is the raw ISO
string, whose `title` is `formatIsoTitle(iso)` and whose text is `formatAbsoluteTime(iso)` —
the same pair `SourcePanel` already uses for a run's finish time. The null branch must be taken
*before* the formatter is called: `format.ts` degrades a null to an em dash by design, and this
one field deliberately overrides that convention, because an em dash reads as
unknown-or-error and a never-sent instance is neither. Do not edit `format.ts` to accommodate
this; add a one-line comment in the component recording that the deviation is intentional so a
future reader does not "fix" it back.

This row is read-only in this phase and will stay null on every real instance until Phase 22
writes the watermark — that is exactly why the empty state has to be built now.

**Tests.** Extend `web/app/components/system/DigestSettings.test.tsx` with the six behaviors
above, reusing the existing `makeSettings()` fixture builder and the existing partial mock of
`~/lib/api`. Drive the cadence control the way a user does — open it and choose the option by
its visible title-case name — rather than by calling a handler directly. If jsdom cannot drive
the popup because the primitive needs a browser API jsdom lacks, add the minimal polyfill to
`web/vitest.setup.ts` (and say in a one-line comment which primitive needs it) rather than
weakening the assertion into a direct handler call or skipping the case; that file is the
project's single shared setup hook and this is exactly what it is for.

Pin the timestamp case against a fixed ISO instant and assert on the `dateTime` attribute plus
the formatter's own return value for that instant — the runner's timezone is pinned to UTC in
`vitest.config.ts`, so a hardcoded expected string is stable, but deriving it from the shared
formatter keeps the case honest if the format ever changes deliberately.
  </action>
  <acceptance_criteria>
    - `web/app/components/system/DigestSettings.tsx` contains the exact strings `Cadence`, `Daily`, `Weekly`, `Last digest sent` and `Never sent yet`.
    - `grep -c "formatAbsoluteTime" web/app/components/system/DigestSettings.tsx` prints at least `1` and `grep -c "formatIsoTitle" web/app/components/system/DigestSettings.tsx` prints at least `1` — the shared formatters are reused, not reimplemented.
    - `git diff --name-only -- web/app/lib/format.ts` prints nothing — the null-handling override lives in the component.
    - `grep -cE "status-ok|status-warn|sonner" web/app/components/system/DigestSettings.tsx` prints `0`.
    - `grep -c "<Button" web/app/components/system/DigestSettings.tsx` prints `0`.
    - The test file contains a case asserting the cadence control is present and disabled while `digest_enabled` is false and still displays the stored `Weekly` value.
    - The test file contains a case asserting a cadence change calls `updateDigestSettings` once with both fields populated.
    - The test file contains a case asserting the exact text `Never sent yet` for a null watermark and a `<time>` with the right `dateTime` for a populated one.
    - `corepack pnpm --dir web exec vitest run --coverage.enabled=false app/components/system/DigestSettings.test.tsx` exits 0 with every case green.
    - `corepack pnpm --dir web test` exits 0 with all four coverage axes at or above 70.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm --dir web exec prettier --write "**/*.{ts,tsx}" &amp;&amp; corepack pnpm --dir web exec prettier --check "**/*.{ts,tsx}"</automated>
    <fails_when>non-zero exit, or the output lists any file under "Code style issues found"</fails_when>
    <automated>corepack pnpm --dir web exec vitest run --coverage.enabled=false app/components/system/DigestSettings.test.tsx app/routes/system.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary reports a non-zero "failed" count, or either named file reports "No test files found"</fails_when>
    <automated>corepack pnpm --dir web test</automated>
    <fails_when>non-zero exit, or the output contains a line beginning "ERROR: Coverage for", or the summary reports a non-zero "failed" count</fails_when>
    <automated>git diff --name-only -- web/app/lib/format.ts web/package.json web/pnpm-lock.yaml | wc -l</automated>
    <fails_when>prints anything other than 0 — the shared formatter module or the dependency manifest was modified, both of which this plan forbids</fails_when>
  </verify>
  <done>An operator can pick Daily or Weekly, see the control dimmed-but-populated while digest mode is off, and read an honest "Never sent yet" until Phase 22 ships. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild the embedded SPA bundle and close the phase gate</name>
  <files>internal/webassets/build/client</files>
  <precondition>`corepack pnpm install --frozen-lockfile` can resolve from the local store or the registry — `make web` runs it before building.</precondition>
  <read_first>
    - `Makefile` — the `web` target (frozen-lockfile install, `pnpm run build`, then replacing `internal/webassets/build/client`), plus `test`, `coverage-gate`, `sqlc-check` and `db-up`.
    - `internal/webassets/` — the `//go:embed all:build/client` directive that makes this committed tree the thing a built binary actually serves.
    - `.claude/CLAUDE.md` — the Definition of Done list this task runs end to end.
  </read_first>
  <action>
Run `make web` and commit the refreshed `internal/webassets/build/client` tree. This is the step
that makes the phase real in a built binary: `go build` embeds the committed tree, not the
`web/` source, so without it a Go-only clone serves the pre-phase SPA while every frontend test
passes. It is deliberately the phase's final commit, after every `web/app` change has landed.

Then run the full Definition of Done from `.claude/CLAUDE.md` in order — `go vet ./...`,
`golangci-lint run`, `make test` (with `make db-up` first), `make coverage-gate`,
`make sqlc-check`, and the frontend pair — and fix anything red before committing. Do not use
the commit hook bypass for any reason.

Finally, confirm the phase's blast radius: apart from the embedded bundle, nothing outside
`web/`, `internal/settings/`, `internal/httpserver/`, `internal/db/`, `queries/`,
`cmd/server/main.go` and `.planning/` should have changed across the whole phase. In
particular `internal/notifier`, `internal/poller` and `internal/detection` must carry no diff at
all — a fresh install with digest off has to behave exactly as v1.4 shipped.
  </action>
  <acceptance_criteria>
    - `make web` completes and `git status --porcelain internal/webassets/build/client` is non-empty before staging — the bundle genuinely changed.
    - `go vet ./...` and `golangci-lint run` are clean.
    - `make test` passes and `make coverage-gate` reports a backend total at or above 80.
    - `make sqlc-check` exits 0.
    - `corepack pnpm --dir web exec prettier --check "**/*.{ts,tsx}"` passes and `corepack pnpm --dir web test` exits 0 with all four coverage axes at or above 70.
    - `git diff --name-only -- internal/notifier internal/poller internal/detection` prints nothing.
    - The commit is made with the hooks enabled (no bypass flag anywhere in the command).
  </acceptance_criteria>
  <verify>
    <automated>make web &amp;&amp; git status --porcelain internal/webassets/build/client | wc -l</automated>
    <fails_when>non-zero exit from the build, or the count prints 0 — an unchanged bundle means the rebuild did not pick up this phase's SPA changes</fails_when>
    <automated>go vet ./... &amp;&amp; golangci-lint run &amp;&amp; make db-up &amp;&amp; make test &amp;&amp; make coverage-gate &amp;&amp; make sqlc-check</automated>
    <fails_when>non-zero exit at any stage, or the coverage gate prints "FAIL: ...% is below the required 80% threshold", or sqlc-check prints a diff under internal/db/sqlc/</fails_when>
    <automated>corepack pnpm --dir web exec prettier --check "**/*.{ts,tsx}" &amp;&amp; corepack pnpm --dir web test</automated>
    <fails_when>non-zero exit, or prettier lists files under "Code style issues found", or the output contains a line beginning "ERROR: Coverage for"</fails_when>
    <automated>git diff --name-only -- internal/notifier internal/poller internal/detection | wc -l</automated>
    <fails_when>prints anything other than 0</fails_when>
  </verify>
  <done>The embedded bundle serves this phase's SPA, every Definition of Done gate is green, and real-time notification behavior is provably untouched. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external registry → repo source tree | the shadcn CLI fetches a component definition over the network and writes it into `web/app/components/ui/` |
| settings JSON → React DOM | server-authored values (cadence, timestamp) rendered into the page |
| SPA source → embedded binary | `internal/webassets/build/client` is committed and embedded at Go build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-17 | Tampering | shadcn registry add of `select` | medium | mitigate | Provenance is pre-recorded in `20-UI-SPEC.md` (Component Inventory + Registry Safety): first-party shadcn registry, `base-maia` style, sole dependency `cn` already vendored, backed by `@base-ui/react/select` — a subpath of an existing dependency. No `[ASSUMED]`/`[SUS]`/`[SLOP]` item exists for this phase and no package-manager install occurs, so no human legitimacy checkpoint is required. The acceptance gate proves the action added nothing to `web/package.json` or `web/pnpm-lock.yaml`, and the vendored file is reviewed as ordinary source in the commit diff. |
| T-20-18 | Tampering (XSS) | cadence and timestamp rendering | low | mitigate | The cadence is rendered from a closed two-value option list, never by echoing the server string, and the timestamp goes through the shared formatters and a `<time>` element as a JSX text node. No raw-HTML injection prop exists anywhere under `web/app` (repo-wide zero matches since Phase 06) and no settings value builds a URL. |
| T-20-19 | Tampering | full-object PUT from the cadence control | medium | mitigate | The cadence handler sends the current `digest_enabled` alongside the new cadence, so a cadence change cannot silently clear digest mode; the test asserts both fields on the call, and the server rejects an incomplete body with a 400 (plan 20-02). |
| T-20-20 | Tampering | embedded bundle drift | medium | mitigate | `make web` rebuilds the committed tree from this phase's source in the final commit, gated on the tree actually changing; without it a built binary would serve a stale SPA while every test passed. |
| T-20-SC | Tampering | npm/pnpm installs | medium | mitigate | Task 1's gate proves the manifest and lockfile are byte-identical after the registry action; a dirtied manifest is a hard failure with a documented revert-and-hand-write fallback, exactly the failure mode 19-03 hit and recovered from. |

**No threat in this plan reaches the configured `high` block-on severity.** It adds no auth,
session, crypto, persistence, secret, env var or outbound-URL surface; the highest-rated items
are supply-chain and build-artifact integrity, both gated by an automated diff assertion.
</threat_model>

<verification>
- `corepack pnpm --dir web exec prettier --check "**/*.{ts,tsx}"` clean and `corepack pnpm --dir web test` green at or above 70 on all four axes.
- `go vet ./... && golangci-lint run && make db-up && make test && make coverage-gate && make sqlc-check` all green.
- `git diff --name-only -- web/package.json web/pnpm-lock.yaml web/app/lib/format.ts` prints nothing.
- `git status --porcelain internal/webassets/build/client` was non-empty after `make web`.
- `git diff --name-only -- internal/notifier internal/poller internal/detection` prints nothing.
</verification>

<success_criteria>
- An operator can choose Daily or Weekly and the choice survives a reload.
- The cadence control is dimmed but visible and populated while digest mode is off.
- A never-sent instance reads "Never sent yet"; a populated watermark renders through the shared formatters.
- The embedded bundle serves this phase's SPA.
- Every Definition of Done gate is green and real-time notification behavior is untouched.
</success_criteria>

<output>
Create `.planning/phases/20-digest-settings-operator-control/20-04-SUMMARY.md` when done
</output>
