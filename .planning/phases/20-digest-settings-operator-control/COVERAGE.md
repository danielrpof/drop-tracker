No external API integration: this phase adds a Postgres-backed singleton settings row, two routes on the app's own gated chi router, and an SPA card that reads them through the existing internal `apiFetch` client. No third-party API, SDK, or service is contacted, added, or wrapped — the detector fired only on the roadmap phrase "type the new wire shape in `web/app/lib/api.ts`", which names this project's own internal API client module, not an external one. The phase's only outbound-network action is a first-party shadcn component vendoring (plan 20-04, T-20-17), which fetches source code rather than integrating a service.
