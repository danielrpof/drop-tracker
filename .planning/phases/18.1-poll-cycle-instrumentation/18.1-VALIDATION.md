---
phase: "18.1"
slug: "poll-cycle-instrumentation"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-10"
---

# Phase 18.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `18.1-RESEARCH.md` → "Validation Architecture". Per-task map is
> filled once `18.1-*-PLAN.md` exist (task IDs unknown at seed time).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `go test` (stdlib `testing`); whitebox `package poller` for `internal/poller/poller_test.go` |
| **Config file** | none — `go test` |
| **Quick run command** | `go test ./internal/poller/... ./internal/detection/... -count=1` |
| **Full suite command** | `make db-up && TEST_DATABASE_URL=postgres://drop_tracker:drop_tracker@localhost:5432/drop_tracker?sslmode=disable go test ./... -count=1 -coverprofile=coverage.out -coverpkg=$(shell go list ./... \| grep -vE '(^\|/)(internal/db/sqlc\|cmd/coverage-report\|cmd/migrate\|cmd/migration-check)$$' \| paste -sd, -)` then `make coverage-gate` |
| **Estimated runtime** | ~60–120 seconds (full suite + coverage gate) |

**`-race` note:** `go test -race` fails on this dev box (ThreadSanitizer allocation failure under WSL2) and is absent from CI. Established substitution (Phases 11.1, 15, 16, 18): run the suite without `-race` + `make coverage-gate`, log a Broken Windows Ledger `deviation`. For this phase the **looped ~1000× invariant test** (`TestRunCycle_CounterInvariant`) is the contracted compensating control — correctness is by construction (channel fold, no shared mutable counter).

---

## Sampling Rate

- **After every task commit:** Run `go test ./internal/poller/... ./internal/detection/... -count=1`
- **After every plan wave:** Run the full backend suite + `make coverage-gate`
- **Before `/gsd-verify-work`:** Full `go test ./...` green (race substituted), `golangci-lint run` 0 issues, `go vet ./...` clean, `make coverage-gate` ≥ 80%, `docs/api/status-contract.md` drift check green, `TestRunCycle_CounterInvariant -count=3` green
- **Max feedback latency:** ~15 seconds (`internal/poller` + `internal/detection` package tests)

---

## Per-Task Verification Map

*Pending — populated when `18.1-*-PLAN.md` exist. Requirement → behavior → test mapping from RESEARCH.md "Phase Requirements → Test Map":*

| Req / SC | Behavior | Test Type | Automated Command | File Exists | Status |
|----------|----------|-----------|-------------------|-------------|--------|
| SC1 / RUN-01 | One `RunResult` per source per cycle, all fields populated, leak-free summary | unit (whitebox, real `pollruns.NewStore()`, `Snapshot()` assert) | `go test ./internal/poller/ -run TestRunRecorder -count=1` | ❌ W0 | ⬜ pending |
| SC1 / RUN-02 | Shutdown-interrupted cycle records with `outcome == "cancelled"` (not missing) | unit | `go test ./internal/poller/ -run TestRunRecorder_RecordsCancelledOutcome -count=1` | ❌ W0 | ⬜ pending |
| SC2 / RUN-02 | Overlap-skipped tick: no run entry; `RecordSkip` bumps `consecutive_skips` + `last_skipped_at`; next real run resets | unit | `go test ./internal/poller/ -run TestRunRecorder_Skip -count=1` | ❌ W0 | ⬜ pending |
| SC3 / RUN-01 | Exact counts under fan-out: looped ~1000× invariant, K erroring + P panicking artists, `checked == errored + succeeded`, `errored == K + P` every iteration | unit (the `-race` substitute) | `go test ./internal/poller/ -run TestRunCycle_CounterInvariant -count=3` | ❌ W0 | ⬜ pending |
| SC4 / RUN-03 | Erroring recorder → cycle still logs "poll cycle complete", returns `nil`, overlap guard released, shutdown not extended | unit | `go test ./internal/poller/ -run TestRunRecorder_ErrorSwallowed -count=1` | ❌ W0 | ⬜ pending |
| SC5 / RUN-01 | `events_recorded` real: widened `Detect*` `(int, error)`, threads through both closures, folded, never hard-coded 0 | unit (poller + detection) + integration | `go test ./internal/poller/ ./internal/detection/ -count=1` | ❌ W0 | ⬜ pending |
| regression | `pollruns.Store` two-source invariant still green (RUN-04) | unit | `go test ./internal/pollruns/ -run TestStore_TwoSourceConcurrent -count=3` | ✅ exists | ⬜ pending |
| regression | Existing poller fan-out / cancellation / guard / lifecycle tests still green | unit + integration | full suite | ✅ exists | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Invert / rewrite `internal/poller/poller_test.go` `TestRunRecorderInertThisPhase` → asserts `runCalls == 1` per cycle
- [ ] `TestRunCycle_CounterInvariant` — looped ~1000× exact-equality with K erroring + P panicking fake artists (new) — **the required `-race` substitute**
- [ ] `TestRunRecorder_RecordsCancelledOutcome` — ctx-cancelled cycle → one entry, `outcome == "cancelled"` (new)
- [ ] `TestRunRecorder_SkipRecordsNoEntry` — overlap tick → `RecordSkip` only, no `RecordRun` (new)
- [ ] `TestRunRecorder_ErrorSwallowed` — erroring `fakeRunRecorder` → cycle returns `nil`, guard released, log present (new)
- [ ] `fakeEventRecorder` (`poller_test.go`): widen `fn` / `deezerFn` to `(int, error)`, methods return `(0, nil)` default
- [ ] `sed` the ~83 `internal/detection/*_test.go` call sites `if err := d.Detect` → `if _, err := d.Detect`
- [ ] At least one `detector_test.go` / `deezer_test.go` case asserting the returned count equals inserted rows
- [ ] Extend `TestPoller_RunMusicBrainzCycle_RecordsNewRelease` / `...DeezerCycle...` to assert `RunResult.EventsRecorded`
- [ ] Framework install: none

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| A live cycle records a real run entry visible via `GET /status` | RUN-01 | End-to-end wiring across poller + store + handler on a real instance | Boot a gated instance with `make db-up`, add a watchlist artist, wait one poll interval (or trigger a cycle), `curl` the gated `/status`, confirm `runs`/`history` shows one entry per source with populated counts and no leaked strings |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
