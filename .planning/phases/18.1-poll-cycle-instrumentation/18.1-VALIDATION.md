---
phase: "18.1"
slug: "poll-cycle-instrumentation"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: true
wave_0_complete: true
created: "2026-09-10"
---

# Phase 18.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `18.1-RESEARCH.md` → "Validation Architecture". Per-task map is
> filled once `18.1-*-PLAN.md` exist (task IDs unknown at seed time).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `go test` (stdlib `testing`); whitebox `package poller` for `internal/poller/poller_test.go` |
| **Config file** | none — `go test` |
| **Quick run command** | `go test ./internal/poller/... ./internal/detection/... -count=1` |
| **Full suite command** | `make db-up && TEST_DATABASE_URL=postgres://drop_tracker:drop_tracker@localhost:5432/drop_tracker?sslmode=disable go test ./... -count=1 -coverprofile=coverage.out -coverpkg=$(shell go list ./... \| grep -vE '(^\|/)(internal/db/sqlc\|cmd/coverage-report\|cmd/migrate\|cmd/migration-check)$$' \| paste -sd, -)` then `make coverage-gate` |
| **Estimated runtime** | ~60–120 seconds (full suite + coverage gate) |

**`-race` note:** `go test -race` fails on this dev box (ThreadSanitizer allocation failure under WSL2) and is absent from CI. Established substitution (Phases 11.1, 15, 16, 18): run the suite without `-race` + `make coverage-gate`, log a Broken Windows Ledger `deviation`. For this phase the **looped ~1000× invariant test** (`TestRunCycle_CounterInvariant`) is the contracted compensating control — correctness is by construction (channel fold, no shared mutable counter).

---

## Sampling Rate

- **After every task commit:** Run `go test ./internal/poller/... ./internal/detection/... -count=1`
- **After every plan wave:** Run the full backend suite + `make coverage-gate`
- **Before `/gsd-verify-work`:** Full `go test ./...` green (race substituted), `golangci-lint run` 0 issues, `go vet ./...` clean, `make coverage-gate` ≥ 80%, `docs/api/status-contract.md` drift check green, `TestRunCycle_CounterInvariant -count=3` green
- **Max feedback latency:** ~15 seconds (`internal/poller` + `internal/detection` package tests)

---

## Per-Task Verification Map

*Populated 2026-09-10 after Wave 3 (`18.1-03`). Every row backed by a committed automated test, run green on this box with `-race` substituted per the standing WSL2 limitation (WINDOWS.md entry 13).*

| Req / SC | Behavior | Test | Automated Command | File Exists | Status |
|----------|----------|------|-------------------|-------------|--------|
| SC1 / RUN-01 | One `RunResult` per source per cycle, all fields populated, leak-free summary | `TestRunRecorder_RecordsOneRunPerCycle` (+ real `pollruns.NewStore()` leg), `TestRunRecorder_RecordsErrorOutcomeOnListFailure`, `TestPoller_RunMusicBrainzCycle_RecordsNewRelease` / `...RunDeezerCycle...` | `go test ./internal/poller/ -run TestRunRecorder -count=1` | ✅ `internal/poller/poller_test.go` | ✅ green |
| SC1 / RUN-02 | Shutdown-interrupted cycle records with `outcome == "cancelled"` (not missing), mid-cycle and pre-dispatch | `TestRunRecorder_RecordsCancelledOutcome` (2 subtests) | `go test ./internal/poller/ -run TestRunRecorder_RecordsCancelledOutcome -count=1` | ✅ `internal/poller/poller_test.go` | ✅ green |
| SC2 / RUN-02 | Overlap-skipped tick: no run entry; `RecordSkip` bumps the skip signal; next real run resets `ConsecutiveSkips` | `TestRunRecorder_SkipRecordsNoEntry` (fake + real-store subtests) | `go test ./internal/poller/ -run TestRunRecorder_Skip -count=1` | ✅ `internal/poller/poller_test.go` | ✅ green |
| SC3 / RUN-01 | Exact counts under fan-out: looped 1000× invariant, K=3 erroring + P=2 panicking artists, `checked == M`, `errored == K+P`, `checked-errored == M-K-P`, `events == (M-K-P)*E` every iteration; Deezer leg `skipped == S`, `checked == M-S` | unit (the `-race` substitute) | `go test ./internal/poller/ -run TestRunCycle_CounterInvariant -count=3 -timeout 120s` | ✅ `internal/poller/poller_test.go` | ✅ green |
| SC4 / RUN-03 | Erroring recorder → cycle still logs "poll cycle complete", returns `nil`, overlap guard released, `RecordRun` called once, slow recorder not retried/unbounded | `TestRunRecorder_ErrorSwallowed` (2 subtests) | `go test ./internal/poller/ -run TestRunRecorder_ErrorSwallowed -count=1` | ✅ `internal/poller/poller_test.go` | ✅ green |
| SC5 / RUN-01 | `events_recorded` real: widened `Detect*` `(int, error)`, threads through both closures, folded, never hard-coded 0 | 4 `internal/detection` count-binding cases + `TestRunRecorder_RecordsOneRunPerCycle` real-store leg (`EventsRecorded == 9`) + 2 poller integration tests asserting `LastRun.EventsRecorded` | `go test ./internal/poller/ ./internal/detection/ -count=1` | ✅ `internal/poller/poller_test.go`, `internal/detection/*_test.go` | ✅ green |
| regression | `pollruns.Store` two-source invariant still green (RUN-04) | `TestStore_TwoSourceConcurrent` | `go test ./internal/pollruns/ -run TestStore_TwoSourceConcurrent -count=3` | ✅ `internal/pollruns/pollruns_test.go` | ✅ green |
| regression | Existing poller fan-out / cancellation / guard / lifecycle tests still green | full poller + backend suite | `go test ./... -count=1` (`-race` substituted) | ✅ exists | ✅ green |

*Status legend: ⬜ = not-yet-run · ✅ = green · ❌ = red · ⚠️ = flaky*

**Full-suite gate run (2026-09-10, Wave 3 close-out):** `go vet ./...` clean · `golangci-lint run` 0 issues · `go test ./... -count=1` all packages `ok` (no `--- FAIL`), `-race` substituted per WINDOWS.md entry 13 · `make coverage-gate` 90.73% (floor 80%) · `make sqlc-check` clean · `docs/api/status-contract.md` and `web/` byte-identical since the phase base (`d77b3a7`).

---

## Wave 0 Requirements

- [x] Invert / rewrite `internal/poller/poller_test.go` `TestRunRecorderInertThisPhase` → asserts `runCalls == 1` per cycle *(18.1-01: `TestRunRecorder_RecordsOneRunPerCycle`)*
- [x] `TestRunCycle_CounterInvariant` — looped 1000× exact-equality with K=3 erroring + P=2 panicking fake artists (new) — **the required `-race` substitute** *(18.1-03, commit `c9cd9bf`)*
- [x] `TestRunRecorder_RecordsCancelledOutcome` — ctx-cancelled cycle → one entry, `outcome == "cancelled"` (new) *(18.1-02)*
- [x] `TestRunRecorder_SkipRecordsNoEntry` — overlap tick → `RecordSkip` only, no `RecordRun` (new) *(18.1-02)*
- [x] `TestRunRecorder_ErrorSwallowed` — erroring `fakeRunRecorder` → cycle returns `nil`, guard released, log present (new) *(18.1-02)*
- [x] `fakeEventRecorder` (`poller_test.go`): widen `fn` / `deezerFn` to `(int, error)`, methods return `(0, nil)` default *(18.1-01)*
- [x] `sed` the ~83 `internal/detection/*_test.go` call sites `if err := d.Detect` → `if _, err := d.Detect` *(18.1-01, refactor `72bcff3`)*
- [x] At least one `detector_test.go` / `deezer_test.go` case asserting the returned count equals inserted rows *(18.1-01: 4 cases)*
- [x] Extend `TestPoller_RunMusicBrainzCycle_RecordsNewRelease` / `...DeezerCycle...` to assert `RunResult.EventsRecorded` *(18.1-01)*
- [x] Framework install: none

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| A live cycle records a real run entry visible via `GET /status` | RUN-01 | End-to-end wiring across poller + store + handler on a real instance | Boot a gated instance with `make db-up`, add a watchlist artist, wait one poll interval (or trigger a cycle), `curl` the gated `/status`, confirm `runs`/`history` shows one entry per source with populated counts and no leaked strings |

> **Outstanding human check** (the only item not closed by automation): the live-instance `/status` walkthrough above. All in-process wiring (poller → `pollruns.Store` → `StatusStore` → `/status` handler) is proven by unit + integration tests; what a real running instance surfaces through the gated endpoint over a real poll interval is not exercisable from this box. Carry to `/gsd-verify-work 18.1`.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 15s (`internal/poller` + `internal/detection` package tests ~5s)
- [x] Nyquist compliance flag flipped on in frontmatter

**Approval:** automated verification map complete (Wave 3, 2026-09-10); one live-instance `/status` human check outstanding for `/gsd-verify-work`.
