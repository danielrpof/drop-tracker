---
status: testing
phase: 18.1-poll-cycle-instrumentation
source: [18.1-VERIFICATION.md]
started: 2026-09-10T10:36:20Z
updated: 2026-09-10T10:36:20Z
---

## Current Test

number: 1
name: Live GET /status walkthrough on a gated instance
expected: |
  Boot a gated instance (`make db-up` + `docker compose up --build` with
  `INSTANCE_PASSPHRASE` set), add a watchlist artist with a deezer_id, wait one
  real poll interval (or trigger a cycle), then curl the gated `GET /status`.

  Each source shows exactly one history entry per completed cycle carrying
  source, started/finished timestamps, artists_checked / artists_skipped /
  artists_errored, events_recorded, an outcome, and a composed summary with no
  DSN, webhook URL, filesystem path, or raw driver error string. An
  overlap-skipped tick shows a bumped `consecutive_skips` and `last_skipped_at`
  but no new history entry; the next real run resets `consecutive_skips` to 0.
awaiting: user response

## Tests

### 1. Live GET /status walkthrough on a gated instance
expected: |
  Each source shows exactly one history entry per completed cycle carrying
  source, started/finished timestamps, artists_checked / artists_skipped /
  artists_errored, events_recorded, an outcome, and a composed summary with no
  DSN / webhook URL / path / raw driver error. An overlap-skipped tick shows a
  bumped `consecutive_skips` and `last_skipped_at` but no new history entry; the
  next real run resets `consecutive_skips` to 0.
why_human: |
  End-to-end wiring across a live poller + pollruns.Store + StatusStore +
  /status handler over a real cron interval is not exercisable from this dev
  box. Every in-process link is proven by unit + integration tests; only the
  running-instance walkthrough remains.
result: [pending]

## Summary

total: 1
passed: 0
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps
