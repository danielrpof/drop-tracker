---
status: complete
phase: 18.1-poll-cycle-instrumentation
source: [18.1-VERIFICATION.md]
started: 2026-09-10T10:36:20Z
updated: 2026-09-10T16:40:53Z
---

## Current Test

[testing complete]

## Tests

### 1. Live GET /status walkthrough on a gated instance
expected: |
  Each source shows exactly one history entry per completed cycle carrying
  source, started/finished timestamps, artists_checked / artists_skipped /
  artists_errored, events_recorded, an outcome, and a composed summary with no
  DSN / webhook URL / path / raw driver error. An overlap-skipped tick shows a
  bumped `consecutive_skips` and `last_skipped_at` but no new history entry; the
  next real run resets `consecutive_skips` to 0.
why_human: |
  End-to-end wiring across a live poller + pollruns.Store + StatusStore +
  /status handler over a real cron interval is not exercisable from this dev
  box. Every in-process link is proven by unit + integration tests; only the
  running-instance walkthrough remains.
result: pass
notes: |
  Gated instance booted with INSTANCE_PASSPHRASE + POLL_INTERVAL=30s, 13-artist
  watchlist. GET /status verified:
  - deezer: 11 history entries, one per 30s cycle; all fields populated;
    artists_skipped=1 (the no-deezer_id artist); consecutive_skips=0.
  - musicbrainz: 3 history entries (cycles 1/4/9), one per completed run;
    events_recorded=2/2/0 (real, not hard-coded); runs take 40-78s so they
    overlap the 30s interval -> consecutive_skips=2, last_skipped_at populated,
    NO history entry for the skipped ticks. consecutive_skips read 2 (not 6+),
    proving RecordRun reset it to 0 when cycle-9 completed.
  - Every summary is the composed "ok - N checked, N errored, N events" form;
    no DSN / webhook URL / path / driver error string anywhere in the payload.

## Summary

total: 1
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none]

## Deferred Follow-Ups

- test: 1
  idea: "MusicBrainz artists_errored ran high during the walkthrough (6/12, 4/12, 10/12). The instrumentation records it correctly with outcome=ok (per-artist errors do not fail the cycle, by design), but the underlying error rate — most likely MusicBrainz 503 rate-limiting — should be investigated before v1.4 ships the observability UI on top of this data."
  deferred_at: 2026-09-10
