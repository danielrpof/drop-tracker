---
phase: 18.1-poll-cycle-instrumentation
verified: 2026-09-10T00:00:00Z
status: human_needed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Boot a gated instance (make db-up), add a watchlist artist with a deezer_id, wait one real poll interval (or trigger a cycle), then curl the gated GET /status."
    expected: "Each source shows exactly one history entry per completed cycle carrying source, started/finished timestamps, artists_checked/skipped/errored, events_recorded, an outcome, and a composed summary with no DSN / webhook URL / path / raw driver error. An overlap-skipped tick shows a bumped consecutive_skips and last_skipped_at but no new history entry; the next real run resets consecutive_skips to 0."
    why_human: "End-to-end wiring across a live poller + pollruns.Store + StatusStore + /status handler over a real cron interval is not exercisable from this box. Every in-process link is proven by unit + integration tests; only the running-instance walkthrough remains."
---

# Phase 18.1: Poll Cycle Instrumentation Verification Report

**Phase Goal:** Every poll cycle records exactly one run entry through the RunRecorder seam — with correct counts even under the worker fan-out — and polling stays green no matter what the recorder does.

**Verified:** 2026-09-10
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth (ROADMAP Success Criteria) | Status | Evidence |
|---|----------------------------------|--------|----------|
| 1 | One `RunResult` per source per cycle with all fields + leak-free summary; a shutdown-interrupted cycle appears with `cancelled` outcome, not missing | ✓ VERIFIED | `poller.go:385-413` defer-registered `RecordRun` fires exactly once per post-CAS cycle; `RunResult` literal sets Source/CycleID/StartedAt/FinishedAt/DurationMS/4 counters/Outcome, `Summary` left unset and recomposed by `pollruns.composeSummary` from outcome+ints only. Tests: `TestRunRecorder_RecordsOneRunPerCycle` (runCalls==1/cycle, real-store history len==1, all fields asserted incl. `"ok — 3 checked, 0 errored, 9 events"`), `TestRunRecorder_RecordsCancelledOutcome` (mid-cycle + pre-dispatch, both len==1 outcome=cancelled), `TestRunRecorder_RecordsErrorOutcomeOnListFailure` (List failure still records 1 entry, no sentinel text in summary). All green. |
| 2 | Overlap-skipped tick records NO run entry; bumps consecutive-skip count + last-skipped timestamp (visible in /status); next real run resets the count | ✓ VERIFIED | `poller.go:359-363`: `!CompareAndSwap` branch calls `p.runs.RecordSkip(source)` and returns `ErrCycleInProgress` before `cycleID`/`cycleStart`/either defer exists. `pollruns.RecordSkip` bumps `consecutiveSkips` + stamps `lastSkippedAt`, no ring append; `RecordRun` resets `consecutiveSkips = 0`. `/status` handler (`internal/httpserver/status.go:62-63,144-145`) exposes `last_skipped_at` + `consecutive_skips`. Test `TestRunRecorder_SkipRecordsNoEntry` (fake + real-store subtests): history len unchanged across skip, `ConsecutiveSkips` 0→1→0 after a completed `RecordRun`. Green. |
| 3 | Per-cycle counts exact under the worker fan-out — looped ~1000x invariant test with K erroring + P panicking artists; aggregation is a channel fold, not shared mutable ints | ✓ VERIFIED | `poller.go:438,462-477`: `resultCh := make(chan artistResult, len(entries))`, single-threaded `for r := range resultCh` fold after `wg.Wait()`; `artistsSkipped++` only in the single-threaded dispatch loop (`:444`); `runOneArtist` named-return recover yields `{checked:true, errored:true}` on panic. No `atomic.`/shared counter in any worker body. Test `TestRunCycle_CounterInvariant`: M=20, K=3, P=2, `WithMusicBrainzWorkers(8)`, 1000 iterations, exact `==` assertions (`ArtistsChecked==20`, `ArtistsErrored==5`, `checked-errored==15`, `EventsRecorded==30`, `ArtistsSkipped==0`), Deezer leg S=4. Ran green at `-count=3`. |
| 4 | Polling stays green regardless of the recorder — a recorder that errors still logs "poll cycle complete" and returns its normal result; `RecordRun` never wedges the overlap guard and never measurably extends shutdown | ✓ VERIFIED | `poller.go:410-412`: `RecordRun` error is logged (`record poll run failed` / `run_recorder_error`) and swallowed, never returned; `defer running.Store(false)` (`:364`) releases the guard unconditionally on every return + panic. Test `TestRunRecorder_ErrorSwallowed`: erroring recorder → `RunMusicBrainzCycle` returns nil, both `poll cycle complete` + `record poll run failed` logged, `RecordRun` called once (no retry), `mbRunning==false`; slow recorder (50ms) → returns nil within bound, guard released. Green. See WR-01/WR-03 below — latent design notes on non-shipped recorder implementations. |
| 5 | `events_recorded` is real: `DetectMusicBrainz`/`DetectDeezer` return `(int, error)`; the count threads through both `fetchAndRecord` closures and is folded; never hard-coded 0 while displayed | ✓ VERIFIED | `musicbrainz.go:46,129` `DetectMusicBrainz` returns `newRelease + gf + dc` (all three passes summed, A2); `deezer.go:33,113` `DetectDeezer` returns running `inserted`. Both `fetchAndRecord` closures (`poller.go:554-563,615-624`) capture `events` and return it; fold does `eventsRecorded += r.events`. Tests: 4 `internal/detection` cases bind+assert the count against inserted rows (incl. `TestDetectMusicBrainz_GuestFeature` proving the summed pass), `TestPoller_Run{MusicBrainz,Deezer}Cycle_RecordsNewRelease` assert `LastRun.EventsRecorded` == inserted rows, `TestRunRecorder_RecordsOneRunPerCycle` asserts `EventsRecorded==9`. All green. |

**Score:** 5/5 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `internal/poller/poller.go` — `artistResult` struct | one worker's fold contribution | ✓ VERIFIED | `:300-304`, `{checked, errored bool; events int}` |
| `internal/poller/poller.go` — `runOneArtist` | worker body + own defer/recover, named return | ✓ VERIFIED | `:312-340`, recover sets `res = {checked:true, errored:true}` |
| `internal/poller/poller.go` — resultCh fold + deferred RecordRun | channel fold + one entry per cycle | ✓ VERIFIED | `:438` buffer `len(entries)`; `:468-477` single-threaded fold; `:385-413` defer RecordRun with ok/error/cancelled switch |
| `internal/poller/poller.go` — RecordSkip on !CAS branch + cycleErr on List error | skip signal + error outcome | ✓ VERIFIED | `:361` `RecordSkip(source)`; `:417` `cycleErr = fmt.Errorf("poller: list watchlist: %w", err)` |
| `internal/detection/musicbrainz.go` / `deezer.go` — `(int, error)` widening | Detect* / helpers widened, MB sums 3 passes | ✓ VERIFIED | `DetectMusicBrainz`, `detectGuestFeatures`, `detectDeluxeChanges`, `DetectDeezer` all `(int, error)`; partial counts returned alongside errors |
| `internal/poller/poller_test.go` — `TestRunRecorder_RecordsOneRunPerCycle` (+4 more) + `TestRunCycle_CounterInvariant` | replaces Phase 18 inert-seam test; -race substitute | ✓ VERIFIED | All 6 new tests present and green |

### Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| `detection.Detect*` count | `runCycle` fold → `pollruns.RunResult.EventsRecorded` | `fetchAndRecord` closure → `runOneArtist` → `resultCh` → fold `eventsRecorded += r.events` | ✓ WIRED — traced in code + asserted by integration tests against real inserted rows |
| `runCycle` `cycleErr` | `pollruns.Outcome{OK,Error,Cancelled}` | outcome switch inside `RecordRun` defer (`errors.Is` context.Canceled/DeadlineExceeded) | ✓ WIRED — `TestRunRecorder_RecordsCancelledOutcome` / `...ErrorOutcomeOnListFailure` |
| `!running.CompareAndSwap` | `pollruns` per-source `consecutiveSkips` + `lastSkippedAt` | `p.runs.RecordSkip(source)`, no ring append | ✓ WIRED — `TestRunRecorder_SkipRecordsNoEntry` |
| `pollruns.Store` snapshot | `GET /status` body | `internal/httpserver/status.go` maps `ConsecutiveSkips`/`LastSkippedAt`/history | ✓ WIRED (Phase 18 handler, unchanged) — live instance walk = human check |

### Data-Flow Trace (Level 4)

| Value | Source | Produces Real Data | Status |
|-------|--------|--------------------|--------|
| `RunResult.EventsRecorded` | real `insertEvent` row count summed across 3 MB passes / Deezer inserts, folded from `artistResult.events` | Yes — asserted against DB row counts in real-Postgres integration tests | ✓ FLOWING |
| `RunResult.Artists{Checked,Errored}` | single-threaded fold over `resultCh` | Yes — 1000x exact-equality invariant | ✓ FLOWING |
| `RunResult.ArtistsSkipped` | `artistsSkipped++` in dispatch loop | Yes — `TestPoller_RunDeezerCycle_SkipsNilDeezerID` (skipped==1, checked==2) | ✓ FLOWING |
| `RunResult.Summary` | `pollruns.composeSummary` (outcome enum + 3 ints) | Yes — no free text; caller value discarded in `RecordRun` | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Counter fold exact under fan-out with error+panic artists | `go test ./internal/poller/ -run TestRunCycle_CounterInvariant -count=3` | ok (3 runs, ~0.4s each) | ✓ PASS |
| Run-recorder path (all 5 new tests) | `go test ./internal/poller/ -run 'TestRunRecorder\|TestRunCycle_CounterInvariant' -count=1` | ok | ✓ PASS |
| events_recorded bound to real inserted rows | `go test ./internal/detection/ -run 'TestDetectMusicBrainz_NewRelease\|...ReDetection...\|...GuestFeature\|TestDetectDeezer_NewRelease' -v` | 4 PASS | ✓ PASS |
| Poller real-Postgres integration + empty/skip edges | `go test ./internal/poller/ -run 'TestPoller_Run...RecordsNewRelease\|...SkipsNilDeezerID\|...EmptyWatchlist' -v` | 4 PASS | ✓ PASS |
| RUN-04 not regressed | `go test ./internal/pollruns/ -run TestStore_TwoSourceConcurrent -count=3` | ok | ✓ PASS |
| Affected packages full suite | `go test ./internal/poller/ ./internal/detection/ ./internal/pollruns/ -count=1` | all ok | ✓ PASS |
| `go vet` affected packages | `go vet ./internal/poller/ ./internal/detection/ ./internal/pollruns/` | clean | ✓ PASS |

Live `-race` unavailable on this box — standing documented limitation (WINDOWS.md entries 12/13); `TestRunCycle_CounterInvariant` is the contracted compensating control and passes.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| RUN-01 | 18.1-01, 18.1-02, 18.1-03 | One run entry per invocation with all fields + leak-free summary | ✓ SATISFIED | Truths 1, 3, 5; marked Complete in REQUIREMENTS.md:83 |
| RUN-02 | 18.1-02 | Skip = per-source signal not entry; shutdown cycle records `cancelled` | ✓ SATISFIED | Truths 1, 2; marked Complete in REQUIREMENTS.md:84 |
| RUN-03 | 18.1-01, 18.1-02 | Recorder failure logged, never fails/blocks/delays the cycle | ✓ SATISFIED | Truth 4; marked Complete in REQUIREMENTS.md:85 |

No orphaned requirements — REQUIREMENTS.md maps only RUN-01/02/03 to Phase 18.1 (RUN-04 → Phase 18, verified as regression only).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `internal/poller/poller.go` | 303 | Doc comment `// events recorded for this artist (0 on error / panic / bail)` contradicts code — `runOneArtist` keeps the **partial** count on the error path (intentional per A1: committed rows are real) | ℹ️ Info | Misleading comment only; behaviour is correct and asserted. Matches REVIEW WR-02. |
| `internal/poller/poller.go` | 227 / 385-413 | `cron.New()` has no `cron.Recover` chain; a panic inside a future `RunRecorder.RecordRun`/`RecordSkip` implementation would crash the process (the defer-first `RecordRun` fires during unwind) | ⚠️ Warning | Latent — shipped `*pollruns.Store` is an O(1) mutex write that cannot panic. Matches REVIEW WR-03. Not a goal blocker. |
| `internal/poller/poller.go` | 364 vs 385 | `RecordRun` defer registered after `defer running.Store(false)` → LIFO runs `RecordRun` while the overlap guard is still held; a slow recorder would skip the next tick | ⚠️ Warning | Intentional design (decision A3, documented in 18.1-01 must_haves). Latent for the shipped store. Matches REVIEW WR-01. Goal wording ("never wedges … never measurably extends shutdown") holds for the real implementation. |

No debt markers (`TODO`/`FIXME`/`XXX`/`TBD`/`HACK`) in any modified production file. `docs/api/status-contract.md` and `web/` byte-identical since the phase base (`d77b3a7`).

### Human Verification Required

**1. Live `/status` walkthrough over a real poll interval**

- **Test:** Boot a gated instance (`make db-up`), add a watchlist artist with a `deezer_id`, wait one real poll interval (or trigger a cycle), then `curl` the gated `GET /status`.
- **Expected:** Each source shows exactly one history entry per completed cycle carrying source, started/finished timestamps, the four artist counters, `events_recorded`, an outcome, and a composed summary with no DSN / webhook URL / path / raw driver error. An overlap-skipped tick shows a bumped `consecutive_skips` and `last_skipped_at` but no new history entry; the next real run resets `consecutive_skips` to 0.
- **Why human:** End-to-end wiring across a live poller + `pollruns.Store` + `StatusStore` + `/status` handler over a real cron interval is not exercisable from this box. Every in-process link is proven by unit + integration tests; only the running-instance walkthrough remains. (Documented in 18.1-VALIDATION.md and all three plan SUMMARYs.)

### Gaps Summary

No gaps. All five ROADMAP success criteria are observably true in the codebase and backed by green automated tests (unit, real-Postgres integration, and the 1000-iteration exact-equality counter invariant that substitutes for `-race`). All three declared requirements (RUN-01/02/03) are satisfied and marked Complete. The three code-review warnings are latent concerns about hypothetical future `RunRecorder` implementations, not defects in the shipped `*pollruns.Store` path, and do not block the phase goal.

Status is `human_needed` solely because of the one carried-forward live-instance `/status` verification — every automated criterion passes.

---

_Verified: 2026-09-10_
_Verifier: Claude (gsd-verifier)_
