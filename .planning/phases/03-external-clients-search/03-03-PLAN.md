---
phase: 03-external-clients-search
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - internal/musicbrainz/releasegroups.go
  - internal/musicbrainz/client.go
  - internal/musicbrainz/releasegroups_test.go
autonomous: true
requirements: [CLNT-01]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ReleaseGroupsByArtist fetches a watchlisted artist's release-groups from /ws/2/release-group?artist=, through the same limiter-gated, User-Agent-carrying request path as artist search (CLNT-01, D-10)"
    - "Two release-groups returned with identical title and identical first-release-date but distinct MBIDs are returned as two distinct entries — the client never merges or dedupes by title, and never collapses same-dated entries"
    - "first-release-date is carried as the opaque string MusicBrainz returned: a partial date of 2013 or 2013-09 is preserved verbatim, never zero-padded, reformatted, or parsed into a time.Time"
    - "MusicBrainzRateLimitPerSec is passed to rate.NewLimiter as rate.Limit(cfg.MusicBrainzRateLimitPerSec) with no rounding or integer truncation, so a fractional value such as 0.5 yields one request every two seconds"
    - "An artist with more release-groups than one page returns every release-group across pages: the client advances offset until it has collected release-group-count entries, and every page request passes through the limiter"
    - "Pagination is bounded: a hostile or runaway release-group-count cannot drive an unbounded request loop — the client stops at a fixed maximum page count and returns what it has"
    - "An artist with zero release-groups yields a non-nil, zero-length slice and a nil error; an empty artist MBID returns a sentinel error without issuing any HTTP request"
    - "ReleaseGroupsByArtist preserves MusicBrainz's response order within and across pages — no client-side sorting"
    - statement: "MusicBrainz's live per-IP throttling actually accepts the pace this client self-imposes — CI proves only the self-imposed pacing against a fake server, never that musicbrainz.org accepted it (CLAUDE.md forbids live calls in CI)"
      verification: backstop
  prohibitions:
    - statement: "MUST NOT treat MusicBrainz's free, non-profit-funded public API as an unmetered resource: no retry-storm on 503 or 429, no unbounded pagination loop, no request path that bypasses or disables the per-client rate limiter, and no concurrent fan-out that multiplies the configured rate for a single logical fetch"
      status: unverified
      verification: judgment
  artifacts:
    - path: "internal/musicbrainz/releasegroups.go"
      provides: "browse release-groups by artist with bounded pagination (D-10)"
      exports: ["ReleaseGroup", "ReleaseGroupsByArtist"]
    - path: "internal/musicbrainz/client.go"
      provides: "the ReleaseGroupLister seam the poller depends on"
      exports: ["ReleaseGroupLister"]
  key_links:
    - from: "internal/musicbrainz/releasegroups.go"
      to: "internal/musicbrainz/client.go"
      via: "every page request goes through the same unexported doRequest helper, so pagination cannot bypass the limiter or the User-Agent header"
      pattern: "c\\.doRequest\\("
---

<objective>
Give the MusicBrainz client the fetch path CLNT-01's poll cycle consumes: browse a watchlisted
artist's release-groups, correctly paginated and paced through the same single limiter-gated
request helper the search path already uses (D-10).

This plan is deliberately narrow and runs in parallel with plan 03-02 — it adds files inside the
package plan 03-01 created and touches nothing 03-02 touches. Plan 03-04 turns these two fetch
paths into scheduled poll cycles.

Purpose: supply CLNT-01's data path, and prove the client stays a well-behaved citizen of a free
public API when a single artist needs many requests.
Output: `ReleaseGroupsByArtist` with bounded pagination and the `ReleaseGroupLister` seam.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-external-clients-search/03-CONTEXT.md
@.planning/phases/03-external-clients-search/03-RESEARCH.md
@.planning/phases/03-external-clients-search/03-PATTERNS.md
@.planning/phases/03-external-clients-search/03-01-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by plan 03-03 (not pre-existing — exclude from drift/source-grounding checks):

- **`internal/musicbrainz/releasegroups.go`** (new file): `ReleaseGroup`,
  `(*Client).ReleaseGroupsByArtist`, unexported `releaseGroupEnvelope`, sentinel `ErrEmptyMBID`,
  consts `maxReleaseGroupPages`, `releaseGroupPageSize`.
- **`internal/musicbrainz/client.go`** (extended): `ReleaseGroupLister` interface and its
  compile-time assertion.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Browse release-groups by artist (D-10)</name>
  <files>internal/musicbrainz/releasegroups.go, internal/musicbrainz/client.go, internal/musicbrainz/releasegroups_test.go</files>
  <read_first>
    - `internal/musicbrainz/client.go` — the `Client` field set, `doRequest` limiter helper, `clampLimit`, the unexported base-URL override the tests use, and the `ArtistSearcher` seam declaration this new seam sits beside
    - `internal/musicbrainz/search.go` — the `SearchArtists` method shape this method copies: sentinel on empty input, `url.Values` construction, non-200 handling without echoing the body, typed envelope decode, `make([]T, 0, n)` return in upstream order
    - `internal/musicbrainz/search_test.go` — the `httptest.NewServer` fake style, fixture-literal placement, and the recorded-request assertions to copy
    - `internal/watchlist/service.go` — `Entry.MBID` is a non-nullable `string` (Phase 2 D-01), so unlike the Deezer path there is no nil case, only an empty-string guard
    - `.planning/phases/03-external-clients-search/03-RESEARCH.md` — the live-verified `/ws/2/release-group?artist={mbid}&type=album|ep&fmt=json&limit=3` response body (use verbatim as fixture), including the `release-group-count` / `release-group-offset` pagination fields
  </read_first>
  <behavior>
    - Given the live-verified `/ws/2/release-group` fixture, `ReleaseGroupsByArtist(ctx,
      "9fff2f8a-21e6-47de-a2b8-7f449929d43f")` returns one `ReleaseGroup` with `MBID`
      `0b6c2da8-ac60-4663-beae-bd3dc6dea94a`, `Title` `Nothing Was the Same`, `PrimaryType` `Album`,
      `FirstReleaseDate` `2013-09-19`, empty `SecondaryTypes`, empty `Disambiguation`.
    - The fake server records a request to path `/release-group` carrying query params `artist`
      (the MBID), `fmt=json`, `limit` and `offset`, and a non-empty `User-Agent` header.
    - No `type` filter is sent: every release-group type is fetched, because Phase 4's per-artist
      release-type preferences (Phase 2 WLST-05) are applied at detection time, and filtering at
      fetch time would make a preference change silently invisible until the next full refetch.
    - A fixture whose two release-groups share `"title": "Nothing Was the Same"` and
      `"first-release-date": "2013-09-19"` but carry distinct `id` values returns two entries, both
      preserved, in fixture order across three repeated calls.
    - A fixture whose `first-release-date` values are `2013`, `2013-09` and `""` returns those exact
      strings unchanged.
    - A body of `{"release-group-count":0,"release-group-offset":0,"release-groups":[]}` returns a
      non-nil, zero-length slice and a nil error.
    - `ReleaseGroupsByArtist(ctx, "")` and `ReleaseGroupsByArtist(ctx, "   ")` return `ErrEmptyMBID`
      and the fake server records zero requests.
    - A `503` response returns a non-nil error naming the status, with no release-groups and no
      retry attempt — the fake server records exactly one request.
    - A call whose `ctx` is already cancelled returns `ctx.Err()` with zero recorded requests.
  </behavior>
  <action>
    Create `internal/musicbrainz/releasegroups.go`. Define `ReleaseGroup` with `MBID string` (json
    tag `id`), `Title string`, `PrimaryType string` (json tag `primary-type`), `SecondaryTypes
    []string` (json tag `secondary-types`), `FirstReleaseDate string` (json tag
    `first-release-date`), `Disambiguation string`. Keep `FirstReleaseDate` a string:
    MusicBrainz returns partial dates (year-only, year-month) and an empty string for undated
    release-groups, so parsing into `time.Time` here would either fail or invent a month and day
    that Phase 4's new-release diff would then treat as real data. Define the unexported envelope
    `releaseGroupEnvelope` with `ReleaseGroups []ReleaseGroup` (json tag `release-groups`), `Count
    int` (json tag `release-group-count`) and `Offset int` (json tag `release-group-offset`).

    Implement `ReleaseGroupsByArtist(ctx context.Context, mbid string) ([]ReleaseGroup, error)`:
    `strings.TrimSpace` the mbid and return the new sentinel `ErrEmptyMBID` before building any
    request. Build the `/release-group` URL with `url.Values` carrying `artist` (the mbid), `fmt`
    (`json`), `limit` (the const `releaseGroupPageSize`, set to `maxLimit` = 100 so a typical artist
    needs one request) and `offset`. Do **not** send a `type` filter. Use
    `http.NewRequestWithContext` and route the request through the existing `doRequest` helper so
    the limiter and `User-Agent` are applied — never call `c.httpClient.Do` directly from this file.
    Treat any status other than 200 as an error naming the status code without echoing the body, and
    return immediately without retrying: MusicBrainz answers an over-rate client with 503, so a
    retry here would convert a throttle into a storm against a free public service. Decode into
    `releaseGroupEnvelope` and return `make([]ReleaseGroup, 0, ...)` in upstream order with no
    sorting.

    In this first task, issue exactly one page request and return that page's release-groups; task 2
    adds the offset loop. Structure the method so the request-and-decode step is an unexported
    helper taking an `offset` and returning `(releaseGroupEnvelope, error)`, so task 2 wraps it in a
    loop rather than rewriting it.

    In `internal/musicbrainz/client.go`, declare the narrow seam `ReleaseGroupLister` with
    `ReleaseGroupsByArtist(ctx context.Context, mbid string) ([]ReleaseGroup, error)` beside the
    existing `ArtistSearcher`, and assert `var _ ReleaseGroupLister = (*Client)(nil)`. Plan 03-04's
    poller depends on this seam, never on `*Client`. Doc-comment it citing D-10 and explaining that
    recordings-by-artist-credit is deliberately absent until Phase 4's guest-feature detection needs it.

    Create `internal/musicbrainz/releasegroups_test.go` covering every `<behavior>` case above,
    using the live-verified body from 03-RESEARCH.md as the fixture literal.
  </action>
  <acceptance_criteria>
    - `go build ./...` and `go vet ./...` exit 0.
    - `go test ./internal/musicbrainz/... -short -count=1` exits 0.
    - `go test ./internal/musicbrainz/... -run TestReleaseGroups -short -count=1 -v` shows passing tests whose names include `EmptyMBID`, `PartialDate` and `DuplicateTitle`.
    - `grep -v '^\s*//' internal/musicbrainz/releasegroups.go | grep -c 'c.doRequest('` is at least 1, and `grep -v '^\s*//' internal/musicbrainz/releasegroups.go | grep -c 'httpClient.Do('` equals 0 — no request path in this file bypasses the shared limiter-and-header helper.
    - `grep -v '^\s*//' internal/musicbrainz/releasegroups.go | grep -c 'FirstReleaseDate *string'` equals 1.
    - `grep -v '^\s*//' internal/musicbrainz/client.go | grep -c 'var _ ReleaseGroupLister'` equals 1.
    - `go test -short ./... -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./... &amp;&amp; go test ./internal/musicbrainz/... -run TestReleaseGroups -short -count=1 &amp;&amp; go test -short ./... -count=1</automated>
  </verify>
  <done>`ReleaseGroupsByArtist` returns typed, unsorted, verbatim-dated release-groups for a real MBID; refuses an empty MBID without touching the network; surfaces a 503 as an error with no retry; every request carries the limiter and User-Agent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bounded pagination and proven pacing across pages</name>
  <files>internal/musicbrainz/releasegroups.go, internal/musicbrainz/releasegroups_test.go</files>
  <read_first>
    - `internal/musicbrainz/releasegroups.go` — the single-page fetch and the unexported per-offset helper written in task 1, which this task wraps in a loop
    - `internal/musicbrainz/client.go` — `doRequest` and the limiter field, to confirm each loop iteration is separately paced
    - `internal/musicbrainz/search_test.go` — the existing limiter-pacing test (three sequential calls through a fast limiter), whose timing technique this task reuses across pages instead of across calls
    - `internal/config/config.go` — `MusicBrainzRateLimitPerSec` is a `float64` with default `1`
    - `.planning/phases/03-external-clients-search/03-RESEARCH.md` — the `release-group-count` / `release-group-offset` pagination fields and the documented `limit` maximum of 100
  </read_first>
  <behavior>
    - A fake server reporting `release-group-count` 250 and serving 100, 100 then 50 entries across
      three offsets returns all 250 release-groups in a single call, in page order, with the fake
      server recording exactly three requests carrying `offset` values 0, 100 and 200.
    - The same three-page fetch through a `rate.NewLimiter(rate.Limit(20), 1)` client takes at least
      100ms in total — every page request is separately paced, so pagination cannot outrun the
      configured rate.
    - A fake server reporting `release-group-count` 61 and serving all 61 in one page records
      exactly one request — the loop stops as soon as it has collected `release-group-count` entries.
    - A fake server that always reports `release-group-count` 1000000 while returning a full page
      every time records exactly `maxReleaseGroupPages` requests and returns that many pages' worth
      of entries with a nil error — a runaway or hostile count cannot drive an unbounded loop.
    - A fake server returning a page with zero entries while still reporting a large
      `release-group-count` terminates the loop rather than spinning forever.
    - An error on page 2 of 3 returns a non-nil error naming the status; the fake server records
      exactly two requests (no retry, no continuation past the failure).
    - Cancelling `ctx` between pages aborts the loop and returns `ctx.Err()`; the fake server
      records fewer requests than the full page count.
    - `rate.Limit(0.5)` built from a `float64` config value of `0.5` admits its second request no
      earlier than two seconds after the first (asserted on the `rate.Limiter` directly, not by
      sleeping through a real fetch).
  </behavior>
  <action>
    Extend `ReleaseGroupsByArtist` to loop over offsets. Start at offset 0 and call the task-1
    per-offset helper; after each page, append its entries to the accumulator and advance the offset
    by the number of entries actually returned (not by the requested page size, so a short page
    cannot skip records). Terminate when any of these holds: the accumulator has reached the
    envelope's `Count`; the page returned zero entries; or the loop has issued
    `maxReleaseGroupPages` requests. Declare `maxReleaseGroupPages` as a package const of 10 — with
    `releaseGroupPageSize` of 100 that is a 1000 release-group ceiling per artist per cycle, far
    above any real discography, and it bounds the worst case where a malformed or hostile `Count`
    would otherwise drive requests forever against a free public API. Doc-comment the const with
    that reasoning. Return the accumulated slice and a nil error when the ceiling is hit — a
    truncated fetch is a data-completeness limit, not a failure, and turning it into an error would
    make one prolific artist abort the whole poll cycle.

    Propagate `ctx` into every iteration so a cancelled context stops the loop at the next boundary,
    and return the first page error immediately without retrying or continuing.

    Do not add concurrency: the pages of one artist's fetch are issued sequentially, because
    parallel page requests would multiply this client's instantaneous rate against MusicBrainz
    beyond the value the operator configured.

    Extend `internal/musicbrainz/releasegroups_test.go` with every `<behavior>` case above. The
    multi-page fake server should serve pages based on the `offset` query param it receives and
    record each request's offset, so the offset-advance assertion is exact rather than a count. Add
    the direct `rate.Limiter` assertion for the fractional-rate case using
    `limiter.Reserve().Delay()` or an equivalent non-sleeping check, so the test does not spend two
    real seconds.
  </action>
  <acceptance_criteria>
    - `go test ./internal/musicbrainz/... -short -count=1` exits 0 in under 30 seconds.
    - `go test ./internal/musicbrainz/... -run 'TestReleaseGroups.*(Pagination|Pages|Cap|Pacing)' -short -count=1 -v` shows at least four passing test functions.
    - `grep -v '^\s*//' internal/musicbrainz/releasegroups.go | grep -c 'maxReleaseGroupPages'` is at least 2 (declaration plus the loop guard).
    - `grep -v '^\s*//' internal/musicbrainz/releasegroups.go | grep -c 'go func'` equals 0 — pages are fetched sequentially, never concurrently.
    - `grep -v '^\s*//' internal/musicbrainz/releasegroups.go | grep -Ec 'for .*(retry|attempt)'` equals 0 — there is no retry loop in this file.
    - `go build ./... && go vet ./... && go test -short ./... -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./... &amp;&amp; go test ./internal/musicbrainz/... -short -count=1 &amp;&amp; go test -short ./... -count=1</automated>
  </verify>
  <done>An artist with 250 release-groups returns all 250 across three limiter-paced sequential requests; a runaway count stops at the page ceiling with a nil error; a mid-fetch error or cancellation stops immediately with no retry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| musicbrainz.org → drop-tracker | untrusted JSON crosses in, including the `release-group-count` value that drives this client's own loop-termination decision |
| drop-tracker → musicbrainz.org | one logical fetch can now issue many requests; outbound volume per watchlisted artist becomes variable rather than fixed |
| `watchlist.Entry.MBID` → MusicBrainz URL construction | a stored artist identifier reaches an outbound query-string value |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12 | Denial of Service | `ReleaseGroupsByArtist` pagination loop | high | mitigate | The loop's termination depends on `release-group-count`, a value the *upstream* controls. A malformed or hostile count would otherwise drive requests forever against a free public API and stall the whole poll cycle. Bounded by the `maxReleaseGroupPages` const (10) plus a zero-entries-returned termination, both asserted by a fake server that always reports a huge count. |
| T-03-13 | Denial of Service | 503/429 handling in the MusicBrainz client | high | mitigate | MusicBrainz answers an over-rate client with 503. No retry or backoff loop exists in this file — a non-200 returns immediately — so a throttle can never be amplified into a retry storm. Asserted by a fake server that records exactly one request for a 503 and exactly two for a mid-page failure. |
| T-03-14 | Denial of Service | instantaneous outbound rate during a multi-page fetch | high | mitigate | Every page goes through the same `doRequest` helper and therefore through the same single `rate.Limiter`; pages are issued sequentially with no goroutines, so a 10-page fetch cannot exceed the operator-configured rate. Asserted both by the cross-page pacing test and by a grep gate proving no goroutine is spawned in this file. |
| T-03-15 | Tampering | `artist` query value built from `watchlist.Entry.MBID` | low | mitigate | The MBID is encoded as a query-string *value* via `url.Values.Encode()` and never as a host, scheme or path segment; `baseURL` remains a package const. Empty/whitespace MBIDs are rejected by `ErrEmptyMBID` before any URL is built. |
| T-03-16 | Information Disclosure | error text returned from a non-200 response | medium | mitigate | Non-200 handling names the status code only and never includes the response body, so an upstream error page cannot travel back through the poller's log lines or (via a future Phase 4 consumer) into a user-visible surface. |
| T-03-SC | Tampering | Go module installs | high | mitigate | No new module is added in this plan — the file uses stdlib plus the already-audited, already-pinned `golang.org/x/time/rate`. 03-RESEARCH.md's Package Legitimacy Audit records no `[ASSUMED]`/`[SUS]`/`[SLOP]` entries, so no blocking human legitimacy checkpoint applies. |
</threat_model>

<verification>
- `go build ./...`, `go vet ./...` clean.
- `go test ./internal/musicbrainz/... -short -count=1` green, covering single-page, multi-page,
  page-ceiling, mid-fetch-error, cancellation and pacing.
- `go test -short ./... -count=1` green — this plan adds files only inside `internal/musicbrainz`
  and cannot affect any other package.
</verification>

<success_criteria>
- `ReleaseGroupsByArtist` exists on `*musicbrainz.Client` behind the `ReleaseGroupLister` seam.
- All of an artist's release-groups are returned across pages, in upstream order, with dates verbatim.
- Pagination is sequential, limiter-paced, and bounded by a fixed page ceiling.
- No retry loop exists anywhere in the MusicBrainz client.
</success_criteria>

<output>
Create `.planning/phases/03-external-clients-search/03-03-SUMMARY.md` when done
</output>
