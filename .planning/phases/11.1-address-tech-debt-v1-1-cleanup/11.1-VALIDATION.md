---
phase: 11.1
slug: address-tech-debt-v1-1-cleanup
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-17
---

# Phase 11.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Backend: Go stdlib `testing` (whitebox, table-driven — matches `cmd/server/main_test.go`, `internal/detection/*_test.go`, `internal/db/pool_timeout_test.go` conventions). Frontend: Vitest 4.1.10 + React Testing Library 16.3.2, jsdom environment |
| **Config file** | Backend: `Makefile` (`test-short`, `test-integration`, `coverage-gate` targets) — no separate test-runner config. Frontend: `web/vitest.config.ts` |
| **Quick run command** | Backend: `go test ./... -short -race -count=1` (D-06's own test needs `testutil.RequirePostgresDSN`). Frontend: `pnpm --dir web test` |
| **Full suite command** | Backend: `make test-integration` (requires `make db-up` first). Frontend: `pnpm --dir web test` (coverage thresholds enforced inline) |
| **Estimated runtime** | Not independently measured this phase — existing suite; this phase only extends/fixes existing tests, no new slow paths introduced |

---

## Sampling Rate

- **After every task commit:** the relevant quick-run command scoped to the changed file (e.g. `go test ./cmd/server/... -run TestRun_BootServesHealthThenGracefulShutdownOnCancel -race -count=1`, or `pnpm --dir web test -- watchlist.test.tsx`)
- **After every plan wave:** full backend suite (`make test-integration`) for the backend wave; full frontend suite (`pnpm --dir web test`) for the frontend wave
- **Before `/gsd-verify-work`:** both full suites green, plus `make coverage-gate` (backend) and Vitest's inline threshold check (frontend) still passing at their existing 80%/70% floors — this phase adds test coverage, it must never *reduce* the measured percentage
- **Max feedback latency:** 1 task commit (per-task quick command; no watch-mode)

---

## Per-Task Verification Map

*Task IDs not yet assigned — plans do not exist yet (VALIDATION.md is seeded before planning). This phase has no REQ-IDs (ROADMAP.md carried none; CONTEXT.md's 13 locked decisions D-01…D-13 are the unit of scope — see 11.1-RESEARCH.md's Phase Scope table). Rows below are keyed by D-ID per RESEARCH.md's Phase Scope → Test Map; reconcile Task/Plan/Wave columns once PLAN.md files exist.*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | D-03 | — | Doc comment only, no behavior change | none (doc-only) | — | N/A | ⬜ pending |
| TBD | TBD | TBD | D-04 | — | `EventCard.tsx` matches `.prettierrc`; CI fails on future formatting drift | format-check (CI) | `pnpm --dir web exec prettier --check "**/*.{ts,tsx}"` | ✅ `.prettierrc` exists; ❌ CI step itself — Wave 0 gap | ⬜ pending |
| TBD | TBD | TBD | D-05a | — | Watchlist row disappears from DOM after a successful `removeWatchlist` | unit/component | `pnpm --dir web test -- watchlist.test.tsx` | ✅ `web/app/routes/watchlist.test.tsx` exists — new `it(...)` block only | ⬜ pending |
| TBD | TBD | TBD | D-05b | — | Selecting an event type calls `onChange` with the new `eventType` | unit/component | `pnpm --dir web test -- HistoryFilters.test.tsx` | ✅ `web/app/components/history/HistoryFilters.test.tsx` exists — new `it(...)` block only | ⬜ pending |
| TBD | TBD | TBD | D-06 | — | `run()`'s error surfaces immediately on early boot failure, not after a 10s generic timeout | integration (real Postgres) | `TEST_DATABASE_URL=... go test ./cmd/server/... -run TestRun_BootServesHealthThenGracefulShutdownOnCancel -race -count=1` | ✅ existing test file, rewritten in place | ⬜ pending |
| TBD | TBD | TBD | D-07 | — | `internal/db/sqlc` excluded from `COVER_PKGS` via an anchored match; a hypothetical `internal/db/sqlcgen` is not excluded | manual/`make` invocation trace | `go list ./... \| grep -vE '(^|/)internal/db/sqlc$'` (spot-check output by hand — Makefile shell pipeline, not `go test`-able) | N/A — Makefile logic | ⬜ pending |
| TBD | TBD | TBD | D-08 | — | Audit doc records the actual Phase 9 closure date/evidence | manual doc review | — (no automated check for markdown doc content) | N/A | ⬜ pending |
| TBD | TBD | TBD | D-09 | — | `08-VALIDATION.md`/`09-VALIDATION.md`/`10-VALIDATION.md` status flips from `draft` to `validated` | orchestration output | `/gsd-validate-phase 08` (and 09, 10) — verify resulting frontmatter `status:` | Files exist, currently `status: draft` | ⬜ pending |
| TBD | TBD | TBD | D-10 | — | Doc comment only, no behavior change | none (doc-only) | — | N/A | ⬜ pending |
| TBD | TBD | TBD | D-11 | T-04-01 (existing range-only-iteration convention) | The two `PoolConfig` parse-failure paths produce distinguishable error text; `redactedTarget(dsn)` remains the only interpolated value (never raw `dsn`) | unit | New/extended test asserting `strings.Contains(err.Error(), "parse pool_max_conns override")` for the `dsnSetsMaxConns` failure path — home file (`internal/db/pool_timeout_test.go` vs. new `pool_test.go`) not confirmed by research, planner decides | Check `internal/db/pool_timeout_test.go` for the right home — not confirmed this session | ⬜ pending |
| TBD | TBD | TBD | D-12 | — | The `Warn` line on the baseline-advanced/insert-failed window carries a distinguishing `window` field | unit (nice-to-have) / build+vet (minimum) | `go build ./... && go vet ./... && go test ./internal/detection/...` at minimum; a new assertion on the `slog` field is additive, not blocking | `internal/detection/musicbrainz_test.go` or `baseline_test.go` — exact home not confirmed this session | ⬜ pending |
| TBD | TBD | TBD | D-13 | — | Both History-page dropdowns render legible (non-white-on-white) option text | manual/visual — native `<select>` popup rendering is not reliably testable via jsdom | — (`verification: backstop`-style truth) | N/A — plan a `checkpoint:human-verify` task (open History page in a real browser, open both dropdowns, confirm legible text) | ⬜ pending |
| TBD | TBD | TBD | D-01/D-02 | — | `docker-compose.yml`/`Makefile` comments carry the "stray container is gone" narrative; working-tree revert to `5432:5432` committed as-is | manual doc/config review | `git diff docker-compose.yml Makefile` (visual review of comment text) then `git log` (confirm committed) | ✅ files already reverted in working tree | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] CI step: add `prettier --check` inside the existing `frontend-test` job (or a new job appended to `build-scan`'s `needs:`) in `.github/workflows/full-pipeline.yml` — D-04 has no existing automated gate today
- [ ] New/extended unit test asserting `PoolConfig`'s second parse-failure path produces distinguishable error text (D-11) — confirm home file first (`internal/db/pool_timeout_test.go` vs. a new file)
- [ ] `checkpoint:human-verify` task for D-13 (native `<select>` popup contrast cannot be automated via jsdom)
- [ ] `checkpoint:human-verify` or executor-attempted task confirming `.env.example`'s `DATABASE_URL` port matches the reverted `5432` value (RESEARCH.md Assumption A3 / Open Question 2 — research could not read this file; permission-denied, mirrors 11-REVIEW.md IN-02)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|--------------------|
| History page filter dropdown text is legible (non-white-on-white) after the `color-scheme` fix | D-13 | jsdom does not render actual OS-native `<select>` popup chrome — this is exactly the failure mode being fixed | Open the History page in a real browser (Chromium/Firefox), open the "Event types" and "Artists" filter dropdowns, confirm option text is visible against the popup background in both |
| `.env.example`'s `DATABASE_URL` port value | D-01/D-02 (Assumption A3) | Research session hit permission-denied reading this file via both `Read` and `Bash cat`; executor must attempt with its own tool permissions | Read `.env.example`, confirm `DATABASE_URL` uses port `5432`; if it still says `5433`, apply the same D-02 comment/value treatment for consistency |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 1 task commit
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
