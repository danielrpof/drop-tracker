---
phase: quick-260824-339
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/httpserver/search.go
  - web/app/routes/watchlist.test.tsx
autonomous: true
requirements: [QUICK-260824-339]
user_setup: []

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low   # 0 calibration samples for this project; default factor applied

must_haves:
  truths:
    - "internal/httpserver/search.go's three empty-string-to-nil-pointer conversions (musicBrainzSource's disambiguation and country fields, deezerSource's imageURL field) all call one shared unexported nilIfEmpty helper instead of repeating the same four-line if/else block three times."
    - "The GET /search JSON response is byte-identical for every input before and after the refactor -- internal/httpserver/search_test.go passes completely unmodified, including the tests that pin nil-vs-non-nil pointer behavior for disambiguation, country, and image_url."
    - "Every renderRoute(Watchlist, ...) call site in web/app/routes/watchlist.test.tsx passes the Watchlist tab's real landing route (the root path, per web/app/routes.ts's index route) instead of the stale prior path literal."
    - "watchlist.test.tsx's test assertions and rendered output are completely unchanged -- only the renderRoute path argument differs, since renderRoute's memory-router stub renders at whatever path it is given regardless of the app's real route table (cosmetic accuracy fix, not a functional one)."
  artifacts:
    - "internal/httpserver/search.go"
    - "web/app/routes/watchlist.test.tsx"
  key_links:
    - "musicBrainzSource.SearchArtists's disambiguation/country fields -> nilIfEmpty(a.Disambiguation) / nilIfEmpty(a.Country)"
    - "deezerSource.SearchArtists's imageURL field -> nilIfEmpty(a.Picture)"
    - "watchlist.test.tsx's five renderRoute(Watchlist, ...) call sites -> the root path used by web/app/routes.ts's index route"
---

<objective>
Fix two independent code-quality-review findings: extract a shared `nilIfEmpty` helper for the empty-string-to-nil-pointer idiom repeated three times in `internal/httpserver/search.go` (finding #5), and correct a stale path literal in `web/app/routes/watchlist.test.tsx`'s `renderRoute` calls that predates the Watchlist tab's route moving to the root path (finding #6).

Purpose: Both are pure cleanups flagged by code review -- neither changes observable behavior. Finding #5 removes duplicated mapping logic from the GET /search response builder. Finding #6 removes a misleading test literal that no longer matches the app's real route table, even though the test itself still passes today because `renderRoute`'s stub renders at whatever path it's given.

Output: `internal/httpserver/search.go` with a new private `nilIfEmpty` helper used at all three call sites, and `web/app/routes/watchlist.test.tsx` with every `renderRoute` call site using the correct current path. Both existing test suites pass completely unmodified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.claude/CLAUDE.md
@internal/httpserver/search.go
@web/app/routes/watchlist.test.tsx
@web/app/routes.ts
</context>

<planning_findings>
Verified during planning by reading all four files directly -- the executor should act on these, not re-derive them:

- **The three call sites in `search.go` are structurally identical**, each a `var x *string; if upstream != "" { local := upstream; x = &local }` block: `musicBrainzSource.SearchArtists` does this for `a.Disambiguation` -> `disambiguation` and `a.Country` -> `country`; `deezerSource.SearchArtists` does it for `a.Picture` -> `imageURL`. `deezerSource.SearchArtists`'s `Country: nil` field is a plain literal (Deezer's response has no country-equivalent field) and is NOT one of the three sites -- do not touch it.
- **`internal/httpserver/search_test.go` never reaches into any unexported symbol this refactor touches.** It exercises the mapping only through the exported `httpserver.NewMusicBrainzSource`/`httpserver.NewDeezerSource` constructors and the exported `httpserver.SearchArtist` struct, asserting on `.Disambiguation`, `.Country`, and `.ImageURL` pointer values (nil vs non-nil vs dereferenced value) via real HTTP requests and direct adapter calls (`TestSearch_MusicBrainzCountryReachesResponseBody`, `TestNewDeezerSource_MapsFields`, `TestNewMusicBrainzSource_MapsFields`, `TestSearchArtist_WireShapeKeySet`). No test references an unexported identifier from `search.go` by name. The file needs zero edits.
- **`web/app/lib/test/routeStub.tsx`'s `renderRoute(Component, path)` renders `Component` inside `createRoutesStub([{ path, Component }])` with `initialEntries={[path]}`** -- the `path` argument is purely a self-contained router stub configuration, unrelated to `web/app/routes.ts`'s real route table. Changing the literal does not change what the test renders or asserts; it only makes the literal describe where the Watchlist tab actually lives today (`web/app/routes.ts`'s `index("routes/watchlist.tsx", ...)` entry -- the root path, not `/watchlist`).
- **`watchlist.test.tsx` has exactly five `renderRoute(Watchlist, "/watchlist")` call sites**, one per `it(...)` block, all textually identical. All five need the same one-character path-argument change; no other line in the file changes.
- Module path is `github.com/danielrpof/drop-tracker`; no new imports needed for `search.go` (no new packages), no `go.mod` change. `watchlist.test.tsx` needs no new imports either. No new files in either task.
</planning_findings>

<tasks>

<task type="auto">
  <name>Task 1: Extract nilIfEmpty helper in search.go (finding #5)</name>
  <files>internal/httpserver/search.go</files>
  <action>
Add a new private helper function directly above `musicBrainzSource.SearchArtists` (after the `NewMusicBrainzSource` function, before its `Name()` method, or any other single clear location above the first call site): `func nilIfEmpty(s string) *string`. Its body returns `nil` when `s == ""`, and otherwise returns a pointer to `s`. Give it a one-line doc comment worded as: "nilIfEmpty returns nil for an empty string and a pointer to the value otherwise -- the shared idiom search.go's per-source adapters use to map an optional upstream string field into search.go's *string JSON fields (disambiguation, country, image_url), which encode as JSON null rather than an empty string when absent." Do not phrase the comment with the function name immediately followed by an opening parenthesis (avoid writing literally "nilIfEmpty(" inside the comment text) so the helper's real call sites stay unambiguous to grep.

Then replace all three inline blocks with a call to this helper, preserving every other line exactly as-is:
- In `musicBrainzSource.SearchArtists`: replace the `var disambiguation *string; if a.Disambiguation != "" { d := a.Disambiguation; disambiguation = &d }` block with `disambiguation := nilIfEmpty(a.Disambiguation)`, and the `var country *string; if a.Country != "" { c := a.Country; country = &c }` block with `country := nilIfEmpty(a.Country)`.
- In `deezerSource.SearchArtists`: replace the `var imageURL *string; if a.Picture != "" { p := a.Picture; imageURL = &p }` block with `imageURL := nilIfEmpty(a.Picture)`.

Do not touch `deezerSource.SearchArtists`'s `Country: nil` literal or its adjacent comment -- Deezer's response has no country-equivalent field, so that assignment is not part of this refactor. Do not edit `search_test.go`.
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./...</automated>
    <automated>go test ./internal/httpserver/... -v</automated>
    <automated>grep -c 'func nilIfEmpty' internal/httpserver/search.go</automated>
    <automated>grep -o 'nilIfEmpty(' internal/httpserver/search.go | wc -l</automated>
  </verify>
  <done>
`go build ./...` and `go vet ./...` succeed. `go test ./internal/httpserver/... -v` passes in full, unmodified, including `TestSearch_MusicBrainzCountryReachesResponseBody`, `TestNewDeezerSource_MapsFields`, `TestNewMusicBrainzSource_MapsFields`, and `TestSearchArtist_WireShapeKeySet`. `grep -c 'func nilIfEmpty' internal/httpserver/search.go` returns 1 (helper defined exactly once). `grep -o 'nilIfEmpty(' internal/httpserver/search.go | wc -l` returns 4 (one function definition plus three call sites). `search_test.go` is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix stale route literal in watchlist.test.tsx (finding #6)</name>
  <files>web/app/routes/watchlist.test.tsx</files>
  <action>
Find every `renderRoute(Watchlist, ...)` call site in the file (there are five, one per `it(...)` test block, all currently passing the same stale path argument left over from before the Watchlist tab's landing route moved to the root path in `web/app/routes.ts`). Update the second argument of each call site so it matches the Watchlist tab's real current landing route -- the root path `"/"` -- instead of the stale prior literal. Change nothing else in the file: no test names, no assertions, no imports, no other string literals. This is a path-argument-only edit since `renderRoute`'s stub renders at whatever path it is given regardless of the app's real route table (per `<planning_findings>`) -- the fix is descriptive accuracy, not a behavior change.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npm test -- watchlist.test.tsx</automated>
    <automated>grep -c 'renderRoute(Watchlist, "/")' web/app/routes/watchlist.test.tsx</automated>
  </verify>
  <done>
`npm test -- watchlist.test.tsx` (run from `web/`) passes in full, with every existing assertion unchanged. `grep -c 'renderRoute(Watchlist, "/")' web/app/routes/watchlist.test.tsx` returns 5, confirming all five call sites now use the root path and none were missed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MusicBrainz/Deezer upstream response -> SearchArtist JSON fields | `nilIfEmpty` sits on the last step of mapping an untrusted upstream string field (disambiguation, country, picture URL) into the GET /search response body's nullable JSON fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-339-01 | Tampering | nilIfEmpty (shared empty-string-to-nil-pointer mapping) | medium | mitigate | Task 1 reruns the pre-existing, unmodified `internal/httpserver` test suite after consolidating all three call sites, including the tests that pin exact nil-vs-non-nil pointer behavior for disambiguation, country, and image_url -- a regression in the shared mapping fails a test immediately rather than silently changing the JSON response shape. |
| T-339-02 | Repudiation | watchlist.test.tsx route literal accuracy | low | accept | Test-only, no user-facing surface; the change is descriptive (matching the real route table) not functional, since `renderRoute`'s stub is self-contained and ignores the app's real routes. Full test-file rerun after the edit confirms no assertion or rendered output changed. |
| T-339-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs in this plan -- no new imports in either file, no `go.mod` or `package.json` change. No legitimacy audit required. |
</threat_model>

<verification>
1. `go build ./...` and `go vet ./...` succeed.
2. `go test ./internal/httpserver/... -v` passes in full, with zero test file changes to `search_test.go`.
3. `internal/httpserver/search.go` defines exactly one `nilIfEmpty` helper, called at all three of the identified sites (musicBrainzSource's disambiguation and country, deezerSource's imageURL).
4. `npm test -- watchlist.test.tsx` (run from `web/`) passes in full, with zero assertion changes.
5. All five `renderRoute(Watchlist, ...)` call sites in `watchlist.test.tsx` pass the root path.
</verification>

<success_criteria>
- `internal/httpserver/search.go` has one private `nilIfEmpty(s string) *string` helper used at all three former inline call sites; GET /search's JSON response is byte-identical to before this change for every input; `search_test.go` is unmodified and its full suite passes.
- `web/app/routes/watchlist.test.tsx`'s five `renderRoute(Watchlist, ...)` call sites all use the root path (matching `web/app/routes.ts`'s real index route) instead of the stale prior literal; every test assertion and rendered output is unchanged; the full frontend test file passes.
</success_criteria>

<output>
Create `.planning/quick/260824-339-fix-code-review-findings-5-and-6-extract/260824-339-SUMMARY.md` when done.
</output>
