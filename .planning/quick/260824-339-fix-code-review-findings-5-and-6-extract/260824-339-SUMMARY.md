---
phase: quick-260824-339
plan: 01
subsystem: api
tags: [go, refactor, testing-library, react-router, code-review]

# Dependency graph
requires: []
provides:
  - "internal/httpserver/search.go's nilIfEmpty(s string) *string helper, consolidating three identical empty-string-to-nil-pointer blocks in musicBrainzSource.SearchArtists and deezerSource.SearchArtists"
  - "web/app/routes/watchlist.test.tsx's renderRoute call sites corrected to the Watchlist tab's real root-path landing route"
affects: []

# Actuals (#2632)
actuals:
  tokens: 936
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "nilIfEmpty(s string) *string as the shared idiom for optional-upstream-string-to-nullable-JSON-field mapping in internal/httpserver"

key-files:
  created: []
  modified:
    - internal/httpserver/search.go
    - web/app/routes/watchlist.test.tsx

key-decisions:
  - "nilIfEmpty placed directly above musicBrainzSource.SearchArtists (between NewMusicBrainzSource and its Name() method), the single clear shared location the plan allowed"
  - "Frontend deps installed via pnpm (pnpm-lock.yaml is this project's actual lockfile) after an initial npm install attempt hit an unrelated npm/arborist bug and left a stray package-lock.json, which was removed"

patterns-established: []

requirements-completed: [QUICK-260824-339]

coverage:
  - id: D1
    description: "search.go's three empty-string-to-nil-pointer conversions (disambiguation, country, image_url) consolidated into one shared nilIfEmpty helper, GET /search response unchanged"
    requirement: "QUICK-260824-339"
    verification:
      - kind: unit
        ref: "internal/httpserver/search_test.go#TestSearch_MusicBrainzCountryReachesResponseBody"
        status: pass
      - kind: unit
        ref: "internal/httpserver/search_test.go#TestNewDeezerSource_MapsFields"
        status: pass
      - kind: unit
        ref: "internal/httpserver/search_test.go#TestNewMusicBrainzSource_MapsFields"
        status: pass
      - kind: unit
        ref: "internal/httpserver/search_test.go#TestSearchArtist_WireShapeKeySet"
        status: pass
    human_judgment: false
  - id: D2
    description: "watchlist.test.tsx's five renderRoute(Watchlist, ...) call sites updated from the stale /watchlist literal to the real root-path landing route"
    requirement: "QUICK-260824-339"
    verification:
      - kind: unit
        ref: "web/app/routes/watchlist.test.tsx (all 5 it() blocks)"
        status: pass
    human_judgment: false

duration: 25min
completed: 2026-08-24
status: complete
---

# Quick Task 260824-339: Fix Code Review Findings #5 and #6 Summary

**Extracted a shared `nilIfEmpty` helper for `search.go`'s three repeated empty-string-to-nil-pointer blocks, and corrected `watchlist.test.tsx`'s stale `/watchlist` route literal to the app's real root-path landing route**

## Performance

- **Duration:** 25 min
- **Started:** 2026-08-24T02:15:00Z
- **Completed:** 2026-08-24T02:40:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Added a private `nilIfEmpty(s string) *string` helper in `internal/httpserver/search.go` and replaced all three identical inline empty-string-to-nil-pointer blocks (`musicBrainzSource.SearchArtists`'s disambiguation/country, `deezerSource.SearchArtists`'s imageURL) with calls to it
- Updated all five `renderRoute(Watchlist, ...)` call sites in `web/app/routes/watchlist.test.tsx` from the stale `"/watchlist"` literal to `"/"`, matching `web/app/routes.ts`'s real index route

## Task Commits

Each task was committed atomically:

1. **Task 1: Extract nilIfEmpty helper in search.go (finding #5)** - `02ffb28` (refactor)
2. **Task 2: Fix stale route literal in watchlist.test.tsx (finding #6)** - `17938b8` (test)

_Docs/metadata commit handled separately by the orchestrator._

## Files Created/Modified
- `internal/httpserver/search.go` - Added `nilIfEmpty` helper; three call sites now delegate to it instead of repeating the inline if/else block
- `web/app/routes/watchlist.test.tsx` - All five `renderRoute(Watchlist, ...)` calls now pass `"/"` instead of the stale `"/watchlist"` literal

## Decisions Made
- Placed `nilIfEmpty` directly above `musicBrainzSource.SearchArtists`, between `NewMusicBrainzSource` and its `Name()` method, per the plan's allowed single clear location
- Installed frontend dependencies with `pnpm install` (this project's real lockfile is `pnpm-lock.yaml`) after an `npm install` attempt hit an unrelated npm/arborist internal error (`Cannot read properties of null (reading 'edgesOut')`) and left a stray, uncommitted `package-lock.json` in the worktree, which was deleted before running tests

## Deviations from Plan

None - plan executed exactly as written. The `npm install` false start above was a local environment/tooling hiccup (not a code change) and left no artifacts in the final diff — no deviation to the plan's task list or `<must_haves>` was required.

## Issues Encountered
- The worktree had no `web/node_modules` and an initial `npm install` failed with an internal npm/arborist error, then (via `--legacy-peer-deps`) produced an incomplete tree missing `@testing-library/dom`, a peer dependency `@testing-library/jest-dom` and `@testing-library/react` need at test-setup time. Discovered the project's actual package manager is pnpm (`pnpm-lock.yaml` present, no npm lockfile normally committed) and resolved by deleting the broken `node_modules`/stray `package-lock.json` and running `pnpm install`, which succeeded cleanly and let the full frontend test suite (56 tests, including all 5 in `watchlist.test.tsx`) pass.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
Both code-review findings (#5, #6) are closed. `go build`, `go vet`, and `go test ./internal/httpserver/...` all pass with `search_test.go` unmodified; the full frontend suite (`pnpm test`) passes with `watchlist.test.tsx`'s 5 tests unchanged in assertions, only the route literal corrected. No blockers for follow-on work.

---
*Phase: quick-260824-339*
*Completed: 2026-08-24*

## Self-Check: PASSED

- FOUND: internal/httpserver/search.go
- FOUND: web/app/routes/watchlist.test.tsx
- FOUND: .planning/quick/260824-339-fix-code-review-findings-5-and-6-extract/260824-339-SUMMARY.md
- FOUND commit: 02ffb28
- FOUND commit: 17938b8
