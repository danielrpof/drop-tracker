---
phase: quick
plan: 260824-2q4
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app/lib/sources.ts
  - web/app/components/watchlist/SearchResultsColumns.tsx
  - web/app/routes/watchlist.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Only a MusicBrainz-sourced search result can be added to the watchlist -- decided in exactly one place"
    - "The identity field used to detect an already-watched search result (mbid vs deezer_id) is decided in exactly one place"
    - "No visible behavior change: every existing frontend test passes unchanged"
  artifacts:
    - web/app/lib/sources.ts
  key_links:
    - "SearchResultsColumns.tsx's alreadyWatching check and canAdd flag both call the shared helper instead of comparing sourceName inline"
    - "watchlist.tsx's handleAddSearchResult guard calls the shared helper instead of comparing sourceName inline"
---

<objective>
Centralize the frontend's "only MusicBrainz results are addable / mbid vs deezer_id identity field" business rule into one small pure helper module, replacing the three inline `sourceName === "musicbrainz"` / `sourceName === "deezer"` string comparisons currently duplicated across `SearchResultsColumns.tsx` (two call sites) and `watchlist.tsx` (one call site).

Purpose: Code-review finding #4 -- this rule is re-derived independently in multiple places today; centralizing it means a future third source (or a change to the rule) only needs one edit instead of a grep-and-fix across components.

Output: A new `web/app/lib/sources.ts` module exporting `isAddableSource` and `identityField`, wired into both existing call sites. Purely additive, behavior-preserving -- no test files change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/CodeProjects/drop-tracker/web/app/components/watchlist/SearchResultsColumns.tsx
@C:/CodeProjects/drop-tracker/web/app/routes/watchlist.tsx
@C:/CodeProjects/drop-tracker/web/app/lib/api.ts

Verified during planning (do not re-derive):
- `web/app/lib/api.ts`'s own header comment scopes it to "Typed fetch wrappers for all five backend endpoints" -- this business rule is not a wire type or fetch wrapper, so it does NOT belong in `api.ts`. Create `web/app/lib/sources.ts` instead.
- Grepped all of `web/app/` for `musicbrainz`/`deezer_id`/`sourceName`: the only two call sites duplicating this specific rule are `SearchResultsColumns.tsx` (the `alreadyWatching` ternary in `SourceColumn`, and `canAdd` in `SearchResultRow`) and `watchlist.tsx` (the `if (sourceName !== "musicbrainz")` guard in `handleAddSearchResult`). `EventCard.tsx`'s `guestFeatureHref` also branches on `event.source === "musicbrainz"`/`"deezer"` but that is a different rule (building an external link URL per source) -- out of scope, do not touch it.
- Checked every test file that references `musicbrainz`/`deezer_id` (`SearchResultsColumns.test.tsx`, `watchlist.test.tsx`, `SearchBox.test.tsx`, `PreferenceToggles.test.tsx`, `HistoryFilters.test.tsx`, `history.test.tsx`): none reach into an internal symbol this refactor touches -- all test through component props/DOM behavior. No test file edits needed.
- `web/package.json` scripts: `npm test` runs `vitest run`; `npm run typecheck` runs `react-router typegen && tsc`. No separate lint script exists in `web/package.json`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared source-identity helper module</name>
  <files>web/app/lib/sources.ts</files>
  <action>
    Create `web/app/lib/sources.ts` as a small pure module (no imports needed) exporting:
    - `export type IdentityField = "mbid" | "deezer_id"`
    - `export function isAddableSource(sourceName: string): boolean` -- returns `sourceName === "musicbrainz"`. This is the single source of truth for the business rule "only a MusicBrainz-sourced search result can be added to the watchlist, because Deezer's numeric id has no relation to a real mbid" (previously re-derived independently in SearchResultsColumns.tsx's `canAdd` and watchlist.tsx's guard).
    - `export function identityField(sourceName: string): IdentityField` -- returns `"deezer_id"` when `sourceName === "deezer"`, otherwise `"mbid"`. This is the single source of truth for which `WatchlistEntry` field identifies an existing row for a given source's search result (previously re-derived independently in SearchResultsColumns.tsx's `alreadyWatching` ternary).
    Include a short module-level doc comment explaining both are the one place this rule lives, referencing the two call sites this plan updates. Do not add this to `web/app/lib/api.ts` -- that file's header comment scopes it to wire-type fetch wrappers only, and this rule is neither.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "^$" ; echo "tsc-exit-check-in-task2"</automated>
  </verify>
  <done>web/app/lib/sources.ts exists, exports isAddableSource and identityField with the signatures above, and compiles standalone (verified fully once wired in Task 2's typecheck run).</done>
</task>

<task type="auto">
  <name>Task 2: Wire both call sites to the shared helper</name>
  <files>web/app/components/watchlist/SearchResultsColumns.tsx, web/app/routes/watchlist.tsx</files>
  <action>
    In `SearchResultsColumns.tsx`: import `{ identityField, isAddableSource }` from `~/lib/sources`. Replace the `alreadyWatching` computation's inline ternary (`sourceName === "deezer" ? entry.deezer_id === artist.id : entry.mbid === artist.id`) with a lookup that calls `identityField(sourceName)` once and compares `entry[field] === artist.id` against that computed field name. Replace `const canAdd = sourceName === "musicbrainz"` with `const canAdd = isAddableSource(sourceName)`. Do not change any other logic, JSX, or the existing doc comments' meaning (update comment wording only if it now literally says "hard-coded" or similarly describes the removed inline comparison).
    In `watchlist.tsx`: import `{ isAddableSource }` from `~/lib/sources`. Replace `if (sourceName !== "musicbrainz")` with `if (!isAddableSource(sourceName))`. Leave the toast message, the surrounding try/catch, and the doc comment above `handleAddSearchResult` unchanged (the comment's prose describing the rule stays accurate since the rule itself is unchanged, only its implementation moved).
    This is a pure call-site substitution -- no behavior change for any input value, since both helpers implement exactly the same comparisons the inline code performed.
  </action>
  <verify>
    <automated>cd web && npm test -- --run 2>&1 | tail -40</automated>
  </verify>
  <done>Both files import from ~/lib/sources and no longer contain a literal `=== "musicbrainz"` or `=== "deezer"` comparison outside of `web/app/lib/sources.ts` itself and the unrelated `EventCard.tsx`; `npm test` passes with the same test count as before the change; `npm run typecheck` passes with zero errors.</done>
</task>

</tasks>

<verification>
Run from `web/`:
1. `npm run typecheck` -- must pass with zero errors (confirms `entry[field]` indexed access in `SearchResultsColumns.tsx` type-checks against `WatchlistEntry`).
2. `npm test` (`vitest run`) -- every existing test file passes unchanged, including `SearchResultsColumns.test.tsx` and `watchlist.test.tsx` which exercise the exact behavior this refactor must preserve (already-watching disabled state, addable/non-addable button states, onAdd call args).
3. Manual grep confirms no duplicate logic remains: `grep -rn '=== "musicbrainz"\|=== "deezer"' web/app/` should show only `web/app/lib/sources.ts` and `web/app/components/history/EventCard.tsx` (the unrelated, out-of-scope guestFeatureHref rule).
</verification>

<success_criteria>
- `web/app/lib/sources.ts` exists with `isAddableSource` and `identityField` exported.
- `SearchResultsColumns.tsx` and `watchlist.tsx` both call the shared helper instead of inline string comparison.
- All existing frontend tests pass unchanged (no test file edited).
- `npm run typecheck` passes.
- No behavior change for any source/artist/entry input.
</success_criteria>

<output>
Create `.planning/quick/260824-2q4-fix-code-review-finding-4-centralize-fro/260824-2q4-SUMMARY.md` when done
</output>
