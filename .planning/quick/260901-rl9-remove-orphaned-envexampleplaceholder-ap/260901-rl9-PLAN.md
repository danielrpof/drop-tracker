---
phase: quick/260901-rl9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/authgate/weak_test.go
  - internal/authgate/weak.go
autonomous: true
requirements: [QUICK-260901-rl9]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`grep -rn \"envExamplePlaceholder\" internal/` returns zero matches."
    - "`grep -rn -i \"caliber\" internal/` returns zero matches."
    - "`IsWeakPassphrase` still flags every generic denylist word and every value shorter than 16 runes — behavior of the surviving heuristic is byte-for-byte unchanged."
    - "`go test ./internal/authgate/ ./cmd/server/...` is green and `go test -cover ./internal/authgate/` still reports 91.0% of statements (baseline measured 2026-09-01, pre-change)."
    - "No comment left in `internal/authgate/` describes `.env.example` as shipping a non-empty `INSTANCE_PASSPHRASE` placeholder."
  artifacts:
    - internal/authgate/weak.go
    - internal/authgate/weak_test.go
  key_links:
    - "`knownDefaults` slice literal ← consumed by `IsWeakPassphrase`'s normalized-compare loop; removing one entry must not change the loop or any other entry."
    - "`cmd/server/main.go` boot WARN call site ← calls `IsWeakPassphrase`; signature and semantics untouched, so the call site needs no edit."
---

<objective>
Remove the orphaned `envExamplePlaceholder` ("caliber") apparatus from `internal/authgate`.

Purpose: commit `465260c` blanked `.env.example:29` to `INSTANCE_PASSPHRASE=` (empty, like every other optional secret). That left a const holding an arbitrary word, a doc comment describing a file state that no longer exists (actively misleading — the worst class of comment per CLAUDE.md's comment discipline), and a drift-guard test pinning a subject that no longer exists. The generic `knownDefaults` denylist and the 16-rune length floor are the real defense and stay untouched.

Output: two trimmed files, two green commits, zero behavior change to `IsWeakPassphrase`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@internal/authgate/weak.go
@internal/authgate/weak_test.go
</context>

<!-- planner-discipline-allow: envExamplePlaceholder, caliber -->
<!-- Rationale: this plan's entire subject is the deletion of two named literals, so the task actions cannot avoid naming them. Both acceptance greps are directory-scoped to `internal/`; this plan file lives under `.planning/`, so no occurrence here can satisfy or invalidate either grep. -->

<working_tree_note>
`internal/authgate/weak_test.go` carries ONE uncommitted working-tree edit that belongs to this task: the 3-line doc comment above `TestWeakPassphrase_EnvExamplePlaceholderOnDenylist` was already deleted. Confirmed via `git diff` at plan time (2026-09-01) — the diff is exactly that one 3-line deletion, nothing else. Task 1 subsumes it: deleting the whole function removes the region that edit touched. Re-check `git diff -- internal/authgate/weak_test.go` before starting; if the diff has grown beyond that single hunk, stop and surface it rather than folding unknown changes into this commit.
</working_tree_note>

<tasks>

<task type="tracer">
  <name>Task 1: Drop the drift-guard test and the placeholder table case from weak_test.go</name>
  <files>internal/authgate/weak_test.go</files>
  <precondition>`git diff -- internal/authgate/weak_test.go` shows only the single 3-line doc-comment deletion described in `<working_tree_note>`; any additional hunk means stop and report.</precondition>
  <action>
Three deletions in `internal/authgate/weak_test.go`, test-file only — do not touch `weak.go` in this task.

1. In the package-level comment block near the top, the sentence explaining why these tests live in `package authgate` currently names two identifiers it needs direct access to. Drop the second one and the separator, leaving the sentence referring only to the `knownDefaults` slice. Keep the rest of that comment block verbatim — it is already at CLAUDE.md's 1-3-line intent-only standard.

2. In the `TestIsWeakPassphrase` table, delete the entire case row whose name string is `.env.example placeholder is flagged weak`. Delete the row, do not rewrite it with an inline literal: the short-value branch is already exercised by the `short-pass`, `exactly 15 runes is short`, `Zq9-improbabl-x`, and `15 multi-byte runes` rows, so no branch of `IsWeakPassphrase` loses coverage. Leave every other row byte-for-byte unchanged, including their ordering.

3. Delete `func TestWeakPassphrase_EnvExamplePlaceholderOnDenylist` in full — signature, body, closing brace, and the blank line that separated it from its neighbours (leave exactly one blank line between the function that precedes it and the comment block that follows it). Its 3-line doc comment is already gone in the working tree; do not restore it.

Do NOT touch `TestWeakPassphraseBootWarn_OneWarnLineNeverContainsValue` or `TestWeakPassphraseBootWarn_EmptyOrStrongLogsNothing`. Do NOT add any replacement test — this is a deletion, and adding a stand-in would recreate the orphan under a new name.

The package still compiles after this task: the const being retired is still referenced from the `knownDefaults` literal in `weak.go`, so nothing goes unused mid-sequence. That is deliberate — it keeps this task independently green and independently committable.
  </action>
  <verify>
    <automated>cd /c/CodeProjects/drop-tracker && go build ./... && go vet ./internal/authgate/ && go test ./internal/authgate/ ./cmd/server/... && test "$(grep -c 'envExamplePlaceholder' internal/authgate/weak_test.go)" = "0"</automated>
  </verify>
  <done>
- `internal/authgate/weak_test.go` contains zero occurrences of the retired identifier.
- `TestIsWeakPassphrase` has exactly 11 table rows (was 12); every surviving row is unchanged.
- `TestWeakPassphrase_EnvExamplePlaceholderOnDenylist` no longer exists anywhere in the repo.
- Both boot-WARN tests are untouched.
- `go build ./...`, `go vet ./internal/authgate/`, and `go test ./internal/authgate/ ./cmd/server/...` are all clean.
- Committed as a `test:`-scoped commit with no AI attribution trailer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete the orphaned const from weak.go and run the Definition of Done gates</name>
  <files>internal/authgate/weak.go</files>
  <action>
Three deletions plus one comment trim in `internal/authgate/weak.go`.

1. Delete the `envExamplePlaceholder` const declaration in full, together with the entire 6-line doc comment above it that describes it as "the exact INSTANCE_PASSPHRASE value .env.example ships as its placeholder". Both go — the comment is the actively misleading half, since `.env.example:29` has been an empty `INSTANCE_PASSPHRASE=` since commit `465260c`.

2. Delete the first entry of the `knownDefaults` slice literal — the line that names that const. Do not substitute a string literal in its place: the word only ever carried weight as *the example file's value*, and as a generic denylist word it defends nothing (at 7 runes it is already below `minPassphraseRunes`, so the length floor flags it regardless). Every remaining entry (`changeme` through `replaceme`) stays exactly as-is, same order — `changeme` in particular is the canonical placeholder and is load-bearing.

3. Trim the `knownDefaults` doc comment so it no longer promises a second category the list no longer has: the phrase enumerating "obvious placeholders plus the .env.example placeholder" becomes a plain description of the generic denylist. Keep the sentence about case-insensitive comparison against a lower-cased, whitespace-trimmed copy and the `(D-11)` reference — one design-doc reference is exactly what CLAUDE.md's comment discipline calls for. Result should be ~3 lines.

Leave `minPassphraseRunes` and its doc comment alone. The clause about 16 being "well below the 24-character random value .env.example recommends" is STILL ACCURATE — the example file's comment does recommend 24+ characters, only the value was blanked. Leave `IsWeakPassphrase`'s body and doc comment alone. Do not touch `.env.example`, `cmd/server/main.go`, or any other file in the package.

Then run the Definition of Done gates from CLAUDE.md before committing (see `<verify>`), and commit both this file and the surviving state of Task 1's work with a `refactor:`-scoped message. Never pass `--no-verify`; the pre-commit hooks (gitleaks, `golangci-lint --fix`) are the fast local mirror of CI. No AI attribution trailer of any kind.

CI-only / not-runnable-here gates, for the record — do not treat their absence as a skipped gate: `make test` and `make coverage-gate` both require a Docker Postgres via `make db-up`; run them if Docker is up, otherwise CI is the backstop. `make sqlc-check` is a no-op for this change (no `.sql` or `sqlc.yaml` touched) but is local-only with no CI counterpart, so run it if sqlc is installed. No frontend files change, so the prettier/pnpm gates do not apply.
  </action>
  <verify>
    <automated>cd /c/CodeProjects/drop-tracker && go vet ./... && golangci-lint run && go test ./internal/authgate/ ./cmd/server/... && go test -cover ./internal/authgate/ && test "$(grep -rn 'envExamplePlaceholder' internal/ | wc -l)" = "0" && test "$(grep -rni 'caliber' internal/ | wc -l)" = "0"</automated>
  </verify>
  <done>
- `grep -rn "envExamplePlaceholder" internal/` returns zero matches.
- `grep -rn -i "caliber" internal/` returns zero matches.
- `knownDefaults` has 15 entries (was 16), starting at `changeme`, in the original order.
- The `knownDefaults` doc comment no longer mentions the example file; `minPassphraseRunes`'s comment and `IsWeakPassphrase`'s body and doc comment are byte-for-byte unchanged.
- `go vet ./...` and `golangci-lint run` are both clean.
- `go test ./internal/authgate/ ./cmd/server/...` is green and `go test -cover ./internal/authgate/` reports 91.0% of statements or higher (91.0% was the pre-change baseline; no production statement lost its exerciser).
- Committed without `--no-verify` and without any `Co-Authored-By` / `Claude-Session` / `Generated with` trailer.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → process env | `INSTANCE_PASSPHRASE` crosses into the process at boot; `IsWeakPassphrase` is the advisory check on that value (D-11, WARN only, never a refusal to start). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-rl9-01 | Spoofing | `knownDefaults` denylist shrinks by one entry | low | accept | The retired word is only reachable as a real passphrase if an operator independently invents it. It is 7 runes, so `minPassphraseRunes = 16` flags it on the length branch anyway — the denylist entry was strictly redundant even before `.env.example` was blanked. No detection capability is lost. |
| T-rl9-02 | Tampering | drift-guard test deleted | low | accept | The guard pinned "whatever literal `.env.example` ships must be on the denylist". `.env.example` now ships an empty value, so the guard has no subject; an empty value means the gate is disabled (GATE-07) and `IsWeakPassphrase("")` correctly returns not-weak. Keeping a test that asserts against a nonexistent subject is worse than deleting it — it reads as coverage while proving nothing. |
| T-rl9-03 | Information Disclosure | fixed-reason phrases in `IsWeakPassphrase` | low | mitigate | Untouched by this change. The `reason` return values remain fixed operator-authored phrases that never embed `p`; the surviving table test still asserts `!strings.Contains(reason, tc.in)` on every non-empty row (D-11, D-13). |
| T-rl9-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs — deletion-only change, `go.mod`/`go.sum`/`package.json` untouched. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
1. `git diff --stat` shows exactly two files changed, both under `internal/authgate/`, all deletions plus one comment rewrite — no third file, no `.env.example`, no `cmd/server/main.go`.
2. `grep -rn "envExamplePlaceholder" internal/` → zero matches.
3. `grep -rn -i "caliber" internal/` → zero matches.
4. `go vet ./...` clean; `golangci-lint run` clean (in particular no `unused` finding, since the const and its sole consumer were removed together).
5. `go test ./internal/authgate/ ./cmd/server/...` green; `go test -cover ./internal/authgate/` at or above the 91.0% pre-change baseline.
6. `git log -2 --format=%B` shows no `Co-Authored-By:`, `Claude-Session:`, or `Generated with` line.
7. Historical `.planning/` documents that mention the retired identifier (`14-04-SUMMARY.md`, `14-UAT.md`, `14-05-PLAN.md`, `.planning/debug/passphrase-gate-bypassed.md`) are deliberately NOT edited — they are an accurate record of what Phase 14 shipped at the time.
</verification>

<success_criteria>
- The orphaned apparatus is gone from `internal/authgate/`: no const, no denylist entry, no drift-guard test, no table case, no comment referencing it.
- `IsWeakPassphrase`'s observable behavior is unchanged for every input except the retired 7-rune word, which is still reported weak via the length floor with reason `shorter than 16 characters`.
- Both commits are green under `go vet ./...`, `golangci-lint run`, and `go test ./internal/authgate/ ./cmd/server/...`, made through the pre-commit hooks with no AI attribution.
</success_criteria>

<output>
Create `.planning/quick/260901-rl9-remove-orphaned-envexampleplaceholder-ap/260901-rl9-SUMMARY.md` when done.
</output>
