---
phase: quick-260823-sp6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/CLAUDE.md
autonomous: true
requirements:
  - QUICK-260823-sp6

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "A Claude session working in this repo reads .claude/CLAUDE.md and is told to query the graphify knowledge graph in graphify-out/ before grepping or reading files one by one for codebase/architecture questions."
    - "The note names a concrete, runnable entry point (`/graphify query \"<question>\"`) so an agent can act on it without further lookup."
    - "The note survives GSD regeneration — `gsd-tools generate-claude-md` and `generate-claude-profile` splice only between their own marker pairs, and the note sits outside all of them."
    - "All seven pre-existing GSD-managed sections in .claude/CLAUDE.md remain intact and unmodified."
  artifacts:
    - ".claude/CLAUDE.md — one new hand-maintained section appended after the `<!-- GSD:profile-end -->` marker"
  key_links:
    - "Placement after the profile-end marker is load-bearing: `updateSection()` in gsd-core/bin/lib/profile-output.cjs rebuilds each managed section as `before + newContent + after`, so anything placed INSIDE a marker pair is silently destroyed on the next regeneration, while anything outside every pair survives."
    - "The cited subcommands must match the installed global graphify skill's documented usage (`query`, `path`, `explain`, `--update` from ~/.claude/skills/graphify/SKILL.md) — a wrong command name makes the instruction dead on arrival."
---

<objective>
Add a short, hand-maintained section to `.claude/CLAUDE.md` telling agents working in this repo to query the existing graphify knowledge graph in `graphify-out/` before falling back to grep-and-read exploration for codebase questions.

Purpose: `graphify-out/graph.json`, `GRAPH_REPORT.md`, and `graph.html` are already built and committed, but nothing in the project instructions points agents at them — so every session re-discovers the codebase by reading files, burning tokens on context the graph already holds.

Output: One new `##` section at the end of `.claude/CLAUDE.md`, placed outside every GSD marker pair so it survives regeneration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
</context>

<research_findings>
Verified during planning — the executor does not need to re-derive these:

1. **`.claude/CLAUDE.md` is fully GSD-managed.** All 184 current lines sit inside seven marker pairs: `project`, `stack`, `conventions`, `architecture`, `skills`, `workflow`, `profile`. There is currently zero hand-written content in the file.

2. **Content outside marker pairs is preserved.** `updateSection()` (`~/.claude/gsd-core/bin/lib/profile-output.cjs:261-272`) locates `<!-- GSD:{name}-start` / `<!-- GSD:{name}-end -->` and returns `before + newContent + after`. `generate-claude-profile` uses the same splice for the profile block (same file, lines 972-981). Appending after `<!-- GSD:profile-end -->` therefore survives both generators. (Only `generate-claude-md --force` rewrites the whole file; that is an explicit destructive opt-in.)

3. **`claude_md_path` is `./.claude/CLAUDE.md`** per `.planning/config.json:85` — this is the file GSD manages and the one to edit. Do not create or edit a root-level `CLAUDE.md`.

4. **The graph exists and is committed.** `graphify-out/` holds `graph.json` (2.9 MB), `graph.html`, `GRAPH_REPORT.md` (49 KB), `cost.json`, `manifest.json` — all tracked by git. Only `graphify-out/cache/` is gitignored (`.gitignore:37`).

5. **Subcommand names verified** against `~/.claude/skills/graphify/SKILL.md` Usage block: `query "<question>"` (with `--dfs`, `--budget <n>`), `path "<A>" "<B>"`, `explain "<node>"`, and `. --update` for an incremental rebuild.
</research_findings>

<section_content>
Append exactly this block to the end of `.claude/CLAUDE.md`, separated from the `<!-- GSD:profile-end -->` line by one blank line:

```markdown
## Codebase Questions — Query the Knowledge Graph First

This repo ships a prebuilt graphify knowledge graph in `graphify-out/` (`graph.json`, `GRAPH_REPORT.md`, `graph.html`; everything but `cache/` is committed). For any question about architecture, file relationships, or where a behavior lives, query the graph before grepping or reading files one by one — one query returns targeted context instead of spending tokens on exploratory reads.

- Ask a question: `/graphify query "<question>"` (`--dfs` to trace a single path, `--budget <n>` to cap answer size)
- Connect two concepts: `/graphify path "<A>" "<B>"`
- Explain one node: `/graphify explain "<name>"`

Use the graph to narrow the search, then read the specific files it points at before editing anything. If it looks stale relative to recent commits, refresh with `/graphify . --update`.

*Hand-maintained section — it sits outside the GSD marker blocks above so `generate-claude-md` never overwrites it. Keep it last.*
```
</section_content>

<tasks>

<task type="tracer">
  <name>Task 1: Append the knowledge-graph-first section to .claude/CLAUDE.md</name>
  <files>.claude/CLAUDE.md</files>
  <precondition>`.claude/CLAUDE.md` ends with the line `&lt;!-- GSD:profile-end --&gt;` and contains 14 GSD marker lines (7 start, 7 end). If the file has already been hand-edited past that shape, stop and report rather than guessing at placement.</precondition>
  <action>
    Read `.claude/CLAUDE.md`, then use Edit to append the block given verbatim in `&lt;section_content&gt;` above to the very end of the file, after the final GSD profile end-marker line, with exactly one blank line between the marker and the new `##` heading.

    Reproduce the block character-for-character — heading text, backticked paths, bullet order, subcommand spellings, and the closing italic line. The subcommand names were verified against the installed global graphify skill; do not paraphrase them, do not invent flags, and do not substitute a bare `graphify` for the `/graphify` slash form (agents reading this file invoke the skill, not a shell binary).

    Do NOT touch any existing line. Every current line of the file lives inside a GSD marker pair, and those pairs are machine-regenerated; edits inside them are lost on the next `generate-claude-md` run and would also show up as spurious deletions here. The only correct diff for this task is pure addition at the tail of the file.

    Match the file's existing tone: `##` heading level like every other section, sentence-case prose, em dashes, backticks around paths and commands, hyphen bullets.
  </action>
  <verify>
    <automated>grep -c '&lt;!-- GSD:' .claude/CLAUDE.md | grep -qx 14 && for h in '## Project' '## Technology Stack' '## Conventions' '## Architecture' '## Project Skills' '## GSD Workflow Enforcement' '## Developer Profile'; do grep -qF "$h" .claude/CLAUDE.md || { echo "CLOBBERED: $h"; exit 1; }; done && { grep -qF 'Knowledge Graph First' .claude/CLAUDE.md || { echo 'SECTION ABSENT'; exit 1; }; } && test "$(grep -n 'GSD:profile-end' .claude/CLAUDE.md | cut -d: -f1)" -lt "$(grep -nF 'Knowledge Graph First' .claude/CLAUDE.md | cut -d: -f1)" && grep -qF 'graphify query' .claude/CLAUDE.md && grep -qF 'graphify-out/' .claude/CLAUDE.md && echo VERIFY_OK</automated>
    <automated>test "$(git diff HEAD -- .claude/CLAUDE.md | grep -c '^-[^-]')" -eq 0 &amp;&amp; echo NO_DELETIONS</automated>
  </verify>
  <done>
    `.claude/CLAUDE.md` still holds all 14 GSD marker lines and all seven managed section headings; the new `## Codebase Questions — Query the Knowledge Graph First` heading appears at a line number greater than the profile end-marker's; the section names `graphify-out/` and the `/graphify query` entry point; and the working-tree diff against HEAD contains additions only (zero removed lines).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project instructions → agent behavior | `.claude/CLAUDE.md` is auto-loaded into every agent session and its text steers tool use, so its content is an instruction-injection surface. |
| committed graph artifact → agent context | `graphify-out/graph.json` is repo-derived data that agents will now be told to pull context from. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-SP6-01 | Tampering | new CLAUDE.md section | medium | mitigate | Section cites only the already-installed global graphify skill's read-only subcommands (`query`/`path`/`explain`/`--update`). No new endpoint, credential, install step, or arbitrary shell command is introduced into the always-loaded instruction file. |
| T-SP6-02 | Information Disclosure | `graphify-out/graph.json` | low | accept | The graph is derived from files already committed to this repo, and gitleaks (pre-commit + CI) already gates secrets from entering those files. Pointing agents at the graph discloses nothing the repo does not. |
| T-SP6-03 | Repudiation | stale graph steering edits | medium | mitigate | Section explicitly instructs agents to read the specific source files the graph points at before editing, and to refresh with `/graphify . --update` when the graph looks stale — the graph narrows the search, it never authorizes a blind edit. |
| T-SP6-04 | Denial of Service | GSD section regeneration | high | mitigate | Section is appended outside every `<!-- GSD:*-start/-end -->` pair; `updateSection()` splices only between matching markers, so regeneration cannot delete it. Both verify gates assert the 14-marker count, all seven managed headings, and after-profile-end ordering. |

No package-manager installs (npm/pip/cargo) occur in this change, so no supply-chain (`T-*-SC`) legitimacy checkpoint applies.
</threat_model>

<verification>
1. Both `<automated>` gates in Task 1 pass (`VERIFY_OK` and `NO_DELETIONS`).
2. Human read-through: the new section reads as a natural part of `.claude/CLAUDE.md`, not a bolt-on — same heading level, same prose register as the surrounding GSD-generated sections.
3. Regeneration safety spot-check (optional, non-blocking): after committing, `node ~/.claude/gsd-core/bin/gsd-tools.cjs query generate-claude-md` (without `--force`) leaves the new section present. Skip if the command would rewrite unrelated managed sections mid-milestone.
</verification>

<success_criteria>
- `.claude/CLAUDE.md` carries one new `##` section instructing agents to query the `graphify-out/` knowledge graph before ad-hoc file exploration for codebase questions.
- The section gives at least one copy-pasteable entry point (`/graphify query "<question>"`).
- The section is positioned after `<!-- GSD:profile-end -->`, outside every GSD marker pair.
- Zero lines removed from the file; all seven GSD-managed sections intact.
- Change committed with a conventional-commit `docs:` message.
</success_criteria>

<output>
Create `.planning/quick/260823-sp6-add-a-note-in-claude-md-instructing-agen/260823-sp6-SUMMARY.md` when done.
</output>
