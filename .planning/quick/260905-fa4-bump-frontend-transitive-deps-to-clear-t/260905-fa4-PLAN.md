---
phase: quick/260905-fa4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/pnpm-workspace.yaml
  - web/pnpm-lock.yaml
  - internal/webassets/build/client/ (only if `make web` changes it)
  - .planning/todos/pending/2026-09-05-bump-frontend-deps-to-clear-trivy-fs-high-cves.md (moved to completed/)
  - .planning/todos/pending/2026-09-05-delete-stale-web-package-lock-json.md (new)
  - .planning/todos/pending/2026-09-05-move-shadcn-out-of-frontend-dependencies.md (new)
autonomous: true
requirements: [QUICK-260905-fa4, TODO-2026-09-05-trivy-fs-high-cves]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A Trivy 0.70.0 filesystem scan of the regenerated `web/pnpm-lock.yaml` reports `Total: 0` at `--severity CRITICAL,HIGH` and exits 0. Baseline reproduced at plan time with the identical command: `Total: 6 (HIGH: 6, CRITICAL: 0)`, exit 1."
    - "`fast-uri` resolves inside the 3.1.x line (>= 3.1.6), never 4.x — `ajv@8.20.0` declares `fast-uri: ^3.0.1`, and an override bypasses the parent's declared range, so a 4.x resolution would silently hand ajv a major-incompatible dependency that Trivy would happily call clean."
    - "`browserslist` resolves >= 4.28.7; no `browserslist@4.28.2` entry survives in the lockfile."
    - "`web/pnpm-workspace.yaml` still contains exactly one `overrides:` key, and its diff is +2/-0 lines — not a whole-file rewrite (the CRLF/.gitattributes hazard from quick task 260901-lvn)."
    - "The lockfile diff is confined to the browserslist family plus the two target packages (~66 changed lines) and carries zero `(supports-color@7.2.0)` peer-suffix churn — the signature of a regen done with the wrong pnpm version."
    - "`pnpm install --frozen-lockfile` accepts the regenerated lockfile, so CI's `frontend-test` install step and the Dockerfile's `RUN pnpm install --frozen-lockfile` (Dockerfile:35) both still work."
    - "`pnpm test` (vitest) and `pnpm exec prettier --check \"**/*.{ts,tsx}\"` stay green under `web/`."
    - "If `make web` changes `internal/webassets/build/client/`, that change lands in the SAME commit as the lockfile — precedent commit `f6647ec` did exactly this after the last override bump."
    - "The fix is one commit touching `web/pnpm-workspace.yaml` + `web/pnpm-lock.yaml` (+ the webassets tree if it moved). No AI attribution trailer on any commit. No `--no-verify`."
    - "Two follow-up todos exist on disk as pending files: delete the stale tracked `web/package-lock.json`, and move `shadcn` out of `dependencies` — the second explicitly recorded as cleanup, NOT a security fix."
  artifacts:
    - web/pnpm-workspace.yaml
    - web/pnpm-lock.yaml
    - .planning/todos/pending/2026-09-05-delete-stale-web-package-lock-json.md
    - .planning/todos/pending/2026-09-05-move-shadcn-out-of-frontend-dependencies.md
    - .planning/todos/completed/2026-09-05-bump-frontend-deps-to-clear-trivy-fs-high-cves.md
  key_links:
    - "override entry → pnpm resolution → lockfile pin → Trivy verdict. The override is the *only* mechanism that forces re-resolution here: `ajv@8.20.0` (`fast-uri: ^3.0.1`) and `shadcn@4.16.2` (`browserslist: ^4.26.2`) already permit the fixed versions, but pnpm will not re-resolve an already-locked transitive unless told to."
    - "`^` vs `>=` → ajv's declared range. RESEARCH tested `fast-uri: '>=3.1.6'` and pnpm resolved **4.1.4**, crossing the major boundary. The existing `nanoid`/`postcss` entries use `>=` safely only because neither has a newer major. Do not pattern-match off them."
    - "pnpm 11.8.0 (Dockerfile:29) → lockfile resolution style. Bare `pnpm` on PATH is 11.8.0 and `corepack pnpm@11.8.0` is 11.8.0, but plain `corepack pnpm` resolves **11.25.0** (confirmed at plan time) and produces a 240-line diff of unrelated peer-suffix churn."
    - "Dockerfile:34 copies exactly `web/package.json web/pnpm-lock.yaml web/pnpm-workspace.yaml` into the SPA build stage — the override must live in one of those three files, and `pnpm-workspace.yaml` is one of them. A `pnpm.overrides` block added to `package.json` would also be copied, but pnpm 10+ reads overrides from `pnpm-workspace.yaml`, and a second `overrides:` key in that YAML file is a duplicate-key error."
    - "`trivy-fs` (`.github/workflows/full-pipeline.yml:103-115`) → `build-scan` → `release`. `trivy-fs` runs `scan-ref: .` with `severity: CRITICAL,HIGH` and `exit-code: '1'`; `build-scan` `needs:` it, so the whole publish half of the pipeline is blocked until this is green."
---

<objective>
Clear the 6 HIGH CVEs that make CI's `trivy-fs` job red, unblocking `build-scan` and `release`.

Purpose: `trivy-fs` scans the repo with `severity: CRITICAL,HIGH` / `exit-code: '1'`, and `build-scan` (hence `release`) `needs:` it. Six HIGH findings — `browserslist` 4.28.2 (CVE-2026-73088/73089) and `fast-uri` 3.1.5 (CVE-2026-75899/75931/75975/76172) — all come from `web/pnpm-lock.yaml` alone. Both are pure build/tooling transitives, reachable from the production dependency graph only because `shadcn` (a CLI) sits in `dependencies`. This is dependency drift disclosed after the frontend was last touched, not a regression in project code, and the publish half of the pipeline stays blocked until it is fixed.

Output: two lines added to the existing `overrides:` block in `web/pnpm-workspace.yaml`, a regenerated `web/pnpm-lock.yaml`, a possibly-regenerated `internal/webassets/build/client/` tree, and two follow-up todos. No application source changes.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/quick/260905-fa4-bump-frontend-transitive-deps-to-clear-t/260905-fa4-RESEARCH.md
@.planning/todos/pending/2026-09-05-bump-frontend-deps-to-clear-trivy-fs-high-cves.md
@web/pnpm-workspace.yaml
</context>

<!-- planner-discipline-allow: fast-uri@4, supports-color@7.2.0, browserslist@4.28.2, >=3.1.6 -->
<!-- Rationale: this plan's subject IS a set of package-version literals, so the actions cannot avoid naming them. Every negative gate below is file-scoped to `web/pnpm-lock.yaml` or to `git diff -- web/pnpm-lock.yaml` output; this plan file lives under `.planning/quick/`, so no occurrence here can satisfy or invalidate any gate. -->

<working_tree_note>
`git status --short` at plan time shows only untracked planning scratch (`.planning/milestone.lock`, this quick-task directory). Nothing under `web/` or `internal/webassets/` is modified. Re-check `git status --short web/ internal/webassets/` before starting; if either is already dirty, stop and surface it rather than folding unknown changes into the fix commit.
</working_tree_note>

<grounding>
Facts verified against the live tree and live tooling at plan time (2026-09-05). Do not re-derive these — but do re-confirm line anchors before editing, since Task 1 shifts them.

**MUTABLE-SCOPE AUTHORITY — `web/pnpm-workspace.yaml` edit scope is authorized from the live file, read this session.** The file is 12 lines: `packages: []` (1), an `allowBuilds:` block (3-5), a 3-line comment (7-9) explaining the existing overrides, then `overrides:` (10) with `nanoid: '>=3.3.17'` (11) and `postcss: '>=8.5.18'` (12). Exactly one `overrides:` key exists. The two new entries are appended after line 12; nothing else in the file may change.

- Lockfile today: `browserslist@4.28.2` at lines 1161 and 3744, `fast-uri@3.1.5` at 1497 and 4085 — exactly two entries per package, both two-space-indented at column 0+2. Bare package names in `overrides` are therefore sufficient; no `parent>child` selector is needed.
- `web/pnpm-workspace.yaml` is CRLF in the working tree; `git check-attr text eol` reports `text: auto`, `eol: lf`. `git status --short web/` is clean because `text=auto` normalizes on compare.
- `pnpm --version` (bare, on PATH) → **11.8.0**. `corepack pnpm@11.8.0 --version` → **11.8.0**. Plain `corepack pnpm --version` → **11.25.0** — `web/package.json` has no `packageManager` key, so corepack resolves latest 11.x. This is the churn hazard; pin it.
- `corepack pnpm@11.8.0 --dir web root` → `C:\CodeProjects\drop-tracker\web\node_modules`. The `--dir web` flag works and avoids a `cd` in every command.
- Docker daemon is up (server 29.6.2) and `aquasec/trivy:0.70.0` is already pulled locally.
- **Trivy baseline reproduced at plan time** with the exact command Task 1 re-runs — scanning a temp dir holding only the current lockfile printed `pnpm-lock.yaml (pnpm)` / `Total: 6 (HIGH: 6, CRITICAL: 0)` and exited 1. The `cygpath -m` + `MSYS_NO_PATHCONV=1` form is required on this Git Bash host; a raw `mktemp -d` path handed to `-v` is not host-visible to Docker Desktop.
- `make web` (Makefile:139-144) runs `pnpm install --frozen-lockfile`, `pnpm run build`, then replaces `internal/webassets/build/client/` — it shells bare `pnpm`, which is 11.8.0 here, so no version hazard in that target.
- `.planning/todos/completed/` exists and holds moved todo files with their frontmatter intact — that is the repo's convention for closing a todo.
</grounding>

<tasks>

<task type="tracer">
  <name>Task 1: Add the two overrides, regenerate the lockfile, and prove Trivy goes clean</name>
  <precondition>Docker daemon is running and `aquasec/trivy:0.70.0` is available locally (both confirmed at plan time); `corepack pnpm@11.8.0 --version` prints 11.8.0.</precondition>
  <files>web/pnpm-workspace.yaml, web/pnpm-lock.yaml</files>
  <read_first>
    `web/pnpm-workspace.yaml` in full (12 lines) — confirm the `overrides:` block and its two existing entries still read as described in `<grounding>` before editing. Do not read the lockfile up front; it is ~168 KB and the gates below inspect it by targeted grep.
  </read_first>
  <action>
Append exactly two entries to the EXISTING `overrides:` block in `web/pnpm-workspace.yaml`, directly after the `postcss` line, at the same two-space indentation and using the same single-quoted style: `browserslist` set to caret-4.28.7, and `fast-uri` set to caret-3.1.6.

Use caret ranges. Do NOT use `>=` for either. RESEARCH tested the `>=` form for the second package and pnpm resolved it to 4.1.4 — an unconstrained `>=` crosses the major boundary, and because an override bypasses the parent's declared range, `ajv@8.20.0` (which asks for a 3.x-compatible range) would silently receive a major-incompatible dependency that Trivy still reports as clean. The two existing entries in this block use `>=` safely only because neither of those packages has a newer major; that is not a pattern to copy here.

Do not add a `pnpm.overrides` key to `web/package.json`, and do not add a second `overrides:` key anywhere in the YAML file — pnpm 10+ reads overrides from `pnpm-workspace.yaml`, and a duplicate key is a parse error. Leave the existing 3-line comment above the block alone; it already explains why this mechanism exists, and per CLAUDE.md's comment discipline this change does not need its own second explanation.

Then regenerate the lockfile with the pinned resolver: `corepack pnpm@11.8.0 --dir web install --lockfile-only`. The version pin is load-bearing — it is what Dockerfile:29 installs, and RESEARCH measured plain `corepack pnpm` (11.25.0 here) producing a 240-line diff carrying 86 unrelated peer-suffix annotations versus 66 clean lines from 11.8.0. `--lockfile-only` leaves `node_modules` untouched; Task 2 does the real install. Do not reach for `pnpm dedupe` — it collapses duplicate versions, it does not apply a new override.

Inspect `git diff --stat web/` before running the gates. Expect only the two files, with the workspace file at +2/-0. If the workspace file instead shows a whole-file rewrite, the editor wrote LF into a CRLF file: revert it and redo the edit preserving the file's existing CRLF terminators (quick task 260901-lvn is the precedent for this hazard).

Finally re-run the plan-time Trivy check against the regenerated lockfile and confirm it now reports zero findings.
  </action>
  <verify>
    <automated>test "$(grep -c '^overrides:' web/pnpm-workspace.yaml)" = 1</automated>
    <automated>grep -qE "^  browserslist: '\^4\.28\.7'$" web/pnpm-workspace.yaml</automated>
    <automated>grep -qE "^  fast-uri: '\^3\.1\.6'$" web/pnpm-workspace.yaml</automated>
    <automated>test "$(git diff --numstat -- web/pnpm-workspace.yaml | cut -f1,2)" = "$(printf '2\t0')"</automated>
    <automated>! grep -q '^  fast-uri@4\.' web/pnpm-lock.yaml</automated>
    <automated>test "$(grep -c '^  fast-uri@3\.' web/pnpm-lock.yaml)" = 2</automated>
    <automated>! grep -q '^  browserslist@4\.28\.2:' web/pnpm-lock.yaml</automated>
    <automated>test "$(grep -c '^  browserslist@4\.' web/pnpm-lock.yaml)" = 2</automated>
    <automated>test "$(git diff --numstat -- web/pnpm-lock.yaml | awk '{print $1+$2}')" -lt 120</automated>
    <automated>! git diff -- web/pnpm-lock.yaml | grep -q 'supports-color@7\.2\.0'</automated>
    <automated>D="$(mktemp -d)" && cp web/pnpm-lock.yaml "$D/" && MSYS_NO_PATHCONV=1 docker run --rm -v "$(cygpath -m "$D"):/scan" aquasec/trivy:0.70.0 fs --scanners vuln --severity CRITICAL,HIGH --exit-code 1 /scan; RC=$?; rm -rf "$D"; exit $RC</automated>
  </verify>
  <done>
`web/pnpm-workspace.yaml` carries four override entries under a single `overrides:` key, the two new ones caret-ranged, and its diff is +2/-0. The lockfile pins `browserslist` >= 4.28.7 and `fast-uri` in the 3.1.x line with no 4.x entry, and its diff is under 120 lines with no `(supports-color@7.2.0)` churn. Trivy 0.70.0 at `CRITICAL,HIGH` reports `Total: 0` and exits 0 against the regenerated lockfile — down from the 6 HIGH / exit 1 reproduced at plan time. Nothing is committed yet.
  </done>
  <reversibility rating="reversible">Reverting two YAML lines and `git checkout -- web/pnpm-lock.yaml` restores the prior state exactly. No released artifact, no runtime state, and no schema depends on it.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Run the full-install and Definition-of-Done gates, refresh the embedded SPA, and land one commit</name>
  <files>internal/webassets/build/client/ (only if `make web` changes it), web/pnpm-workspace.yaml, web/pnpm-lock.yaml</files>
  <read_first>
    `.claude/CLAUDE.md` "Definition of Done" (lines 3-22) — the gate list, the frontend-format-first rule, the no-AI-attribution rule, and the never-`--no-verify` rule. `Makefile:139-144` (the `web` target) — confirm it still installs frozen, builds, and replaces the committed client tree.
  </read_first>
  <action>
Prove CI's install path accepts the new lockfile, then run the gates, then commit once.

First run a real frozen install to refresh `node_modules` and mirror what `frontend-test` and the Dockerfile do: `corepack pnpm@11.8.0 --dir web install --frozen-lockfile`. A non-zero exit here means the lockfile and manifest disagree and the CI job would fail the same way — stop and surface it rather than regenerating with `--no-frozen-lockfile`.

Then the frontend gates: vitest via `corepack pnpm --dir web test`, and the format check via `corepack pnpm --dir web exec prettier --check "**/*.{ts,tsx}"`. Nothing in this change touches a `.ts`/`.tsx` file and YAML/JSON manifests are outside that glob, so no reformat should be needed — but CLAUDE.md requires the check on any `web/` change, so run it. If it somehow reports a file, run the `--write` form before staging rather than hand-editing.

Then `make web`, and check `git status --short internal/webassets/` afterwards. A browserslist/caniuse-lite bump can shift transpile targets and move the content-hashed chunk filenames; precedent commit `f6647ec` re-ran this target and committed the result after the last override bump. This tree is NOT CI-blocking — the Dockerfile deliberately builds `web/` itself (Dockerfile:7-13) and no workflow job diffs it — its sole purpose is letting a Node-less clone `go build`. If it changed, stage it into the same commit; if it did not, that is equally fine and worth recording in the SUMMARY.

Then the backend Definition-of-Done gates from CLAUDE.md — `go vet ./...`, `golangci-lint run`, `make test` (after `make db-up`), `make coverage-gate`, `make sqlc-check`. All are structural no-ops for a lockfile-only change, but DoD requires every commit to clear them and this is the run that proves the tree was green when the commit landed. The documented `-race`/cgo limitation on this machine applies as usual; substitute as prior phases did and record the substitution rather than skipping silently.

Finally, ONE commit covering `web/pnpm-workspace.yaml`, `web/pnpm-lock.yaml`, and `internal/webassets/build/client/` if it moved. Conventional-commit `fix(deps):` scope, subject naming both packages and the `trivy-fs` gate they unblock; body may cite the CVE IDs. Let the pre-commit hooks run — never `--no-verify`. No `Co-Authored-By`, no `Claude-Session:`, no `Generated with` trailer of any kind, regardless of session defaults.
  </action>
  <verify>
    <automated>corepack pnpm@11.8.0 --dir web install --frozen-lockfile</automated>
    <automated>corepack pnpm --dir web test</automated>
    <automated>corepack pnpm --dir web exec prettier --check "**/*.{ts,tsx}"</automated>
    <automated>go vet ./...</automated>
    <automated>golangci-lint run</automated>
    <automated>make coverage-gate</automated>
    <automated>make sqlc-check</automated>
    <automated>git status --short web/ internal/webassets/</automated>
    <automated>! git log -1 --format=%B | grep -qiE 'co-authored-by|claude-session|generated with'</automated>
    <automated>git show --stat --format= HEAD</automated>
  </verify>
  <done>
`pnpm install --frozen-lockfile` exits 0 against the regenerated lockfile; vitest and `prettier --check` are green; `make web` has run and any change to `internal/webassets/build/client/` is staged; the backend DoD gates are green (or their documented machine-limitation substitutions recorded). Exactly one `fix(deps):` commit exists carrying `web/pnpm-workspace.yaml` + `web/pnpm-lock.yaml` (+ the webassets tree if it moved), with no AI-attribution trailer, created without `--no-verify`. `git status --short web/ internal/webassets/` is clean afterwards.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close the source todo and file the two follow-ups</name>
  <files>.planning/todos/pending/2026-09-05-bump-frontend-deps-to-clear-trivy-fs-high-cves.md, .planning/todos/pending/2026-09-05-delete-stale-web-package-lock-json.md, .planning/todos/pending/2026-09-05-move-shadcn-out-of-frontend-dependencies.md</files>
  <read_first>
    Any one file in `.planning/todos/completed/` for the frontmatter shape (`created`, `title`, `area`, `severity`, `files`, then `## Problem` / `## Solution`).
  </read_first>
  <action>
Move `.planning/todos/pending/2026-09-05-bump-frontend-deps-to-clear-trivy-fs-high-cves.md` to `.planning/todos/completed/` unchanged (`git mv`) — the repo's convention is to relocate the file with its frontmatter intact, not to rewrite it. Note in the SUMMARY that the CI-green half of its own verification checklist (step 4, scratch-branch push) is still outstanding.

Then create two new pending todos, both dated today, both `area: tooling`:

The first covers deleting the stale tracked `web/package-lock.json`. Record that it is a second, drifting npm lockfile living beside the authoritative pnpm one; that Trivy scans it too (`scan-ref: .` is repo-wide); that it contributes zero of the six findings this task fixed — it already carries fixed versions of both packages — but that nobody maintains it, so it is a live source of future findings and of confusion about which lockfile is authoritative. Severity minor.

The second covers moving `shadcn` from `dependencies` to `devDependencies` in `web/package.json`. This one must carry an explicit warning, because it is the trap this task walked past: Trivy excludes devDependencies by default for pnpm lockfile v9, so reclassifying `shadcn` makes findings under it *disappear from the report while the vulnerable versions remain pinned in the lockfile*. RESEARCH confirmed this empirically — the scratch reclassification scanned clean while still pinning the vulnerable versions. Record it as a correctness cleanup only (`shadcn` is a CLI with a `bin` entry, invoked as `npx shadcn@latest add ...` per `web/README.md`, imported by no app code), state that it must never substitute for a version fix, and note the related open question of whether the pinned CLI belongs in the manifest at all. Severity minor.

Commit these separately from the fix, as a `docs:` commit — the fix commit stays scoped to the two dependency files.
  </action>
  <verify>
    <automated>test -f .planning/todos/completed/2026-09-05-bump-frontend-deps-to-clear-trivy-fs-high-cves.md</automated>
    <automated>! test -f .planning/todos/pending/2026-09-05-bump-frontend-deps-to-clear-trivy-fs-high-cves.md</automated>
    <automated>test -f .planning/todos/pending/2026-09-05-delete-stale-web-package-lock-json.md</automated>
    <automated>test -f .planning/todos/pending/2026-09-05-move-shadcn-out-of-frontend-dependencies.md</automated>
    <automated>grep -qi 'never substitute' .planning/todos/pending/2026-09-05-move-shadcn-out-of-frontend-dependencies.md</automated>
    <automated>grep -qi 'devdependencies' .planning/todos/pending/2026-09-05-move-shadcn-out-of-frontend-dependencies.md</automated>
    <automated>git status --short .planning/todos/</automated>
  </verify>
  <done>
The source todo is in `completed/` with its content unchanged; two new pending todos exist, the `shadcn` one explicitly stating that reclassification hides findings rather than fixing them and must never stand in for a version bump. One `docs:` commit, separate from the fix commit, no AI attribution.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → `web/pnpm-lock.yaml` | new package versions and integrity hashes enter the repo from a third party |
| lockfile → `trivy-fs` gate → `build-scan`/`release` | the lockfile's contents decide whether the publish half of the pipeline can run |
| lockfile → Dockerfile SPA build stage | the same file drives `pnpm install --frozen-lockfile` inside the released image build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-QT-01 | Tampering | override → transitive resolution | high | mitigate | Caret ranges keep both packages inside their parents' declared ranges. The `fast-uri@4` negative gate in Task 1 is the specific guard: a `>=` override resolves to 4.1.4, which is patched (so Trivy passes) but violates `ajv@8.20.0`'s declared 3.x range — a silent break the vulnerability scanner cannot see. |
| T-QT-02 | Tampering | supply chain, new lockfile entries | medium | mitigate | No new *packages* are introduced — only version bumps of already-present transitives, each with a registry integrity hash written by pnpm. pnpm 11's own supply-chain policy check runs during resolution. Both target versions were OSV-checked clean during research. Not a package-legitimacy-gate case (no new dependency is added to any manifest). |
| T-QT-03 | Denial of Service (of the gate) | `trivy-fs` → `build-scan` → `release` | high | mitigate | This task exists to clear the gate honestly. The explicit rejection of the two theater fixes — reclassifying `shadcn` to devDependencies, and weakening the trivy-action config — is what keeps the gate meaningful; both are recorded as non-solutions in RESEARCH §3b/§3d, and Task 3's todo carries the warning forward so a future reader cannot mistake one for the fix. |
| T-QT-04 | Tampering | `internal/webassets/build/client/` | low | accept | The committed SPA tree can drift from the lockfile. Accepted: the Dockerfile deliberately rebuilds `web/` rather than trusting this tree (Dockerfile:7-13), so drift cannot reach the released image; the tree exists only so a Node-less clone can `go build`. Task 2 refreshes it anyway to keep that path honest. |
| T-QT-05 | Information Disclosure | commit metadata | low | mitigate | Pre-commit gitleaks runs on both commits (never `--no-verify`). No secret-bearing file is touched — the change is two YAML lines, a lockfile, and generated static assets. |
</threat_model>

<verification>
Definition of Done (`.claude/CLAUDE.md`). This change is dependency metadata plus generated assets — no Go source, no `.ts`/`.tsx`, no migrations — so several gates are structural no-ops, but all are named and run before the fix commit:

1. `go vet ./...` — no-op, must stay clean.
2. `golangci-lint run` — no-op, must stay clean.
3. `make test` (after `make db-up`) — no-op; the documented `-race`/cgo limitation on this machine applies, substitute as prior phases did and record it.
4. `make coverage-gate` — unchanged denominator, must still clear the 80% floor.
5. `make sqlc-check` — no-op (no `queries/` change).
6. Frontend: `corepack pnpm --dir web exec prettier --check "**/*.{ts,tsx}"` and `corepack pnpm --dir web test` — both required, both expected green untouched.
7. `pnpm install --frozen-lockfile` under `web/` — the CI-parity gate specific to this change; a failure here is a `frontend-test` failure and a Docker build failure.
8. Trivy 0.70.0 `fs --scanners vuln --severity CRITICAL,HIGH --exit-code 1` against the regenerated lockfile — the gate this whole task exists to flip, reproduced red at plan time so a green result is meaningful.
9. Hooks run normally on both commits. Never `--no-verify`. No AI attribution trailers.
</verification>

<success_criteria>
- Trivy 0.70.0 at `CRITICAL,HIGH` reports `Total: 0` and exits 0 on the regenerated `web/pnpm-lock.yaml` — the plan-time baseline of `Total: 6 (HIGH: 6, CRITICAL: 0)` / exit 1 is cleared.
- `fast-uri` sits in the 3.1.x line and `browserslist` at >= 4.28.7; no 4.x `fast-uri` entry exists anywhere in the lockfile.
- `web/pnpm-workspace.yaml` shows a +2/-0 diff under one `overrides:` key; the lockfile diff is under 120 lines and free of peer-suffix churn.
- `pnpm install --frozen-lockfile`, vitest, and `prettier --check` are all green under `web/`.
- Exactly two commits: one `fix(deps):` for the dependency files (+ webassets if it moved), one `docs:` for the todos. Neither carries an AI-attribution trailer, and neither used `--no-verify`.
- Two follow-up todos are on disk, the `shadcn` one explicitly framed as cleanup that must never substitute for a version fix.
</success_criteria>

<output>
Create `.planning/quick/260905-fa4-bump-frontend-transitive-deps-to-clear-t/260905-fa4-SUMMARY.md` when done.

Record in the summary:
- The exact two lines added to `overrides:` and the exact resolved versions the lockfile landed on for both packages.
- The measured lockfile diff size, and confirmation it stayed inside the browserslist family plus the two targets.
- Whether `make web` changed `internal/webassets/build/client/`, and if so that it went into the same commit.
- Any substitution made for a backend DoD gate blocked by this machine's documented cgo/`-race` limitation.
- **Explicitly: this closes the local reproduction only.** Trivy going green here proves the fix on the same scanner version CI runs, but the CI-green confirmation is still pending a scratch-branch push showing `trivy-fs` green and `build-scan` actually running — the same live-proof pattern Phase 16's G-16-1 and Phase 09's coverage-gate verification used. Do not mark the source todo's verification checklist fully satisfied until that push is observed; note the two follow-up todos filed in Task 3 as the remaining frontend-dependency hygiene work.
</output>
