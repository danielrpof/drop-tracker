---
phase: quick/260905-et1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/full-pipeline.yml
  - internal/db/migrations/README.md
  - .planning/phases/16-rollback-safe-migrations/16-CONTEXT.md
autonomous: true
requirements: [QUICK-260905-et1, G-16-1]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On a migration-touching branch whose N-1 release image predates the ahead-of-source guard, `n1-boot` finishes GREEN and its run log carries a `::notice::` naming the guard-adoption window and G-16-1 — no throwaway Postgres starts, no ghcr.io pull happens, no health/read/write probe runs."
    - "Every `n1-boot` step that gates on the true-bootstrap step's output ALSO gates on the new guard-adoption step's output; neither gate alone can let an expensive step run."
    - "`Teardown` still runs on `always() && needs.changes.outputs.migrations_changed == 'true'` — unchanged, so a partially-started run is still cleaned up."
    - "The two skip reasons stay independently readable in the run log: 'no prior release tag' comes from the true-bootstrap step, 'prior release predates the guard' from the new step, each with its own step name and its own message."
    - "The fix self-clears with zero workflow edits: once a release carrying `func maxSourceVersion(` becomes the resolved N-1 tag, the same job boots the image and runs all probes again."
    - "If the grep token itself goes stale (the function is renamed at HEAD), `n1-boot` turns RED with a message naming the coupling — it never rots into a permanent silent skip."
    - "`actionlint .github/workflows/full-pipeline.yml` exits 0 (verified clean on the pre-change file at plan time, so any new finding is this change's)."
    - "`internal/db/migrations/README.md` documents the guard-adoption window under the N-1 invariant, and `go test ./internal/db/ -run TestMigrationsReadme_ContainsRequiredPhrases` stays green."
  artifacts:
    - .github/workflows/full-pipeline.yml
    - internal/db/migrations/README.md
    - .planning/phases/16-rollback-safe-migrations/16-CONTEXT.md
  key_links:
    - "`steps.prevtag.outputs.tag` → the new step's `PREV_TAG` env var → `git show \"$PREV_TAG:internal/db/migrate.go\"`. Passed through `env:` only, never string-interpolated into the `run:` body (16-CONTEXT T-16-21; same shape the existing `Pull previous release image` step uses)."
    - "The grep token → `func maxSourceVersion(` at `internal/db/migrate.go:326`. This is a deliberate coupling between a workflow step and a Go identifier; the step self-tests the token against the checked-out tree so a rename fails loudly instead of silently skipping forever."
    - "The new step's `proceed` output → the `if:` of all 8 downstream `n1-boot` steps. Missing it on even one step reintroduces G-16-1 for that step; the pairing grep in Task 1's verify is what proves none was missed."
    - "Checkout already uses `fetch-depth: 0` + `fetch-tags: true` (16-CONTEXT D-13/S8, hard requirement) — `git show` at a tag only works because of this. Do not weaken it."
---

<objective>
Stop `n1-boot` from reporting a false N-1 rollback break on every migration-touching branch during the guard-adoption window.

Purpose: closes Phase 16 gap G-16-1. `n1-boot` boots `ghcr.io/danielrpof/drop-tracker:$PREV_TAG` (today `v1.7.0`) and runs *that* binary's embedded `RunMigrations` against the HEAD schema. `v1.7.0` was built before Phase 16 added its own ahead-of-source no-op guard (`maxSourceVersion` / `runMigrationsWithSource`, `internal/db/migrate.go`), so golang-migrate hard-errors `no migration found for version 8: ...` no matter what the migration does. Live evidence: a destructive `DROP COLUMN` (run 33974299591) and a purely additive nullable `ADD COLUMN` (run 33975084671) fail *identically*. The job has no discriminating signal at all until a guard-carrying release becomes the N-1 rollback target, so a blocking red there is noise, not information.

Output: a new gated step in `n1-boot` that detects the pre-guard N-1 image statically and skip-greens with an explanatory `::notice::`, ANDed into every expensive step's `if:`; plus a guard-adoption-window note in the migrations README and a dated amendment in `16-CONTEXT.md`. No Go source changes.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/quick/260905-et1-n1-boot-skip-greens-when-the-previous-re/260905-et1-CONTEXT.md
@.planning/phases/16-rollback-safe-migrations/16-UAT.md
@.github/workflows/full-pipeline.yml
@internal/db/migrations/README.md
</context>

<!-- planner-discipline-allow: steps.bootstrap.outputs.proceed, steps.guardcheck.outputs.proceed, maxSourceVersion -->
<!-- Rationale: this plan's subject is a gate expression and a grep token, so the actions cannot avoid naming them verbatim. Task 1's pairing gate greps `.github/workflows/full-pipeline.yml` only; this plan file lives under `.planning/quick/`, so no occurrence here can satisfy or invalidate it. -->

<working_tree_note>
`git status` at plan time (2026-09-05) shows exactly one untracked file, `.planning/milestone.lock`, and no modifications to any of the three files this plan edits. Re-check `git status --short` before starting; if any of the three targets is already dirty, stop and surface it rather than folding unknown changes into these commits.
</working_tree_note>

<grounding>
Facts verified against the live working tree at plan time — do not re-derive, but do re-confirm the line anchors before editing (they shift once Task 1 inserts its step):

- `n1-boot` starts at `.github/workflows/full-pipeline.yml:380`. `True-bootstrap guard` (`id: bootstrap`) is lines 408-419; its `run:` block ends at line 419. `Start throwaway Postgres` begins at line 420.
- Exactly 8 steps currently carry `&& steps.bootstrap.outputs.proceed == 'true'` (pre-insertion line of the `if:`): Start throwaway Postgres (421), Apply HEAD schema (439), Pull previous release image (447), Run previous release image (459), Health probe -- sustained green (472), Read probes -- GET /watchlist and GET /events (491), Write probe -- POST /watchlist then re-GET (504), N-1 boot failure -- explain the rule (521).
- Two steps gate on `needs.changes.outputs.migrations_changed == 'true'` alone and stay that way: `Resolve previous release tag` (398) and `True-bootstrap guard` (410).
- `Teardown` (525) gates on `always() && needs.changes.outputs.migrations_changed == 'true'` and stays exactly that.
- `actionlint .github/workflows/full-pipeline.yml` exits 0 with no output on the unmodified file.
- `git show "v1.7.0:internal/db/migrate.go" | grep -c '^func maxSourceVersion('` → `0`. Same grep against `HEAD:internal/db/migrate.go` → `1`. `git show` against a nonexistent tag exits 128.
- `go test ./internal/db/ -run TestMigrationsReadme_ContainsRequiredPhrases -count=1` → `ok`, and needs no Postgres.
- The README's phrase test (`internal/db/migrations_readme_test.go`) is presence-only with a 60-line floor, so Task 2's addition can only help it. Do not remove any existing README phrase.
</grounding>

<tasks>

<task type="tracer">
  <name>Task 1: Add the guard-adoption gate to n1-boot and AND it into every N-1 boot step</name>
  <files>.github/workflows/full-pipeline.yml</files>
  <read_first>
    `.github/workflows/full-pipeline.yml` lines 373-528 (the whole `n1-boot` job). The `True-bootstrap guard` step at 408-419 is the exact pattern to mirror: step-level `if:` on `migrations_changed`, tag supplied through `env:`, a shell branch that writes `proceed=false` plus a `::notice::` on the skip path and `proceed=true` otherwise, no `set -e`.
  </read_first>
  <action>
Insert ONE new step immediately after `True-bootstrap guard` (after its `run:` block ends, before `- name: Start throwaway Postgres`), then AND its output into the 8 steps enumerated in `<grounding>`.

The new step: name it so the run log reads as its own distinct skip reason (e.g. `Guard-adoption check -- does the N-1 image carry the ahead-of-source guard?`), give it `id: guardcheck`, and gate it on `if: needs.changes.outputs.migrations_changed == 'true'` — the same condition the true-bootstrap step carries, per the locked step-placement decision. Supply the tag through `env: PREV_TAG: ${{ steps.prevtag.outputs.tag }}` and never string-interpolate it into the `run:` body (16-CONTEXT T-16-21). Always quote `"$PREV_TAG"`.

The `run:` body is a five-branch if/elif chain writing `proceed=true` or `proceed=false` to `"$GITHUB_OUTPUT"`, in this order:

1. `PREV_TAG` empty → write `proceed=false` and echo a plain (non-`::notice::`) line saying the true-bootstrap step already reported this. No second notice — the bootstrap step owns that message, and a duplicate would read as a third, invented skip reason.
2. The token is absent from the CHECKED-OUT tree (`grep -q '^func maxSourceVersion(' internal/db/migrate.go` fails) → emit `::error::` and `exit 1`. This is the detector self-test: the token being gone at HEAD means the grep is stale, not that a release predates the guard, and a stale grep would skip-green this job forever. The message must name `internal/db/migrate.go`, the identifier being grepped, and say the workflow's token needs updating.
3. `git show "$PREV_TAG:internal/db/migrate.go"` fails (capture into a shell variable inside the `if !` condition so `git show`'s own exit status drives the branch; send its stderr to `/dev/null`) → `::notice::` + `proceed=false`. Fail-safe, same direction as D-04's empty-tag handling. Message must be distinct from branch 4's: say the previous release's `internal/db/migrate.go` could not be read at that tag and is therefore assumed pre-guard.
4. That captured content does not match `^func maxSourceVersion(` → `::notice::` + `proceed=false`. This is the real G-16-1 path. The message must state: the previously-released image (naming `$PREV_TAG`) predates the ahead-of-source migration guard, so it cannot boot against any newer schema and N-1 rollback boot is not verifiable this cycle; cite `G-16-1`; and say it self-clears once a guard-carrying release becomes N-1. Anchor the grep to the function definition (`^func maxSourceVersion(`) rather than a bare substring, so a passing mention in a comment at that tag can never be mistaken for the guard itself.
5. Otherwise → `proceed=true`, no output.

Add a comment above the step in the style the rest of this job already uses: 3 lines max, one design-doc reference (`G-16-1`), stating why the skip exists and that it self-clears. Do not re-argue the decision — the README and `16-CONTEXT.md` (Task 2) are the source of truth. Do not name either `proceed` output expression inside a YAML comment; the pairing gate below is line-based and a comment mentioning one token but not the other would trip it.

Then edit each of the 8 steps listed in `<grounding>` so its `if:` reads `needs.changes.outputs.migrations_changed == 'true' && steps.bootstrap.outputs.proceed == 'true' && steps.guardcheck.outputs.proceed == 'true'` (the failure explainer keeps its leading `failure() &&`). Append the new clause — do not reorder or reword the existing two, and do not refactor the three into a single composed boolean output: the flat AND keeps both skip reasons visible at each call site and keeps the diff mechanical. Change nothing else: `Checkout`, `Set up Go`, `Resolve previous release tag`, `True-bootstrap guard`, and `Teardown` keep their current `if:` verbatim, and `Teardown` in particular must stay on `always() && needs.changes.outputs.migrations_changed == 'true'` so a half-started run still tears down.
  </action>
  <verify>
    <automated>actionlint .github/workflows/full-pipeline.yml</automated>
    <automated>test "$(grep -c "steps.guardcheck.outputs.proceed == 'true'" .github/workflows/full-pipeline.yml)" = 8</automated>
    <automated>test "$(grep -c "steps.bootstrap.outputs.proceed == 'true'" .github/workflows/full-pipeline.yml)" = 8</automated>
    <automated>grep -n "steps\.bootstrap\.outputs\.proceed" .github/workflows/full-pipeline.yml | grep -v "steps\.guardcheck\.outputs\.proceed"; test $? -eq 1</automated>
    <automated>awk '/id: guardcheck/{g=NR} /id: bootstrap/{b=NR} /name: Start throwaway Postgres/{p=NR} END{exit !(b>0 && g>b && p>g)}' .github/workflows/full-pipeline.yml</automated>
    <automated>grep -q "always() && needs.changes.outputs.migrations_changed == 'true'" .github/workflows/full-pipeline.yml</automated>
    <automated>git show "v1.7.0:internal/db/migrate.go" | grep -c '^func maxSourceVersion(' ; test $? -eq 1</automated>
    <human-check>Re-trace the new step's shell by hand against the three cases: `PREV_TAG=v1.7.0` (token absent → notice + proceed=false), `PREV_TAG` set to a tag that does carry the guard (proceed=true), and `PREV_TAG` set to a nonexistent tag (git show exits 128 → fail-safe notice + proceed=false). Confirm each writes to `"$GITHUB_OUTPUT"` and that no branch can fall through without writing `proceed`.</human-check>
  </verify>
  <done>
`n1-boot` carries a `guardcheck` step between `bootstrap` and `Start throwaway Postgres`; exactly 8 steps gate on all three conditions; every line referencing the bootstrap output also references the guardcheck output; `Teardown` is untouched; `actionlint` exits 0. Committed as a single `fix(16):` commit.
  </done>
  <reversibility rating="reversible">Deleting the step and dropping the appended clause from 8 `if:` lines restores the current behavior exactly; no state, no artifact, no released binary depends on it.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Document the guard-adoption window in the migrations README and amend 16-CONTEXT</name>
  <files>internal/db/migrations/README.md, .planning/phases/16-rollback-safe-migrations/16-CONTEXT.md</files>
  <read_first>
    `internal/db/migrations/README.md` §"The N-1 invariant" (lines 32-53) — the two-bullet list naming the `n1-boot` CI job and the `internal/db` ahead-of-source test is where the caveat belongs. `.planning/phases/16-rollback-safe-migrations/16-CONTEXT.md` `<revisions>` (lines 227-360) — an existing dated amendment block whose header convention (`### S<n> - <finding> -> D-<n> amended`) the new note should echo without imitating the grill-pass numbering.
  </read_first>
  <action>
README: append a short paragraph (roughly 6-9 lines, matching the file's existing voice) directly after the two-bullet "Two mechanisms prove this holds" list in §"The N-1 invariant". State that the `n1-boot` half is inert during a **guard-adoption window**: the ahead-of-source no-op guard shipped in the same release train that added `n1-boot`, so every already-released image predates it and errors at boot against any newer schema regardless of migration safety. While the resolved N-1 tag's `internal/db/migrate.go` does not carry that guard, `n1-boot` skip-greens with a notice in the run log rather than reporting a rollback break that isn't one, and the `internal/db` ahead-of-source test is the only live proof of the invariant in the meantime. Say it self-clears with no workflow change once a guard-carrying release becomes N-1. Keep every existing phrase in the file intact — a presence test pins them.

`16-CONTEXT.md`: append one dated section at the end of the `<revisions>` block, before `</revisions>`. Header along the lines of `## Amendment - 2026-09-05 (post-UAT, G-16-1)`. Record, in 5-8 lines: Phase 16 UAT found `n1-boot` reds on every migration-touching branch because the N-1 image predates the guard the phase itself introduced (cite the two run URLs from `16-UAT.md`); D-04/D-19a's skip-green set is therefore widened from one condition to two — no prior tag, and prior tag predates the guard — implemented as a second dedicated in-job step, not a job-level `if:`, so the two reasons stay independently readable; D-11's blocking guarantee is unchanged in shape but temporarily has no discriminating signal from the boot half, with the static `cmd/migration-check` guard and the `internal/db` ahead-of-source test carrying the phase's guarantee until the window closes. Note it self-clears. Do not edit D-04, D-11, or D-19a in place — the file's own convention is that revisions are appended and the revision wins.
  </action>
  <verify>
    <automated>go test ./internal/db/ -run TestMigrationsReadme_ContainsRequiredPhrases -count=1</automated>
    <automated>grep -qi "guard-adoption" internal/db/migrations/README.md</automated>
    <automated>grep -q "G-16-1" .planning/phases/16-rollback-safe-migrations/16-CONTEXT.md</automated>
    <automated>grep -c "</revisions>" .planning/phases/16-rollback-safe-migrations/16-CONTEXT.md | grep -qx 1</automated>
    <automated>git diff --stat -- internal/db/migrations/README.md .planning/phases/16-rollback-safe-migrations/16-CONTEXT.md</automated>
  </verify>
  <done>
README §"The N-1 invariant" explains the guard-adoption window and its self-clearing condition without dropping any pinned phrase; `16-CONTEXT.md` carries one appended dated amendment inside `<revisions>` with D-04/D-19a and D-11 named and the original decision text unedited; the README phrase test is green. Committed as a single `docs(16):` commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git tag → CI shell | `svu current` resolves a tag from repo history into `$PREV_TAG`, which reaches a `git show` invocation |
| released image → this branch's schema | the thing `n1-boot` exists to test; this change decides when that test is meaningful |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-QT-01 | Tampering | `guardcheck` step `run:` body | medium | mitigate | `PREV_TAG` reaches the shell only via `env:`, never string-interpolated into `run:` (16-CONTEXT T-16-21), and is always double-quoted. Same shape as the existing `Pull previous release image` step. |
| T-QT-02 | Spoofing | guard-token grep | medium | mitigate | Anchored `^func maxSourceVersion(` definition match, not a loose substring — a comment or string mentioning the name at the previous tag cannot forge a "guard present" reading and let the job proceed into a known-bad boot. |
| T-QT-03 | Repudiation | skip-green path | medium | mitigate | Every skip path emits a `::notice::` naming the reason, the tag, `G-16-1`, and the self-clearing condition, so a green `n1-boot` is never silently unverified. The two skip reasons keep separate step names and separate messages. |
| T-QT-04 | Denial of Service (of the gate itself) | workflow↔Go-identifier coupling | high | mitigate | Renaming `maxSourceVersion` would otherwise make the grep fail forever and skip-green `n1-boot` permanently. Branch 2 self-tests the token against the checked-out tree and fails the job loudly with a message naming the coupling, converting a silent permanent skip into an immediate red. |
| T-QT-05 | Elevation of Privilege | weakened merge gate | medium | accept | This change deliberately narrows when `n1-boot` can block a merge. Accepted because the job currently produces a constant red carrying zero signal (16-UAT.md G-16-1: identical failure for a destructive and a purely additive migration); `cmd/migration-check`'s static guard and the `internal/db` ahead-of-source test stay blocking and unmodified throughout the window. |
</threat_model>

<verification>
Definition of Done (`.claude/CLAUDE.md`). This change is workflow-YAML + Markdown only — no Go source, no `web/` files, no migrations — so most gates are structurally no-ops but are still named and run:

1. `go vet ./...` — no-op (no Go changes), must stay clean.
2. `golangci-lint run` — no-op, must stay clean.
3. `make test` (after `make db-up`) — the only genuinely affected test is `TestMigrationsReadme_ContainsRequiredPhrases`; Task 2's verify runs it directly, the full suite confirms nothing else regressed.
4. `make coverage-gate` — unchanged denominator, must still clear the 80% floor.
5. `make sqlc-check` — no-op (no `queries/` change).
6. Frontend prettier/vitest — not applicable, nothing under `web/` changes.
7. `actionlint .github/workflows/full-pipeline.yml` — the real gate for this change; exits 0 on the pre-change file, so any finding is new.
8. Hooks run normally on both commits. Never `--no-verify`.
</verification>

<success_criteria>
- A migration-touching branch pushed while `v1.7.0` is the resolved N-1 tag produces a GREEN `n1-boot` whose log shows the guardcheck notice and shows Postgres/pull/probe steps as skipped, not run.
- A docs-only push still produces a GREEN `n1-boot` in runner spin-up time, unchanged (all steps skipped on `migrations_changed`).
- `build-scan` still lists `n1-boot` in `needs:` and both guard jobs still finish `success`, never `skipped` — the RESEARCH Finding 2 shape from 16-CONTEXT D-11/D-12 is untouched.
- No workflow edit is required to re-arm the check once a guard-carrying release becomes N-1.
- Two commits: `fix(16):` for the workflow, `docs(16):` for the docs. No AI attribution trailers in either.
</success_criteria>

<output>
Create `.planning/quick/260905-et1-n1-boot-skip-greens-when-the-previous-re/260905-et1-SUMMARY.md` when done.

Record in the summary: the exact notice strings as shipped, the final `if:` expression used on the 8 steps, and a note that live confirmation is still pending — this fix cannot be proven green until the next migration-touching push, and the Phase 16 UAT gap G-16-1 should be re-checked against that run before it is marked resolved.
</output>
