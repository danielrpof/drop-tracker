---
phase: quick-260824-23o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/poller/poller.go
autonomous: true
requirements: [QUICK-260824-23o]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low   # 0 calibration samples for this project; default factor applied

must_haves:
  truths:
    - "RunMusicBrainzCycle and RunDeezerCycle both delegate their bounded-fan-out-with-overlap-guard logic (CAS guard, semaphore worker pool, cancellation handling, panic recovery, cycle-complete logging, notify-inline) to one private runCycle method; neither method declares its own semaphore, WaitGroup, or cycleErr variable anymore."
    - "Deezer's nil-DeezerID skip case still applies to RunDeezerCycle only -- it runs before the semaphore is acquired via a shouldDispatch closure, with the identical log message and attributes as today -- and MusicBrainz gains no equivalent skip behavior."
    - "internal/poller/poller_test.go is byte-for-byte unchanged and every test in it passes, including the four tests that read p.mbRunning.Load()/p.dzRunning.Load() directly."
    - "No exported API on Poller changed: RunMusicBrainzCycle(ctx) error and RunDeezerCycle(ctx) error keep their exact signatures, ErrCycleInProgress and all Option funcs are untouched."
    - "No package outside internal/poller needs a call-site change -- cmd/server/main.go and internal/detection/detector.go only reference these method names in comments, never call them directly."
  artifacts:
    - "internal/poller/poller.go"
  key_links:
    - "RunMusicBrainzCycle -> p.runCycle(ctx, &p.mbRunning, sourceMusicBrainz, p.mbWorkers, shouldDispatch, fetchAndRecord)"
    - "RunDeezerCycle -> p.runCycle(ctx, &p.dzRunning, sourceDeezer, p.dzWorkers, shouldDispatch, fetchAndRecord)"
    - "New()'s two cron.AddFunc closures -> RunMusicBrainzCycle/RunDeezerCycle -- unchanged call sites, since neither method's exported signature moves"
---

<objective>
Factor the ~150-line bounded-fan-out-with-overlap-guard pattern duplicated between `RunMusicBrainzCycle` and `RunDeezerCycle` in `internal/poller/poller.go` into one shared private `runCycle` method, leaving both public methods as thin per-source closures over it.

Purpose: A code-quality review found the two cycle methods independently implementing the same atomic-guard-plus-semaphore-plus-panic-recovery shape, differing only in which guard field, source name, entry ID field, fetch method, and EventRecorder method they use. Consolidating removes ~150 lines of duplication and gives future changes to the fan-out mechanics (e.g. a retry policy) one place to land instead of two kept in sync by hand.

Output: `internal/poller/poller.go` with a new `runCycle` method carrying the shared mechanics, and `RunMusicBrainzCycle`/`RunDeezerCycle` shrunk to building their own `shouldDispatch`/`fetchAndRecord` closures and delegating. No behavior change; `poller_test.go` (~2500 lines) passes completely unmodified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.claude/CLAUDE.md
@internal/poller/poller.go
</context>

<planning_findings>
Verified during planning by reading `internal/poller/poller.go` in full and grepping `internal/poller/poller_test.go` -- the executor should act on these, not re-derive them:

- **Both cycle methods share the exact same shape**, differing only in: which `atomic.Bool` guard field is used (`p.mbRunning` vs `p.dzRunning`), the source string (`sourceMusicBrainz` vs `sourceDeezer`), the worker count (`p.mbWorkers` vs `p.dzWorkers`), which entry field is read (`entry.MBID` vs `*entry.DeezerID`, with Deezer's nil-`DeezerID` skip check running before the semaphore is acquired -- MusicBrainz has no equivalent skip), which fetch method is called (`p.mb.ReleaseGroupsByArtist` vs `p.dz.ArtistAlbums`, the latter also passing `deezerAlbumPageSize`), the fetch-error log's attribute key (`musicbrainz_error` vs `deezer_error`), and which `EventRecorder` method is called (`DetectMusicBrainz` vs `DetectDeezer`). Everything else -- the CAS guard (`CompareAndSwap(false, true)` / `defer ....Store(false)`), the `cycleID`/per-cycle `logger` construction (`fmt.Sprintf("%s-%d", source, nextCycleID.Add(1))`), the `p.store.List(ctx)` call and its wrapped error, the semaphore (`make(chan struct{}, workers)`), the `dispatch:`-labeled loop with its `select { case sem <- struct{}{}: case <-ctx.Done(): }`, the per-worker panic-recovery `defer`/`recover()` logging `"poll worker panicked"`, the post-acquire `ctx.Err()` recheck, the post-`wg.Wait()` `cycleErr` recheck, the `"poll cycle complete"` log, and the final `p.notifier.NotifyPending(ctx, logger)` call (error logged as `"notify pending failed"`, never returned) -- is byte-identical between the two methods.
- **`internal/poller/poller_test.go` is package `poller` (whitebox)** and reads `p.mbRunning.Load()` (lines 1392, 1854, 2269) and `p.dzRunning.Load()` (line 1473) directly. These fields **must keep their exact names and `atomic.Bool` type on the `Poller` struct** -- the refactor must route through pointers to these existing fields (`&p.mbRunning`, `&p.dzRunning`), not rename, relocate, or replace them with a map/slice.
- **No test in `poller_test.go` reaches into any other unexported symbol** this refactor touches (no reference to a `runCycle`, `sem`, `wg`, or `cycleErr` identifier) -- confirmed by grepping the whole file for those patterns plus `.mb`/`.dz` field access. The file needs zero edits.
- **No package outside `internal/poller` calls either method.** `cmd/server/main.go` and `internal/detection/detector.go` both matched a grep for `RunMusicBrainzCycle`/`RunDeezerCycle`, but in both files every match is a doc-comment reference, not a call -- confirmed by reading the matched lines directly. The only real call sites are the two `cron.AddFunc` closures inside `poller.go`'s own `New()`, which call the public methods and are unaffected since neither method's exported signature changes.
- Module path is `github.com/danielrpof/drop-tracker`; no new imports, no `go.mod` changes, no new files. This is a same-file, same-package, behavior-preserving refactor.

<!-- planner-discipline-allow: cycleErr -->
</planning_findings>

<tasks>

<task type="auto">
  <name>Task 1: Introduce shared runCycle and migrate RunMusicBrainzCycle onto it</name>
  <files>internal/poller/poller.go</files>
  <action>
Add a new private method directly above `RunMusicBrainzCycle`:

`func (p *Poller) runCycle(ctx context.Context, running *atomic.Bool, source string, workers int, shouldDispatch func(entry watchlist.Entry, logger *slog.Logger) bool, fetchAndRecord func(ctx context.Context, logger *slog.Logger, entry watchlist.Entry)) error`

Its body reproduces RunMusicBrainzCycle's current body generalized over the five parameters, in this exact order: (1) the CAS guard using `running` instead of `p.mbRunning` -- on failure, `p.logger.Warn("skipping poll cycle: previous cycle still in progress", slog.String("source", source))` then `return ErrCycleInProgress`; on success, `defer running.Store(false)`; (2) `cycleID := fmt.Sprintf("%s-%d", source, nextCycleID.Add(1))`, `logger := p.logger.With(slog.String("source", source), slog.String("cycle_id", cycleID))`, `cycleStart := time.Now()`; (3) `entries, err := p.store.List(ctx)`, returning `fmt.Errorf("poller: list watchlist: %w", err)` on error, unchanged; (4) the bounded fan-out: `sem := make(chan struct{}, workers)`, `var wg sync.WaitGroup`, `var cycleErr error`, then the `dispatch:`-labeled `for _, entry := range entries` loop -- add exactly one new line at the top of the loop body, before the semaphore-acquire select: `if !shouldDispatch(entry, logger) { continue }`. Keep the rest of the loop unchanged: the `select { case sem <- struct{}{}: case <-ctx.Done(): cycleErr = ctx.Err(); break dispatch }`, the `wg.Add(1)` plus `go func(entry watchlist.Entry) { ... }(entry)` launch, the `defer wg.Done()`, `defer func() { <-sem }()`, the panic-recovery defer logging `"poll worker panicked"` with `artist_mbid`/`artist_name`/`panic_value`, and the post-acquire `if err := ctx.Err(); err != nil { return }` recheck -- but replace the goroutine body's fetch-and-record steps (the `p.mb.ReleaseGroupsByArtist` call, its result/error logging, and the `p.events.DetectMusicBrainz` call) with a single call: `fetchAndRecord(ctx, logger, entry)`; (5) after `wg.Wait()`: the `if cycleErr == nil { cycleErr = ctx.Err() }` recheck, the `"poll cycle complete"` info log with `artist_count`/`duration_ms`, `if cycleErr != nil { return cycleErr }`, then the `p.notifier.NotifyPending(ctx, logger)` call (error logged as `"notify pending failed"`, never returned), then `return nil`.

Move the substantive rationale comments currently attached inline to `RunMusicBrainzCycle` (why CAS not a mutex, why the panic-recovery defer exists, why the loop always `break`s to `dispatch` rather than `return`s so `wg.Wait()` is always reached, why the post-acquire and post-`wg.Wait()` `ctx.Err()` rechecks both exist, why notify is logged rather than returned) onto `runCycle`'s own doc comment and inline comments, since this is now the single place that logic lives -- do not leave a duplicate copy on `RunMusicBrainzCycle`.

Then rewrite `RunMusicBrainzCycle` itself down to: build a local `shouldDispatch` closure that unconditionally returns `true` (MusicBrainz has no skip case today); build a local `fetchAndRecord` closure reproducing exactly RunMusicBrainzCycle's current per-entry body -- call `p.mb.ReleaseGroupsByArtist(ctx, entry.MBID)`, on error log `"poll artist failed"` with `artist_mbid`/`artist_name`/`musicbrainz_error` and return, on success log `"poll result"` with `artist_mbid`/`artist_name`/`item_count` (from `len(groups)`), then call `p.events.DetectMusicBrainz(ctx, logger, entry, groups)`, logging `"detection failed"` with `artist_mbid`/`artist_name`/`detection_error` on error; then `return p.runCycle(ctx, &p.mbRunning, sourceMusicBrainz, p.mbWorkers, shouldDispatch, fetchAndRecord)`. Trim `RunMusicBrainzCycle`'s doc comment to describe only MusicBrainz-specific facts (the `ReleaseGroupsByArtist`/`DetectMusicBrainz` seam, that it has no skip case, `ErrCycleInProgress` semantics) and point to `runCycle` for the shared fan-out rationale instead of duplicating that prose.

Leave `RunDeezerCycle` completely untouched in this task -- it is migrated in Task 2. Do not edit `poller_test.go`.
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./...</automated>
    <automated>go test ./internal/poller/... -v</automated>
  </verify>
  <done>
`go build ./...` and `go vet ./...` succeed. `go test ./internal/poller/... -v` passes in full -- every MusicBrainz-specific test (overlap-guard skip, guard-released-after-error, panic recovery, per-artist error isolation) now exercises `runCycle` indirectly and still passes, and every Deezer test still passes unchanged against the still-untouched `RunDeezerCycle`. `RunMusicBrainzCycle`'s body no longer declares its own `sem`, `wg`, or `cycleErr` variables.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate RunDeezerCycle onto runCycle and verify no external regressions</name>
  <files>internal/poller/poller.go</files>
  <action>
Rewrite `RunDeezerCycle` the same way Task 1 rewrote `RunMusicBrainzCycle`: build a local `shouldDispatch` closure reproducing exactly today's nil-`DeezerID` skip check -- `if entry.DeezerID == nil { logger.Info("skipping deezer poll: no deezer_id", slog.String("artist_mbid", entry.MBID), slog.String("artist_name", entry.Name)); return false }; return true` -- so this skip keeps running before the semaphore is acquired (via `runCycle`'s `shouldDispatch` call site added in Task 1) and keeps applying to Deezer only, never MusicBrainz (MusicBrainz's `shouldDispatch` from Task 1 stays an unconditional `true`, untouched by this task). Build a local `fetchAndRecord` closure reproducing exactly RunDeezerCycle's current per-entry body -- call `p.dz.ArtistAlbums(ctx, *entry.DeezerID, deezerAlbumPageSize)`, on error log `"poll artist failed"` with `artist_mbid`/`artist_name`/`deezer_error` and return, on success log `"poll result"` with `artist_mbid`/`artist_name`/`item_count` (from `len(albums)`), then call `p.events.DetectDeezer(ctx, logger, entry, albums)`, logging `"detection failed"` with `artist_mbid`/`artist_name`/`detection_error` on error; then `return p.runCycle(ctx, &p.dzRunning, sourceDeezer, p.dzWorkers, shouldDispatch, fetchAndRecord)`. Trim `RunDeezerCycle`'s doc comment the same way Task 1 trimmed `RunMusicBrainzCycle`'s -- keep only the Deezer-specific facts (the `ArtistAlbums`/`DetectDeezer` seam, the nil-`DeezerID` skip and why there is no name-search fallback for it) and point to `runCycle` for the shared mechanics rationale.

After both methods delegate to `runCycle`, read back through the whole file and confirm no dead code remains: neither `RunMusicBrainzCycle` nor `RunDeezerCycle` should declare its own `sem`, `wg`, `cycleErr`, `cycleID`, or per-cycle `logger` variable anymore -- those exist only inside `runCycle`. Do not edit `poller_test.go` under any circumstances -- per `<planning_findings>`, it only reads `p.mbRunning.Load()`/`p.dzRunning.Load()`, both of which remain untouched fields with unchanged names and types on `Poller`, and it never references `runCycle` or any other symbol this refactor introduces. Finally, grep the rest of the module for any other caller of `RunMusicBrainzCycle`/`RunDeezerCycle` outside `internal/poller` to reconfirm this refactor's blast radius stayed inside `internal/poller/poller.go` (expect only comment references in `cmd/server/main.go` and `internal/detection/detector.go`, no actual calls).
  </action>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./...</automated>
    <automated>go test ./internal/poller/... -v</automated>
    <automated>git diff --name-only -- internal/poller/poller_test.go</automated>
    <automated>grep -n "chan struct{}, p\.\(mb\|dz\)Workers" internal/poller/poller.go</automated>
    <automated>grep -rn "RunMusicBrainzCycle\|RunDeezerCycle" --include=*.go . | grep -v "internal/poller"</automated>
  </verify>
  <done>
`go build ./...` and `go vet ./...` succeed across the whole module. `go test ./internal/poller/... -v` passes in full -- every pre-existing test, including the four that read `p.mbRunning.Load()`/`p.dzRunning.Load()` directly, passes unchanged. `git diff --name-only -- internal/poller/poller_test.go` outputs nothing, confirming the test file was never touched. The `chan struct{}, p.mbWorkers`/`chan struct{}, p.dzWorkers` grep returns zero matches, confirming both cycle methods no longer construct their own semaphore (only `runCycle`'s `make(chan struct{}, workers)` remains). The module-wide grep for `RunMusicBrainzCycle`/`RunDeezerCycle` outside `internal/poller` shows only comment references in `cmd/server/main.go` and `internal/detection/detector.go`, no calls needing a signature-change update.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal/poller.runCycle -> MusicBrainz/Deezer fetch + EventRecorder + Notifier seams | Both poll cycles now funnel their fan-out/guard/panic-recovery mechanics through one shared code path; a defect here has double the blast radius of a defect in either cycle's own former copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23o-01 | Denial of Service | internal/poller.runCycle (CAS guard, semaphore, panic recovery) | high | mitigate | Task 1 and Task 2 each re-run the full pre-existing `internal/poller` test suite unchanged after migrating one method at a time (overlap-guard skip, guard-released-after-error, panic-recovery, per-artist isolation, cancellation tests for both sources) -- a regression in the consolidated guard/semaphore/panic-recovery logic fails a test immediately rather than silently stopping polling for both sources in production. |
| T-23o-02 | Denial of Service | shouldDispatch closures (Deezer nil-DeezerID skip vs MusicBrainz no-skip) | medium | mitigate | Task 2 explicitly reproduces Deezer's skip check unchanged inside its own closure and leaves MusicBrainz's closure an unconditional `true`, so the skip cannot leak across sources; the existing Deezer skip-log test and MusicBrainz per-artist tests both re-run unchanged to confirm neither source's dispatch behavior shifted. |
| T-23o-03 | Repudiation | Per-source log attribute keys (`musicbrainz_error` vs `deezer_error`) | low | accept | Kept inside each source's own `fetchAndRecord` closure rather than generalized in `runCycle`, per `<planning_findings>` -- no log fidelity is lost, and this is not security-relevant, only operational-log readability. |
| T-23o-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs in this plan -- no new imports, no `go.mod` change. No legitimacy audit required. |
</threat_model>

<verification>
1. `go build ./...` succeeds.
2. `go vet ./...` succeeds.
3. `go test ./internal/poller/... -v` passes in full, with zero test file changes.
4. `git diff --name-only -- internal/poller/poller_test.go` outputs nothing.
5. `grep -n "chan struct{}, p\.\(mb\|dz\)Workers" internal/poller/poller.go` returns zero matches (both cycle methods no longer build their own semaphore).
6. A module-wide grep for `RunMusicBrainzCycle`/`RunDeezerCycle` outside `internal/poller` shows only comment references, confirming no external call site needs a change.
</verification>

<success_criteria>
- `internal/poller/poller.go` has one private `runCycle` method carrying the CAS guard, semaphore-bounded worker pool, cancellation handling, panic recovery, cycle-complete logging, and inline notify -- used by both `RunMusicBrainzCycle` and `RunDeezerCycle`.
- Neither public method contains its own copy of that mechanics; each is reduced to a `shouldDispatch`/`fetchAndRecord` closure pair plus a delegating call into `runCycle`.
- Deezer's nil-DeezerID skip stays Deezer-only; no behavior change for either source.
- `poller_test.go` is unmodified and its full suite passes; no exported API changed; no other package's call sites need updates.
</success_criteria>

<output>
Create `.planning/quick/260824-23o-fix-code-review-finding-2-factor-runmusi/260824-23o-SUMMARY.md` when done.
</output>
