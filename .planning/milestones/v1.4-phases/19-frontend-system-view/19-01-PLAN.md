---
phase: "19"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app/lib/api.ts
  - web/app/lib/api.test.ts
  - web/app/routes.ts
  - web/app/root.tsx
  - web/app/root.test.tsx
  - web/app/routes/system.tsx
  - web/app/routes/system.test.tsx
autonomous: true
requirements: [SYS-01, SYS-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An operator clicking the System tab in the SPA nav lands on /system and sees a page headed 'System' rendering a real field from the live GET /status body — the route, the nav entry, getStatus(), apiFetch, and the render path all work end-to-end (SYS-01, ROADMAP success criterion 1)."
    - "The System view issues exactly one getStatus() call on mount and issues no further call while it sits mounted — no interval timer, no page-visibility handler, no window-focus refetch exists anywhere under web/app (D-10, SYS-03, ROADMAP success criterion 4). [edge probe SYS-01/SYS-03 unclassified — resolved explicit from D-10 + CONTEXT Reusable Assets]"
    - "A 401 from GET /status on mount renders no error surface in the System view at all: no error EmptyState, no inline refresh-error line, no white screen. apiFetch's D-16 interceptor flips authStore and <App> early-returns <PassphraseScreen>; the view's only 401 code is an early return (SYS-03, ROADMAP success criterion 5). [UI-SPEC UI Considerations: error | 401 at any point]"
    - "Every wire type in web/app/lib/api.ts matches docs/api/status-contract.md and internal/httpserver/status.go's json tags character-for-character, including schema_applied typed number | null and history typed as a never-null array."
    - "getStatus() reaches the URL path /status through apiFetch, so the X-Instance-Gated latch and the D-16 401 interceptor apply with zero per-view auth code (CONTEXT Reusable Assets, RESEARCH Don't Hand-Roll)."
  prohibitions:
    - "The System view must not introduce any interval timer, page-visibility listener, or window-focus refetch — auto-refresh is OBS-01 and was explicitly offered for rejection and rejected (D-10)."
  artifacts:
    - web/app/lib/api.ts
    - web/app/lib/api.test.ts
    - web/app/routes.ts
    - web/app/root.tsx
    - web/app/root.test.tsx
    - web/app/routes/system.tsx
    - web/app/routes/system.test.tsx
  key_links:
    - "routes.ts route('system', 'routes/system.tsx', { id: 'system-path' }) -> the route component actually mounts at /system"
    - "root.tsx <NavLink to='/system'> -> tabLinkClassName active indigo underline on /system"
    - "system.tsx mount effect -> getStatus() -> apiFetch('/status') -> D-16 401 interceptor -> authStore -> <App> <PassphraseScreen> early return"
---

<objective>
Prove the whole `/system` path end-to-end with one production-quality vertical slice: a real
route entry, a real nav tab, real wire types, a real `getStatus()` through `apiFetch`, and a
real render of one live field from the `GET /status` body — with the mount effect, the
`mountedRef` guard, the `reloadToken` Retry seam, and the render-precedence guard chain all
in their final shape.

Purpose: every later plan in this phase expands horizontally from this slice (formatters,
About rows, source panels, history table, refresh semantics). If the route/nav/fetch/render
spine is wrong, that is a one-commit correction here instead of a five-plan unwind.

Output: `/system` reachable from the nav, fetching once on mount, rendering a real payload
field, yielding silently to the passphrase screen on 401.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-frontend-system-view/19-CONTEXT.md
@.planning/phases/19-frontend-system-view/19-RESEARCH.md
@.planning/phases/19-frontend-system-view/19-PATTERNS.md
@.planning/phases/19-frontend-system-view/19-UI-SPEC.md
@docs/api/status-contract.md
</context>

## Artifacts this phase produces

Phase-wide new-symbol inventory. Later plans add to this list; nothing outside it is in scope.

| Symbol / artifact | Home | Plan |
|---|---|---|
| `KnownOutcome`, `StatusRun`, `StatusSource`, `StatusInstance`, `StatusResponse` | `web/app/lib/api.ts` | 19-01 |
| `getStatus(): Promise<StatusResponse>` | `web/app/lib/api.ts` | 19-01 |
| route id `system-path` at path `system` | `web/app/routes.ts` | 19-01 |
| `<NavLink to="/system">System</NavLink>` | `web/app/root.tsx` | 19-01 |
| default-exported `System` route component | `web/app/routes/system.tsx` | 19-01 |
| `formatRelativeTime`, `formatAbsoluteTime`, `formatIsoTitle`, `formatClock`, `formatDuration`, `formatPollInterval` | `web/app/lib/format.ts` | 19-02 |
| `sourceDisplayName(name)`, `SOURCE_ORDER` | `web/app/lib/sources.ts` | 19-03 |
| `--color-status-ok`, `--color-status-warn` (`@theme` tokens) | `web/app/app.css` | 19-03 |
| `Table`, `TableHeader`, `TableBody`, `TableRow`, `TableHead`, `TableCell`, `TableCaption` | `web/app/components/ui/table.tsx` (vendored) | 19-03 |
| `classifyOutcome(run)`, `OutcomeBadge` | `web/app/components/system/OutcomeBadge.tsx` | 19-04 |
| `AboutInstance` | `web/app/components/system/AboutInstance.tsx` | 19-04 |
| `SourcePanel` | `web/app/components/system/SourcePanel.tsx` | 19-04 |
| `SourceHistoryTable` | `web/app/components/system/SourceHistoryTable.tsx` | 19-05 |
| `deriveLoadedShape(data)` | `web/app/routes/system.tsx` | 19-05 |
| New test files: `system.test.tsx`, `format.test.ts`, `sources.test.ts`, `OutcomeBadge.test.tsx` | co-located | 19-01…05 |

No new env var, no new build-time config, no new runtime dependency, no backend change.

<assumption_delta_decision>
**Primary noun:** a top-level SPA route / nav tab.
**Transition observed:** two → three (Watchlist, History, + System).
**Decision:** `no-change`.
**Rationale:** the route table in `web/app/routes.ts` is already an array and the nav bar in
`web/app/root.tsx` already maps a list of `<NavLink>` entries — both were list-shaped from
Phase 6. Adding a third entry is the list's intended extension, not a new abstraction. The
only real debt is the stale two-tab comments in both files, fixed in Task 1 of this plan per
CLAUDE.md comment discipline. Non-blocking; recorded for audit.
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "/system renders a live /status field" — one path only</name>
  <files>web/app/lib/api.ts, web/app/routes.ts, web/app/root.tsx, web/app/routes/system.tsx, web/app/routes/system.test.tsx</files>
  <read_first>
    - `docs/api/status-contract.md` — the frozen wire contract; the Envelope and Run object tables are the type source.
    - `internal/httpserver/status.go` lines 46-77 — the Go structs whose `json:"..."` tags must match character-for-character (quoted verbatim in `19-RESEARCH.md` Pattern 1).
    - `web/app/lib/api.ts` — the file being modified. Read the header comment ("typed against the real Go response bodies… not guessed from documentation"), `apiFetch` (the single fetch path, the `X-Instance-Gated` latch, the D-16 401 flip), `ApiError`, and the `EventsPage` / `listEvents` precedent that this wrapper copies.
    - `web/app/routes/history.tsx` — the analog per `19-PATTERNS.md`: the mount `useEffect` with the `cancelled` flag, the `reloadToken` Retry seam, the `initialLoading` guard chain, and the `flex flex-col gap-6 p-8` container with the `text-display font-semibold text-foreground` heading.
    - `web/app/routes.ts` and `web/app/root.tsx` — both files being modified; note the stale two-tab comments and `tabLinkClassName`.
    - `web/app/routes/history.test.tsx` — the route-test analog; `makeEvent()` fixture-builder shape and the `renderRoute` usage.
    - `web/app/lib/test/routeStub.tsx` — `renderRoute(Component, path)`; do not hand-roll a router wrapper.
    - `19-PATTERNS.md` sections for `system.tsx`, `system.test.tsx`, `api.ts`, `routes.ts`, `root.tsx`.
    - `19-UI-SPEC.md` Copywriting Contract → Page chrome (the locked `System` strings).
    - `19-RESEARCH.md` Pattern 1, Pattern 2, Pattern 6, Pitfall 1, Pitfall 5.
  </read_first>
  <action>
Wire one entry point through every layer of this phase's stack, end to end, and stop there.

In `web/app/lib/api.ts`, add the exported wire types `KnownOutcome` (the union of the three
frozen literals `ok`, `error`, `cancelled`), `StatusRun`, `StatusSource`, `StatusInstance`,
and `StatusResponse`, with field names and types taken character-for-character from
`docs/api/status-contract.md` plus the Go `json` tags in `internal/httpserver/status.go`.
`StatusRun.outcome` is typed as `KnownOutcome` widened with a bare-string arm so an N-1/N
deploy value keeps literal autocomplete without breaking typecheck (D-04, D-07).
`StatusInstance.schema_applied` is `number | null`; `schema_expected` is a plain `number`
(the Go field is `uint`, never absent). `StatusSource.history` is a non-nullable array; the
handler allocates it zero-length so it encodes as an empty array, never null. The run object
has no source key — the source name is the map key in `StatusResponse.sources`, typed as a
string-keyed record. Then add `getStatus()` returning `Promise<StatusResponse>`, whose whole
body delegates to `apiFetch` with the path `/status` — mirroring the `listEvents` precedent
in the same file. Add nothing else to `api.ts`: no signal parameter, no retry, no per-view
auth code (RESEARCH Open Question 2 resolved in favour of a mounted-ref guard).

In `web/app/routes.ts`, add a third entry `route("system", "routes/system.tsx", { id:
"system-path" })` after the existing history entry, and rewrite the stale leading comment so
it describes three tabs instead of two — same change, per CLAUDE.md comment discipline. Keep
it to the existing 1-3 line intent shape; do not expand it into a header block.

In `web/app/root.tsx`, add `<NavLink to="/system" className={tabLinkClassName}>System</NavLink>`
immediately after the History link and before the `gateActive` logout slot, and fix the stale
"two-tab bar" wording in the `App` doc comment in the same change. Reuse `tabLinkClassName`
unchanged — it already supplies the active-tab indigo underline; add no new styling.

Create `web/app/routes/system.tsx` as the default-exported `System` component in its final
structural shape, carrying only the one field this tracer proves. State: `data`
(`StatusResponse | null`), `initialLoading` (true), `loadError` (false), `reloadToken` (0),
and a `mountedRef` ref seeded true. One `useEffect` keyed on `[reloadToken]` that mirrors
`history.tsx`'s mount effect — a local cancelled flag, `setInitialLoading(true)` and
`setLoadError(false)` at the top, `getStatus()` then set `data` on success — but with two
mandatory divergences: the catch must branch on `err instanceof ApiError && err.status ===
401` and return without setting any state (apiFetch already handled it), and the cleanup must
flip both the local cancelled flag and `mountedRef.current` to false so a later plan's refresh
handler has its guard. Do not swallow all errors into one string the way `history.tsx` does —
that shape hides the 401.

Render precedence, as the final guard chain (later plans fill each branch's body): first
`loadError` renders the error `EmptyState` with heading `Couldn't load system status.`, body
`The server didn't return a status. Try again in a moment.`, and a secondary-variant `Retry`
button wired to a handler that bumps `reloadToken`; second `initialLoading && !data` renders
a `Skeleton` placeholder; third, with `data` present, renders the loaded body. All three sit
inside the `flex flex-col gap-6 p-8` container under an `h1` carrying the locked copy `System`
in `text-display font-semibold text-foreground`.

For this tracer the loaded body renders exactly one real payload field — a definition row
labelled `Version` whose value is `data.instance.app_version` in `font-mono`. That is the far
end of the stack proven with real data. Add no other call site, no other panel, no table, no
badge, no formatter, no refresh control. Fetch happens on mount and on a Retry bump only;
register no interval timer, no page-visibility listener and no window-focus listener anywhere
(D-10). When you note that rule in a comment, cite D-10 by name and do not spell the timer or
listener API in the comment text — an acceptance gate greps this tree for those identifiers.

Create `web/app/routes/system.test.tsx` with the partial-mock form of `vi.mock("~/lib/api")`
that spreads `importOriginal` and replaces only `getStatus` with a spy — a bare automock would
erase the real `ApiError` class and silently break the 401 branch (RESEARCH Pitfall 5). Add a
`makeStatus()` fixture builder mirroring `makeEvent()` in `history.test.tsx`, returning a
contract-shaped fresh-instance payload with both `musicbrainz` and `deezer` source keys. Cover
three things only: the mount render (heading `System` plus the `app_version` value visible,
and `getStatus` called exactly once), a second assertion that the call count stays at one
while the view remains mounted, and the mount-401 case (spy rejects a real `ApiError` with
status 401) asserting the error heading `Couldn't load system status.` is absent from the
document. Use `renderRoute(System, "/system")` from the project's route stub.
  </action>
  <acceptance_criteria>
    - `web/app/lib/api.ts` exports `async function getStatus(): Promise<StatusResponse>` and the five status wire types (`KnownOutcome`, `StatusRun`, `StatusSource`, `StatusInstance`, `StatusResponse`).
    - Every `json` tag name in `internal/httpserver/status.go` lines 46-77 appears as a field name in the new `api.ts` types, and every new field name appears as a `json` tag there — no extra fields, no renames.
    - `web/app/lib/api.ts` declares `schema_applied` as `number | null` and `schema_expected` as `number`.
    - `web/app/routes.ts` contains `route("system", "routes/system.tsx", { id: "system-path" })` and its leading comment no longer says two tabs/routes.
    - `web/app/root.tsx` contains `<NavLink to="/system"` using `tabLinkClassName`, placed after the History link and before the `gateActive` slot, and its `App` doc comment no longer says two-tab bar.
    - `web/app/routes/system.tsx` default-exports a component rendering an `h1` with the exact string `System` and a `Version` row bound to `data.instance.app_version`.
    - `web/app/routes/system.tsx` catch path contains a branch on `err instanceof ApiError && err.status === 401` that returns before any error state is set.
    - `grep -rn "setInterval\|setTimeout\|visibilitychange" web/app --include="*.ts" --include="*.tsx" | wc -l` prints `0`.
    - `corepack pnpm --dir web exec vitest run --coverage.enabled=false app/routes/system.test.tsx` exits 0 with the mount, no-repeat-fetch, and mount-401 cases green.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm --dir web exec prettier --write "**/*.{ts,tsx}" &amp;&amp; corepack pnpm --dir web exec prettier --check "**/*.{ts,tsx}"</automated>
    <fails_when>non-zero exit, or the output lists any file under "Code style issues found" instead of "All matched files use Prettier code style!"</fails_when>
    <automated>corepack pnpm --dir web exec vitest run --coverage.enabled=false app/routes/system.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary reports a non-zero "failed" count, or reports "No test files found" (the file was never created)</fails_when>
    <automated>grep -rn "setInterval\|setTimeout\|visibilitychange" web/app --include="*.ts" --include="*.tsx" | wc -l</automated>
    <fails_when>prints anything other than 0 — any match means an interval timer, deferred-retry timer, or page-visibility refetch entered the SPA, violating D-10</fails_when>
  </verify>
  <reversibility rating="costly">Typing `api.ts` against the frozen `/status` contract binds the SPA to Phase 18's published shape; changing a key later requires a paired backend change in the same or a following phase (status-contract.md "Changing this contract"). Low risk — the contract is frozen by Phase 18 D-11 and 18.1 shipped without amending it.</reversibility>
  <done>Clicking the System tab loads `/system`, which fetches `GET /status` exactly once through `apiFetch` and renders the real `app_version` value. A 401 on that fetch renders no error surface. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the two seams the tracer crosses — getStatus() in api.test.ts, the System tab in root.test.tsx</name>
  <files>web/app/lib/api.test.ts, web/app/root.test.tsx</files>
  <read_first>
    - `web/app/lib/api.test.ts` — the file being modified. Read the `jsonResponse(body, status, headers)` helper, the existing 200 and 401 cases around lines 164-204, and the `vi.resetModules()` + re-import `beforeEach` at lines 152-158 (the in-file precedent this extends, per `19-PATTERNS.md`).
    - `web/app/root.test.tsx` — the file being modified. Read the existing nav-active tests that drive `createRoutesStub` directly with more than one route (around lines 73-85).
    - `web/app/lib/api.ts` — the `getStatus()` and `apiFetch` implementation committed in Task 1.
    - `web/app/root.tsx` — `tabLinkClassName` and the nav block committed in Task 1.
    - `docs/api/status-contract.md` — the fresh-instance example body to use as the 200 fixture.
  </read_first>
  <action>
Extend `web/app/lib/api.test.ts` with three `getStatus` cases, following the in-file
conventions exactly (the existing `jsonResponse` helper, the `fetchSpy` mock, the
`vi.resetModules()` re-import in `beforeEach`) rather than inventing a new harness: that
`getStatus()` issues its request to the path `/status`; that a 200 response resolves to the
parsed body with `instance.app_version`, `instance.schema_applied`, `poll_interval_seconds`,
`watchlist_size`, and both `sources` keys intact; and that a 401 response rejects with a real
`ApiError` carrying `status` 401 — asserted through the real module the way the existing
`listWatchlist` 401 case does, so the class identity is genuine. Use the fresh-instance
example body from the status contract document as the 200 fixture so the test doubles as a
contract-shape assertion.

Extend `web/app/root.test.tsx` with one case: rendering the app shell at `/system` shows a
nav link whose accessible name is `System`, and that link carries the active-tab class
`border-accent-indigo` while the Watchlist and History links do not. Drive it with
`createRoutesStub` over all three routes the way the existing multi-route nav test does, so
the assertion exercises the real `tabLinkClassName` active branch rather than a stubbed one.
Do not restyle or duplicate `tabLinkClassName`.
  </action>
  <acceptance_criteria>
    - `web/app/lib/api.test.ts` contains a case asserting the fetch URL passed for `getStatus()` ends in `/status`.
    - `web/app/lib/api.test.ts` contains a case asserting a 401 from `/status` rejects with `instanceof api.ApiError` and `.status === 401`.
    - `web/app/root.test.tsx` contains a case asserting a link named `System` exists and carries `border-accent-indigo` when the router is at `/system`.
    - `corepack pnpm --dir web exec vitest run --coverage.enabled=false app/lib/api.test.ts app/root.test.tsx` exits 0.
    - `corepack pnpm --dir web test` exits 0 with all four coverage axes at or above 70.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm --dir web exec prettier --write "**/*.{ts,tsx}" &amp;&amp; corepack pnpm --dir web exec prettier --check "**/*.{ts,tsx}"</automated>
    <fails_when>non-zero exit, or the output lists any file under "Code style issues found"</fails_when>
    <automated>corepack pnpm --dir web exec vitest run --coverage.enabled=false app/lib/api.test.ts app/root.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary reports a non-zero "failed" count, or either named file reports "No test files found"</fails_when>
    <automated>corepack pnpm --dir web test</automated>
    <fails_when>non-zero exit, or the output contains a line beginning "ERROR: Coverage for" (an axis fell under the 70 threshold), or the summary reports a non-zero "failed" count</fails_when>
  </verify>
  <done>`getStatus()`'s URL, success body, and 401 propagation are pinned by unit tests; the System nav tab's presence and active state are pinned by a root test; the full frontend suite is green at or above the 70% coverage floor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gated instance -> browser | `GET /status` crosses `gate.Authenticate`; a missing/expired session cookie yields 401 with the gate's own body, never a partial payload |
| `/status` JSON -> React DOM | server-authored operator data is rendered into the page as text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Information disclosure | `system.tsx` mount effect / `getStatus()` | medium | mitigate | Route the call solely through `apiFetch` so the D-16 interceptor flips `authStore` and `<App>` early-returns `<PassphraseScreen>`; the mount catch returns early on `ApiError` 401 without setting `loadError`, so no `/status`-derived text can render behind the login screen (ROADMAP success criterion 5). Pinned by the mount-401 test in Task 1. |
| T-19-02 | Tampering | `web/app/lib/api.ts` wire types | low | mitigate | Type only the fields enumerated in `docs/api/status-contract.md`; add no generic unknown-field renderer. `outcome` is typed as the known union widened with a string arm so an N-1/N deploy value cannot break typecheck or throw. |
| T-19-03 | Information disclosure | `system.tsx` error copy | low | mitigate | The error state renders fixed operator-authored copy from the UI-SPEC Copywriting Contract; `ApiError.message` and the raw `err` are never interpolated into rendered output, matching `history.tsx`'s fixed-string catch. |
| T-19-04 | Tampering (XSS) | `system.tsx` rendering `instance.app_version` | low | mitigate | The value is rendered as a JSX text node; React escapes it. No raw-HTML injection prop is used anywhere under `web/app` (repo-wide zero matches since Phase 06) and `app_version` is never used to build an `href` (D-03 — no link). |
| T-19-SC | Tampering | npm/pnpm installs | low | accept | This plan runs no package install; `web/package.json` and `web/pnpm-lock.yaml` are untouched. `19-RESEARCH.md` `## Package Legitimacy Audit` records zero package installs for the whole phase; the single vendoring action is registered against plan 19-03. |

**No threat in this plan reaches the configured `high` block-on severity.** This is a
read-only client view over a server-authored, already-hardened contract (STAT-02): no new
auth, session, crypto, persistence, secret, env var, or outbound URL surface is introduced.
The highest-rated item (T-19-01) is `medium` and is structurally mitigated by reusing the
Phase 14 401 path rather than by new code.
</threat_model>

<verification>
- `corepack pnpm --dir web exec prettier --check "**/*.{ts,tsx}"` clean.
- `corepack pnpm --dir web test` green with all four coverage axes at or above 70.
- `grep -rn "setInterval\|setTimeout\|visibilitychange" web/app --include="*.ts" --include="*.tsx" | wc -l` prints 0.
- Manual read-back: every `json` tag in `internal/httpserver/status.go` lines 46-77 has a matching field in `api.ts`, and vice versa.
</verification>

<success_criteria>
- `/system` is reachable from the nav and renders a real `GET /status` field end-to-end.
- Exactly one `getStatus()` call per mount; none while idle.
- A mount 401 produces no visible error surface in the view.
- Both seams (api client, nav) carry their own regression tests.
- Frontend Definition of Done green.
</success_criteria>

<output>
Create `.planning/phases/19-frontend-system-view/19-01-SUMMARY.md` when done
</output>
