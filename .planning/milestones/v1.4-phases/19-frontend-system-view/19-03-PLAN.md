---
phase: "19"
plan: "03"
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - web/app/lib/sources.ts
  - web/app/lib/sources.test.ts
  - web/app/app.css
  - web/app/components/ui/table.tsx
autonomous: true
requirements: [SYS-01, SYS-02]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Source panels render in a fixed order — MusicBrainz first, then Deezer — driven by an explicit SOURCE_ORDER constant rather than by JavaScript object-key iteration order, so the layout is stable regardless of how the server serialized the sources map (UI-SPEC Visual Hierarchy). [edge probe SYS-02 ordering — resolved explicit from UI-SPEC]"
    - "A source key renders with its real product capitalization — musicbrainz becomes MusicBrainz, deezer becomes Deezer — from a lookup in sources.ts, never from naive title-casing, which would yield 'Musicbrainz' (CONTEXT Reusable Assets, SYS-01)."
    - "An unrecognised source key passes through the display-name lookup unchanged rather than throwing or rendering undefined, so an N-1/N deploy that adds a third source degrades to a readable panel title."
    - "The recent-runs history table stays usable on a narrow viewport: the vendored Table self-wraps in a horizontally scrollable container, so eight columns never clip or reflow into unreadable stacking (SYS-02). [UI-SPEC UI Considerations: overflow | wide table on a narrow viewport]"
    - "Two and only two new theme tokens enter web/app/app.css this phase — the run-health status palette — sitting beside the Phase 5/6 release-type chip tokens with a recorded one-line rationale for why both greens coexist (UI-SPEC Color)."
  prohibitions:
    - "The run-health status palette must not be used for any button fill, nav item, link, border, focus ring, table row background, or the freshness stamp — its reserved uses are the closed list in the UI-SPEC Color section."
    - "The Phase 5/6 release-type chip tokens must not be reused for run health — they carry an unrelated semantic axis and are reserved to the History event card chip."
  artifacts:
    - web/app/lib/sources.ts
    - web/app/lib/sources.test.ts
    - web/app/app.css
    - web/app/components/ui/table.tsx
  key_links:
    - "sources.ts SOURCE_ORDER -> system.tsx panel iteration order (plan 19-04), replacing Object.keys ordering"
    - "app.css --color-status-ok / --color-status-warn -> Tailwind utilities bg-status-ok/15, text-status-warn consumed by OutcomeBadge and AboutInstance (plan 19-04)"
    - "components/ui/table.tsx exports -> SourceHistoryTable (plan 19-05)"
---

<objective>
Land the three presentation primitives the System view's panels and table depend on, none of
which touch `web/app/routes/system.tsx`: the per-source display-name and ordering rules in
`sources.ts`, the two run-health theme tokens in `app.css`, and the vendored shadcn `table`
component.

Purpose: these are leaf dependencies of plans 19-04 and 19-05 with no file overlap with plan
19-02, so they run in the same wave. Landing them separately keeps the component plans focused
on behavior rather than on setup.

Output: `sourceDisplayName` and `SOURCE_ORDER` exported and tested, `--color-status-ok` and
`--color-status-warn` available as Tailwind utilities, and `Table` and its six siblings vendored.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-frontend-system-view/19-CONTEXT.md
@.planning/phases/19-frontend-system-view/19-RESEARCH.md
@.planning/phases/19-frontend-system-view/19-PATTERNS.md
@.planning/phases/19-frontend-system-view/19-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Source display names and the fixed panel order</name>
  <files>web/app/lib/sources.ts, web/app/lib/sources.test.ts</files>
  <read_first>
    - `web/app/lib/sources.ts` — the file being modified and its own analog. Read the header comment's numbered enumeration of the two existing rules, and the `isAddableSource` / `identityField` implementations that the new rule sits beside.
    - `web/app/lib/authStore.test.ts` — the plain-vitest unit-test analog named by `19-PATTERNS.md` (`sources.ts` has no test file today; this adds first coverage).
    - `docs/api/status-contract.md` — `sources` is keyed by source name and always carries exactly `musicbrainz` and `deezer`, including on a fresh instance.
    - `19-UI-SPEC.md` Visual Hierarchy (panels descend in fixed source order, MusicBrainz then Deezer) and Copywriting Contract → Per-source panel (the panel title comes from the lookup, not from naive title-case).
    - `19-CONTEXT.md` Existing Code Insights → Reusable Assets, `web/app/lib/sources.ts` entry.
  </read_first>
  <behavior>
    - `sourceDisplayName("musicbrainz")` returns `"MusicBrainz"`.
    - `sourceDisplayName("deezer")` returns `"Deezer"`.
    - `sourceDisplayName` given an unrecognised key returns that key unchanged rather than throwing or returning undefined.
    - `sourceDisplayName("")` returns a defined string, never undefined.
    - `SOURCE_ORDER` is exactly the two known keys in the order MusicBrainz first, Deezer second.
  </behavior>
  <action>
Append the third per-source rule to `web/app/lib/sources.ts`, alongside `isAddableSource` and
`identityField`, and extend the file's header comment to a third numbered entry naming the new
rule and its call site (the System view's panel titles and panel ordering) — keeping the existing
1-to-3-line-per-rule density, not a header block.

Export `sourceDisplayName(name: string): string`, a lookup returning the real product
capitalization for each known key and passing an unknown key through unchanged. A lookup rather
than a transform is the point: capitalizing the first letter of the MusicBrainz key produces the
wrong brand spelling, and this module exists precisely so that rule is not re-derived at each
call site.

Export `SOURCE_ORDER` as a readonly two-element tuple of the known source keys in display order,
MusicBrainz first. The System view iterates this constant rather than the payload's own key
order, so the panel sequence cannot drift with the server's map serialization. The consuming
view must still tolerate a source key present in the payload but absent from this constant —
that contract is enforced in plan 19-04, not here.

Create `web/app/lib/sources.test.ts` as a plain vitest unit file — no router harness, no module
mocking — covering every row in the behavior block plus the two pre-existing rules
(`isAddableSource`, `identityField`) so the module reaches real coverage for the first time.
  </action>
  <acceptance_criteria>
    - `web/app/lib/sources.ts` exports `sourceDisplayName` and `SOURCE_ORDER`.
    - `web/app/lib/sources.ts`'s header comment lists three numbered rules, the third naming `sourceDisplayName` and where it is consumed.
    - `web/app/lib/sources.test.ts` asserts `sourceDisplayName("musicbrainz")` equals `"MusicBrainz"` and `sourceDisplayName("deezer")` equals `"Deezer"`.
    - `web/app/lib/sources.test.ts` asserts an unrecognised key passes through unchanged.
    - `web/app/lib/sources.test.ts` asserts `SOURCE_ORDER` has `"musicbrainz"` at index 0 and `"deezer"` at index 1.
    - `corepack pnpm --dir web exec vitest run --coverage.enabled=false app/lib/sources.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm --dir web exec prettier --write "**/*.{ts,tsx}" &amp;&amp; corepack pnpm --dir web exec prettier --check "**/*.{ts,tsx}"</automated>
    <fails_when>non-zero exit, or the output lists any file under "Code style issues found"</fails_when>
    <automated>corepack pnpm --dir web exec vitest run --coverage.enabled=false app/lib/sources.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary reports a non-zero "failed" count, or reports "No test files found" (sources.test.ts was never created)</fails_when>
  </verify>
  <reversibility rating="reversible">Two additive exports on an existing pure module with no callers yet; removable in one commit.</reversibility>
  <done>`sourceDisplayName` and `SOURCE_ORDER` are exported, tested including the unknown-key passthrough, and `sources.ts` carries test coverage for the first time.</done>
</task>

<task type="auto">
  <name>Task 2: The two run-health theme tokens</name>
  <files>web/app/app.css</files>
  <read_first>
    - `web/app/app.css` — the file being modified. Read the `@theme` block, in particular the `--color-accent-indigo` entry and the `--color-event-*` block whose comment pattern (which UI-SPEC locked the color, and the closed list of places it may be used) is the analog named by `19-PATTERNS.md`.
    - `19-UI-SPEC.md` Color section — the exact two hex values, the "why this is a legitimate second/third semantic color" paragraph, the closed five-item reserved-uses list, and the explicit not-used-for list.
    - `19-RESEARCH.md` Pattern 7.
  </read_first>
  <action>
Add exactly two entries to the existing `@theme` block in `web/app/app.css`, beside the
`--color-event-*` group: `--color-status-ok` set to the green-500 hex and `--color-status-warn`
set to the amber-500 hex, using the values quoted verbatim in the UI-SPEC Color section.

Precede them with a comment of the same shape and density as the neighbouring `--color-event-*`
comment: name the source document and phase, state that this is the run-health palette, and give
the one-line reason both greens coexist in the same theme — different semantic axis (release-type
chip versus run health) and the two never co-render on one screen (History card versus System
panel). Three lines at most, per CLAUDE.md comment discipline; do not restate the reserved-uses
list here, the UI-SPEC owns it.

Add nothing else: no new spacing token, no new typography token, no light-theme variant. Tailwind
v4 derives `bg-status-ok`, `text-status-warn`, and the slash-opacity forms from these two entries
automatically, so no utility class needs to be authored.
  </action>
  <acceptance_criteria>
    - `grep -c "^\s*--color-status-ok:" web/app/app.css` prints 1.
    - `grep -c "^\s*--color-status-warn:" web/app/app.css` prints 1.
    - Both new entries sit inside the existing `@theme` block, not in a new block and not in the `.dark` selector.
    - `git diff --stat -- web/app/app.css` shows an additive change only — no existing token line removed or retyped.
    - The preceding comment is three lines or fewer and states why the two greens coexist.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^\s*--color-status-ok:\|^\s*--color-status-warn:" web/app/app.css</automated>
    <fails_when>prints anything other than 2 — either token is missing, duplicated, or was indented outside the @theme block in a way that breaks the anchored match</fails_when>
    <automated>corepack pnpm --dir web exec vite build --mode production 2>&amp;1 | tail -5</automated>
    <fails_when>non-zero exit, or the output contains "error" / "Unknown at rule" — a malformed @theme entry breaks the Tailwind v4 build</fails_when>
  </verify>
  <reversibility rating="reversible">Two additive CSS custom properties with no consumers yet; deleting the two lines restores the prior theme exactly.</reversibility>
  <done>`--color-status-ok` and `--color-status-warn` exist in `@theme`, the CSS build is clean, and the coexistence rationale is recorded in at most three comment lines.</done>
</task>

<task type="auto">
  <name>Task 3: Vendor the shadcn table component</name>
  <files>web/app/components/ui/table.tsx</files>
  <read_first>
    - `web/app/components/ui/card.tsx` — the vendored-primitive shape analog named by `19-PATTERNS.md`: a `cn()`-only module with `data-slot` attributes exporting a family of sub-components.
    - `web/components.json` — the shadcn config that governs the add command (`rsc: false`, so the registry's client directive is stripped at add time; `registries: {}`, so no third-party registry is in play).
    - `19-UI-SPEC.md` Component Inventory → "To vendor this phase" and Registry Safety.
    - `19-RESEARCH.md` `## Package Legitimacy Audit` (the OK verdict and its evidence) and Environment Availability (the offline fallback).
    - `web/vitest.config.ts` coverage `exclude` — `app/components/ui/**` is excluded as a non-first-party primitive, so this file adds no coverage burden.
  </read_first>
  <action>
Vendor the shadcn `table` component into `web/app/components/ui/table.tsx` by running the
registry add command for `table` against the project's existing `web/components.json` config, from
the `web/` directory. The audit verdict is already recorded in `19-RESEARCH.md`: first-party
shadcn registry, no registry dependencies, single dependency `cn` which is already vendored — so
no legitimacy checkpoint is required and none is inserted.

If the registry is unreachable at execution time, take the documented fallback rather than
blocking: hand-write the file to match the `card.tsx` shape — a `cn()`-only module with `data-slot`
attributes — exporting `Table`, `TableHeader`, `TableBody`, `TableRow`, `TableHead`, `TableCell`,
and `TableCaption`, with `Table` self-wrapping in a horizontally scrollable container so eight
columns stay reachable on a narrow viewport. Record which path was taken in the plan summary.

Either way, run the project's prettier write pass immediately afterward and before staging — a
CLI-generated file is not formatted to this repo's prettier plus tailwind-class-ordering
configuration, and hand-formatting TSX fails CI's frontend-test job.

Confirm the vendoring added nothing to the dependency graph: `web/package.json` and
`web/pnpm-lock.yaml` must be byte-identical to their pre-task state. The add command writes a
source file; it is not an install, and `pnpm install` is not run in this task.

Do not import the component anywhere yet — plan 19-05 is its only consumer. Do not vendor any
other registry component; the UI-SPEC inventory names `table` as the only addition this phase.
  </action>
  <acceptance_criteria>
    - `web/app/components/ui/table.tsx` exists and is tracked by git.
    - `grep -c "TableCaption" web/app/components/ui/table.tsx` prints a value of 2 or greater (definition plus export).
    - The file exports all seven of `Table`, `TableHeader`, `TableBody`, `TableRow`, `TableHead`, `TableCell`, `TableCaption`.
    - The file's only project import is `cn` from `~/lib/utils`; it imports no third-party package beyond React.
    - `git diff --name-only -- web/package.json web/pnpm-lock.yaml` prints nothing — the vendoring added no dependency.
    - `corepack pnpm --dir web exec prettier --check "**/*.{ts,tsx}"` passes, proving the CLI output was reformatted to repo style.
  </acceptance_criteria>
  <verify>
    <automated>test -f web/app/components/ui/table.tsx &amp;&amp; grep -c "Table\b" web/app/components/ui/table.tsx</automated>
    <fails_when>non-zero exit (the file was never written), or prints 0 — no Table symbol present</fails_when>
    <automated>git diff --name-only -- web/package.json web/pnpm-lock.yaml | wc -l</automated>
    <fails_when>prints anything other than 0 — the vendoring mutated the dependency graph, which contradicts the RESEARCH Package Legitimacy Audit's zero-install claim and must be investigated before proceeding</fails_when>
    <automated>corepack pnpm --dir web exec prettier --write "**/*.{ts,tsx}" &amp;&amp; corepack pnpm --dir web exec prettier --check "**/*.{ts,tsx}"</automated>
    <fails_when>non-zero exit, or the output lists any file under "Code style issues found"</fails_when>
    <automated>corepack pnpm --dir web test</automated>
    <fails_when>non-zero exit, or the output contains a line beginning "ERROR: Coverage for" (the vendored file was not excluded from coverage as expected), or the summary reports a non-zero "failed" count</fails_when>
  </verify>
  <reversibility rating="reversible">A single vendored source file with no importers; deleting it restores the prior tree and touches no dependency manifest.</reversibility>
  <done>`web/app/components/ui/table.tsx` exists with all seven exports, is formatted to repo style, added zero dependencies, and has no importers yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external registry -> repo source tree | the shadcn CLI fetches a component definition over the network and writes it into `web/app/components/ui/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-SC | Tampering | shadcn registry add of `table` | medium | mitigate | `19-RESEARCH.md` `## Package Legitimacy Audit` records the verdict OK with evidence: first-party shadcn official registry, no registry dependencies, single dependency `cn` already vendored, registry JSON independently fetched and curl-verified on 2026-09-10 in both RESEARCH and the UI-SPEC. No `[ASSUMED]`, `[SUS]`, or `[SLOP]` item exists for this phase, so no human legitimacy checkpoint is required. The acceptance gate proves the action added nothing to `web/package.json` or `web/pnpm-lock.yaml`, and the vendored file is reviewed as ordinary source in the commit diff. |
| T-19-08 | Tampering | `web/app/components/ui/table.tsx` content | low | mitigate | The file is committed as readable source, not pulled at build time; its only project import is `cn`. A vendored file that imported anything beyond React and `cn` would fail the acceptance criteria and the commit would not proceed. |
| T-19-09 | Information disclosure | `web/app/app.css` theme tokens | low | accept | Two hex color literals; no secret, no env var, no URL. The file is already public in the committed SPA bundle. |
| T-19-10 | Spoofing | `sourceDisplayName` unknown-key passthrough | low | accept | A key not in the lookup renders verbatim as a panel title. The key originates server-side from `internal/pollruns` snapshot keys, which are compile-time source identifiers, not user input — and React escapes the rendered text node regardless. |

**No threat in this plan reaches the configured `high` block-on severity.** The one non-trivial
item, T-19-SC, is a first-party registry vendoring with a pre-recorded audit verdict, an
offline hand-write fallback, and a dependency-graph no-change gate.
</threat_model>

<verification>
- `corepack pnpm --dir web exec prettier --check "**/*.{ts,tsx}"` clean.
- `corepack pnpm --dir web test` green with all four coverage axes at or above 70.
- `git diff --name-only -- web/package.json web/pnpm-lock.yaml` prints nothing.
- Both new theme tokens present inside the existing `@theme` block; the production CSS build is clean.
</verification>

<success_criteria>
- `sourceDisplayName` and `SOURCE_ORDER` exported and covered, including the unknown-key passthrough.
- Exactly two new theme tokens, with a three-line-or-fewer coexistence rationale.
- `table.tsx` vendored with all seven exports, repo-formatted, zero new dependencies, no importers yet.
</success_criteria>

<output>
Create `.planning/phases/19-frontend-system-view/19-03-SUMMARY.md` when done
</output>
