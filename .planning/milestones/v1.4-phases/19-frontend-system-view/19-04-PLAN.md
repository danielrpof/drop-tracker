---
phase: "19"
plan: "04"
type: execute
wave: 3
depends_on: ["19-01", "19-02", "19-03"]
files_modified:
  - web/app/components/system/OutcomeBadge.tsx
  - web/app/components/system/OutcomeBadge.test.tsx
  - web/app/components/system/AboutInstance.tsx
  - web/app/components/system/SourcePanel.tsx
  - web/app/routes/system.tsx
  - web/app/routes/system.test.tsx
autonomous: true
requirements: [SYS-01, SYS-02, SYS-03]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "For each source the panel shows the last run's outcome as a human-phrased badge, its finished time, its duration, and its store-composed summary — never the raw enum value and never a bare timestamp string (SYS-01, ROADMAP success criterion 1). [edge probe SYS-01 unclassified — resolved explicit from D-07 + UI-SPEC Per-source panel]"
    - "'Time since last clean run' scans that source's history newest-first for the first entry whose outcome is ok AND whose artists_errored is zero; a run that completed with errored artists is excluded, because labelling it the last successful run would contradict the amber badge it wears (D-08, SYS-01). [edge probe SYS-01 unclassified — resolved explicit from D-08]"
    - "When no entry in the ring buffer qualifies as clean, the panel renders the literal 'No clean run in recent history' — not a date, not a blank (D-08). [UI-SPEC UI Considerations: empty | no clean run in the ring buffer]"
    - "A source with runs and a source with none render side by side correctly: the runless one shows 'No poll cycles recorded for {SourceName} yet.' while the other renders normally. This is never a whole-page state. [UI-SPEC UI Considerations: empty | one source has runs, the other has none]"
    - "When schema_applied equals schema_expected the About block renders one line naming the single version; when they differ it renders both with a warning treatment, because drift is exactly what an operator opens this panel to catch (D-02). [edge probe SYS-02 adjacency — resolved explicit from D-02]"
    - "Database reachability is derived from the status payload alone — an integer schema_applied renders a reachable pill, a null one renders an unreachable pill and the schema row shows an em dash. No second request is issued (D-01). [UI-SPEC UI Considerations: partial | schema drift; partial | DB blip]"
    - "When watchlist_size is zero an inline callout appears above the source panels with a link to the Watchlist, and the rest of the view still renders normally around it — cycles run against an empty watchlist, so this is a callout, not a whole-page state (D-05). [UI-SPEC UI Considerations: empty | watchlist size 0]"
    - "When a source's latest run was cancelled the panel shows both the amber Interrupted badge with its distinguishing icon and a per-source escalation line telling the operator to check for a restart or crash loop (D-07). [UI-SPEC UI Considerations: partial | latest run cancelled]"
    - "An outcome value outside the frozen set renders a neutral grey badge with a title-cased fallback label and the run's summary line still renders — an N-1/N deploy never white-screens the view (D-04, D-07). [UI-SPEC UI Considerations: partial | unrecognised outcome]"
    - "Counts of zero, one, and many all read correctly: pluralized copy for artists and cycles, and a dedicated copy row wherever zero has its own meaning (UI-SPEC zero-one-many). [UI-SPEC UI Considerations: zero-one-many]"
  prohibitions:
    - "An unrecognised outcome value must never white-screen the view, throw, render as undefined, or render the raw enum string — an N-1/N deploy putting a newer backend behind an older SPA bundle is a resilience line, not routine hygiene (D-04)."
    - "Database reachability must not be derived from a second request to the readiness endpoint — a bare readiness badge was considered and deferred; the single reachability signal is the status payload's schema field (D-01)."
  artifacts:
    - web/app/components/system/OutcomeBadge.tsx
    - web/app/components/system/OutcomeBadge.test.tsx
    - web/app/components/system/AboutInstance.tsx
    - web/app/components/system/SourcePanel.tsx
  key_links:
    - "classifyOutcome(run) -> OutcomeBadge -> both the per-source last-run line and every history table row (plan 19-05), one classifier, two call sites"
    - "AboutInstance reads instance.schema_applied -> the D-01 reachability pill and the D-02 schema row, one field driving two rows"
    - "SOURCE_ORDER (plan 19-03) -> system.tsx panel iteration -> SourcePanel per source"
    - "format.ts formatRelativeTime (plan 19-02) -> the single relative-primary line in the view, D-08's last clean run"
---

<objective>
Build the three presentational components that carry SYS-01 and SYS-02 — the outcome badge
classifier, the About block, and the per-source panel — and wire them into the route body that
plan 19-01's tracer left as a single Version row.

Purpose: this is the phase's real surface area. Every locked decision about how run health,
schema drift, database reachability, and an empty watchlist read to an operator lands here.

Output: a `loaded` System view that renders both source panels and a complete About block from
a real `GET /status` payload.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-frontend-system-view/19-CONTEXT.md
@.planning/phases/19-frontend-system-view/19-RESEARCH.md
@.planning/phases/19-frontend-system-view/19-PATTERNS.md
@.planning/phases/19-frontend-system-view/19-UI-SPEC.md
@.planning/phases/19-frontend-system-view/19-01-SUMMARY.md
@.planning/phases/19-frontend-system-view/19-02-SUMMARY.md
@.planning/phases/19-frontend-system-view/19-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: classifyOutcome and the five badge tiers</name>
  <files>web/app/components/system/OutcomeBadge.tsx, web/app/components/system/OutcomeBadge.test.tsx</files>
  <read_first>
    - `web/app/components/ui/badge.tsx` — the analog named by `19-PATTERNS.md`: a custom `className` merges over the chosen variant, `variant="secondary"` is the grey neutral fill, `variant="destructive"` is the tinted red, and a built-in svg size rule makes a leading icon work without extra styling.
    - `19-UI-SPEC.md` Color → the badge rendering-pattern table (the exact markup shape per tier) and the closed reserved-uses list for the status palette.
    - `19-CONTEXT.md` D-07 and the D-07-adjacent escalation paragraph.
    - `19-RESEARCH.md` Pattern 4 and Pitfall 3.
    - `web/app/lib/api.ts` — the `StatusRun` type and the widened `outcome` field committed in plan 19-01.
    - `web/app/routes/history.test.tsx` — the component-test conventions this file follows.
  </read_first>
  <behavior>
    - An `ok` run with zero errored artists classifies as the Success tier, green status-ok tinted fill.
    - An `ok` run with one or more errored artists classifies as the "Completed with errors" tier, amber status-warn tinted fill — a derived label, since no partial outcome exists on the wire.
    - An `error` run classifies as the Failed tier using the badge's destructive variant.
    - A `cancelled` run classifies as the Interrupted tier, amber status-warn, carrying a leading icon that the "Completed with errors" tier does not, so the two amber tiers stay glanceably distinct.
    - An outcome value outside the frozen set classifies as a grey neutral tier whose label is the title-cased value — the raw value is never rendered as-is.
    - An empty-string outcome yields a defined non-empty fallback label, never an empty badge.
    - Rendering the component for every tier, including the unrecognised one, throws no error.
  </behavior>
  <action>
Create `web/app/components/system/OutcomeBadge.tsx` exporting two things: a pure
`classifyOutcome` function and the `OutcomeBadge` component that renders its result.

`classifyOutcome` takes the two run fields it needs — the outcome and the errored-artist count —
and returns a tier descriptor carrying a label plus the presentation choice: either a custom
class string for the tinted-fill tiers or a badge variant name for the destructive and grey
tiers, and optionally a leading icon component. Implement it as a switch over the three frozen
outcome values with a mandatory default arm. The default arm is the resilience line, not a
formality: a newer backend behind an older bundle must degrade to a grey badge with a
title-cased label, never throw and never surface the raw value. Title-casing an empty value must
still produce a readable word rather than an empty string.

The two `ok` tiers split on the errored-artist count, because the wire has no partial outcome —
the amber completed-with-errors label is derived on the client. Use the exact tier labels and
the exact tinted-fill class shapes from the UI-SPEC Color badge table; the status palette's use
here is one of its five reserved uses, and it must not leak into any button, nav item, link,
border, or focus ring.

`OutcomeBadge` renders the underlying `Badge` with the classified label, merging the custom class
over the variant the way `badge.tsx` already supports, and rendering the leading icon with an
aria-hidden attribute when the tier carries one. Keep the component free of any fetch, state, or
formatting logic — it takes a run and renders a badge.

Create `web/app/components/system/OutcomeBadge.test.tsx` covering every row in the behavior
block: all five tiers by label, the icon presence on the Interrupted tier and its absence on the
other amber tier, the unrecognised-value fallback, and the empty-value fallback.
Assert on accessible text and on the presence of the status-palette class for the tinted tiers.
  </action>
  <acceptance_criteria>
    - `web/app/components/system/OutcomeBadge.tsx` exports both `classifyOutcome` and `OutcomeBadge`.
    - `classifyOutcome` contains a `default` arm; `grep -c "default:" web/app/components/system/OutcomeBadge.tsx` prints a value of 1 or greater.
    - `web/app/components/system/OutcomeBadge.test.tsx` asserts the five labels `Success`, `Completed with errors`, `Failed`, `Interrupted`, and a title-cased fallback for an unrecognised value.
    - A test asserts that rendering an unrecognised outcome value produces a badge whose text is not that raw value.
    - A test asserts the Interrupted tier renders a leading icon element and the "Completed with errors" tier does not.
    - `grep -c "bg-status-ok\|bg-status-warn" web/app/components/system/OutcomeBadge.tsx` prints a value of 2 or greater.
    - `corepack pnpm --dir web exec vitest run --coverage.enabled=false app/components/system/OutcomeBadge.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm --dir web exec prettier --write "**/*.{ts,tsx}" &amp;&amp; corepack pnpm --dir web exec prettier --check "**/*.{ts,tsx}"</automated>
    <fails_when>non-zero exit, or the output lists any file under "Code style issues found"</fails_when>
    <automated>corepack pnpm --dir web exec vitest run --coverage.enabled=false app/components/system/OutcomeBadge.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary reports a non-zero "failed" count, or reports "No test files found" (OutcomeBadge.test.tsx was never created)</fails_when>
  </verify>
  <reversibility rating="reversible">A leaf presentational component with one consumer added in the next task; the tier mapping is copy and color, both revisable without structural change.</reversibility>
  <done>All five badge tiers classify and render correctly, the unrecognised-value path degrades to a grey title-cased badge without throwing, and both amber tiers are distinguishable by icon.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The About block and the empty-watchlist callout</name>
  <files>web/app/components/system/AboutInstance.tsx, web/app/routes/system.tsx, web/app/routes/system.test.tsx</files>
  <read_first>
    - `web/app/routes/system.tsx` and `web/app/routes/system.test.tsx` — the files being modified, as committed by plan 19-01's tracer. The bare `Version` row in the loaded body is what this task replaces.
    - `web/app/components/ui/card.tsx` — the surface analog. Note that `CardTitle` ships a 16px medium weight that the UI-SPEC Typography note requires overriding to the heading token, or replacing with a bare heading element inside `CardHeader`.
    - `web/app/components/ui/alert.tsx` — the callout component; the default variant, not destructive.
    - `19-UI-SPEC.md` Copywriting Contract → About block (all five row rules verbatim) and Empty-watchlist callout (the locked title, description, and link label), plus Color → the pill markup pattern and Typography.
    - `19-CONTEXT.md` D-01, D-02, D-03, D-05.
    - `19-RESEARCH.md` "About-block schema row (D-02)" code example and Pitfall 7 (assert the callout link's target rather than clicking it in a single-route stub).
    - `web/app/lib/format.ts` — `formatPollInterval`, committed by plan 19-02.
    - `docs/api/status-contract.md` — the `instance` block, `watchlist_size`, and `poll_interval_seconds` semantics, including that a schema-read failure still returns a success response with a null schema value.
  </read_first>
  <behavior>
    - Version row renders `instance.app_version` verbatim in a monospace face — a short SHA as-is, and the literal development-build value as-is — with no link anywhere in the row.
    - When `schema_applied` equals `schema_expected`, the Schema row renders a single line naming that one version, with no warning color.
    - When they differ and neither is null, the Schema row renders both values with the amber status-warn treatment and a leading warning icon.
    - When `schema_applied` is null, the Schema row renders an em dash and the Database row renders the unreachable pill using the destructive color.
    - When `schema_applied` is an integer, the Database row renders the reachable pill using the status-ok color.
    - Rendering the About block issues no additional network request — the reachability signal comes from the already-fetched payload.
    - The Watchlist row renders the raw count including when it is zero, with correct singular and plural wording.
    - The Poll interval row renders the humanized interval.
    - When `watchlist_size` is zero, the callout renders above the source panels with its locked title and description and a link whose target is the watchlist root path; the About block and everything else still render around it.
    - When `watchlist_size` is greater than zero, the callout is absent.
  </behavior>
  <action>
Create `web/app/components/system/AboutInstance.tsx` as a presentational component taking the
already-fetched status payload (or just the fields it needs) and rendering a `Card` titled with
the locked About heading, laid out as a two-column definition list: a muted label at the label
type token, a value at the body token.

Implement the five rows exactly as the UI-SPEC About block table specifies. The Version row is
plain monospace text with no anchor — wiring a repository base URL into the frontend is new
config surface explicitly out of scope (D-03). The Schema row has three arms driven solely by the
schema fields: equal renders one line naming the version; unequal-and-non-null renders both
values in the amber status-warn treatment with a leading warning icon; null renders the em dash
and defers the story to the Database row. The Database row derives reachability from the same
schema field — an integer means reachable, null means unreachable — and renders the
corresponding tinted pill using the pill markup pattern from the UI-SPEC Color section. Issue no
request to the readiness endpoint; that badge was considered and deferred, and the whole point of
D-01 is that the already-loaded payload carries the signal. The Watchlist row always shows the
raw count, pluralized, even at zero, because the callout below carries the guidance. The Poll
interval row renders the humanized interval from the format module.

Override the card title's built-in size and weight to the heading type token, or use a bare
heading element inside the card header, per the UI-SPEC Typography note.

In `web/app/routes/system.tsx`, replace the tracer's bare Version row with this component as the
first element of the loaded body, directly under the page heading — the About block anchors the
page per the UI-SPEC Visual Hierarchy. Below it, render the empty-watchlist callout conditionally
on a zero watchlist size: an `Alert` in its default variant with a leading warning icon, carrying
the locked title and description verbatim and an inline React Router link labelled with the
locked link text pointing at the watchlist root path. Render the callout in both the first-run
and loaded shapes — cycles still run against an empty watchlist, so this is a callout above the
panels, never a whole-page state.

Extend `web/app/routes/system.test.tsx` with cases for every row in the behavior block, reusing
and extending the `makeStatus()` fixture builder from plan 19-01 with per-case overrides. For the
callout, assert the link's target attribute rather than clicking it — the route stub renders only
the system route, so a click has nowhere to land.
  </action>
  <acceptance_criteria>
    - `web/app/components/system/AboutInstance.tsx` exists and is imported by `web/app/routes/system.tsx`.
    - `grep -c "fetch(" web/app/components/system/AboutInstance.tsx` prints 0 — reachability is derived, not requested.
    - `grep -c "/ready" web/app/components/system/AboutInstance.tsx web/app/routes/system.tsx` prints 0 for both files.
    - `web/app/routes/system.test.tsx` contains a case where `schema_applied` equals `schema_expected` asserting a single schema line, and a case where they differ asserting both numbers are present.
    - `web/app/routes/system.test.tsx` contains a case with `schema_applied` null asserting the unreachable pill text is present and the schema row shows the em dash.
    - `web/app/routes/system.test.tsx` contains a case with `watchlist_size` zero asserting the callout's locked title renders and its link carries the watchlist root path, plus a case with a non-zero size asserting the callout is absent.
    - `corepack pnpm --dir web exec vitest run --coverage.enabled=false app/routes/system.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm --dir web exec prettier --write "**/*.{ts,tsx}" &amp;&amp; corepack pnpm --dir web exec prettier --check "**/*.{ts,tsx}"</automated>
    <fails_when>non-zero exit, or the output lists any file under "Code style issues found"</fails_when>
    <automated>corepack pnpm --dir web exec vitest run --coverage.enabled=false app/routes/system.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary reports a non-zero "failed" count, or the schema-drift / null-schema / empty-watchlist case names are absent from the reported test list</fails_when>
    <automated>grep -rn "/ready" web/app/components/system web/app/routes/system.tsx | wc -l</automated>
    <fails_when>prints anything other than 0 — a readiness request or link leaked into the view, contradicting D-01's single-signal rule</fails_when>
  </verify>
  <reversibility rating="reversible">Presentational rows over already-fetched data; no schema, no persistence, no new request path.</reversibility>
  <done>The About block renders all five rows with schema-drift, in-sync, and database-unreachable all distinguishable, and the empty-watchlist callout appears exactly when the count is zero without displacing anything else.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The per-source panel — last run, clean-run scan, skip and escalation lines</name>
  <files>web/app/components/system/SourcePanel.tsx, web/app/routes/system.tsx, web/app/routes/system.test.tsx</files>
  <read_first>
    - `web/app/routes/system.tsx` and `web/app/routes/system.test.tsx` — the files being modified, as committed by Task 2.
    - `web/app/components/system/OutcomeBadge.tsx` — the badge and classifier from Task 1, this panel's first consumer.
    - `web/app/lib/format.ts` — `formatAbsoluteTime`, `formatRelativeTime`, `formatIsoTitle`, `formatDuration` from plan 19-02.
    - `web/app/lib/sources.ts` — `sourceDisplayName` and `SOURCE_ORDER` from plan 19-03.
    - `19-UI-SPEC.md` Copywriting Contract → Per-source panel (every line rule verbatim, including the D-08 subtlety callout) and Visual Hierarchy (fixed source order, summary before table).
    - `19-CONTEXT.md` D-07, D-08, D-09, D-09-a.
    - `19-RESEARCH.md` Pattern 5 (the clean-run scan) and the "Rendering a timestamp (D-09)" code example.
    - `docs/api/status-contract.md` — `last_run` is byte-identical to the newest history entry when present; `history` is newest-first and at most 50; `consecutive_skips` resets to zero on the next recorded run.
    - `web/app/components/ui/card.tsx` and `web/app/components/ui/separator.tsx` — the panel surface and the divider before the history table that plan 19-05 adds.
  </read_first>
  <behavior>
    - A source with a last run renders its outcome badge, its finished time as an absolute value inside a time element carrying a title attribute, and its humanized duration.
    - The run's store-composed summary string renders verbatim on its own line.
    - The per-field counts line renders only when the errored-artist count is greater than zero, with the errored figure in the warn color; on a clean run the line is absent.
    - The clean-run line scans history newest-first for the first entry whose outcome is ok and whose errored count is zero, and renders its finished time as a relative phrase — the one relative-primary line in the view.
    - A history containing only ok runs that each errored some artists yields the literal no-clean-run copy, proving the errored-count clause is enforced and not just the outcome clause.
    - A source whose last run is null renders the locked runless line naming that source, and does not render a last-run line, badge, or duration.
    - A source with a non-zero consecutive-skips count renders the warn-colored skip line with a leading warning icon and the absolute time of the last skip, and does so even when that source has no runs.
    - A source whose latest run has the cancelled outcome renders both the Interrupted badge and the per-source escalation line; a source whose latest run is not cancelled renders no escalation line.
    - Panels render in the fixed order from the source-order constant, MusicBrainz first, regardless of the payload's own key order; a source key present in the payload but absent from the constant still renders rather than being dropped.
    - Singular and plural skip wording both read correctly.
  </behavior>
  <action>
Create `web/app/components/system/SourcePanel.tsx` taking a source key and that source's status
block, and rendering a `Card` whose title is the display name from the source lookup, at the
heading type token.

Render, in order, exactly the lines the UI-SPEC Per-source panel table specifies. The last-run
line combines the outcome badge, the absolute finished time, and the humanized duration; render
the timestamp inside a time element whose machine-readable attribute is the raw wire value and
whose title attribute carries the relative phrasing plus the full local timestamp with offset, so
the visible value never goes stale while the hover detail stays available (D-09). The summary line
renders the run's summary string verbatim — it is store-composed from integer counts and a
normalized outcome, so it is leak-safe by construction and re-deriving it on the client would
only risk drifting from backend semantics. The per-field counts line renders only when the
errored count is above zero, with that figure in the warn color and its labels muted; on a clean
run the summary above already says it and a second near-identical line on every healthy panel is
noise.

The clean-run line is D-08 and it is the phase's most easily mis-implemented rule: find the first
history entry that is both outcome-ok and zero-errored, and render its finished time as a
relative phrase. A run that completed with errored artists is not clean — it wears the amber
badge, and calling that same run the last successful run would contradict the badge on screen.
When nothing in the buffer qualifies, render the locked no-clean-run literal; the wording says
"recent" honestly because the ring buffer holds at most fifty entries per source.

When the source has no last run, replace the last-run, summary, counts, and clean-run lines with
the single locked runless line naming the source's display name. The skip and escalation lines
below still render when they apply — a runless-but-skipping source is precisely the failure this
view exists to surface.

Render the skip line only when the consecutive-skips count is above zero: warn color, leading
warning icon, the count pluralized, and the absolute time of the last skip. Render the escalation
line only when the latest run's outcome is cancelled: warn color, leading warning icon, the locked
crash-loop copy. Drive it from the latest run alone so one shutdown-cancelled run during a deploy
trips it briefly while a real loop keeps it lit. End the panel with a separator, leaving the space
below it for plan 19-05's history table.

In `web/app/routes/system.tsx`, render one panel per source below the About block and the
callout, iterating the source-order constant rather than the payload's key order. Any source key
present in the payload but not in the constant must still render, appended after the known ones —
dropping a source silently is worse than an unordered extra panel.

Extend `web/app/routes/system.test.tsx` with a case per behavior row, extending the `makeStatus()`
fixture with per-case source overrides — in particular a source whose history contains only
errored-but-ok runs, a runless-but-skipping source, and a source whose latest run is cancelled.
  </action>
  <acceptance_criteria>
    - `web/app/components/system/SourcePanel.tsx` exists and is imported by `web/app/routes/system.tsx`.
    - `web/app/components/system/SourcePanel.tsx` renders at least one `<time>` element carrying both a `dateTime` and a `title` attribute.
    - `grep -c "artists_errored === 0\|artists_errored !== 0\|artists_errored > 0" web/app/components/system/SourcePanel.tsx` prints a value of 2 or greater — the clean-run scan and the counts-line condition both key off the errored count.
    - `web/app/routes/system.test.tsx` contains a case whose history holds only ok runs with a non-zero errored count, asserting the no-clean-run literal renders.
    - `web/app/routes/system.test.tsx` contains a case with a null last run and a non-zero consecutive-skips count, asserting both the runless line and the skip line render.
    - `web/app/routes/system.test.tsx` contains a case whose latest run outcome is cancelled, asserting both the Interrupted badge and the escalation copy render, and a counter-case asserting the escalation copy is absent when the latest run is ok.
    - `web/app/routes/system.test.tsx` contains a case asserting the MusicBrainz panel heading precedes the Deezer panel heading in document order even when the fixture lists them in the opposite order.
    - `corepack pnpm --dir web exec vitest run --coverage.enabled=false app/routes/system.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>corepack pnpm --dir web exec prettier --write "**/*.{ts,tsx}" &amp;&amp; corepack pnpm --dir web exec prettier --check "**/*.{ts,tsx}"</automated>
    <fails_when>non-zero exit, or the output lists any file under "Code style issues found"</fails_when>
    <automated>corepack pnpm --dir web exec vitest run --coverage.enabled=false app/routes/system.test.tsx app/components/system/OutcomeBadge.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest summary reports a non-zero "failed" count, or the clean-run-excludes-errored / runless-but-skipping / cancelled-escalation case names are absent from the reported test list</fails_when>
    <automated>corepack pnpm --dir web test</automated>
    <fails_when>non-zero exit, or the output contains a line beginning "ERROR: Coverage for" — the new components under app/components/system are inside the coverage include glob and are not excluded like app/components/ui</fails_when>
  </verify>
  <reversibility rating="reversible">Presentational composition over already-fetched data; every rule is copy, color, or a pure scan over the payload.</reversibility>
  <done>Both source panels render from a real payload with badge, absolute time, duration, verbatim summary, the D-08 clean-run line, and the conditional skip and escalation lines — and a run that completed with errored artists is provably excluded from the clean-run scan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/status` payload -> React DOM | server-authored run summaries, source keys, version strings, and counts are rendered into the operator's page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-11 | Information disclosure | `SourcePanel` rendering `run.summary` verbatim (D-06) | medium | mitigate | `summary` is composed server-side by `internal/pollruns` from integer counts plus a normalized outcome drawn from a closed three-value set; it is never derived from an error string, and the contract declares no error, detail, message, or last_error key anywhere in the success shape (`docs/api/status-contract.md`, ROADMAP security note). The client renders it as a JSX text node and adds no other free-text field to the panel. |
| T-19-12 | Tampering (XSS) | every payload value rendered by `AboutInstance` and `SourcePanel` | medium | mitigate | Every value is rendered as a JSX text node or as an attribute value, both escaped by React. No raw-HTML injection prop is used anywhere under `web/app` — the repo has had zero matches since Phase 06 and the phase gate in plan 19-05 re-asserts it. No payload field is used to construct an `href`: the version row is deliberately unlinked (D-03) and the only link in the view targets a compile-time literal path. |
| T-19-13 | Tampering | unrecognised `outcome` from an N-1/N deploy | medium | mitigate | `classifyOutcome`'s mandatory default arm renders a grey badge with a title-cased fallback label; the run's summary line still renders. Pinned by a dedicated test asserting the raw value is not the badge text and that rendering does not throw. |
| T-19-14 | Information disclosure | a future `/status` field leaking an internal string | low | mitigate | The view renders only the fields enumerated in the plan 19-01 wire types. No generic unknown-field or object-dump renderer exists, so a field added upstream cannot reach the screen without a deliberate client change. |
| T-19-15 | Spoofing | a source key present in the payload but absent from `SOURCE_ORDER` | low | accept | It renders appended after the known panels with its key as the title. The keys originate from compile-time source identifiers in `internal/pollruns`, not from user input, and React escapes the rendered text regardless. Dropping such a source silently would be the worse failure for an operator panel. |
| T-19-SC | Tampering | npm/pnpm installs | low | accept | This plan runs no package install; `web/package.json` and `web/pnpm-lock.yaml` are untouched. `19-RESEARCH.md` `## Package Legitimacy Audit` records zero package installs for the phase. |

**No threat in this plan reaches the configured `high` block-on severity.** The three `medium`
items are all output-encoding or robustness concerns over a server-authored, already-hardened
contract (STAT-02), each mitigated by rendering discipline plus a pinning test rather than by
new security code.
</threat_model>

<verification>
- `corepack pnpm --dir web exec prettier --check "**/*.{ts,tsx}"` clean.
- `corepack pnpm --dir web test` green with all four coverage axes at or above 70.
- `grep -rn "/ready" web/app/components/system web/app/routes/system.tsx | wc -l` prints 0.
- Manual read-back against the UI-SPEC Copywriting Contract: every locked string in the About block, the empty-watchlist callout, and the per-source panel appears verbatim in the implementation.
</verification>

<success_criteria>
- Five badge tiers classify and render, unrecognised values included, without throwing.
- The About block distinguishes in-sync schema, schema drift, and an unreachable database from one payload field, with no second request.
- The empty-watchlist callout appears exactly when the count is zero and displaces nothing.
- The D-08 clean-run scan provably excludes a run that completed with errored artists.
- A runless-but-skipping source renders its runless line and its skip line together.
- Panels render in fixed source order.
</success_criteria>

<output>
Create `.planning/phases/19-frontend-system-view/19-04-SUMMARY.md` when done
</output>
