---
phase: 08-frontend-test-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/pnpm-lock.yaml
  - web/vitest.config.ts
  - web/vitest.setup.ts
  - web/app/components/history/HistoryFilters.test.tsx
  - .github/workflows/full-pipeline.yml
autonomous: false
requirements: [TEST-01, TEST-02]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`pnpm --dir web test` runs the Vitest suite in a jsdom environment and exits non-zero when any component test fails"
    - "The suite never reports a vacuous green: `web/vitest.config.ts` does not enable Vitest's pass-with-no-tests escape hatch, so a run that discovers zero test files exits non-zero"
    - "Test outcomes do not depend on file or test execution order: mock state is reset between tests and a shuffled run is still green"
    - "Running the full suite twice in a row produces the same green result: no state leaks between runs"
    - "The whole suite passes with no dev server, no Go backend, and no network access: test files run in isolated environments and share no external state"
    - "The history/event-filter surface has a passing test asserting the artist list populates from `listWatchlist` and that a filter change is reported upward through `onChange`"
    - "Clearing the artist filter reports `artistId: null`, never `0`: the empty select value is never coerced through a numeric conversion"
    - "Every push runs the frontend suite as its own `frontend-test` job in Full Pipeline's parallel tier"
  prohibitions:
    - requirement_id: TEST-01
      category: transparency
      statement: "MUST NOT reach green by weakening an assertion, skipping/`.skip`-ing a test, or enabling a no-tests-still-passes escape — a failing test is fixed in the component under test, never in the expectation"
    - requirement_id: TEST-02
      category: safety
      statement: "MUST NOT issue a real network request to MusicBrainz, Deezer, Discord, or a local API server from any test — third-party services are never contacted by CI on every push"
  artifacts:
    - path: "web/vitest.config.ts"
      provides: "Standalone Vitest config: `~` alias, jsdom environment, setup file, per-test mock reset"
      contains: "jsdom"
    - path: "web/vitest.setup.ts"
      provides: "jest-dom matcher registration for every test file"
      contains: "@testing-library/jest-dom/vitest"
    - path: "web/package.json"
      provides: "`test` and `test:watch` scripts plus the five test devDependencies"
      contains: "vitest"
    - path: "web/app/components/history/HistoryFilters.test.tsx"
      provides: "History/event-filter surface test: watchlist population + upward filter reporting"
      min_lines: 40
    - path: ".github/workflows/full-pipeline.yml"
      provides: "`frontend-test` job in the parallel tier"
      contains: "frontend-test"
  key_links:
    - from: "web/vitest.config.ts"
      to: "web/app/"
      via: "resolve.alias maps `~` to the app directory, replicating tsconfig.json's only path alias"
      pattern: "alias"
    - from: "web/vitest.config.ts"
      to: "web/vitest.setup.ts"
      via: "test.setupFiles registers the jest-dom matchers before any test runs"
      pattern: "setupFiles"
    - from: "web/app/components/history/HistoryFilters.test.tsx"
      to: "web/app/lib/api.ts"
      via: "bare module mock replaces every named export with a vi.fn() stand-in"
      pattern: "vi\\.mock"
    - from: ".github/workflows/full-pipeline.yml"
      to: "web/package.json"
      via: "the frontend-test job's final step runs the `test` script from the web working directory"
      pattern: "pnpm test"
---

<objective>
Stand up the frontend test toolchain end-to-end and prove it with one real, permanently-kept
component test, then wire the same command into CI.

This is the phase's tracer slice: install -> config -> one green component test -> CI job. Every
later plan in this phase is an expansion outward from the harness proven here. Nothing in this plan
is scaffolding to be replaced later — `vitest.config.ts`, `vitest.setup.ts`, the `test` script, the
`HistoryFilters.test.tsx` surface test, and the `frontend-test` CI job are all final deliverables.

Purpose: TEST-01 and TEST-02 both hinge on a working, API-boundary-mocking harness. Until one real
test runs green in jsdom against a real component with `~/lib/api` mocked, every other test in this
phase is speculative.

Output: `web/vitest.config.ts`, `web/vitest.setup.ts`, `web/package.json` scripts + devDependencies,
`web/app/components/history/HistoryFilters.test.tsx`, and a `frontend-test` job in
`.github/workflows/full-pipeline.yml`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-frontend-test-suite/08-CONTEXT.md
@.planning/phases/08-frontend-test-suite/08-RESEARCH.md
@.planning/phases/08-frontend-test-suite/08-PATTERNS.md
@web/app/components/history/HistoryFilters.tsx
@web/app/lib/api.ts
@web/vite.config.ts
@web/tsconfig.json
@.github/workflows/full-pipeline.yml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for three [SUS]-flagged packages and two new pinned Action SHAs</name>
  <read_first>
    - `.planning/phases/08-frontend-test-suite/08-RESEARCH.md` section "## Package Legitimacy Audit" — the six-row table and the paragraph explaining why three rows are flagged
    - `.planning/REQUIREMENTS.md` CICD-08 (third-party Actions pinned to commit SHAs, not tags)
  </read_first>
  <what-built>
    Nothing yet — this gate runs BEFORE any package-manager install, per the Package Legitimacy Gate
    protocol. RESEARCH.md's audit table flags three of the five packages this phase installs as
    `[SUS]`. In all three cases the sole signal is "too-new" (a version published within the last few
    days/weeks), not low downloads, not a missing source repo. All three have 46M-91M weekly
    downloads and a verified official GitHub org repo. This checkpoint is expected to pass trivially;
    it exists because a legitimacy checkpoint is never auto-approvable.
  </what-built>
  <how-to-verify>
    1. Open `https://www.npmjs.com/package/jsdom` — confirm the repository link points at
       `github.com/jsdom/jsdom` and weekly downloads are in the tens of millions.
    2. Open `https://www.npmjs.com/package/@testing-library/jest-dom` — confirm the repository link
       points at `github.com/testing-library/jest-dom`.
    3. Open `https://www.npmjs.com/package/@testing-library/user-event` — confirm the repository link
       points at `github.com/testing-library/user-event`.
    4. Confirm no near-miss typosquat name is being installed: the exact strings to be installed are
       `vitest`, `jsdom`, `@testing-library/react`, `@testing-library/jest-dom`,
       `@testing-library/user-event` — five packages, no others.
    5. Confirm the two NEW pinned GitHub Action SHAs resolve to the tags RESEARCH.md claims:
       `https://github.com/pnpm/action-setup/commits/v6.0.10` should show
       `ff378ebe6b225b0680b81c1ad4498ae0d1d3a5e3`, and
       `https://github.com/actions/setup-node/commits/v7.0.0` should show
       `820762786026740c76f36085b0efc47a31fe5020`. If either differs, report the correct SHA rather
       than approving.
  </how-to-verify>
  <acceptance_criteria>
    - Human has confirmed all three flagged npm packages resolve to their official
      `testing-library` / `jsdom` GitHub org repositories.
    - Human has confirmed both new Action SHAs match their claimed tags, or has supplied corrected
      SHAs for Task 3 to use.
    - No install command has been run before this checkpoint returned.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with the install, or supply corrected SHAs / flag a package</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: Vitest harness green end-to-end — install, config, setup, script, first real component test</name>
  <read_first>
    - `web/vite.config.ts` — the production config that must NOT be reused or merged (it loads the
      React Router Vite dev plugin, which breaks under Vitest; RESEARCH.md Pitfall 1)
    - `web/tsconfig.json` — confirm the only path alias is `~/*` -> `./app/*`, and that
      `include: ["**/*"]` already covers new root-level `.ts` and co-located `.test.tsx` files
    - `web/package.json` — current scripts and devDependencies (zero test tooling today)
    - `web/app/components/history/HistoryFilters.tsx` — the component under test, in full
    - `web/app/lib/api.ts` lines 12-51 (`EventItem`, `WatchlistEntry` wire types used as fixture
      shapes) and line 147-149 (`listWatchlist`)
    - `.planning/phases/08-frontend-test-suite/08-RESEARCH.md` sections "Standard Stack",
      "Code Examples", "Common Pitfalls" (Pitfalls 1, 2, 3)
    - `.planning/phases/08-frontend-test-suite/08-PATTERNS.md` section
      "`web/app/components/history/HistoryFilters.test.tsx` (test)"
  </read_first>
  <reversibility rating="reversible">
    A standalone Vitest config that never merges the production Vite config is a config-file-level
    choice (D-01); reverting is deleting one file. Threading it through `mergeConfig` later would be
    equally cheap.
  </reversibility>
  <action>
    Install exactly five devDependencies from `web/`, at the versions RESEARCH.md verified against the
    live npm registry: `vitest@4.1.10`, `jsdom@30.0.1`, `@testing-library/react@16.3.2`,
    `@testing-library/jest-dom@7.0.1`, `@testing-library/user-event@14.6.4`. Do not install
    `@vitest/coverage-v8` — coverage is Phase 9's scope (D-05). An `EBADENGINE` warning from jsdom
    against this machine's Node v22.21.1 is expected and non-blocking (RESEARCH.md Pitfall 3); do not
    downgrade jsdom to silence it.

    Create `web/vitest.config.ts` as a fully independent file — never import from, merge with, or
    reference `web/vite.config.ts`, and never load the React Router Vite dev plugin
    (`@react-router/dev/vite`), which does virtual-module resolution that only works inside
    `react-router dev`/`build`. Import `defineConfig` from `vitest/config` and Node's `path`. Set
    `resolve.alias` so `~` resolves to the `app` directory (a manual alias replicating tsconfig's one
    path mapping — do not add `vite-tsconfig-paths`). Under `test`, set `environment` to `jsdom`,
    `setupFiles` to the setup file below, and `mockReset` to true. `mockReset` is load-bearing, not
    hygiene: D-06 has every test file set per-test resolved/rejected values on an auto-mocked
    `~/lib/api`, and without a per-test reset one test's queued value silently satisfies the next
    test's expectation. Do NOT set Vitest's pass-with-no-tests escape hatch — a run that discovers
    zero test files must exit non-zero.

    Create `web/vitest.setup.ts` containing the single side-effect import of
    `@testing-library/jest-dom/vitest`, which registers the DOM matchers and (because tsconfig's
    `include` already covers this file) supplies their TypeScript augmentation to `tsc`.

    Add two scripts to `web/package.json`: `test` running `vitest run` (single-shot, never watch mode)
    and `test:watch` running `vitest`.

    Write `web/app/components/history/HistoryFilters.test.tsx`, co-located beside its source per D-02.
    Mock the API module with a bare `vi.mock("~/lib/api")` call at the top of the file — no factory,
    no passthrough that re-exports the real module, so no real `apiFetch` can ever reach the runtime's
    own fetch (TEST-02). Build a local (non-exported) `WatchlistEntry[]` fixture using the exact field
    names from `web/app/lib/api.ts`. Cover three behaviors as separate `it(...)` cases, RTL-idiomatic
    sentence naming (not Go's `Test_X_Y` casing), one behavior per test per D-02's carried-over
    philosophy:
    (a) the artist select populates from `listWatchlist` — resolve the mock with the fixture, render
        with a `value` of both-axes-null and a `vi.fn()` `onChange`, and find each fixture artist's
        name as an option;
    (b) choosing an artist reports the whole new value upward — select a fixture artist and assert
        `onChange` was called with the previous value spread plus that artist's numeric `artist_id`;
    (c) choosing the "All artists" option reports `artistId` as `null`, never `0` — this pins the
        empty-string branch of the select's change handler so a future refactor cannot collapse
        "no filter" and "artist id zero" into the same value.
    Query the two selects by their wrapping label text, `Artist` and `Event type`; if a label query
    does not resolve, fall back to `getByRole("combobox", { name: ... })` with the same strings rather
    than adding test ids to the component. If a JSX transform error appears, add
    `@vitejs/plugin-react@6.0.5` to `vitest.config.ts`'s plugins array — never add the React Router
    dev plugin back (RESEARCH.md Pitfall 2, assumption A1).
  </action>
  <verify>
    <automated>pnpm --dir web test</automated>
    <automated>pnpm --dir web typecheck</automated>
    <automated>pnpm --dir web exec vitest run --sequence.shuffle</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir web test` exits 0 and its summary line matches `Test Files +[0-9]+ passed`.
    - `pnpm --dir web test` run a second time immediately afterwards also exits 0 with the same
      passing summary (idempotent across runs).
    - `pnpm --dir web exec vitest run --sequence.shuffle` exits 0. If the installed Vitest rejects
      that CLI flag spelling, set `sequence: { shuffle: true }` under `test` in `vitest.config.ts`
      and run the plain suite instead — the criterion is that a shuffled run is green, not the flag
      spelling.
    - `pnpm --dir web typecheck` exits 0 (the new `.ts`/`.test.tsx` files are already inside
      tsconfig's `include: ["**/*"]`).
    - `grep -v '^\s*//' web/vitest.config.ts | grep -c 'jsdom'` is at least 1.
    - `grep -v '^\s*//' web/vitest.config.ts | grep -c 'mockReset'` is at least 1.
    - `grep -c 'passWithNoTests' web/vitest.config.ts` is 0.
    - `! grep -q 'react-router/dev' web/vitest.config.ts` succeeds.
      <!-- planner-discipline-allow: react-router/dev -->
    - `grep -c '"test": "vitest run"' web/package.json` is 1.
    - `grep -c 'vi.mock("~/lib/api")' web/app/components/history/HistoryFilters.test.tsx` is 1.
    - `! grep -rnE "(global|globalThis)\.fetch" web/app --include='*.test.tsx'` succeeds — no test
      patches the runtime's own network function.
    - Killing any running dev server and disconnecting the network does not change the result: the
      suite still exits 0.
  </acceptance_criteria>
  <done>
    The five devDependencies are installed and locked, `vitest.config.ts` and `vitest.setup.ts` exist,
    `pnpm --dir web test` runs green in jsdom, and HistoryFilters' three named behaviors are asserted
    against a bare-mocked `~/lib/api` with no server running.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add the frontend-test job to Full Pipeline's parallel tier</name>
  <read_first>
    - `.github/workflows/full-pipeline.yml` in full — note the five existing parallel-tier jobs
      (`vet`, `lint`, `test`, `gitleaks`, `trivy-fs`), their `timeout-minutes` convention, and the
      `actions/checkout` SHA already pinned throughout
    - `.planning/phases/08-frontend-test-suite/08-CONTEXT.md` D-04 — new job, every push, report-only,
      no coverage threshold
    - `.planning/phases/08-frontend-test-suite/08-RESEARCH.md` "Code Examples" (the verified job block
      with pinned SHAs) and Pitfall 4 (step ordering)
    - `.planning/ROADMAP.md` Phase 9 success criterion 3 — the phase that owns making a coverage
      failure block the downstream build/scan/release jobs
  </read_first>
  <reversibility rating="reversible">
    D-04 scopes this phase's job to report-only, so `build-scan`'s `needs` array is left untouched;
    Phase 9's success criterion 3 owns adding the gating edge. Adding or removing that one array
    element later is a single-line change either way.
  </reversibility>
  <action>
    Add a `frontend-test` job to `.github/workflows/full-pipeline.yml`, placed among the existing
    parallel-tier jobs (after `trivy-fs`, before `pr-title`) so it runs on every push and pull request
    alongside them. Give it `runs-on: ubuntu-latest`, `timeout-minutes: 10` (matching the other
    parallel jobs), and `defaults.run.working-directory: web` so every step runs from the frontend
    directory.

    Four steps, in this exact order — the pnpm setup MUST come before the Node setup, because
    `actions/setup-node`'s `cache: pnpm` option shells out to `pnpm store path` and fails if pnpm is
    not yet on PATH (RESEARCH.md Pitfall 4):
    1. Checkout, using the same `actions/checkout` SHA already pinned everywhere else in this file
       (`3d3c42e5aac5ba805825da76410c181273ba90b1`, v7.0.1).
    2. `pnpm/action-setup` pinned to `ff378ebe6b225b0680b81c1ad4498ae0d1d3a5e3` (v6.0.10) with
       `version: 11`, matching the local pnpm 11.8.0 this project develops against. Use the SHA the
       Task 1 checkpoint confirmed if it differed.
    3. `actions/setup-node` pinned to `820762786026740c76f36085b0efc47a31fe5020` (v7.0.0) with
       `node-version: '22'` (major-only, so GitHub resolves a 22.x patch new enough for jsdom's
       engines floor), `cache: pnpm`, and `cache-dependency-path: web/pnpm-lock.yaml`. Note the cache
       path is repo-root-relative even though the job's working directory is `web`.
    4. Install with `pnpm install --frozen-lockfile`, then run `pnpm test`.

    Every third-party action must carry a full commit SHA plus a trailing `# vX.Y.Z` comment, per
    CICD-08 and the existing convention in this file — never a bare tag.

    Do NOT add `frontend-test` to `build-scan`'s `needs` array in this phase. D-04 scopes this job to
    report-only: the job itself still exits non-zero and shows red when a component test fails
    (success criterion 1), but it does not yet block the downstream build/scan/release jobs. Phase 9's
    success criterion 3 explicitly owns making a frontend failure block those jobs, and it edits this
    same job. Leave the existing `needs` array byte-identical.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/full-pipeline.yml','utf8'); if(!/^  frontend-test:$/m.test(y)) throw new Error('frontend-test job not declared'); if(!/needs: \[vet, lint, test, gitleaks, trivy-fs\]/.test(y)) throw new Error('build-scan needs array was modified'); console.log('ok')"</automated>
    <automated>grep -v '^\s*#' .github/workflows/full-pipeline.yml | grep -c 'pnpm install --frozen-lockfile'</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/full-pipeline.yml` declares a top-level `frontend-test:` job under `jobs:`.
    - The job's steps appear in the order checkout, pnpm setup, node setup, install, test — the pnpm
      setup step precedes the `actions/setup-node` step that requests `cache: pnpm`.
    - Both new `uses:` lines carry a 40-character commit SHA followed by a `# v...` comment; running
      `grep -v '^\s*#' .github/workflows/full-pipeline.yml | grep -cE 'uses: [^@]+@v[0-9]'` returns 0
      (no bare-tag pins anywhere in the file).
    - `grep -v '^\s*#' .github/workflows/full-pipeline.yml | grep -c 'cache-dependency-path: web/pnpm-lock.yaml'` is 1.
    - `build-scan`'s `needs:` line is still exactly `needs: [vet, lint, test, gitleaks, trivy-fs]` —
      unchanged by this phase.
    - The workflow file still parses as valid YAML.
  </acceptance_criteria>
  <done>
    Every push and pull request runs the Vitest suite as its own `frontend-test` job in the parallel
    tier, using SHA-pinned actions, with `build-scan`'s gating relationships untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> `web/node_modules` | Third-party package code enters the local and CI test toolchain; five new packages this phase |
| GitHub Actions marketplace -> CI runner | Two new third-party actions execute with repository-scoped permissions on every push |
| Test process -> public internet | A test that does not mock the API boundary would reach the real MusicBrainz/Deezer APIs from CI on every push |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-SC | Tampering | pnpm installs in `web/` (vitest, jsdom, @testing-library/*) | high | mitigate | RESEARCH.md Package Legitimacy Audit ran a slopcheck on all six candidates; the three `[SUS]` ("too-new") rows are gated behind Task 1's blocking human checkpoint before any install runs. Versions are exact-pinned, and `web/pnpm-lock.yaml` is committed so CI installs with `--frozen-lockfile`. |
| T-08-SC2 | Tampering | `pnpm/action-setup`, `actions/setup-node` in `full-pipeline.yml` | high | mitigate | Both pinned to full 40-char commit SHAs (CICD-08), and both SHAs are re-verified against their claimed tags by the human in Task 1 before the workflow edit lands. |
| T-08-03 | Tampering | Test suite reaching live third-party APIs | low | mitigate | Every test file that imports a function from `~/lib/api` calls `vi.mock("~/lib/api")` bare (no passthrough factory), so no real `apiFetch` can reach the runtime's fetch. Enforced by acceptance criteria greps and by the invariant that the suite passes offline with no server running. |
| T-08-04 | Information Disclosure | `frontend-test` CI job logs | low | accept | The job consumes no secrets — it checks out, installs from a committed lockfile, and runs the test suite. It requests no `permissions:` beyond the workflow-level `contents: read`. No credential can appear in its output. Accepted rather than mitigated. |
</threat_model>

## Artifacts this phase produces

Created by this plan:

| Artifact | Kind | Notes |
|----------|------|-------|
| `web/vitest.config.ts` | new file | Standalone Vitest config; default export only, no named exports |
| `web/vitest.setup.ts` | new file | Side-effect-only module; no exports |
| `web/package.json` -> `scripts.test` | new script | `vitest run` |
| `web/package.json` -> `scripts.test:watch` | new script | `vitest` |
| `web/package.json` -> five new `devDependencies` | new deps | `vitest@4.1.10`, `jsdom@30.0.1`, `@testing-library/react@16.3.2`, `@testing-library/jest-dom@7.0.1`, `@testing-library/user-event@14.6.4` |
| `web/pnpm-lock.yaml` | modified | Locked resolutions for the five new packages |
| `web/app/components/history/HistoryFilters.test.tsx` | new file | Test file; no exports. Local fixture helpers stay file-local (not exported) |
| `.github/workflows/full-pipeline.yml` -> `frontend-test` job | new CI job | Parallel tier; not referenced by `build-scan.needs` this phase |

Produced by sibling plans in this phase (for cross-plan reference):

| Artifact | Plan | Notes |
|----------|------|-------|
| `web/app/lib/test/routeStub.tsx` -> `renderRoute` | 08-02 | The one shared `createRoutesStub` helper (D-03); exported |
| `web/app/routes/watchlist.test.tsx` | 08-02 | Test file; no exports |
| `web/app/components/watchlist/PreferenceToggles.test.tsx` | 08-02 | Test file; no exports |
| `web/app/components/watchlist/SearchBox.test.tsx` | 08-03 | Test file; no exports |
| `web/app/lib/api.ts` -> `searchArtists(query, signal?)` | 08-03 | Widened signature (second optional `AbortSignal` param) |
| `web/app/components/history/EventCard.test.tsx` | 08-04, 08-05 | Test file; no exports |
| `web/app/components/history/EventCard.tsx` -> fallback badge | 08-04 | `guestFeatureHref` stays unexported — asserted through the rendered anchor |

<assumptions>
Explicit flagged assumptions carried into execution (from RESEARCH.md; none are blockers, each has a
documented cheap fallback):

- **A1 — esbuild's default JSX transform is sufficient.** Vite's built-in transform should handle
  `.tsx` test files without `@vitejs/plugin-react` once the React Router dev plugin is absent.
  MEDIUM confidence. Fallback: add `@vitejs/plugin-react@6.0.5` (version-verified) to
  `vitest.config.ts`'s plugins array.
- **A2 — `sonner`'s `toast.error(...)` is safe in jsdom with no `<Toaster />` mounted.** Not
  confirmed against `sonner@2.0.8`. Fallback: add a minimal `ResizeObserver` stub to
  `vitest.setup.ts`. Relevant to plan 08-02, not this plan.
- **A3 / Open Question 1 — `build-scan` gating.** RESEARCH.md recommended adding `frontend-test` to
  `build-scan`'s `needs`. This plan deliberately does NOT, reading D-04's "report-only" as scoping
  this phase to running-and-reporting, and reading ROADMAP Phase 9 success criterion 3 ("a coverage
  failure on either side blocks the downstream build/scan/release jobs") as the phase that owns the
  gating edge. Reversal is a one-line array edit.
- **Open Question 2 — router-stub location.** `web/app/lib/test/routeStub.tsx` adopted (importable
  via the same `~/lib/...` convention as `~/lib/api`, mirroring the Go `internal/testutil/`
  precedent). No locked decision constrained the path. Implemented in plan 08-02.
- **Pitfall 3 — local Node v22.21.1 is below `jsdom@30.0.1`'s `^22.22.2` engines floor.** Neither
  npm nor pnpm hard-fails on an engines mismatch here (`engine-strict` is false, no `.npmrc`
  override). Expect a warning locally; CI resolves a new-enough 22.x patch. If jsdom genuinely
  crashes locally, upgrade Node — never downgrade jsdom.

Edge-probe coverage bookkeeping (spec-less fallback, no SPEC.md exists for this phase): 8 probe items
surfaced across TEST-01/TEST-02, 8 authored as `must_haves.truths` (5 in this plan, 1 in 08-02,
2 in 08-03), 0 backstop markers, 0 unresolved. Prohibition recall: 12 raw Stage-1 candidates, 2 kept
after Stage-2 precision (both authored above, descriptor-less), 1 canon-referral breadcrumb —
secrets-in-CI-logs is canon, covered by `/gsd-secure-phase` and the existing gitleaks pre-commit hook
and CI job; not minted here.
</assumptions>

<verification>
- `pnpm --dir web test` exits 0 from a clean checkout with no server running and no network.
- `pnpm --dir web test` exits non-zero if any assertion in `HistoryFilters.test.tsx` is broken
  (verify by temporarily inverting one expectation, observing the failure, then restoring it).
- `pnpm --dir web typecheck` exits 0.
- The `frontend-test` job appears in the Full Pipeline run for the commit that adds it, and its
  "Run Vitest suite" step is green.
- `build-scan`'s `needs` array is unchanged from before this plan.
</verification>

<success_criteria>
- One command (`pnpm --dir web test`) runs the frontend suite locally and in CI and exits non-zero on
  a component regression (ROADMAP SC1).
- The history/event-filter surface has at least one passing test asserting user-visible behavior
  (ROADMAP SC2, partial — remaining surfaces land in 08-02 and 08-03).
- The test mocks `web/app/lib/api.ts`, not raw fetch, and the suite passes with no server running
  (ROADMAP SC3, TEST-02).
- The `frontend-test` job runs on every push in Full Pipeline's parallel tier (D-04).
</success_criteria>

<output>
Create `.planning/phases/08-frontend-test-suite/08-01-SUMMARY.md` when done
</output>
