---
phase: 10-event-retention-window
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/config_test.go
  - .env.example
  - queries/events.sql
  - internal/db/sqlc/events.sql.go
  - internal/db/sqlc/querier.go
  - internal/events/service.go
  - cmd/server/main.go
  - internal/httpserver/events_test.go
  - internal/httpserver/boot_e2e_test.go
autonomous: true
requirements: [DATA-01, DATA-02]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "With EVENT_RETENTION_DAYS unset, config.Load() returns EventRetentionDays == 90; with it set to 30, Load() returns 30 (DATA-01, D-02)."
    - "With EVENT_RETENTION_DAYS set to 0 or a negative value, config.Load() returns a non-nil error whose text names EVENT_RETENTION_DAYS, and the process does not boot (DATA-01, D-03)."
    - ".env.example documents an EVENT_RETENTION_DAYS key, so TestEnvExampleCompleteness passes (DATA-01)."
    - "GET /events omits every event whose created_at is strictly older than now minus EVENT_RETENTION_DAYS days, and still returns every event inside the window (DATA-02, D-01)."
    - "An event whose created_at falls exactly on the cutoff boundary is still returned by GET /events — the comparison is >=, never > (DATA-02, D-04)."
    - "An events row aged past the retention window is still returned by ListExternalIDs, still makes HasAnyEvent report true for its artist+source, still contributes its track_count to GroupTrackCountBaseline, and is still visible to ListUnnotified while notified_at is NULL (DATA-02, roadmap success criteria 3-5)."
    - "Two sequential cursor-paginated GET /events pages never return the same event id twice, even though each page recomputes its own retention cutoff from time.Now() (DATA-02, edge-probe DATA-02/concurrency resolved covered)."
    - statement: "The retention read path holds no shared mutable state and performs no write — a GET /events request that is interrupted or issued in parallel with others cannot leave the events table in a partial or inconsistent state."
      verification: backstop
  prohibitions:
    - statement: "Retention must never remove or modify an events row — no row-removal or row-mutating statement may be introduced anywhere in the retention read path."
      status: required
      verification: flagged-unverified
    - statement: "The retention cutoff must never be applied to ListExternalIDs, HasAnyEvent, GroupTrackCountBaseline, or ListUnnotified."
      status: required
      verification: flagged-unverified
    - statement: "The retention cutoff parameter must never be optional or nullable — no caller may pass a null cutoff and receive unfiltered rows."
      status: required
      verification: flagged-unverified
  artifacts:
    - internal/config/config.go (Config.EventRetentionDays field + post-parse validation in Load)
    - .env.example (EVENT_RETENTION_DAYS key)
    - queries/events.sql (ListEvents gains a required cutoff parameter)
    - internal/db/sqlc/events.sql.go (regenerated ListEventsParams.Cutoff)
    - internal/events/service.go (Service.retentionDays, widened NewService, cutoff computation)
    - internal/httpserver/events_test.go (retention + integrity tests, insertTestEventAt helper)
  key_links:
    - "config.Load() -> cmd/server/main.go -> events.NewService(q, cfg.EventRetentionDays) -> Service.List -> sqlc.ListEventsParams.Cutoff -> ListEvents WHERE created_at >= cutoff"
    - "events.Service.List computes the cutoff in the Go domain layer (never SQL now(), never the HTTP boundary), mirroring the existing PageSize clamp precedent"
---

<objective>
Deliver the retention window end-to-end: an operator-configurable `EVENT_RETENTION_DAYS` (default 90) that causes `GET /events` to hide events older than the window, while every detection-state query keeps seeing the full, unfiltered table.

Purpose: DATA-01 and DATA-02's core behavior, plus automated proof of the roadmap's success criteria 3-5 — the three regressions a hard-delete implementation would have reintroduced (dedup-key loss, seed-mode reset, deluxe-baseline loss).
Output: A configured, validated retention window applied as one always-on predicate on exactly one SQL query, with integration tests proving both the filter and the untouched-query invariant.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-event-retention-window/10-CONTEXT.md
@.planning/phases/10-event-retention-window/10-RESEARCH.md
@.planning/phases/10-event-retention-window/10-PATTERNS.md
</context>

## Corrections to 10-RESEARCH.md (read before starting)

Three claims in `10-RESEARCH.md` were verified wrong against the live tree during planning. Trust this section over the research document:

1. **`events.NewService(` has SIX call sites, not two.** RESEARCH.md Pitfall 3 lists only `cmd/server/main.go:104` and `internal/httpserver/boot_e2e_test.go:54`. The real list, confirmed by grep over `*.go`: `cmd/server/main.go:104`, `internal/httpserver/boot_e2e_test.go:54`, and `internal/httpserver/events_test.go` lines **317, 383, 484, 582**. All six need the new second argument. The rest of Pitfall 3 still holds — `httpserver.New`'s signature is NOT widened, and `stubEventsStore`-based call sites need no change.
2. **`insertTestEvent` cannot set `created_at`.** `internal/httpserver/events_test.go:343` and `insertTestEventTyped` at line 437 both insert without a `created_at` column, relying on the `DEFAULT now()`. Retention tests need a new helper that takes an explicit timestamp — they cannot reuse the existing two.
3. **`HasAnyEvent` uses no `LIMIT 1` inside its `EXISTS`.** RESEARCH.md's "Don't Hand-Roll" row suggests `EXISTS(SELECT 1 ... LIMIT 1)`. The existing idiom at `queries/events.sql:30-32` has no `LIMIT`. Follow the existing file, not the research snippet. (Relevant to plan 10-02, noted here for continuity.)

## Artifacts this phase produces

New symbols created by this plan — none of these exist in the tree today:

| Symbol | Kind | Location |
|---|---|---|
| `EVENT_RETENTION_DAYS` | env var | `.env.example`, `internal/config/config.go` |
| `Config.EventRetentionDays` | Go struct field (`int`) | `internal/config/config.go` |
| `cutoff` | sqlc named query parameter | `queries/events.sql` (`ListEvents`) |
| `ListEventsParams.Cutoff` | generated Go struct field | `internal/db/sqlc/events.sql.go` |
| `Service.retentionDays` | unexported Go struct field | `internal/events/service.go` |
| `NewService(q, retentionDays)` | widened constructor signature | `internal/events/service.go` |
| `insertTestEventAt` | test helper func | `internal/httpserver/events_test.go` |
| `TestLoad_EventRetentionDaysDefaultsTo90` | test func | `internal/config/config_test.go` |
| `TestLoad_EventRetentionDaysOverride` | test func | `internal/config/config_test.go` |
| `TestLoad_EventRetentionDaysRejectsNonPositive` | test func | `internal/config/config_test.go` |
| `TestListEvents_RetentionExcludesAgedOutRows` | test func | `internal/httpserver/events_test.go` |
| `TestListEvents_RetentionBoundaryIsInclusive` | test func | `internal/httpserver/events_test.go` |
| `TestListEvents_RetentionPagesNeverRepeatAnID` | test func | `internal/httpserver/events_test.go` |
| `TestRetention_DetectionStateQueriesStayUnfiltered` | test func | `internal/httpserver/events_test.go` |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end retention filter — one aged-out event disappears from GET /events</name>
  <files>internal/config/config.go, .env.example, queries/events.sql, internal/db/sqlc/events.sql.go, internal/db/sqlc/querier.go, internal/events/service.go, cmd/server/main.go, internal/httpserver/boot_e2e_test.go, internal/httpserver/events_test.go</files>
  <read_first>
    - `internal/config/config.go` — the `Config` struct's per-phase field-grouping comment convention and `Load()`'s current shape
    - `queries/events.sql` — the whole file: `ListEvents`'s comment block and `sqlc.narg` filter idiom, plus the four queries that must stay untouched and the comments explaining why
    - `internal/events/service.go` — `Service`, `NewService`, `List`'s PageSize clamp, `Page`, `toEvent`
    - `internal/detection/detector.go` lines 90-105 — `seedNotifiedAt`, this codebase's only precedent for hand-building a `pgtype.Timestamptz` from a Go `time.Time`
    - `internal/httpserver/events_test.go` — `insertTestEvent` (line 343), `insertTestArtist` (line 356), `recordingQuerier` (line 189), `testMBID`, and the four `events.NewService(` call sites at lines 317, 383, 484, 582
    - `cmd/server/main.go` around line 104 — the existing `eventsStore := events.NewService(sqlc.New(pool))` wiring and how `cfg` is already in scope
    - `internal/httpserver/boot_e2e_test.go` around line 54
    - `.env.example` — the existing key/comment formatting the new line must match
    - `sqlc.yaml` and `Makefile` — the pinned sqlc version and the `sqlc` / `sqlc-check` targets
  </read_first>
  <behavior>
    - Given `EVENT_RETENTION_DAYS` unset, `config.Load()` yields `EventRetentionDays == 90`.
    - Given an events row created 120 days ago and another created 1 day ago, `GET /events` returns only the 1-day-old row.
    - Given the same two rows, the aged-out row is still physically present in the events table after the request.
  </behavior>
  <action>
    Wire the retention window through every backend layer in one pass, proving the whole path before any edge case is added.

    **Config (D-02, D-03).** Add to `Config` a new `// Phase 10 —` grouped field `EventRetentionDays int` tagged `env:"EVENT_RETENTION_DAYS" envDefault:"90"` — a plain `int`, deliberately not the `time.Duration` shape `PollInterval` uses, per D-02; note in the field comment that this is a per-field judgment call and not a project-wide rule. In `Load()`, after `env.Parse(cfg)` returns nil and before the final `return cfg, nil`, add a manual check rejecting `cfg.EventRetentionDays <= 0` with an error naming `EVENT_RETENTION_DAYS` and echoing the offending value. `caarlos0/env/v11` has no numeric-minimum struct tag, so this must be a manual post-parse check. Placing it strictly after the `env.Parse` error return preserves `TestLoad_AggregatesAllMissing`'s existing aggregate-error behavior. Add `fmt` to the import block.

    **`.env.example`.** Add an `EVENT_RETENTION_DAYS=90` line matching the file's existing key/comment formatting. `TestEnvExampleCompleteness` reflects over `Config`'s tags and diffs against this file — omitting it fails that test.

    **SQL (D-04).** In `queries/events.sql`, add exactly one new predicate to `ListEvents`, ANDed after the three existing `sqlc.narg` clauses and before `ORDER BY`: `created_at >= sqlc.arg('cutoff')::timestamptz`. Use `sqlc.arg`, never `sqlc.narg` — the cutoff is never caller-optional, and a nullable cutoff would create a code path returning unfiltered rows. The operator is `>=`, not `>`: an event exactly at the boundary stays visible per D-04. Extend `ListEvents`'s existing comment block to record why this one query is retention-filtered while `ListExternalIDs`, `HasAnyEvent`, `GroupTrackCountBaseline`, and `ListUnnotified` deliberately are not. Do not edit those four statements or their comments.

    **Codegen.** Run `make sqlc` and commit the regenerated `internal/db/sqlc/` diff. Do not hand-edit generated files — `make sqlc-check` gates on a clean `git diff`. Confirm the generated `ListEventsParams.Cutoff` field's type; RESEARCH.md assumption A1 predicts `pgtype.Timestamptz`, but if sqlc emits `time.Time` instead, adapt the call site and record the deviation in the summary — it is a one-line change, not a design change.

    **Service.** Widen `Service` with a `retentionDays int` field and `NewService` to `NewService(q sqlc.Querier, retentionDays int) *Service`. In `List`, alongside the existing PageSize clamp, compute the cutoff as `time.Now()` minus `retentionDays` whole days and pass it as `Cutoff` in `sqlc.ListEventsParams`. Construct the `pgtype.Timestamptz` with `Valid: true` set explicitly, following `seedNotifiedAt`'s precedent — a zero-value `pgtype.Timestamptz` marshals as SQL NULL, and `created_at >= NULL` is never true, which would silently return an empty feed for every request instead of erroring. Keep the cutoff computation in this domain service, not at the HTTP boundary and not as a SQL-side `now()` expression, matching this file's own documented "clamped here, not at the HTTP boundary" convention.

    **Call sites — all six.** Update `cmd/server/main.go:104` to pass `cfg.EventRetentionDays`. Update `internal/httpserver/boot_e2e_test.go:54` and `internal/httpserver/events_test.go` lines 317, 383, 484, 582 to pass an explicit literal day count. Do not widen `httpserver.New`'s signature and do not touch any `stubEventsStore` call site.

    **Tracer test.** Add `insertTestEventAt` to `internal/httpserver/events_test.go`, modelled on `insertTestEvent` but with an added `created_at` column in the raw INSERT taking a caller-supplied timestamp. Add `TestListEvents_RetentionExcludesAgedOutRows`: seed one artist, insert one event at 120 days ago and one at 1 day ago, build a real `events.NewService(sqlc.New(pool), 90)`, mount it via `httpserver.New` and `httptest.NewServer`, `GET /events`, decode the envelope, and assert the response carries exactly the recent event's id and not the aged-out one. Then assert with a direct `pool.QueryRow` count that both rows are still present in the table — the filter is a read-side filter and removes nothing.
  </action>
  <precondition>A local Postgres is reachable for `testutil.NewTestPool` — run `make db-up` first (docker-compose maps Postgres to host port 5433 per the Phase 5 UAT fix).</precondition>
  <verify>
    <automated>go test ./internal/config/... -run TestEnvExampleCompleteness -short -count=1 && go test ./internal/httpserver/... -run TestListEvents_RetentionExcludesAgedOutRows -count=1 -p 1 && make sqlc-check</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` succeeds — all six `events.NewService(` call sites compile with the new second argument.
    - `grep -c "events.NewService(" ` across `*.go` files returns 6 matches, and `grep -n "func New(" internal/httpserver/server.go` shows an unchanged parameter list.
    - `TestListEvents_RetentionExcludesAgedOutRows` passes: the 1-day-old event id is present in the decoded `events` array, the 120-day-old id is absent, and a direct `SELECT count(*)` over the seeded artist's rows returns 2.
    - `TestEnvExampleCompleteness` passes, proving `.env.example` carries `EVENT_RETENTION_DAYS`.
    - `make sqlc-check` exits 0 — the committed `internal/db/sqlc/` output matches what `sqlc generate` produces from the edited `queries/events.sql`.
    - `queries/events.sql` contains the exact literal `sqlc.arg('cutoff')` and does not contain the literal `sqlc.narg('cutoff')`.
    - `sed -n '/name: ListEvents/,$p' queries/events.sql | grep -c "created_at >= "` returns 1.
    - The four untouched statements are byte-identical to their pre-task versions: `git diff queries/events.sql` shows added lines only inside the `ListEvents` statement and its comment block.
  </acceptance_criteria>
  <reversibility rating="reversible">Widening an unexported-package constructor and adding an always-true-by-default config field; no schema change, no migration, no published contract. Reverting is a code-only change.</reversibility>
  <done>`GET /events` hides events older than 90 days end-to-end from env var to JSON response, the aged-out rows are still in the table, and the generated sqlc output is committed and clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Config edge cases and the D-04 inclusive boundary</name>
  <files>internal/config/config_test.go, internal/httpserver/events_test.go</files>
  <read_first>
    - `internal/config/config_test.go` — `setRequired`, `unsetViaSetenv`, `TestLoad_Defaults` (line 39), `TestLoad_MissingRequired` (line 115), `TestLoad_EmptyRequired` (line 127), `TestLoad_AggregatesAllMissing` (line 139); these are the templates the new cases mirror
    - `internal/config/config.go` — the validation added in Task 1
    - `internal/httpserver/events_test.go` — `insertTestEventAt` added in Task 1, plus `TestListEvents_OrderedNewestFirstAndKeysetPaginates` (line 367) for the cursor-pagination assertion shape
    - `.planning/phases/10-event-retention-window/10-CONTEXT.md` — D-03 and D-04 verbatim
  </read_first>
  <behavior>
    - `EVENT_RETENTION_DAYS` unset yields 90; set to "30" yields 30.
    - `EVENT_RETENTION_DAYS` set to "0" and to "-1" each make `Load()` return an error naming the variable.
    - An event created exactly `EVENT_RETENTION_DAYS` days ago is returned; one created a moment earlier than that is not.
    - Two cursor-linked pages of a retention-filtered feed share no event id.
  </behavior>
  <action>
    Add the DATA-01 config cases to `internal/config/config_test.go`, following the existing `setRequired(t)` + `t.Setenv` convention rather than inventing a new fixture style:

    - `TestLoad_EventRetentionDaysDefaultsTo90` — `setRequired(t)` with `EVENT_RETENTION_DAYS` unset, assert `cfg.EventRetentionDays == 90`.
    - `TestLoad_EventRetentionDaysOverride` — set the var to `30`, assert the parsed value is 30, proving the default is not hardcoded past the env read.
    - `TestLoad_EventRetentionDaysRejectsNonPositive` — a table over the values `0`, `-1`, and `-90`; for each, assert `Load()` returns a non-nil error and that the error text contains `EVENT_RETENTION_DAYS`. This is D-03's fail-fast posture: an invalid window must abort boot, never be silently reinterpreted as "show everything" or "hide everything".

    Add the DATA-02 boundary and pagination cases to `internal/httpserver/events_test.go` using `insertTestEventAt`:

    - `TestListEvents_RetentionBoundaryIsInclusive` — with a retention window of 90 days, insert one event at exactly 90 days before the test's reference `time.Now()` and one a few minutes older than that. Assert the exactly-at-boundary event IS returned and the older one is not. D-04 locks `>=`; the intuitive `>` reading is the wrong one here, and this test is what pins it. Insert the boundary row with a small safety margin toward the visible side (the service recomputes `time.Now()` after the test does, so a mathematically exact boundary would race the clock) — document that margin in a comment so a future reader does not mistake it for sloppiness.
    - `TestListEvents_RetentionPagesNeverRepeatAnID` — seed 5 in-window events plus 2 aged-out events for one artist, request page 1 with a page size of 2, follow `NextCursor` through the remaining pages, and assert the union of returned ids contains no duplicate and excludes both aged-out ids. This resolves the DATA-02 concurrency edge probe: each page recomputes its own cutoff from `time.Now()`, and because the cutoff only ever advances forward while pagination walks `id DESC` from newest to oldest, a later page can only exclude more old rows — it can never resurrect an already-excluded one or re-serve a row the previous page returned.
  </action>
  <precondition>A local Postgres is reachable for `testutil.NewTestPool` — run `make db-up` first.</precondition>
  <verify>
    <automated>go test ./internal/config/... -run "TestLoad_EventRetentionDays" -short -count=1 && go test ./internal/httpserver/... -run "TestListEvents_Retention" -count=1 -p 1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/config/... -run TestLoad_EventRetentionDaysRejectsNonPositive -short -v` shows three subtests passing, one per invalid value.
    - Temporarily flipping the config check from `<= 0` to `< 0` makes `TestLoad_EventRetentionDaysRejectsNonPositive` fail on the `0` case — the test actually pins the boundary. Revert after confirming.
    - Temporarily flipping the SQL predicate from `>=` to `>` makes `TestListEvents_RetentionBoundaryIsInclusive` fail. Revert after confirming, and leave `>=` in place.
    - `TestListEvents_RetentionPagesNeverRepeatAnID` passes and asserts on a set of collected ids, not on a page count.
    - `go test ./internal/config/... -run TestLoad_AggregatesAllMissing -short -count=1` still passes — the new validation did not displace the aggregate-error path.
  </acceptance_criteria>
  <done>DATA-01's default/override/rejection behavior and DATA-02's inclusive boundary and cross-page disjointness are each pinned by a passing automated test.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the detection-state queries stay unfiltered (success criteria 3-5)</name>
  <files>internal/httpserver/events_test.go</files>
  <read_first>
    - `queries/events.sql` — `ListExternalIDs` (line 21), `HasAnyEvent` (line 27), `ListUnnotified` (line 34), `GroupTrackCountBaseline` (line 42), and each one's comment explaining the invariant it protects
    - `internal/httpserver/events_test.go` — `insertTestEventAt` from Task 1, `insertTestArtist`, `testMBID`
    - `internal/db/sqlc/querier.go` — the exact generated signatures and param struct names for those four queries
    - `.planning/ROADMAP.md` § Phase 10 — success criteria 3, 4, 5 verbatim
    - `.planning/phases/10-event-retention-window/10-RESEARCH.md` § Pitfall 4
  </read_first>
  <action>
    Add `TestRetention_DetectionStateQueriesStayUnfiltered` to `internal/httpserver/events_test.go`. This is the phase's single most load-bearing test: it is the automated proof of the roadmap's success criteria 3-5 and the guardrail against a future "consistency" pass adding the retention predicate to queries that must never have it.

    Seed one artist and, using `insertTestEventAt`, one `musicbrainz` `new_release` event whose `created_at` is 200 days in the past — well outside any plausible window — with a populated `release_group_mbid`, a non-zero `track_count`, and `notified_at` left NULL. Then call the four untouched queries directly through `sqlc.New(pool)` and assert, each with its own named subtest tying back to the criterion it proves:

    - **Dedup key intact (criterion 3):** `ListExternalIDs` for that artist+source+event_type returns the aged-out row's `external_id`. If this regressed, the detector would treat an already-recorded release as fresh and re-notify it.
    - **Seed mode not reset (criterion 4):** `HasAnyEvent` for that artist+source returns true. If this regressed, the artist would fall back into seed mode and re-announce their entire back catalogue on the next poll cycle.
    - **Deluxe baseline survives (criterion 5):** `GroupTrackCountBaseline` for that `release_group_mbid` returns `has_baseline == true` and the seeded `track_count` as the baseline. If this regressed, a later tracklist expansion would either fire a false positive or fire nothing at all.
    - **Pending notification still visible:** `ListUnnotified` includes the aged-out row's id while `notified_at` is NULL.

    Add a fifth subtest asserting the contrast that makes the other four meaningful: the same aged-out row is absent from `Service.List`'s result with a 90-day window. Without this pairing, all four assertions would still pass on a build where the retention filter was never wired at all.

    Extend the test's doc comment to state plainly that adding a retention predicate to any of these four queries is the exact regression this test exists to catch, and name the three failure modes. This mirrors the commenting convention already used throughout `queries/events.sql`.
  </action>
  <precondition>A local Postgres is reachable for `testutil.NewTestPool` — run `make db-up` first.</precondition>
  <verify>
    <automated>go test ./internal/httpserver/... -run TestRetention_DetectionStateQueriesStayUnfiltered -count=1 -p 1 -v</automated>
  </verify>
  <acceptance_criteria>
    - The test runs five named subtests, one per detection-state query plus the contrast case, and all pass.
    - Temporarily adding `AND created_at >= now() - interval '90 days'` to `HasAnyEvent` in `queries/events.sql`, running `make sqlc`, and re-running the test makes the seed-mode subtest fail. Revert the query and regenerate before finishing.
    - `sed -n '/name: ListExternalIDs/,/name: ListEvents/p' queries/events.sql | grep -c "cutoff"` returns 0 — no cutoff parameter leaked into any of the four queries.
    - `grep -vE "^\s*--" queries/events.sql | grep -ciE "delete from|truncate"` returns 0 — the phase introduces no row-removal statement.
    - `make test-integration` passes end to end with no regression in the pre-existing `TestListEvents_*` and `TestHandleListEvents_*` suites.
  </acceptance_criteria>
  <done>An aged-out events row is proven, by automated test, to remain fully visible to every dedup, seed-mode, baseline, and notification query while being hidden from the history feed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → process | `EVENT_RETENTION_DAYS` is read once at boot from the process environment; a malformed value must not produce a silently degraded runtime |
| client → `GET /events` | Unauthenticated HTTP request crosses into the events domain service; the retention cutoff is server-derived and must never be influenced by request input |
| service → Postgres | The cutoff crosses into SQL as a bound query parameter, never as interpolated text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | `config.Load()` / `EVENT_RETENTION_DAYS` | medium | mitigate | Manual post-`env.Parse` check rejects `<= 0` with an error naming the variable (D-03); `TestLoad_EventRetentionDaysRejectsNonPositive` pins all three invalid cases. A typo can never be reinterpreted as "show everything". |
| T-10-02 | Denial of Service | `events.Service.List` cutoff construction | medium | mitigate | The cutoff `pgtype.Timestamptz` is always built with `Valid: true` explicitly (`seedNotifiedAt` precedent). A zero-value struct would bind as SQL NULL, making `created_at >= NULL` never true and silently emptying the entire feed. `TestListEvents_RetentionExcludesAgedOutRows` asserts in-window events are still returned, so this regression fails loudly. |
| T-10-03 | Information Disclosure | `queries/events.sql` `ListEvents` | high | mitigate | The cutoff uses `sqlc.arg` (required), never `sqlc.narg` — there is no code path in which a caller passes a null cutoff and receives unfiltered, out-of-window rows. Acceptance criteria assert the literal `sqlc.narg('cutoff')` is absent. |
| T-10-04 | Repudiation | events table integrity | high | mitigate | Retention is a read-side filter only; the phase introduces no row-removal or row-mutating statement. `TestRetention_DetectionStateQueriesStayUnfiltered` proves aged-out rows remain fully readable by all four detection-state queries, and a keyword gate over `queries/events.sql` asserts no removal statement exists. |
| T-10-05 | Denial of Service | `GET /events` query cost | low | accept | The new predicate is a simple range comparison on an existing `timestamptz` column; result size is already bounded by the pre-existing `events.MaxPageSize` clamp (T-06-02). REQUIREMENTS.md's Out of Scope table already rejected partitioning at this project's data volume, and the same reasoning applies to a speculative `created_at` index. |
| T-10-06 | Elevation of Privilege | request-derived retention window | low | accept | The cutoff is derived solely from boot-time config and `time.Now()`; no request parameter reaches it. `handleListEvents` parses only `artist_id`, `event_type`, `cursor`, and `limit`, all unchanged by this plan. |
| T-10-SC | Tampering | npm/pip/cargo/go installs | low | accept | This phase installs zero new dependencies — `10-RESEARCH.md`'s Package Legitimacy Audit is explicitly "not applicable", and no task runs a package-manager install. No `[ASSUMED]`/`[SUS]` package exists to gate. |
</threat_model>

<verification>
- `make db-up && make test-integration` passes with no regressions in the existing events suites.
- `go test ./internal/config/... -short -count=1` passes, including `TestEnvExampleCompleteness` and `TestLoad_AggregatesAllMissing`.
- `make sqlc-check` exits 0.
- `go vet ./...` and `golangci-lint run` are clean.
- `git diff queries/events.sql` shows changes confined to the `ListEvents` statement and its comment block.
</verification>

<success_criteria>
- An operator sets `EVENT_RETENTION_DAYS`; with it unset the window is 90 days; with it non-positive the process refuses to boot (DATA-01).
- `GET /events` returns no event older than the window, and an event exactly at the boundary is still returned (DATA-02, D-04).
- No event row is deleted — an aged-out event is still in the database and still satisfies every dedup, seed-mode, baseline, and pending-notification query (roadmap success criteria 3-5).
</success_criteria>

<output>
Create `.planning/phases/10-event-retention-window/10-01-SUMMARY.md` when done. Record the generated type of `ListEventsParams.Cutoff` (confirming or correcting RESEARCH.md assumption A1) and the final `EVENT_RETENTION_DAYS` validation error text, since plan 10-02 and the phase verification both depend on them.
</output>
