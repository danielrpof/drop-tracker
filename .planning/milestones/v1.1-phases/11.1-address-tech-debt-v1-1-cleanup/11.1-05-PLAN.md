---
phase: 11.1-address-tech-debt-v1-1-cleanup
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/v1.1-MILESTONE-AUDIT.md
  - .planning/phases/08-frontend-test-suite/08-VALIDATION.md
  - .planning/phases/09-ci-coverage-gates/09-VALIDATION.md
  - .planning/phases/10-event-retention-window/10-VALIDATION.md
autonomous: false
requirements: [D-08, D-09]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "v1.1-MILESTONE-AUDIT.md no longer presents Phase 9's live-CI backstop verification as outstanding; an amendment note points at the 2026-08-13 closure evidence without rewriting the original finding (D-08)."
    - "Phases 08, 09, and 10's VALIDATION.md files are no longer status: draft (D-09)."
  artifacts:
    - ".planning/v1.1-MILESTONE-AUDIT.md — frontmatter `amended:`/`amendment_note:` fields plus a body correction note"
    - "Reconciled 08/09/10 VALIDATION.md frontmatter written by /gsd-validate-phase"
  key_links:
    - "The audit doc's stale claim appears in TWO places — the frontmatter `tech_debt` list under phase 09, and the body's `## Recommended Next Steps` item 1. Correcting one and not the other leaves the document self-contradictory."
    - "STATE.md's `[Phase 09 UAT, closed 2026-08-13]` entry and 09-VERIFICATION.md's Human Verification Required section are the evidence the amendment points at — the amendment cites them, it does not re-derive or re-run them."
---

<objective>
Close the two process/documentation items: correct the milestone audit's stale claim that Phase 9's live-CI backstop verification is still outstanding (D-08), and reconcile Phases 08/09/10's Nyquist VALIDATION.md files out of `draft` status (D-09).

Purpose: The audit was written 2026-08-17 but its Phase 9 entry predates STATE.md's 2026-08-13 closure record; a reader today would think a verification is pending that actually happened. The three `draft` VALIDATION.md files are a process gap predating the validate-phase reconciliation step, and the user chose to close them inside this phase rather than carry them as a separate follow-up.
Output: An amended audit document and three reconciled validation files.

**Critical scoping note — D-09 is orchestration, not code.** Both CONTEXT.md and RESEARCH.md warn about this explicitly. D-09 is three GSD command invocations. No file under `internal/`, `cmd/`, or `web/` is its integration point. Do not create a code task for it, and do not invent an application-side artifact to satisfy it.

**Critical scoping note — D-08 does not re-litigate the underlying claim.** D-08 is a documentation fix: the audit doc is stale. It is NOT a re-verification of whether Phase 9's backstop CI run happened. STATE.md and 09-VERIFICATION.md already hold that evidence. Do not push a scratch branch or trigger a live GitHub Actions run as part of this phase (RESEARCH.md Anti-Patterns).

**Note (no tracer task):** see plan 11.1-01's objective — this phase is many small independent fixes, so a tracer slice would de-risk nothing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-address-tech-debt-v1-1-cleanup/11.1-CONTEXT.md
@.planning/phases/11.1-address-tech-debt-v1-1-cleanup/11.1-RESEARCH.md
@.planning/phases/11.1-address-tech-debt-v1-1-cleanup/11.1-PATTERNS.md
</context>

<artifacts_this_phase_produces>
| Symbol | File | Kind |
|--------|------|------|
| `amended` / `amendment_note` frontmatter keys | `.planning/v1.1-MILESTONE-AUDIT.md` | document metadata (mirrors the existing convention in `09-VERIFICATION.md`) |

No code symbols. D-09 produces no new symbol at all — `/gsd-validate-phase` rewrites existing VALIDATION.md frontmatter in place.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Amend the milestone audit's stale Phase 9 verification claim</name>
  <files>.planning/v1.1-MILESTONE-AUDIT.md</files>
  <read_first>
    - `.planning/v1.1-MILESTONE-AUDIT.md` — read the whole file. The stale claim appears TWICE: in the frontmatter `tech_debt` list under `phase: 09-ci-coverage-gates`, as the item beginning "Human verification outstanding: a real GitHub Actions run..." (currently line 25); and in the body's `## Recommended Next Steps` as item 1, "Run the outstanding backstop verification for Phase 9" (currently line 105). Also note the `## Phase-Level Summary` table's Phase 9 row (currently line 70), whose Notes column says "1 backstop-tier live CI run still outstanding" — a third occurrence.
    - `.planning/STATE.md` — the Accumulated Context entry headed `[Phase 09 UAT, closed 2026-08-13]`. It records all three observed cases, the three GitHub Actions run IDs (31724487315, 31724744670, 31724954534), the scratch branch name `test/coverage-gate-ci-check`, and that the branch was deleted afterward. This is the evidence the amendment cites.
    - `.planning/phases/09-ci-coverage-gates/09-VERIFICATION.md` — read its frontmatter. It already uses `amended:` plus a multi-line `amendment_note:` block, and its body has a "Resolved as a post-report amendment" section. This is the in-project pattern to mirror; do not invent a different one.
  </read_first>
  <action>
Amend `.planning/v1.1-MILESTONE-AUDIT.md` so it no longer presents Phase 9's backstop verification as outstanding, WITHOUT rewriting the original finding text. CONTEXT.md D-08 is explicit: add an amendment/correction note pointing at the closure record, rather than silently editing history.

Mirror `09-VERIFICATION.md`'s existing amendment convention:

1. Add an `amended:` timestamp and a multi-line `amendment_note:` block to the frontmatter, immediately after the existing `audited:` field. The note should state: the Phase 9 "Human verification outstanding" tech_debt item was already closed on 2026-08-13, four days before this audit was written; the audit missed it because STATE.md's closure entry was not cross-referenced; the closure is evidenced by three real GitHub Actions runs on the deleted scratch branch `test/coverage-gate-ci-check` (cite the run IDs and the branch name from STATE.md); and no re-verification was performed as part of this amendment because the evidence already exists.

2. Leave the original `tech_debt` item text under `phase: 09-ci-coverage-gates` in place, and append a bracketed correction marker to it (e.g. a trailing `[AMENDED <date>: closed 2026-08-13 — see frontmatter amendment_note]`) so a reader scanning the list sees the correction inline without the original claim disappearing.

3. Apply the same inline correction marker to the body's `## Recommended Next Steps` item 1 and to the `## Phase-Level Summary` table's Phase 9 Notes cell. All three occurrences must be corrected — correcting one leaves the document self-contradictory, which is the failure mode this task exists to prevent.

4. Do NOT change any score, any requirement row, the `status: tech_debt` field, or the Nyquist Validation Coverage table (that table's own claim about 08/09/10 being `draft` is still true at the time this task runs; Task 3 handles it after `/gsd-validate-phase` has run).

5. Do NOT run a live GitHub Actions push, create a branch, or otherwise attempt to reproduce the verification. Exact wording of the amendment is this phase's discretion (CONTEXT.md Claude's Discretion).
  </action>
  <verify>
    <automated>python -c "import re,sys; t=open('.planning/v1.1-MILESTONE-AUDIT.md',encoding='utf-8').read(); fm=t.split('---')[1]; assert 'amended:' in fm, 'missing amended'; assert 'amendment_note:' in fm, 'missing amendment_note'; assert '2026-08-13' in fm, 'missing closure date'; assert 'test/coverage-gate-ci-check' in fm, 'missing branch evidence'; assert t.count('AMENDED')>=3, ('inline markers', t.count('AMENDED')); assert 'Human verification outstanding' in t, 'original finding was deleted instead of amended'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The automated verify prints `OK`: frontmatter carries `amended:` and `amendment_note:`, the note cites 2026-08-13 and the `test/coverage-gate-ci-check` branch, at least three inline correction markers exist, and the original "Human verification outstanding" text still exists rather than having been deleted.
    - `grep -c '31724487315' .planning/v1.1-MILESTONE-AUDIT.md` returns at least 1 — at least one concrete run ID is cited as evidence rather than a vague reference.
    - `git diff .planning/v1.1-MILESTONE-AUDIT.md` shows no change to any line in the `## Requirements Coverage` table or the `scores:` frontmatter block.
    - `git log --oneline -1 --all -- .github/workflows/full-pipeline.yml` shows no new commit from this task, and `git branch -a | grep -c 'coverage-gate-ci-check'` returns 0 — no live re-verification was attempted.
    - The document reads consistently: no remaining sentence presents the Phase 9 backstop run as pending without an adjacent correction marker.
  </acceptance_criteria>
  <done>All three occurrences of the stale claim carry an inline correction marker, the frontmatter holds an amendment note citing the 2026-08-13 closure with concrete run evidence, the original finding text survives, and nothing was re-verified live.</done>
  <reversibility rating="reversible">Documentation-only amendment.</reversibility>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run /gsd-validate-phase for Phases 08, 09, and 10</name>
  <action>
Hand control back to the developer to run three GSD orchestration commands. This task has NO `files:` target and produces no code — D-09 is a command invocation, and both CONTEXT.md and RESEARCH.md warn explicitly against mis-scoping it as a code task.

Why it is a human action rather than an executor step: each invocation spawns its own multi-agent GSD workflow. Nesting three of those inside a plan executor would blow the executor's context budget and put a workflow orchestrator inside a workflow step. There is also no Bash or API equivalent — these are Claude Code slash commands, not shell commands.

Context for the developer: Phases 08, 09, and 10 each have a `VALIDATION.md` still at `status: draft` with `nyquist_compliant: false`, because they were planned before the validate-phase reconciliation step existed (#2117). The audit flagged this as a cross-phase coverage TODO, and the user chose to close it inside this phase rather than carry it forward.
  </action>
  <instructions>
    Run these three, one at a time, letting each finish before starting the next:

    1. `/gsd-validate-phase 08`
    2. `/gsd-validate-phase 09`
    3. `/gsd-validate-phase 10`

    Notes for each run:
    - These are retroactive audits of already-complete phases. They fill Nyquist validation gaps and reconcile the VALIDATION.md frontmatter; they should not require code changes to phases 08/09/10.
    - If any run proposes changing shipped code in phase 08, 09, or 10, stop and report it rather than accepting — that is outside this cleanup phase's boundary, and CONTEXT.md scopes D-09 to reconciliation only.
    - Expect each run to flip its VALIDATION.md's `status:` out of `draft`. If a run legitimately cannot reach `nyquist_compliant: true` (for example a phase has a genuine backstop-tier truth with no automated equivalent), that is an acceptable outcome — `status` moving off `draft` with `nyquist_compliant: false` is a recorded PARTIAL result, which is materially different from the unreconciled NOT-VALIDATED state the audit flagged. Record which of the three landed where.

    Then resume so Task 3 can verify the result.
  </instructions>
  <verification>
    Task 3 performs the automated verification of this task's outcome: it asserts none of the three `VALIDATION.md` files still matches `^status:\s*draft$` and that no shipped code under `internal/`, `cmd/`, `web/`, or `.github/` was modified by the runs. Do not mark this checkpoint resolved on the basis of the commands having exited — resolve it on Task 3's assertion passing.
  </verification>
  <resume-signal>Type "done" once all three commands have completed, and state for each phase whether its VALIDATION.md ended up validated/compliant, validated/partial, or still blocked — plus anything either run surfaced that looks like it needs its own follow-up.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify the three VALIDATION.md files left draft status and reconcile the audit's Nyquist table</name>
  <files>.planning/v1.1-MILESTONE-AUDIT.md</files>
  <read_first>
    - `.planning/phases/08-frontend-test-suite/08-VALIDATION.md` — read the frontmatter only.
    - `.planning/phases/09-ci-coverage-gates/09-VALIDATION.md` — read the frontmatter only.
    - `.planning/phases/10-event-retention-window/10-VALIDATION.md` — read the frontmatter only.
    - `.planning/v1.1-MILESTONE-AUDIT.md` — the `## Nyquist Validation Coverage` table (currently lines 94-101), whose rows for 08/09/10 assert `draft` / `false (unreconciled)` / "Coverage TODO — run /gsd-validate-phase NN". Those rows become stale the moment Task 2 completes.
  </read_first>
  <action>
Confirm Task 2's outcome and record it, then bring the audit document's Nyquist table into agreement with reality.

1. Read each of the three VALIDATION.md files' frontmatter and record the observed `status` and `nyquist_compliant` values for all three in the SUMMARY. If any file is still `status: draft`, do not paper over it — report it as an unmet criterion and stop, since D-09's whole point is flipping those three out of `draft`.

2. Update the `## Nyquist Validation Coverage` table's rows for phases 08, 09, and 10 to the observed values, and replace each row's "Coverage TODO — run /gsd-validate-phase NN" action with the actual outcome plus a reference to this phase (11.1) as where the reconciliation happened. Leave the Phase 11 row untouched — it was already `validated` / `true`.

3. Update the paragraph immediately under that table (currently the sentence explaining that phases 8-10's draft status predates #2117) so it reads in past tense and points at Phase 11.1's closure, rather than describing an open condition.

4. Do not touch the frontmatter `tech_debt` block's `phase: nyquist-coverage (cross-phase)` item text — apply the same inline correction-marker convention Task 1 established to it instead, so the original finding survives alongside its correction.

5. Do not change any other section of the audit document.
  </action>
  <verify>
    <automated>python -c "import re; ps={'08':'.planning/phases/08-frontend-test-suite/08-VALIDATION.md','09':'.planning/phases/09-ci-coverage-gates/09-VALIDATION.md','10':'.planning/phases/10-event-retention-window/10-VALIDATION.md'}; bad=[]; [bad.append(k) for k,v in ps.items() if re.search(r'^status:\s*draft\s*$', open(v,encoding='utf-8').read(), re.M)]; assert not bad, ('still draft: '+','.join(bad)); a=open('.planning/v1.1-MILESTONE-AUDIT.md',encoding='utf-8').read(); assert 'run `/gsd-validate-phase 08`' not in a and 'run /gsd-validate-phase 08' not in a, 'stale Nyquist TODO row remains'; assert '11.1' in a, 'audit does not reference phase 11.1 as the reconciliation point'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The automated verify prints `OK`: none of the three VALIDATION.md files still carries `status: draft`, the audit's stale "run /gsd-validate-phase 08" action text is gone, and the audit references Phase 11.1 as where reconciliation happened.
    - The SUMMARY records the observed `status` and `nyquist_compliant` values for all three phases individually, not as a blanket "all reconciled" claim.
    - `git diff .planning/phases/08-frontend-test-suite/ .planning/phases/09-ci-coverage-gates/ .planning/phases/10-event-retention-window/` shows changes confined to those phases' `VALIDATION.md` files — `/gsd-validate-phase` did not modify any PLAN, SUMMARY, or VERIFICATION file for those phases.
    - `git diff --stat -- internal/ cmd/ web/ .github/` is empty for this task — no shipped code from phases 08/09/10 was changed by the reconciliation.
    - The audit's Phase 11 Nyquist row is byte-identical to before.
  </acceptance_criteria>
  <done>All three VALIDATION.md files are out of `draft`, their observed statuses are recorded individually, the audit's Nyquist table and surrounding paragraph reflect reality with the original finding preserved under a correction marker, and no shipped code was touched.</done>
  <reversibility rating="reversible">Documentation reconciliation only.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit document -> milestone close decision | `v1.1-MILESTONE-AUDIT.md` is the artifact a human reads when deciding whether v1.1 can be closed. A false claim in either direction misinforms that decision. |
| GSD orchestration commands -> repository state | `/gsd-validate-phase` is a multi-agent workflow with write access to the repo; run against three completed phases, it could in principle touch more than validation metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.1-14 | Repudiation | `.planning/v1.1-MILESTONE-AUDIT.md` | medium | mitigate | Silently rewriting a finding destroys the audit trail and makes it impossible to tell later whether the original claim was wrong or the world changed. Mitigated by CONTEXT.md D-08's amend-don't-rewrite rule, enforced by an acceptance criterion asserting the original "Human verification outstanding" text still exists in the file after the edit, and by mirroring `09-VERIFICATION.md`'s established `amended:`/`amendment_note:` convention rather than inventing one. |
| T-11.1-15 | Tampering | `/gsd-validate-phase` runs against phases 08/09/10 | medium | mitigate | A retroactive validation pass could quietly modify shipped code or other phase artifacts. Mitigated by Task 2's instruction to stop and report if any run proposes a code change, and by Task 3's acceptance criteria asserting `git diff` for `internal/`, `cmd/`, `web/`, `.github/` is empty and that changes under the three phase directories are confined to `VALIDATION.md`. |
| T-11.1-16 | Spoofing | Phase 9 backstop verification evidence | low | accept | The amendment asserts a verification happened based on STATE.md and 09-VERIFICATION.md rather than on a fresh observation — the scratch branch was deleted, so the runs cannot be re-inspected from the repo. Accepted: RESEARCH.md's Anti-Patterns section explicitly forbids re-running a live GitHub Actions verification for this phase, and the amendment cites concrete run IDs so the claim is auditable against GitHub's own run history if ever challenged. |
| T-11.1-SC | Tampering | package installs | high | accept | This plan installs nothing and touches no manifest. Only markdown files under `.planning/` are modified. |
</threat_model>

<verification>
- The Task 1 and Task 3 automated checks both print `OK`.
- `git diff --stat -- internal/ cmd/ web/ .github/ go.mod go.sum` is empty for this entire plan.
- No branch matching `coverage-gate-ci-check` exists.
- The audit document reads consistently end to end: no sentence presents Phase 9's backstop run or phases 08/09/10's draft status as open without an adjacent correction.
</verification>

<success_criteria>
- D-08: three stale occurrences corrected inline, an amendment note in frontmatter citing 2026-08-13 and concrete run IDs, and the original finding text preserved.
- D-09: `/gsd-validate-phase` run for 08, 09, and 10; none of the three VALIDATION.md files remains `status: draft`; each outcome recorded individually; no shipped code changed.
- The audit's Nyquist Validation Coverage table matches the post-reconciliation reality.
</success_criteria>

<output>
Create `.planning/phases/11.1-address-tech-debt-v1-1-cleanup/11.1-05-SUMMARY.md` when done
</output>
