---
phase: 11.1-address-tech-debt-v1-1-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["11.1-01"]
files_modified:
  - web/**/*.ts
  - web/**/*.tsx
  - .github/workflows/full-pipeline.yml
autonomous: true
requirements: [D-04]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`pnpm exec prettier --check \"**/*.{ts,tsx}\"` exits 0 from `web/` — EventCard.tsx's single-quote literals and every other formatting drift in the tree are gone (D-04 part 1)."
    - "The CI `frontend-test` job fails the pipeline when any tracked .ts/.tsx file under web/ drifts from .prettierrc (D-04 part 2)."
    - "The formatting pass changed no behavior: the full Vitest suite and its coverage thresholds still pass unchanged."
  artifacts:
    - ".github/workflows/full-pipeline.yml — a new formatting-check step inside the existing frontend-test job"
  key_links:
    - "The new step lives INSIDE the existing `frontend-test` job, which is already a member of `build-scan`'s `needs:` array — that membership is what makes the gate actually block the pipeline."
    - "The check's glob must match `web/package.json`'s existing `format` script glob (`**/*.{ts,tsx}`) so `pnpm format` is always the exact remedy for a red check."
---

<objective>
Implement D-04: make the frontend tree conform to `.prettierrc` and add a `prettier --check` gate to CI so this class of formatting drift is caught going forward rather than fixed once.

Purpose: 08-REVIEW.md IN-01 found single-quote literals in `EventCard.tsx` violating `.prettierrc`'s `singleQuote: false`, and noted that no CI step exists to catch it. The user locked BOTH halves — fix it and gate it.
Output: A formatted `web/` tree and a blocking formatting step in the `frontend-test` job.

## Scope finding that changes this plan's shape (verified at plan time, supersedes RESEARCH.md)

RESEARCH.md scoped D-04 as "fix 3 lines in EventCard.tsx + add a CI step." Running the check at plan time shows **40 files** under `web/` currently fail `prettier --check`, not one:

```
app/components/common/CoverArt.tsx, app/components/common/EmptyState.tsx,
app/components/history/EventCard.test.tsx, app/components/history/EventCard.tsx,
app/components/history/HistoryFilters.test.tsx, app/components/history/HistoryFilters.tsx,
app/components/ui/{alert,avatar,badge,button,card,checkbox,input,separator,skeleton,sonner,switch,tabs}.tsx,
app/components/watchlist/{PreferenceToggles.test,PreferenceToggles,SearchBox.test,SearchBox,
  SearchResultsColumns.test,SearchResultsColumns,WatchlistRow}.tsx,
app/lib/api.test.ts, app/lib/api.ts, app/lib/test/routeStub.tsx, app/lib/utils.ts,
app/root.test.tsx, app/root.tsx, app/routes.ts,
app/routes/{history.test,history,watchlist.test,watchlist}.tsx,
react-router.config.ts, vite.config.ts, vitest.config.ts, vitest.setup.ts
```

Adding the CI gate while 39 other files are unformatted would turn the pipeline red on the very next push. Formatting the whole tree is therefore a **necessary consequence of the locked decision D-04**, not scope creep — the gate cannot exist without it. This is a single mechanical `pnpm format` invocation, verified by the existing test suite.

**Why this plan is wave 2:** the formatting pass rewrites `HistoryFilters.tsx`, `HistoryFilters.test.tsx`, `watchlist.test.tsx` and `SearchBox.test.tsx` — the exact files plan 11.1-01 edits. Running it after 11.1-01 means the pass normalizes 11.1-01's new code too, instead of racing it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-address-tech-debt-v1-1-cleanup/11.1-CONTEXT.md
@.planning/phases/11.1-address-tech-debt-v1-1-cleanup/11.1-RESEARCH.md
@.planning/phases/11.1-address-tech-debt-v1-1-cleanup/11.1-PATTERNS.md
@.planning/phases/11.1-address-tech-debt-v1-1-cleanup/11.1-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
| Symbol | File | Kind |
|--------|------|------|
| `Check formatting` (GitHub Actions step name, inside the existing `frontend-test` job) | `.github/workflows/full-pipeline.yml` | CI step |

No new npm script, no new job, no new dependency. `web/package.json`'s existing `format` script and `prettier` devDependency are reused as-is.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Format the web tree to .prettierrc</name>
  <files>web/app/**/*.ts, web/app/**/*.tsx, web/*.ts</files>
  <read_first>
    - `web/.prettierrc` — the authoritative config: `endOfLine: lf`, `semi: false`, `singleQuote: false`, `tabWidth: 2`, `trailingComma: es5`, `printWidth: 80`, plugin `prettier-plugin-tailwindcss`, `tailwindStylesheet: app/app.css`, `tailwindFunctions: ["cn", "cva"]`.
    - `web/.prettierignore` — confirms `node_modules/`, `coverage/`, `.pnpm-store/`, and lockfiles are excluded. Note it does NOT exclude `app/components/ui/` (the shadcn-generated components) — those are in scope and will be reformatted.
    - `web/package.json` — the existing `"format": "prettier --write \"**/*.{ts,tsx}\""` script. Use it; do not invent a different glob.
    - `web/app/components/history/EventCard.tsx` — the specific IN-01 target: single-quote case labels at approximately lines 93, 95, 97 (`case 'new_release':`, `case 'guest_feature':`, `case 'deluxe_change':`).
  </read_first>
  <action>
Run the repository's own formatting script from the `web/` directory: `pnpm format`. This is `prettier --write "**/*.{ts,tsx}"` with `.prettierrc` and `.prettierignore` already wired.

Expectations and constraints:
- This rewrites roughly 40 files. That is expected — see the scope finding in this plan's objective. Do not hand-edit individual files to keep the diff small; a partial fix leaves the CI gate added in Task 2 permanently red.
- `prettier-plugin-tailwindcss` will reorder Tailwind class names inside `className` strings and inside `cn(...)`/`cva(...)` calls. This is safe: Tailwind determines emitted-CSS order from its own layer ordering, not from source class order. It is not a styling change.
- Do NOT modify `.prettierrc` or `.prettierignore`. If a file looks like it should be exempt, it is not — the whole point of D-04 is a uniform gate.
- Do NOT rebuild `internal/webassets/build/client/`. This project deliberately does not regenerate the committed embed tree per frontend commit (the Docker Node stage rebuilds it; the committed tree exists only so a Node-less clone can `go build`/`go vet`/`go test`). `git log` confirms `internal/webassets/` last changed at `71de08e` while `web/app/` last changed at `d34ed3f`, so it is already intentionally behind.
- After formatting, run the full frontend suite. If any test fails, the failure is a real regression from the reformat (almost certainly a string-literal or template-literal assertion), not acceptable churn — fix the test or revert and escalate, do not disable the test.
  </action>
  <verify>
    <automated>cd web && pnpm exec prettier --check "**/*.{ts,tsx}" && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && pnpm exec prettier --check "**/*.{ts,tsx}"` exits 0 and prints no `[warn]` lines.
    - `cd web && pnpm test` passes, and Vitest's inline coverage thresholds still pass at the existing 70% frontend floor (CICD-12) — a formatting pass must not move coverage.
    - `grep -c "case 'new_release':" web/app/components/history/EventCard.tsx` returns 0.
    - `grep -c 'case "new_release":' web/app/components/history/EventCard.tsx` returns 1.
    - `git diff --stat web/` shows only `.ts`/`.tsx` files under `web/` — no change to `web/.prettierrc`, `web/.prettierignore`, `web/package.json`, `web/pnpm-lock.yaml`, or anything under `internal/webassets/`.
    - `git diff web/app/components/history/HistoryFilters.tsx` still contains `color-scheme:dark` — plan 11.1-01's D-13 fix survived the reformat.
  </acceptance_criteria>
  <done>`prettier --check` is clean across the whole `web/` tree, the full Vitest suite and coverage thresholds pass, and EventCard.tsx's three case labels are double-quoted.</done>
  <reversibility rating="reversible">Whole-tree formatting is a single tool-generated commit; `git revert` restores the prior formatting exactly. No behavior is encoded in the change.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Add the blocking formatting check to the frontend-test CI job</name>
  <files>.github/workflows/full-pipeline.yml</files>
  <read_first>
    - `.github/workflows/full-pipeline.yml` — read the whole file. Study the existing `frontend-test` job (currently lines 89-111): it sets `defaults.run.working-directory: web`, then runs Checkout, Set up pnpm (`pnpm/action-setup` v6.0.10, version 11), Set up Node (v22, `cache: pnpm`), `Install dependencies` (`pnpm install --frozen-lockfile`), and `Run Vitest suite` (`pnpm test`).
    - The same file's `build-scan` job (currently lines 125-129) and its `needs: [vet, lint, test, gitleaks, trivy-fs, frontend-test]` array, plus the comment above it explaining that `frontend-test` was made blocking in Phase 9 (08-CONTEXT.md D-04 / 09-CONTEXT.md D-11).
    - The `test` job's `Backend coverage gate (80)` step (currently lines 55-60) — the established in-project precedent for "extend an existing job with a gating step rather than adding a new job," including its explanatory comment style.
    - `web/package.json` — confirm the `format` script's glob so the CI check uses the identical pattern.
  </read_first>
  <action>
Add exactly ONE new step to the EXISTING `frontend-test` job, placed after the `Install dependencies` step and before the `Run Vitest suite` step (fail fast on formatting before spending time on the suite).

The step:
- Name it `Check formatting`.
- Its `run:` invokes prettier in check mode against the same glob `web/package.json`'s `format` script uses: `pnpm exec prettier --check "**/*.{ts,tsx}"`. The job's `defaults.run.working-directory: web` already scopes it to the frontend tree — do not add a `cd` or a `working-directory` override.
- Add a short comment above the step, matching the file's established commenting convention, recording WHY it is a step inside `frontend-test` rather than its own job: a new top-level job would have to be appended to `build-scan`'s `needs:` array to gate anything, and this repo already shipped that exact defect once (`frontend-test` existed non-blocking for a whole phase before Phase 9 wired it in). A step inherits the job's existing `needs:` membership for free. Reference 08-REVIEW.md CR-01 in the comment.

Hard constraints:
- Do NOT create a new top-level job.
- Do NOT modify `build-scan`'s `needs:` array — it already contains `frontend-test`, and touching it risks the CR-01 defect class.
- Do NOT give the new step any failure-suppression override; a non-zero prettier exit must fail the job, which is the entire control.
- Do NOT change any other job, any action SHA pin, or the workflow's `permissions`/`concurrency` blocks.
  </action>
  <verify>
    <automated>python -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/full-pipeline.yml')); j=d['jobs']['frontend-test']; s=j['steps']; names=[x.get('name') for x in s]; runs=[x.get('run','') for x in s]; assert 'Check formatting' in names, names; i=names.index('Check formatting'); assert 'prettier --check' in runs[i], runs[i]; assert names.index('Install dependencies') < i < names.index('Run Vitest suite'), names; assert 'continue-on-error' not in yaml.dump(s[i]), s[i]; assert d['jobs']['build-scan']['needs']==['vet','lint','test','gitleaks','trivy-fs','frontend-test'], d['jobs']['build-scan']['needs']; assert set(d['jobs'])=={'vet','lint','test','gitleaks','trivy-fs','frontend-test','pr-title','build-scan','release'}, set(d['jobs']); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The automated verify command prints `OK` (it asserts: the step exists in `frontend-test`, runs `prettier --check`, sits between `Install dependencies` and `Run Vitest suite`, carries no failure-suppression key, `build-scan.needs` is byte-identical to before, and the workflow's job set is unchanged at exactly nine jobs).
    - `grep -c 'continue-on-error' .github/workflows/full-pipeline.yml` returns 0.
    - `grep -c 'prettier --check' .github/workflows/full-pipeline.yml` returns exactly 1.
    - `git diff .github/workflows/full-pipeline.yml` shows additions only inside the `frontend-test` job — no line in the diff belongs to `build-scan`, `release`, `test`, `vet`, `lint`, `gitleaks`, `trivy-fs`, or `pr-title`.
    - Running the same command the CI step runs, locally, exits 0: `cd web && pnpm exec prettier --check "**/*.{ts,tsx}"`. (Task 1 is what makes this true — if it fails, the gate would be red on the first push.)
  </acceptance_criteria>
  <done>The `frontend-test` job runs a blocking formatting check between dependency install and the Vitest suite, `build-scan`'s dependency graph is untouched, and the check passes against the tree as formatted by Task 1.</done>
  <reversibility rating="reversible">A single additive CI step in an existing job; deleting the step fully removes the gate with no other effect. Flagged reversible in CONTEXT.md D-04 for exactly this reason.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contributor push -> CI pipeline | The new formatting step is a gate on untrusted contributor input (arbitrary source formatting). |
| CI job -> `build-scan` -> `release` | `frontend-test` is a `needs:` prerequisite of `build-scan`, which is a prerequisite of `release`. A defeated `frontend-test` gate silently widens what can reach ghcr.io. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.1-03 | Tampering | new `Check formatting` step in `.github/workflows/full-pipeline.yml` | medium | mitigate | The step is added with no failure-suppression override, so a non-zero prettier exit fails the job. Task 2's automated verify asserts this by parsing the YAML and rejecting any suppression key on the step, and a repo-wide count of the suppression key must be 0. This is the exact defect class of 08-REVIEW.md CR-01. |
| T-11.1-04 | Elevation of Privilege | `build-scan.needs` graph | high | mitigate | A new step (not a new job) cannot orphan the gate: `frontend-test` is already in `build-scan.needs`. Task 2 forbids editing `needs:` and the automated verify asserts the array is byte-identical and the job set is unchanged at nine jobs, so a gate cannot be silently detached from the release path. |
| T-11.1-05 | Tampering | whole-tree `prettier --write` pass | medium | mitigate | The reformat is tool-generated across ~40 files, which is a large diff to review by eye. Control: the full Vitest suite plus the 70% coverage threshold must pass unchanged after the pass, and acceptance criteria restrict the diff to `.ts`/`.tsx` files under `web/` — any change to config, lockfile, or `internal/webassets/` fails the task. |
| T-11.1-SC | Tampering | npm/pnpm installs | high | accept | No package is installed. `prettier ^3.8.3` and `prettier-plugin-tailwindcss ^0.8.0` are pre-existing devDependencies pinned by `pnpm-lock.yaml`; CI's `pnpm install --frozen-lockfile` refuses to resolve anything the lockfile does not pin. RESEARCH.md's Package Legitimacy Audit resolved to "Not applicable" for this phase. Escalate rather than install if a task appears to need a new package. |
</threat_model>

<verification>
- `cd web && pnpm exec prettier --check "**/*.{ts,tsx}"` exits 0.
- `cd web && pnpm test` green, coverage thresholds unchanged.
- The YAML assertion in Task 2's verify prints `OK`.
- `git diff --stat` for this plan shows only `web/**/*.{ts,tsx}` and `.github/workflows/full-pipeline.yml`.
</verification>

<success_criteria>
- D-04 part 1: `EventCard.tsx`'s single-quote literals are gone and the entire `web/` tree conforms to `.prettierrc`.
- D-04 part 2: the pipeline now fails on formatting drift, via a step inside the already-blocking `frontend-test` job, with no failure suppression and no change to `build-scan`'s dependency graph.
- No behavior change: the Vitest suite and coverage thresholds pass exactly as before.
</success_criteria>

<output>
Create `.planning/phases/11.1-address-tech-debt-v1-1-cleanup/11.1-02-SUMMARY.md` when done
</output>
