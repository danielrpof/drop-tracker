---
phase: 11.1-address-tech-debt-v1-1-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app/components/watchlist/SearchBox.test.tsx
  - web/app/routes/watchlist.test.tsx
  - web/app/components/history/HistoryFilters.test.tsx
  - web/app/components/history/HistoryFilters.tsx
autonomous: false
requirements: [D-03, D-05a, D-05b, D-13]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "SearchBox.test.tsx's comment above the two AbortSignal tests no longer claims those tests fail against current source (D-03)."
    - "watchlist.test.tsx proves the removed row leaves the DOM and stays gone on the SUCCESS path, with no rollback re-fetch (D-05a)."
    - "HistoryFilters.test.tsx exercises the eventType filter axis end-to-end: selecting an event type reports the whole new value upward (D-05b)."
    - statement: "The History page's Artist and Event type dropdown option text is legible (not white-on-white) when opened in a real browser (D-13)."
      verification: backstop
  artifacts:
    - "web/app/components/history/HistoryFilters.test.tsx — new getEventTypeSelect() helper + at least one eventType test"
    - "web/app/routes/watchlist.test.tsx — new success-path row-removal test"
    - "web/app/components/history/HistoryFilters.tsx — selectClassName carries [color-scheme:dark]"
  key_links:
    - "selectClassName is a single shared constant consumed by BOTH <select> elements in HistoryFilters.tsx — fixing the constant fixes both dropdowns, no per-select divergence."
    - "The eventType test's expected onChange payload must match EVENT_TYPE_OPTIONS in HistoryFilters.tsx (label 'New release' -> value 'new_release')."
---

<objective>
Close the four frontend items from CONTEXT.md's locked decisions: one stale test comment (D-03), two real test-coverage gaps (D-05a, D-05b), and the phase's single genuine user-facing bug — white-on-white option text in the History page's filter dropdowns (D-13).

Purpose: Phase 8's review left two coverage gaps and one false comment; Phase 10 UAT surfaced an unreadable dropdown. All four are small, independent, and were explicitly chosen for inclusion over deferral.
Output: Two new Vitest cases, one corrected comment, one CSS-scope fix, and a human-verified browser check of the dropdown contrast.

**Note (spec-less probe fallback):** ⚠ spec-less probe fallback skipped — this phase has no requirement IDs to probe (ROADMAP.md carried `Requirements: TBD`). `must_haves` above are derived directly from CONTEXT.md's D-IDs and RESEARCH.md's Phase Scope -> Test Map, per the visible-skip rule.

**Note (no tracer task):** TRACER_MODE is on by default, but this plan intentionally has no `type="tracer"` task. A tracer de-risks ONE uncertain end-to-end path; this phase's risk profile is many small, already-diagnosed, mutually independent fixes with zero shared architecture. A representative slice would de-risk nothing about the remaining fixes, so forcing one would add ceremony without information.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-address-tech-debt-v1-1-cleanup/11.1-CONTEXT.md
@.planning/phases/11.1-address-tech-debt-v1-1-cleanup/11.1-RESEARCH.md
@.planning/phases/11.1-address-tech-debt-v1-1-cleanup/11.1-PATTERNS.md
</context>

<artifacts_this_phase_produces>
New symbols/identifiers introduced by THIS plan (everything else is an in-place edit of existing code):

| Symbol | File | Kind |
|--------|------|------|
| `getEventTypeSelect()` | `web/app/components/history/HistoryFilters.test.tsx` | test helper function |
| `[color-scheme:dark]` (Tailwind arbitrary property appended to the existing `selectClassName` constant) | `web/app/components/history/HistoryFilters.tsx` | CSS class token |

No new exports, no new modules, no new dependencies.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Correct SearchBox.test.tsx's stale comment and add the success-path watchlist removal test</name>
  <files>web/app/components/watchlist/SearchBox.test.tsx, web/app/routes/watchlist.test.tsx</files>
  <read_first>
    - `web/app/components/watchlist/SearchBox.test.tsx` — read the whole file; the target comment sits immediately above the `it("passes an AbortSignal as the second argument to searchArtists", ...)` case (currently lines 56-61).
    - `web/app/components/watchlist/SearchBox.tsx` — confirm `runSearch` actually passes the controller's signal as the second argument to `searchArtists` before rewriting the comment. Do not rewrite the comment on the strength of the review report alone.
    - `web/app/routes/watchlist.test.tsx` — read the whole file. It has FOUR existing tests, not one. Test 4 (`"re-fetches the true server state when removeWatchlist fails, so the row reappears"`) already covers the failure path.
    - `web/app/routes/watchlist.tsx` — read `handleRemove` (currently lines 122-154). It removes the row optimistically, and on SUCCESS it fires a toast and deliberately does NOT call `refresh()`. That asymmetry is what the new test pins.
    - `.planning/phases/11.1-address-tech-debt-v1-1-cleanup/11.1-PATTERNS.md` — the Vitest idiom section (`vi.mock("~/lib/api")` + `vi.mocked(...)` + `renderRoute` + `screen.findBy*`).
  </read_first>
  <action>
Two independent edits, both in test files.

**Edit A — D-03, `SearchBox.test.tsx` (doc-only).** The comment block above the two AbortSignal tests currently ends with a sentence asserting that both tests fail against current source because `runSearch` calls `searchArtists(query)` with one argument. That claim became false when fix `14003dd` landed. Keep the first half of the comment (the part explaining WHY these two tests exist: they prove the per-debounce `AbortController` claim at the request level, not merely at the discard-the-stale-result callback level). Replace only the now-false trailing sentence with an accurate statement that these tests pass against current source, that `runSearch` passes the controller's signal as the second argument to `searchArtists`, and that the fix landed in commit `14003dd`. Exact wording is this phase's discretion (CONTEXT.md Claude's Discretion). Change no assertion, no import, no test body.

**Edit B — D-05a, `watchlist.test.tsx` (new test).** Add exactly ONE new `it(...)` case inside the existing `describe("Watchlist route", ...)` block, placed immediately after the existing first test (`"calls removeWatchlist with the entry's id when its remove control is clicked"`). Do NOT add a failure-path/rejection test — test 4 already covers that, and duplicating it is the exact regression RESEARCH.md Pitfall 1 warns about.

The new test asserts the SUCCESS-path post-remove UI state, which no existing test covers (today's test 1 asserts only that the mock was called):
- Arrange with `mockListWatchlist.mockResolvedValue([entry])` — use the non-`Once` variant deliberately, so that if a rollback `refresh()` were ever introduced on the success path, the row would reappear and this test would go red.
- `mockRemoveWatchlist.mockResolvedValue(undefined)`.
- Render via `renderRoute(Watchlist, "/watchlist")`, await `screen.findByText("Drake")`, then `userEvent.click` the button named `"Remove Drake from watchlist"`.
- Assert the row is gone: use `waitForElementToBeRemoved(() => screen.queryByText("Drake"))` (import `waitForElementToBeRemoved` from `@testing-library/react` alongside the existing `screen` import on line 1).
- Assert the empty state took over: `await screen.findByRole("heading", { name: "No artists yet" })` — this is the observable consequence of `entries` becoming an empty array.
- Assert no rollback re-fetch happened: `expect(mockListWatchlist).toHaveBeenCalledTimes(1)`. This is the assertion that makes the test meaningfully different from the failure-path test, which expects 2 calls.

Reuse the file's existing `entry` fixture and mock constants verbatim; add no new fixture, no new mock, no new helper.
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run app/routes/watchlist.test.tsx app/components/watchlist/SearchBox.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && pnpm exec vitest run app/routes/watchlist.test.tsx` reports 5 passing tests (was 4).
    - `cd web && pnpm exec vitest run app/components/watchlist/SearchBox.test.tsx` still passes with the same test count as before the edit (no test added or removed there).
    - `grep -c 'Both fail against current source' web/app/components/watchlist/SearchBox.test.tsx` returns 0.
    - `grep -c '14003dd' web/app/components/watchlist/SearchBox.test.tsx` returns at least 1.
    - `grep -c 'waitForElementToBeRemoved' web/app/routes/watchlist.test.tsx` returns at least 2 (the import and the call site).
    - `grep -c 'toHaveBeenCalledTimes(1)' web/app/routes/watchlist.test.tsx` returns at least 1.
    - `grep -c 'mockRejectedValue' web/app/routes/watchlist.test.tsx` returns exactly 1 — the pre-existing failure-path test only; no duplicate rejection test was added.
    - `git diff --stat web/app/components/watchlist/SearchBox.test.tsx` shows only comment lines changed (no `it(`, `expect(`, or `import` line in the diff).
  </acceptance_criteria>
  <done>SearchBox.test.tsx's comment states the true current behavior with the commit reference, and watchlist.test.tsx has one new passing success-path test proving the row leaves the DOM, the empty state renders, and no rollback re-fetch occurs.</done>
  <reversibility rating="reversible">Test-only and comment-only changes; revert is a single `git revert` with no production-code impact.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Cover the eventType filter axis in HistoryFilters.test.tsx</name>
  <files>web/app/components/history/HistoryFilters.test.tsx</files>
  <read_first>
    - `web/app/components/history/HistoryFilters.test.tsx` — read the whole file. It has 3 tests, all driving the Artist `<select>` via the `getArtistSelect()` helper (currently lines 45-47). The eventType axis has zero coverage.
    - `web/app/components/history/HistoryFilters.tsx` — read the whole file. Confirm: the `EVENT_TYPE_OPTIONS` constant (currently lines 19-23) maps value `new_release` to label `"New release"`, `guest_feature` to `"Guest feature"`, `deluxe_change` to `"Deluxe change"`; the Event type `<select>` is wrapped in a `<label>` whose text is `Event type` (currently lines 75-92), the same wrapping pattern the already-passing Artist tests rely on for accessible-name resolution.
  </read_first>
  <action>
Add eventType coverage to the existing `describe("HistoryFilters", ...)` block. No new fixture, no new mock, no change to any existing test.

1. Add a `getEventTypeSelect()` helper next to the existing `getArtistSelect()` helper, returning `screen.getByRole("combobox", { name: "Event type" })`. The accessible name resolves from the wrapping `<label>` exactly as `{ name: "Artist" }` does today (RESEARCH.md Assumption A4). If the query does not resolve, do NOT change the component to add an `aria-label` — instead re-read the label markup and correct the query string to whatever the accessible name actually computes to, and note the discrepancy in the SUMMARY.

2. Add a test named for the behavior it pins — selecting an event type reports the whole new value upward:
   - `mockListWatchlist.mockResolvedValue(artists)`, `const onChange = vi.fn()`.
   - `render(<HistoryFilters value={emptyValue} onChange={onChange} />)`, then `await screen.findByRole("option", { name: "Drake" })` to let the artist fetch settle (mirrors the existing artist tests).
   - `await userEvent.selectOptions(getEventTypeSelect(), "New release")`.
   - `expect(onChange).toHaveBeenCalledWith({ ...emptyValue, eventType: "new_release" })` — the spread proves the OTHER axis (`artistId`) is carried through untouched, which is the whole point of the component's "reports the full new value" contract.

3. Add a second test pinning the null-not-empty-string boundary on this axis, mirroring the existing artist test `"reports artistId as null, never 0, when 'All artists' is chosen"`: render with a `HistoryFiltersValue` whose `eventType` is `"new_release"`, select the `"All event types"` option, and assert `onChange` was called with `eventType: null` (not `""`). This is the eventType-axis analog of the existing artist-axis boundary test and closes the same class of defect.
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run app/components/history/HistoryFilters.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && pnpm exec vitest run app/components/history/HistoryFilters.test.tsx` reports 5 passing tests (was 3).
    - `grep -c 'getEventTypeSelect' web/app/components/history/HistoryFilters.test.tsx` returns at least 3 (definition plus two call sites).
    - `grep -c 'eventType: "new_release"' web/app/components/history/HistoryFilters.test.tsx` returns at least 1.
    - `grep -c 'eventType: null' web/app/components/history/HistoryFilters.test.tsx` returns at least 1.
    - `git diff web/app/components/history/HistoryFilters.tsx` is empty — the component itself was not modified by this task.
    - The three pre-existing test names still appear verbatim in the file (no test was renamed or replaced).
  </acceptance_criteria>
  <done>HistoryFilters.test.tsx exercises the eventType axis with both a selection test and a clear-to-null boundary test, all 5 tests green, component source untouched.</done>
  <reversibility rating="reversible">Test-only addition.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Scope color-scheme:dark to the History filter selects</name>
  <files>web/app/components/history/HistoryFilters.tsx</files>
  <read_first>
    - `web/app/components/history/HistoryFilters.tsx` — read the whole file. `selectClassName` (currently lines 25-26) is a single shared constant applied to BOTH `<select>` elements; it already carries `text-foreground`.
    - `web/app/app.css` — confirm no `color-scheme` declaration exists anywhere, and confirm the app applies `.dark` unconditionally with `--foreground` set to a near-white oklch value. This is the root cause: browsers render the native `<option>` popup with light-mode chrome (white background) while the popup text inherits the near-white foreground.
    - `.planning/todos/pending/2026-08-17-fix-white-on-white-font-in-history-filter-dropdowns.md` — the originating bug report (folded in as D-13).
  </read_first>
  <action>
Append the Tailwind arbitrary-property token `[color-scheme:dark]` to the end of the existing `selectClassName` string literal in `HistoryFilters.tsx`. Change nothing else in the string and nothing else in the file.

Constraints that are load-bearing, not stylistic:
- Do NOT add `color-scheme` to `:root`, `html`, or the `.dark` block in `web/app/app.css`. A root-level declaration changes browser chrome for every native form control and scrollbar in the app, none of which were visually audited for this phase (RESEARCH.md Pitfall 4). The blast radius must stay at these two `<select>` elements.
- Do NOT introduce a shadcn/Radix `Select` component. These are the only native `<select>` elements in `web/app` (confirmed by grep), so there is no consistency argument for a component swap, and CONTEXT.md scopes this phase to right-sized fixes.
- Do NOT remove or reorder the existing `text-foreground` class — it is what colors the select's CLOSED display text and is still required.
- `prettier-plugin-tailwindcss` may reorder the class list when the formatting pass in plan 11.1-02 runs. That reordering is expected and harmless; do not pre-empt it here.

Also delete the now-resolved todo file `.planning/todos/pending/2026-08-17-fix-white-on-white-font-in-history-filter-dropdowns.md` only AFTER the human-verify checkpoint in Task 4 passes — not in this task.
  </action>
  <verify>
    <automated>cd web && pnpm exec vitest run app/components/history/ && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'color-scheme:dark' web/app/components/history/HistoryFilters.tsx` returns exactly 1.
    - `grep -c 'text-foreground' web/app/components/history/HistoryFilters.tsx` returns at least 1 — the pre-existing class survived.
    - `grep -rc 'color-scheme' web/app/app.css` returns 0 — the fix did not leak into the global stylesheet.
    - `git diff --stat web/app/components/history/HistoryFilters.tsx` shows a single changed line region (the `selectClassName` literal).
    - `git status --short web/app/components/ui/` is empty — no component-library files were added.
    - All History component tests still pass.
  </acceptance_criteria>
  <done>`selectClassName` carries `[color-scheme:dark]`, `app.css` is untouched, no new UI component was introduced, and the History component suite is green.</done>
  <reversibility rating="reversible">One class token on one shared constant; removing it restores the previous rendering exactly.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify the dropdown option text is legible in a real browser (D-13)</name>
  <what-built>
    `selectClassName` in `web/app/components/history/HistoryFilters.tsx` now carries `[color-scheme:dark]`, which tells the browser to render the native `<option>` popup with dark chrome instead of the default light chrome that was producing near-white text on a white popup background.
  </what-built>
  <how-to-verify>
    This cannot be automated: jsdom does not render real OS-native `<select>` popup chrome, which is precisely the surface that was broken. This is the phase's one backstop-tier truth (RESEARCH.md Validation Architecture, D-13 row).

    1. Start the frontend dev server: `cd web && pnpm dev`. The Go API does not need to be running — the Event type options come from the static `EVENT_TYPE_OPTIONS` constant, and the Artist select falls back to showing only "All artists" when `listWatchlist()` fails (its `.catch` sets an empty artist array). If you want real artist names in the list, run `go run ./cmd/server` in a second terminal first; Vite proxies `/watchlist` to `localhost:8080`.
    2. Open the printed dev-server URL in a real browser (Chromium or Firefox) and navigate to the History page.
    3. Click the "Artist" dropdown. Confirm every option's text is readable against the popup background.
    4. Click the "Event type" dropdown. Confirm "All event types", "New release", "Guest feature", and "Deluxe change" are all readable.
    5. Note: `internal/webassets/build/client/` is deliberately NOT rebuilt per frontend commit in this project (the Docker Node stage rebuilds it; the committed tree exists only so a Node-less clone can `go build`/`go vet`/`go test`). So verify against the Vite dev server, not `go run ./cmd/server`'s embedded assets — the latter will still serve the pre-fix bundle and is expected to.
    6. If the text is legible in both dropdowns, also delete `.planning/todos/pending/2026-08-17-fix-white-on-white-font-in-history-filter-dropdowns.md` as part of resuming — the todo is resolved by this fix.
  </how-to-verify>
  <resume-signal>Type "approved" once both dropdowns render legible option text, or describe what still looks wrong (include which dropdown, which browser, and whether the closed display text or the open popup text is the problem).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MusicBrainz/Deezer -> API -> browser | Artist names rendered into the Artist `<option>` list originate upstream and reach the DOM through this component. Pre-existing boundary; this plan changes no rendering path. |
| Developer -> test suite | New Vitest cases run in CI; a test that mocks away the network incorrectly could mask a real failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.1-01 | Information Disclosure | `HistoryFilters.tsx` `<option>` text | low | accept | Artist names render as plain JSX text nodes (Phase 6 closed all XSS-surfaced threats; repo-wide `dangerouslySetInnerHTML` grep is zero). `[color-scheme:dark]` is a static CSS token appended to a string literal — it introduces no interpolation of external data. No change to the escaping path. |
| T-11.1-02 | Tampering | `watchlist.test.tsx` / `HistoryFilters.test.tsx` | low | mitigate | Both files keep the established bare `vi.mock("~/lib/api")` with no factory and no passthrough (D-06/TEST-02 convention), so no new test can reach the runtime's real `fetch`. Task acceptance criteria forbid adding new mocks or fixtures. |
| T-11.1-SC | Tampering | npm/pnpm installs | high | accept | This plan installs no package. RESEARCH.md's Package Legitimacy Audit resolved to "Not applicable" — every dependency used (`@testing-library/react`, `vitest`, `prettier`) is already in `web/package.json` and pinned by `pnpm-lock.yaml`. CI's existing `pnpm install --frozen-lockfile` remains the control. If any task finds itself needing a new package, stop and escalate rather than installing. |
</threat_model>

<verification>
- `cd web && pnpm test` — full frontend suite green, with 2 more watchlist/HistoryFilters tests than before this plan.
- Vitest's inline coverage thresholds (70% frontend floor, CICD-12) still pass; this plan only adds tests, so measured coverage must not drop.
- `git diff web/app/app.css` is empty.
- `git diff web/app/components/history/HistoryFilters.tsx` touches only the `selectClassName` literal.
- Task 4's human checkpoint approved.
</verification>

<success_criteria>
- D-03: the false "both fail against current source" claim is gone from SearchBox.test.tsx and replaced with an accurate statement referencing commit `14003dd`.
- D-05a: watchlist.test.tsx has 5 tests; the new one proves the success-path row removal sticks and triggers no rollback re-fetch.
- D-05b: HistoryFilters.test.tsx has 5 tests; two of them drive the eventType select.
- D-13: `[color-scheme:dark]` is scoped to `selectClassName`, `app.css` untouched, and a human confirmed both dropdowns render legible option text in a real browser.
- The resolved todo file has been deleted.
</success_criteria>

<output>
Create `.planning/phases/11.1-address-tech-debt-v1-1-cleanup/11.1-01-SUMMARY.md` when done
</output>
