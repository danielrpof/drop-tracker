---
phase: 11.1-address-tech-debt-v1-1-cleanup
plan: 02
subsystem: ci-frontend
tags: [prettier, formatting, ci, github-actions, frontend]

# Dependency graph
requires:
  - phase: 11.1-01
    provides: "HistoryFilters.tsx combobox rewrite, watchlist.test.tsx/SearchBox.test.tsx/history.test.tsx changes -- this plan's format pass runs after those land so it normalizes 11.1-01's new code too instead of racing it"
provides:
  - "Whole web/ tree conforms to .prettierrc (prettier --check exits 0)"
  - "EventCard.tsx's three case labels double-quoted (D-04 part 1, 08-REVIEW.md IN-01)"
  - "Blocking Check formatting step in the frontend-test CI job (D-04 part 2)"
affects: [any-future-frontend-plan, ci-pipeline]

# Actuals (#2632)
actuals:
  tokens: 10700
  tasks: 2
  commits: 2

tech-stack:
  added: []
  patterns:
    - "CI formatting gate added as a step inside an existing already-blocking job (frontend-test) rather than a new top-level job, so it inherits build-scan's needs: membership for free -- avoids the 08-REVIEW.md CR-01 defect class."

key-files:
  created: []
  modified:
    - web/app/components/common/CoverArt.tsx
    - web/app/components/common/EmptyState.tsx
    - web/app/components/history/EventCard.test.tsx
    - web/app/components/history/EventCard.tsx
    - web/app/components/history/HistoryFilters.test.tsx
    - web/app/components/history/HistoryFilters.tsx
    - web/app/components/ui/checkbox.tsx
    - web/app/components/ui/sonner.tsx
    - web/app/components/watchlist/PreferenceToggles.test.tsx
    - web/app/components/watchlist/PreferenceToggles.tsx
    - web/app/components/watchlist/SearchBox.test.tsx
    - web/app/components/watchlist/SearchBox.tsx
    - web/app/components/watchlist/SearchResultsColumns.test.tsx
    - web/app/components/watchlist/SearchResultsColumns.tsx
    - web/app/components/watchlist/WatchlistRow.tsx
    - web/app/lib/api.test.ts
    - web/app/lib/api.ts
    - web/app/root.test.tsx
    - web/app/routes/history.test.tsx
    - web/app/routes/watchlist.test.tsx
    - web/app/routes/watchlist.tsx
    - .github/workflows/full-pipeline.yml

key-decisions:
  - "Ran the repo's own pnpm format script (prettier --write against the existing glob) unmodified rather than hand-editing individual files, per the plan's explicit instruction -- a partial fix would have left Task 2's CI gate permanently red."
  - "19 of the 40 files prettier --write touched produced no git diff (re-saved byte-identical content); only the 21 files with real formatting drift were staged and committed, keeping the commit's diff minimal and honest."

requirements-completed: [D-04]

coverage:
  - id: D1
    description: "prettier --check exits 0 across the whole web/ tree, no [warn] lines."
    requirement: "D-04"
    verification:
      - kind: unit
        ref: "cd web && pnpm exec prettier --check \"**/*.{ts,tsx}\" -> All matched files use Prettier code style!"
        status: pass
    human_judgment: false
  - id: D2
    description: "EventCard.tsx's three case labels (new_release, guest_feature, deluxe_change) are double-quoted, not single-quoted."
    requirement: "D-04"
    verification:
      - kind: unit
        ref: "grep -c \"case 'new_release':\" == 0; grep -c 'case \"new_release\":' == 1"
        status: pass
    human_judgment: false
  - id: D3
    description: "Full Vitest suite and coverage thresholds pass unchanged after the reformat."
    requirement: "D-04"
    verification:
      - kind: unit
        ref: "cd web && pnpm test -> 50/50 tests pass, 82.37% stmts / 73.04% branch / 79.82% funcs / 83.22% lines, all above the 70% CICD-12 floor"
        status: pass
    human_judgment: false
  - id: D4
    description: "A blocking Check formatting step exists inside frontend-test, between Install dependencies and Run Vitest suite, with no failure-suppression override; build-scan.needs is untouched."
    requirement: "D-04"
    verification:
      - kind: unit
        ref: "Task 2's yaml-parsing verify script printed OK; grep -c continue-on-error == 0; grep -c 'prettier --check' == 1"
        status: pass
    human_judgment: false

duration: ~35m
completed: 2026-08-17
status: complete
---

# Phase 11.1 Plan 02: Frontend Formatting + CI Gate Summary

**Reformatted the entire `web/` tree to `.prettierrc` (40 files, fixing EventCard.tsx's single-quote case labels) and added a blocking `prettier --check` step inside the existing `frontend-test` CI job so formatting drift is caught going forward.**

## Performance

- **Duration:** ~35m
- **Tasks:** 2 (both auto)
- **Files modified:** 22 (21 web/ source/test files + 1 CI workflow)

## Accomplishments
- `cd web && pnpm exec prettier --check "**/*.{ts,tsx}"` exits 0 across the whole tree (was 40 files failing at plan start).
- `EventCard.tsx`'s three `case` labels (`new_release`, `guest_feature`, `deluxe_change`) are now double-quoted, closing 08-REVIEW.md IN-01.
- Full Vitest suite: 50/50 tests pass, coverage unchanged at 82.37% stmts / 73.04% branch / 79.82% funcs / 83.22% lines -- all above the 70% CICD-12 floor, matching 11.1-01's post-checkpoint baseline exactly.
- `frontend-test`'s CI job gained a `Check formatting` step between `Install dependencies` and `Run Vitest suite`, running `pnpm exec prettier --check "**/*.{ts,tsx}"` (identical glob to `web/package.json`'s `format` script). No new job, no `continue-on-error`, `build-scan.needs` byte-identical to before.
- `git diff web/app/components/history/HistoryFilters.tsx` still contains the string `color-scheme:dark` (in a comment referencing commit `8844812`), confirming 11.1-01's D-13 fix history survived the reformat.

## Task Commits

Each task was committed atomically:

1. **Task 1: Format the web tree to .prettierrc** - `77c6d41` (style)
2. **Task 2: Add the blocking formatting check to the frontend-test CI job** - `c9e31af` (ci)

**Plan metadata:** this commit (docs: complete plan)

## Files Created/Modified
- 20 files under `web/app/**` -- prettier-formatted (quote style, semicolons removed, trailing commas, Tailwind class reordering via `prettier-plugin-tailwindcss`)
- `.github/workflows/full-pipeline.yml` -- added the `Check formatting` step inside `frontend-test`

Note: `pnpm format` (`prettier --write`) touched 40 files total, but only 21 produced an actual git diff (`web/app/components/history/EventCard.tsx`, `.test.tsx`, `HistoryFilters.tsx`/`.test.tsx`, `CoverArt.tsx`, `EmptyState.tsx`, `checkbox.tsx`, `sonner.tsx`, `PreferenceToggles.tsx`/`.test.tsx`, `SearchBox.tsx`/`.test.tsx`, `SearchResultsColumns.tsx`/`.test.tsx`, `WatchlistRow.tsx`, `api.ts`/`.test.ts`, `root.test.tsx`, `history.test.tsx`, `watchlist.tsx`/`.test.tsx`); the other 19 (the `web/app/components/ui/*` shadcn files, `routes.ts`, `root.tsx`, `history.tsx`, `react-router.config.ts`, `vite.config.ts`, `vitest.config.ts`, `vitest.setup.ts`) were re-saved byte-identical and left unstaged, since they had no actual formatting drift despite failing the initial `--check`.

## Decisions Made
- Used the plan's exact prescribed mechanism (`pnpm format`, i.e. `prettier --write "**/*.{ts,tsx}"`) rather than hand-editing files, per the plan's explicit "do not hand-edit" constraint -- necessary because a partial fix would leave Task 2's new CI gate permanently red.
- Staged only the 21 files with real content diffs (not all 40 `prettier --write` touched), keeping the commit honest and matching the acceptance criteria's `git diff --stat` scope check.

## Deviations from Plan

None - plan executed exactly as written. The plan's own objective anticipated and pre-authorized the whole-tree reformat (documented as a "necessary consequence of the locked decision D-04, not scope creep"), so no deviation-rule escalation was needed.

## Issues Encountered

None.

## Self-Check: PASSED

- FOUND: web/app/components/history/EventCard.tsx (grep confirms double-quoted case labels)
- FOUND: .github/workflows/full-pipeline.yml (Check formatting step present, verified by yaml-parsing script)
- FOUND commit 77c6d41 in git log
- FOUND commit c9e31af in git log

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- D-04 fully closed: `web/` tree conforms to `.prettierrc`, and CI now blocks on future drift via the `frontend-test` job.
- No blockers for the remaining plans in Phase 11.1.

---
*Phase: 11.1-address-tech-debt-v1-1-cleanup*
*Completed: 2026-08-17*
