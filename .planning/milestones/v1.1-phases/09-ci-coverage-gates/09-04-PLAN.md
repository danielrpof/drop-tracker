---
phase: 09-ci-coverage-gates
plan: 04
type: execute
wave: 2
depends_on: [09-02]
files_modified:
  - web/app/routes/history.test.tsx
  - web/app/lib/api.test.ts
  - web/vitest.config.ts
  - .planning/phases/09-ci-coverage-gates/09-BASELINE-FRONTEND.md
autonomous: true
requirements: [CICD-12]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The history route's failed-initial-fetch path renders the error state with a working retry control, and retrying re-issues the request rather than only clearing the message"
    - "The history route's load-more path appends the next page to the existing list and drops any event whose id is already rendered, rather than replacing the list or duplicating a row"
    - "The history route distinguishes the filtered empty state from the unfiltered empty state — a user who filtered into nothing is not told they have no release activity at all"
    - "The API module's error path turns a non-OK response into a typed error carrying both the HTTP status and the server's error message, and falls back to the status text when the body is not valid JSON"
    - "The API module returns undefined for a no-content response instead of attempting to parse an empty body"
    - "`pnpm test` in `web/` exits non-zero when any of the four coverage axes falls below 70, with no separate check script and no change to the `test` script or the CI step"
  prohibitions:
    - requirement_id: CICD-12
      category: transparency
      statement: "MUST NOT reach the 70% gate by narrowing the coverage include glob, by adding an exclusion beyond D-06's three carve-outs, or by lowering any of the four threshold axes — the only permitted route to green is more real tests"
    - requirement_id: CICD-12
      category: transparency
      statement: "MUST NOT close a coverage gap with render-only tests that assert nothing, skipped or `.only`-scoped tests, or assertions weakened until they pass — a test that would still pass if the behavior it names were deleted is coverage theatre"
  artifacts:
    - path: "web/app/routes/history.test.tsx"
      provides: "Route-level tests for the history feed's error/retry, load-more append-and-dedupe, and filtered-vs-unfiltered empty states"
      min_lines: 90
    - path: "web/app/lib/api.test.ts"
      provides: "Unit tests for the shared fetch path's error, no-content, and success branches and for the query-string construction of the history endpoint wrapper"
      min_lines: 70
    - path: "web/vitest.config.ts"
      provides: "The enforced 70% threshold across all four coverage axes (D-08)"
      exports: ["test.coverage.thresholds"]
  key_links:
    - from: "web/app/routes/history.test.tsx"
      to: "web/app/lib/test/routeStub.tsx"
      via: "renders the History route default export through the one shared createRoutesStub helper rather than wrapping a router itself"
      pattern: "renderRoute"
    - from: "web/vitest.config.ts `coverage.thresholds`"
      to: "the `frontend-test` CI job"
      via: "vitest run exits non-zero on an unmet threshold, so the existing single `pnpm test` step becomes the gate with no CI YAML change (D-08)"
      pattern: "thresholds"
---

<objective>
Close the frontend coverage gap with real tests for the two first-party files carrying the most
untested behavior, then commit the 70% threshold so `pnpm test` itself becomes the gate.

Purpose: 09-02's explicit include glob put the whole first-party surface into the denominator, which
surfaced two files that no test touches today. `app/routes/history.tsx` has no test file at all and
owns the feed's error, retry, pagination-append, de-duplication, and two-variant empty-state logic.
`app/lib/api.ts` is mocked out of every existing component and route test by TEST-02's own design, so
its shared fetch path — the one place every endpoint's error handling lives — has never executed under
test. Both are exactly the D-09 target profile: untested error and fallback paths, not easy-to-cover
accessors. Committing the threshold last is what makes ROADMAP success criterion 4 true: the baseline
was recorded first, and the number that lands in the config is the required 70, not a number fitted
to whatever was measured.

Output: two new test files and a `thresholds` block in `web/vitest.config.ts` at 70 on all four axes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ci-coverage-gates/09-CONTEXT.md
@.planning/phases/09-ci-coverage-gates/09-RESEARCH.md
@.planning/phases/09-ci-coverage-gates/09-PATTERNS.md
@.planning/phases/09-ci-coverage-gates/09-02-SUMMARY.md
@web/app/routes/history.tsx
@web/app/lib/api.ts
@web/app/routes/watchlist.test.tsx
@web/app/lib/test/routeStub.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route-level tests for the history feed's error, pagination, and empty states</name>
  <precondition>`.planning/phases/09-ci-coverage-gates/09-BASELINE-FRONTEND.md` exists and lists `history.tsx` among the low-coverage first-party files</precondition>
  <files>web/app/routes/history.test.tsx</files>
  <read_first>
    - `web/app/routes/history.tsx` in full (197 lines) — the default export named `History`, the
      shared page-fetch helper that both the mount effect and the load-more handler call, the mount
      effect's reset-then-fetch sequence and its cancellation flag, the catch that sets the error
      message, the load-more handler's append-with-dedupe-by-id block and its separate append-error
      state, the retry handler that bumps a reload token in the effect's dependency list, and the four
      mutually exclusive render branches (error, initial loading, empty, list)
    - `web/app/routes/watchlist.test.tsx` in full (46 lines) — the exact shape to copy: the bare
      module mock with no factory and no passthrough, the typed mock accessors, a file-local fixture
      built from the wire type's real field names, and the render-then-await-then-assert flow
    - `web/app/lib/test/routeStub.tsx` — the single shared render helper and its head comment
      forbidding a hand-rolled router wrapper
    - `web/app/lib/api.ts` lines 11-34 — the event item and page envelope field names, used verbatim
      for fixtures, and lines 132-143 for the history endpoint wrapper's parameter shape
    - `web/app/components/history/EventCard.tsx` and `web/app/components/history/HistoryFilters.tsx`
      briefly — enough to know what text and controls the route renders, so queries match real
      accessible names. Both already have their own tests, so this file must not re-test their
      internals
    - `.planning/phases/09-ci-coverage-gates/09-PATTERNS.md` — the `history.test.tsx` section, which
      names the priority order for these cases
    - `.planning/phases/09-ci-coverage-gates/09-CONTEXT.md` D-09 and D-10
  </read_first>
  <action>
    Create `web/app/routes/history.test.tsx`, co-located beside the route. Land it as a plain passing
    test commit — not a red-then-green cycle, per D-10; no defect is expected because the route
    already works and is simply untested. If a test does surface a real defect, stop and land the fix
    as its own separate commit.

    Mock the API module with a bare module mock at the top of the file, exactly as
    `watchlist.test.tsx` does: no factory, no passthrough that re-exports the real module. Take a
    typed accessor for the history endpoint wrapper. Build file-local, non-exported event fixtures
    using the real wire field names, giving each a distinct id and a distinct title so an assertion
    cannot pass by coincidence. Render through the shared route-stub helper at the history path — do
    not wrap a router yourself.

    Write these cases, in this order, one behavior each, with RTL-idiomatic sentence names. The first
    two are the D-09 priorities; do not reorder them behind the happy path.

    (a) A failed initial fetch renders the error state with a retry control. Reject the mocked
        endpoint, render, wait for the error heading, and assert a retry control is present. Then
        click retry with the endpoint now resolving to a one-event page, and assert both that the
        endpoint was called a second time and that the event's title is rendered — proving retry
        re-issues the request rather than just clearing the message.

    (b) Load more appends the next page and drops an event already rendered. Resolve the first call
        with a two-event page carrying a non-null next cursor, render, wait for the first page's
        titles, click the load-more control, and resolve the second call with a page containing one
        genuinely new event plus a repeat of an id from the first page. Assert that the new event's
        title appears, that the first page's titles are still present (append, not replace), and that
        the repeated event's title appears exactly once in the document. A count assertion is what
        distinguishes de-duplication from a lucky render; asserting mere presence would pass either
        way. Also assert the second call received the cursor value the first page returned.

    (c) The unfiltered empty state and the filtered empty state say different things. Resolve an
        empty page with a null cursor and assert the no-activity heading renders. Then, in a separate
        case, drive a filter through the filters control and, with the endpoint still resolving empty,
        assert the no-matching-events heading renders instead. Query the filter control by its
        accessible name as rendered; do not add a test id to the component and do not modify
        `HistoryFilters.tsx`.

    Do not render the toast host component — the route dispatches no toast, and mounting the renderer
    pulls in browser APIs jsdom does not implement. Do not assert on the loading skeletons' internal
    markup; they are presentational and asserting on their class names would make the test brittle
    without adding meaning. Do not modify `web/app/routes/history.tsx`.
  </action>
  <verify>
    <automated>pnpm --dir web exec vitest run app/routes/history.test.tsx</automated>
    <automated>pnpm --dir web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir web exec vitest run app/routes/history.test.tsx` exits 0 when run alone, proving
      per-file mock isolation.
    - `pnpm --dir web test` exits 0 with the whole suite running.
    - `grep -c 'vi.mock("~/lib/api")' web/app/routes/history.test.tsx` is exactly 1.
    - `grep -c 'renderRoute' web/app/routes/history.test.tsx` is at least 1 — the shared helper is
      used rather than a hand-rolled router wrapper.
    - `grep -c 'MemoryRouter' web/app/routes/history.test.tsx` is exactly 0.
    - `grep -c 'importActual' web/app/routes/history.test.tsx` is exactly 0.
    - `grep -c 'data-testid' web/app/routes/history.test.tsx` is exactly 0.
    - `grep -cE 'it\.(skip|only)|describe\.(skip|only)' web/app/routes/history.test.tsx` is exactly 0.
    - `git diff --stat web/app/routes/history.tsx web/app/components/history/` is empty — no component
      under test was modified.
    - The de-duplication assertion is shown to have teeth: temporarily change the load-more handler's
      filter so repeats are kept, confirm the test fails, then restore the source.
    - The retry assertion is shown to have teeth: temporarily change the assertion to expect a single
      endpoint call, confirm it fails, then restore it.
    - The coverage table from `pnpm --dir web test` shows a materially higher percentage for
      `history.tsx` than the figure recorded in `09-BASELINE-FRONTEND.md`.
    - `pnpm --dir web typecheck` exits 0.
  </acceptance_criteria>
  <done>
    The history route's error-and-retry path, its append-with-de-duplication pagination, and its two
    distinct empty states are all asserted through the shared route stub against a fully mocked API
    boundary, with the two highest-value assertions demonstrated to fail when the behavior they name
    is removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for the shared fetch path's error, no-content, and success branches</name>
  <files>web/app/lib/api.test.ts</files>
  <read_first>
    - `web/app/lib/api.ts` lines 79-122 in full — the exported error class carrying an HTTP status
      alongside its message, and the shared fetch helper's four distinct outcomes: the no-content
      short-circuit, the non-OK path that tries to read an error field out of a JSON body, that same
      path's catch that falls back to the response's status text when the body is not valid JSON, and
      the OK path that parses and returns the body
    - `web/app/lib/api.ts` lines 132-209 — each endpoint wrapper, especially the history wrapper's
      conditional query-string construction (three optional params, each only appended when present)
      and the delete wrapper that relies on the no-content short-circuit
    - `web/vitest.setup.ts` — note it registers DOM matchers and a per-test React cleanup, and that
      `web/vitest.config.ts` sets mocks to reset before each test, which means any global stub must be
      installed per test or in a `beforeEach` and torn down in an `afterEach`
    - `.planning/REQUIREMENTS.md` § Frontend Testing — TEST-02's exact wording, and the assumption
      note in this plan explaining why a unit test of this module is consistent with it
    - `.planning/phases/09-ci-coverage-gates/09-CONTEXT.md` D-09 and D-10
  </read_first>
  <action>
    Create `web/app/lib/api.test.ts` (a plain `.ts` file — this module renders nothing, so it needs no
    JSX and no DOM). Land it as a plain passing test commit per D-10.

    This file is the one place in the suite that stubs the runtime's own fetch, because this module is
    the boundary every other test mocks and there is no seam below it. Install the stub with Vitest's
    global-stubbing helper in a `beforeEach`, and remove it with the corresponding unstub-all helper
    in an `afterEach`, so no stub leaks into any other file. Do not import or touch any component, do
    not start a server, and do not issue a real network request.

    Cover these branches, prioritising the error paths per D-09:

    (a) A non-OK response whose body carries a server error field throws the module's typed error with
        both the HTTP status and that message. Assert on the status property and the message
        separately, and assert the thrown value is an instance of the exported error class — not
        merely that something was thrown.

    (b) A non-OK response whose body is not valid JSON throws the same typed error, carrying the
        response's status text as the message. This is the catch branch that exists precisely for a
        malformed or empty error body and is the highest-value uncovered path in the file.

    (c) A no-content response resolves to undefined without attempting to parse a body. Exercise it
        through the delete wrapper, and assert the promise resolves rather than rejecting.

    (d) An OK response resolves to the parsed body. Exercise it through the history wrapper and assert
        the returned page equals the fixture.

    (e) The history wrapper's query-string construction. Three cases: called with no arguments, the
        requested path carries no query string at all; called with only a cursor, the path carries
        exactly that one parameter; called with all three params, the path carries all three with the
        wire-level parameter names the Go handler expects — read those names off the source rather
        than assuming they match the TypeScript field names, because they do not. Assert on the path
        string the stub received, which is the only place this mapping is observable.

    Do not change the export surface of `web/app/lib/api.ts`, and do not export the shared fetch
    helper to make it directly callable — every branch above is reachable through the already-exported
    wrappers.
  </action>
  <verify>
    <automated>pnpm --dir web exec vitest run app/lib/api.test.ts</automated>
    <automated>pnpm --dir web test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir web exec vitest run app/lib/api.test.ts` exits 0 when run alone.
    - `pnpm --dir web test` exits 0 with the whole suite running in parallel — the global fetch stub
      does not leak into any other file.
    - `grep -c 'unstubAllGlobals' web/app/lib/api.test.ts` is at least 1 — the stub is torn down.
    - `grep -c 'vi.mock' web/app/lib/api.test.ts` is exactly 0 — this file tests the real module, so
      module-mocking it would defeat the entire purpose.
    - `grep -cE 'it\.(skip|only)|describe\.(skip|only)' web/app/lib/api.test.ts` is exactly 0.
    - `grep -rlE "stubGlobal\(\s*['\"]fetch" web/app --include='*.test.tsx'` returns nothing — no
      component or route test stubs fetch; that seam exists only in this one unit-test file, keeping
      TEST-02 intact for every other test.
    - `git diff --stat web/app/lib/api.ts` is empty — the module under test was not modified.
    - The status-text fallback assertion is shown to have teeth: temporarily change the expected
      message to the JSON-body message, confirm the test fails, then restore it.
    - The coverage table from `pnpm --dir web test` shows a materially higher percentage for `api.ts`
      than the figure recorded in `09-BASELINE-FRONTEND.md`.
    - `pnpm --dir web typecheck` exits 0.
  </acceptance_criteria>
  <done>
    Every branch of the shared fetch path — typed error with a JSON message, typed error falling back
    to status text, no-content, and success — plus the history wrapper's query-string mapping are
    asserted against a stubbed global fetch that is installed and torn down per test.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit the 70% threshold and drive the frontend gate to green</name>
  <precondition>Tasks 1 and 2 are committed and `pnpm --dir web test` exits 0</precondition>
  <files>web/vitest.config.ts, .planning/phases/09-ci-coverage-gates/09-BASELINE-FRONTEND.md</files>
  <read_first>
    - `web/vitest.config.ts` as it stands after plan 09-02 — the coverage block with its provider,
      reporter, include, and exclude keys, and the absence of any threshold key
    - `.planning/phases/09-ci-coverage-gates/09-BASELINE-FRONTEND.md` — the recorded starting figures
      for all four axes and the D-09-ordered target list, which is the authority on what to target if
      a gap remains
    - `.planning/phases/09-ci-coverage-gates/09-CONTEXT.md` D-08 (thresholds enforced by Vitest's own
      config, no separate check script, CI step unchanged) and the phase-boundary paragraph
    - `.planning/REQUIREMENTS.md` § CI/CD Pipeline — CICD-12's exact wording and its 70% figure
  </read_first>
  <action>
    Add a `thresholds` key to the existing `coverage` object in `web/vitest.config.ts`, set to 70 on
    all four axes — statements, branches, functions, and lines — each named explicitly rather than
    relying on a single aggregate key, because Vitest evaluates them independently and a config that
    only pins one axis leaves the other three ungated. Comment it with the requirement id CICD-12,
    stating that the run exits non-zero by itself when any axis is unmet, which is why no separate
    check script exists and why the CI step stays a single unchanged test invocation (D-08).

    Then run `pnpm --dir web test`.

    If it passes, record the closing figures for all four axes and the delta from the recorded
    baseline in the plan SUMMARY and stop.

    If it does not pass, keep adding real tests until it does. Choose targets in D-09 order from the
    refreshed coverage table, preferring an untested error path, an untested branch, or an untested
    boundary over an easy-to-cover render. Follow the same conventions as this plan's first two tasks:
    co-located beside the source, bare module mock of the API boundary for anything rendering a
    component, the shared route-stub helper for anything needing router context, and no test id
    attributes added to production components. Land each as its own plain passing test commit per
    D-10.

    Four moves are forbidden as routes to green, and taking any of them makes this task a failure
    rather than a completion: lowering any of the four threshold values; narrowing the coverage
    include glob; adding an exclusion entry beyond the three D-06 carve-outs plan 09-02 committed; and
    writing render-only tests that assert nothing, skipping or `.only`-scoping tests, or weakening an
    assertion until it passes. If the remaining gap genuinely cannot be closed with meaningful tests
    inside this plan, stop and surface it in the SUMMARY as a blocker with the current four figures and
    the remaining targets — do not close it by any of those four moves.

    Finally, append a short "closing measurement" section to
    `.planning/phases/09-ci-coverage-gates/09-BASELINE-FRONTEND.md` recording the post-gap-closing
    figures for all four axes, the delta from the starting baseline, and the test files this plan
    added. Leave the original starting-baseline content above it untouched — it is the evidence for
    ROADMAP success criterion 4.
  </action>
  <verify>
    <automated>pnpm --dir web test</automated>
    <automated>pnpm --dir web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir web test` exits 0.
    - `web/vitest.config.ts` contains a `thresholds` object with four keys — statements, branches,
      functions, lines — each set to 70. Verified by `grep -c '70' web/vitest.config.ts` returning at
      least 4 and by reading the block.
    - `grep -c 'app/\*\*/\*.{ts,tsx}' web/vitest.config.ts` is exactly 1 — the include glob is
      byte-identical to what plan 09-02 committed.
    - The `exclude` array in `web/vitest.config.ts` still has exactly three entries.
    - Temporarily raising every threshold axis to 100 makes `pnpm --dir web test` exit non-zero;
      restore them to 70 afterwards. This is the only direct proof that the gate mechanism actually
      fires, since the passing path alone cannot distinguish an enforced threshold from an ignored one.
    - `grep -c 'coverage' web/package.json` is exactly 1 — only the devDependency entry; the `test`
      script is still the bare run invocation with no coverage flag appended (D-08).
    - `git diff --stat .github/` is empty — no CI change in this plan.
    - `grep -rcE 'it\.(skip|only)|describe\.(skip|only)' web/app --include='*.test.ts' --include='*.test.tsx'`
      reports 0 for every file.
    - `.planning/phases/09-ci-coverage-gates/09-BASELINE-FRONTEND.md` contains both the original
      starting-baseline section and a new closing-measurement section stating all four final figures
      and the delta.
    - `pnpm --dir web test` run twice back to back both exit 0 — the added tests are not flaky.
  </acceptance_criteria>
  <done>
    The frontend threshold is committed at 70 on all four axes, `pnpm test` enforces it by itself with
    no script or CI change, the gate mechanism is proven to fire by raising the threshold and watching
    it go red, and the closing numbers are recorded next to the starting baseline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → public internet | A frontend test that does not mock the API boundary would reach the real Go API and, transitively, MusicBrainz and Deezer on every CI push |
| Stubbed global fetch → the rest of the suite | The API unit test replaces a runtime global; a stub that outlives its test would silently alter every other test file in the same worker |
| Coverage number → gate verdict | Tests written to move a percentage are the most likely place a meaningless assertion enters the codebase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-14 | Tampering | `web/app/lib/api.test.ts` stubbing the runtime's fetch | medium | mitigate | The stub is installed per test and removed with the unstub-all helper in an `afterEach`, and an acceptance criterion greps the whole component/route test tree to prove no other file stubs fetch — so TEST-02's "mock the app's API boundary, not the network" rule stays intact everywhere except the one module that has no boundary below it |
| T-09-15 | Tampering | Frontend gap-closing tests written to move a percentage | high | mitigate | Both test tasks carry mutation-style acceptance criteria requiring the highest-value assertion to be shown failing when the behavior it names is removed, and Task 3 greps the whole tree for skipped or exclusively-scoped tests. Also pinned as a plan prohibition |
| T-09-16 | Tampering | Threshold or denominator drift during gap closure | high | mitigate | Task 3 names the four forbidden routes to green explicitly and re-asserts, after all test work is complete, that the include glob is byte-identical to plan 09-02's and that the exclude array still holds exactly three entries |
| T-09-17 | Repudiation | An ignored threshold reported as an enforced one | medium | mitigate | A passing run cannot distinguish an enforced threshold from a configured-but-ignored one. Task 3 requires temporarily raising all four axes to 100 and observing a non-zero exit, which is the only direct proof the mechanism fires |
| T-09-18 | Tampering | Test-only attributes added to production components | low | mitigate | Acceptance criteria assert no test id attributes appear in the new tests and that `git diff --stat` for the components under test is empty, so the production render surface is unchanged by test work |
| T-09-SC | Tampering | Package-manager installs | low | mitigate | This plan installs nothing. The coverage provider was installed and audited in plan 09-02; every import used here (`vitest`, `@testing-library/react`, `@testing-library/user-event`) is already a pinned devDependency |
</threat_model>

## Artifacts this phase produces

Created or modified by this plan:

| Artifact | Kind | Notes |
|----------|------|-------|
| `web/app/routes/history.test.tsx` | new file | Route-level tests: error-and-retry, load-more append with de-duplication, filtered vs unfiltered empty state. No exports; fixtures stay file-local |
| `web/app/lib/api.test.ts` | new file | Unit tests for the shared fetch path's four outcomes and the history wrapper's query-string mapping. The only file in the suite that stubs the runtime's fetch |
| `web/vitest.config.ts` → `test.coverage.thresholds` | new config key | Four explicit axes — statements, branches, functions, lines — each at 70 (CICD-12, D-08) |
| Additional `*.test.ts` / `*.test.tsx` files under `web/app/` | conditionally new | Only if Task 3's re-measurement shows a remaining gap; chosen in D-09 order from the refreshed coverage table |
| `.planning/phases/09-ci-coverage-gates/09-BASELINE-FRONTEND.md` | appended | Gains a closing-measurement section; the starting-baseline section above it is left untouched |

Not modified by this plan: `web/package.json` (its `test` script stays a bare run invocation per
D-08; the coverage devDependency was added by plan 09-02), `web/vitest.config.ts`'s `include` and
`exclude` keys (owned by plan 09-02 and deliberately out of reach here),
`.github/workflows/full-pipeline.yml` (plan 09-05), `web/app/routes/history.tsx`, `web/app/lib/api.ts`,
and everything under `web/app/components/`.

<assumptions>
- **A unit test of the API module that stubs the runtime's fetch is consistent with TEST-02.** TEST-02
  requires that tests mock the app's API boundary rather than intercepting raw fetch or network calls
  — its purpose is that component and route tests never reach a real server. That module *is* the
  boundary, so a unit test of it has no seam below `fetch` itself, and stubbing that global is the
  only way to exercise its error handling at all. The rule is preserved for every other test by an
  acceptance criterion that greps the whole component and route test tree for a fetch stub and
  requires zero matches. Flagged here so a reviewer can object explicitly rather than discovering it.
- **`api.ts` and `history.tsx` are the two largest first-party coverage gaps.** Grounded in
  09-RESEARCH.md's file inventory and in TEST-02's structural consequence for the API module, but
  unverified against real numbers until 09-02's baseline lands. Task 3 exists precisely so the final
  target list comes from the recorded table rather than from this plan's expectation, and it specifies
  exactly what to do if more work is needed and which four shortcuts are off the table.
- **Vitest's four coverage axes are evaluated independently.** A config pinning only one axis leaves
  the rest ungated, and a baseline that looks comfortable on lines can still trip on branches. All
  four are set explicitly for that reason.
- **Flagged assumption carried from plan 09-02 — CICD-12's edge classification is unresolved.** The
  deterministic edge probe returned `unclassified`/`unresolved` for this requirement; the boundary
  conditions were derived by hand instead and are pinned as `must_haves.truths` and acceptance
  criteria here (the exactly-at-70 boundary, the four-independent-axes boundary, and the
  enforced-versus-ignored-threshold distinction that Task 3's raise-to-100 check settles).
</assumptions>

<verification>
- `pnpm --dir web test` exits 0 with the thresholds committed at 70 on all four axes.
- Raising all four axes to 100 makes the same command exit non-zero; restoring them to 70 makes it
  exit 0 again.
- Each new test file also exits 0 when run alone, proving per-file mock and stub isolation.
- `pnpm --dir web typecheck` exits 0.
- `git diff --stat` for this plan touches only the two new test files, `web/vitest.config.ts`'s
  threshold block, and the appended baseline section.
- `.planning/phases/09-ci-coverage-gates/09-BASELINE-FRONTEND.md` holds both the starting baseline and
  the closing measurement.
</verification>

<success_criteria>
- The frontend aggregate reaches 70% on all four axes through real tests targeting untested error,
  fallback, and boundary behavior (CICD-12, D-09).
- The threshold is enforced by Vitest's own config with no separate check script and no change to the
  `test` script or the CI step (D-08).
- The denominator is byte-identical to what plan 09-02 committed — same include glob, same three
  exclusions.
- Every added test asserts on behavior, and the highest-value assertions are demonstrated to fail
  under a deliberately wrong expectation (D-10, plan prohibition).
</success_criteria>

<output>
Create `.planning/phases/09-ci-coverage-gates/09-04-SUMMARY.md` when done
</output>
