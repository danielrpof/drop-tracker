---
phase: 22-scheduled-digest-send
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/db/migrations/000009_digest_last_slot_at.up.sql
  - internal/db/migrations/000009_digest_last_slot_at.down.sql
  - queries/notification_settings.sql
  - internal/db/sqlc/models.go
  - internal/db/sqlc/notification_settings.sql.go
  - internal/db/sqlc/querier.go
  - internal/db/migrate_test.go
  - internal/settings/slot.go
  - internal/settings/slot_test.go
  - internal/settings/settings.go
  - internal/settings/settings_test.go
  - internal/httpserver/settings_test.go
  - internal/notifier/notifier_test.go
  - cmd/server/main.go
autonomous: true
requirements: [DGST-05, DGST-06, DGST-07, DGST-15]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The notification_settings table carries a nullable digest_last_slot_at timestamptz column added by migration 000009, and a from-scratch RunMigrations lands on schema_migrations version 9."
    - "settings.MostRecentSlot(now, CadenceDaily, loc) returns the 00:05 America/New_York instant on now's own local calendar date when now is at or after it, and the 00:05 instant on the previous local calendar date when now is before it."
    - "settings.MostRecentSlot(now, CadenceWeekly, loc) returns the most recent Friday 00:05 America/New_York instant at or before now."
    - "MostRecentSlot builds every slot with time.Date(year, month, day, 0, 5, 0, 0, loc) on a calendar date reached by AddDate, so a 23-hour or 25-hour DST day still yields exactly one 00:05 local slot for that date: across the 2026-03-08 and 2027-03-14 spring-forward dates and the 2026-11-01 and 2027-11-07 fall-back dates, stepping a clock through the whole day in 5-minute increments produces exactly one distinct daily slot value per calendar date."
    - "settings.GraceFor(CadenceDaily) is 12h and settings.GraceFor(CadenceWeekly) is 48h (D-12)."
    - "Turning digest mode on when it was off re-anchors digest_last_slot_at to MostRecentSlot(now) of the new cadence in the same UPDATE statement, so enabling never leaves the current slot looking unhandled."
    - "Changing digest_cadence while digest mode is already on re-anchors digest_last_slot_at to MostRecentSlot(now) of the new cadence."
    - "A PUT that changes neither digest_enabled nor digest_cadence, and any update that turns digest mode off, leaves digest_last_slot_at byte-identical to its prior value."
    - "DGST-15 three-role separation: digest_last_slot_at (whether due), digest_last_sent_at (last successful send, operator-facing) and events.notified_at IS NULL (what is carried) are three distinct pieces of state; no query in queries/notification_settings.sql writes digest_last_sent_at from the re-anchor path."
    - "cmd/server blank-imports time/tzdata and resolves settings.ZoneName once at startup; a time.LoadLocation error returns a non-nil error from run() so the process exits non-zero, and no branch substitutes a different location on failure (DGST-07)."
    - "A successful zone resolution emits exactly one Info log record with message 'digest zone resolved' carrying a zone attribute equal to America/New_York and an offset attribute."
    - "The GET/PUT /settings/notifications wire contract is unchanged: settingsResponse still carries exactly digest_enabled, digest_cadence, digest_last_sent_at and updated_at, and gains no slot field."
  artifacts:
    - path: "internal/db/migrations/000009_digest_last_slot_at.up.sql"
      provides: "The D-13 slot-record column"
      contains: "ADD COLUMN digest_last_slot_at"
    - path: "internal/db/migrations/000009_digest_last_slot_at.down.sql"
      provides: "Paired down migration (never run by the app; required by the repo's migration rules)"
      contains: "DROP COLUMN digest_last_slot_at"
    - path: "internal/settings/slot.go"
      provides: "D-15 slot math: MostRecentSlot, GraceFor, ZoneName, grace constants"
      exports: ["ZoneName", "MostRecentSlot", "GraceFor", "GraceDaily", "GraceWeekly"]
    - path: "internal/settings/slot_test.go"
      provides: "DST + cadence table tests for the slot math"
    - path: "queries/notification_settings.sql"
      provides: "D-14 re-anchor CASE on UpdateNotificationSettings and the D-16 AckDigestBatch CTE"
      contains: "AckDigestBatch"
  key_links:
    - from: "internal/settings/settings.go"
      to: "queries/notification_settings.sql"
      via: "Service.Update passes the Go-computed re-anchor slot into UpdateNotificationSettings"
      pattern: "ReanchorSlot"
    - from: "cmd/server/main.go"
      to: "internal/settings/slot.go"
      via: "time.LoadLocation(settings.ZoneName) fail-fast, result injected into settings.NewService"
      pattern: "settings\\.ZoneName"
  prohibitions:
    - statement: "MUST NOT silently fall back to UTC, container-local time, or any other location when the configured digest zone cannot be resolved — a silently-wrong schedule is indistinguishable from a correct one and the operator is never told (D-23/D-01)."
      category: transparency
      status: resolved
      verification: judgment
      flagged: true
    - statement: "MUST NOT deliver, or set up state that would deliver, a digest the operator did not ask for at that moment — enabling digest mode or changing cadence must never itself make the current slot look unhandled (D-14)."
      category: values
      status: resolved
      verification: judgment
      flagged: true
---

<objective>
Land the schema, the calendar math, and the boot-time zone resolution that every
later plan in this phase reads. This is the layer the schema gate requires to be
committed and sqlc-regenerated before any Go code touches the new column.

Purpose: DGST-15's three-role model needs a slot record column that is distinct
from the operator-facing watermark; DGST-05/DGST-06 need slot math that is
calendar-based rather than duration-based; DGST-07 needs a fail-fast zone load
in the composition root.
Output: migration 000009, the revised + new sqlc queries, `internal/settings/slot.go`,
a clock- and location-injectable `settings.Service`, and `cmd/server`'s zone
fail-fast.

**Deliberate divergence from tracer-first, recorded here rather than silently:**
the phase's tracer slice is plan 22-02 Task 1, not this plan. The schema gate for
this project (golang-migrate + sqlc) requires migration → `sqlc generate` →
generated code committed → `make sqlc-check` green *before* any Go code depends on
the new column, because `go build` and `go vet` both pass against stale generated
code and would green a verification that proves nothing. That gate outranks the
tracer-first "no standalone schema task" prohibition for this stack.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-scheduled-digest-send/22-CONTEXT.md
@.planning/phases/22-scheduled-digest-send/22-PATTERNS.md
@internal/db/migrations/README.md
@internal/db/migrations/000008_notification_settings.up.sql
@queries/notification_settings.sql
@internal/settings/settings.go
@internal/httpserver/settings.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 000009 plus both notification_settings queries, regenerated and drift-checked [BLOCKING — no later task may write Go against the new column until this is committed]</name>
  <reversibility rating="costly">Undoing migration 000009's column name or shape after release needs a coordinated expand-then-contract across two releases per `internal/db/migrations/README.md`'s N-1 rule, not a refactor — but it is not a one-way door: the column is additive, nullable and unbackfilled, absent from every HTTP DTO, so there is no data migration and no published contract to break. Rated for the record only; CONTEXT.md D-13 already records the operator choosing this exact shape in the grilling session, so no checkpoint re-asks it.</reversibility>
  <read_first>
    - internal/db/migrations/README.md (binding per the project CLAUDE.md before any migration edit)
    - internal/db/migrations/000008_notification_settings.up.sql (the table this alters; the prior file's header-comment shape)
    - queries/notification_settings.sql (both statements being changed/added)
    - queries/events.sql lines 38-93 (ListUnnotified's ordering contract, MarkNotified's idempotent `AND notified_at IS NULL` predicate, AdvanceGroupTrackCountBaseline's data-modifying-CTE idiom)
    - sqlc.yaml (sql_package pgx/v5, emit_pointers_for_null_types true — this is why a nullable timestamptz param generates *time.Time)
    - internal/db/migrate_test.go lines 205-220 (the hardcoded from-scratch schema version pin)
    - .planning/phases/22-scheduled-digest-send/22-CONTEXT.md decisions D-13, D-14, D-16
  </read_first>
  <files>internal/db/migrations/000009_digest_last_slot_at.up.sql, internal/db/migrations/000009_digest_last_slot_at.down.sql, queries/notification_settings.sql, internal/db/sqlc/models.go, internal/db/sqlc/notification_settings.sql.go, internal/db/sqlc/querier.go, internal/db/migrate_test.go</files>
  <action>
Create `internal/db/migrations/000009_digest_last_slot_at.up.sql` as a single additive statement adding a nullable `digest_last_slot_at timestamptz` column to `notification_settings`, with no DEFAULT and no backfill, headed by a 3-line comment naming Phase 22 and D-13 and stating that this column records the most recent scheduled fire the singleton row has handled, kept distinct from `digest_last_sent_at`. Create the paired `.down.sql` dropping that column. Both files are additive/expand-half only — no DROP, RENAME, type narrowing, NOT NULL or CHECK lands here, so `cmd/migration-check` has nothing to flag and no `allow-destructive` annotation is needed.

In `queries/notification_settings.sql`, rewrite `UpdateNotificationSettings` to use sqlc named arguments throughout so the generated param struct keeps its existing `DigestEnabled` and `DigestCadence` field names while gaining a third. Use `sqlc.arg('digest_enabled')` and `sqlc.arg('digest_cadence')` for the two existing SET targets, and set `digest_last_slot_at` from a CASE whose WHEN is `sqlc.arg('digest_enabled') AND (NOT digest_enabled OR digest_cadence <> sqlc.arg('digest_cadence'))` — the unqualified `digest_enabled`/`digest_cadence` on the right of that condition are Postgres's pre-UPDATE row values, which is precisely what D-14 requires. The THEN branch is `sqlc.narg('reanchor_slot')::timestamptz`; the ELSE branch is `digest_last_slot_at`. Keep `updated_at = now()`, `WHERE id = 1` and `RETURNING *` exactly as they are. Add a comment citing D-14 and stating that the right-hand column references are the old values.

Add a new `-- name: AckDigestBatch :exec` statement per D-16: a data-modifying CTE named `acked` that runs `UPDATE events SET notified_at = now() WHERE id = ANY(sqlc.arg('ids')::bigint[]) AND notified_at IS NULL RETURNING id`, followed by an outer `UPDATE notification_settings SET digest_last_slot_at = sqlc.arg('slot')::timestamptz, digest_last_sent_at = COALESCE(sqlc.narg('sent_at')::timestamptz, digest_last_sent_at) WHERE id = 1`. Do not add `updated_at = now()` to that outer UPDATE — `updated_at` tracks operator writes, and a scheduler ack is not one (D-16 discretion, resolved here as: leave it alone). Comment it with D-16 and note that the `acked` CTE is unreferenced by the outer statement on purpose: Postgres executes every data-modifying CTE exactly once regardless of reference, and this is what makes the event ack and both settings-column writes one atomic statement without opening a Go transaction (`WithTx` exists only on the concrete `*Queries`, never on `sqlc.Querier`).

Run `sqlc generate` and commit the regenerated `internal/db/sqlc/` output. After generating, read the produced `UpdateNotificationSettingsParams` and `AckDigestBatchParams` field names and record them verbatim in the SUMMARY — plan 22-02 and this plan's Task 3 both construct them by name. Expect `ReanchorSlot *time.Time` and `SentAt *time.Time` from `sqlc.narg` under `emit_pointers_for_null_types: true`, and `Ids []int64` / `Slot time.Time` from `sqlc.arg`; if sqlc names them differently, the generated names win and the call sites follow them.

In `internal/db/migrate_test.go`, change the from-scratch schema assertion from 8 to 9 in both the `if version != 8` condition and the `t.Fatalf` want-text, and update the adjacent comment to name Phase 22's migration 000009 as the reason, replacing the Phase 20 reference.

Note a deliberate transient state that Task 3 closes: `settings.Service.Update` still constructs `UpdateNotificationSettingsParams` without the new field, so its zero value (a nil `*time.Time`) flows into the CASE and would write SQL NULL into `digest_last_slot_at` on an enabling update. That is acceptable only between this task and Task 3 of this same plan; Task 3 supplies the computed slot. Do not ship the plan with this open.
  </action>
  <acceptance_criteria>
    - `internal/db/migrations/000009_digest_last_slot_at.up.sql` and `.down.sql` both exist; the up file contains the exact substring `ADD COLUMN digest_last_slot_at timestamptz` and the down file contains `DROP COLUMN digest_last_slot_at`.
    - Neither 000009 file contains `NOT NULL`, `DEFAULT`, `CHECK`, `RENAME`, or `CREATE INDEX`.
    - `queries/notification_settings.sql` contains `AckDigestBatch` and `reanchor_slot`.
    - `internal/db/sqlc/models.go`'s `NotificationSetting` struct carries a `DigestLastSlotAt` field.
    - `internal/db/sqlc/querier.go`'s `Querier` interface declares `AckDigestBatch`.
    - `internal/db/migrate_test.go` no longer asserts schema version 8 for the from-scratch case.
    - `make sqlc-check` reports no drift between `queries/` and `internal/db/sqlc/`.
  </acceptance_criteria>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./...</automated>
    <fails_when>non-zero exit, or any compiler/vet diagnostic printed</fails_when>
    <automated>make sqlc-check</automated>
    <fails_when>non-zero exit, or the output reports a diff between committed and regenerated sqlc output</fails_when>
    <automated>make db-up &amp;&amp; go test ./internal/db/ -run 'TestRunMigrations' -count=1 -v</automated>
    <fails_when>non-zero exit, or "FAIL" in the output, or a line reporting `want (9, false)` alongside a different observed version</fails_when>
  </verify>
  <done>Migration 000009 and both queries are committed with regenerated sqlc output; `make sqlc-check` is clean and the from-scratch migration test passes at version 9.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Slot math in internal/settings — MostRecentSlot, grace constants, and the fixed zone name</name>
  <read_first>
    - internal/settings/settings.go lines 18-46 (the Cadence type and its two constants this math switches on)
    - .planning/phases/22-scheduled-digest-send/22-CONTEXT.md decisions D-01, D-02, D-03, D-11, D-12, D-15, D-25
    - .planning/phases/22-scheduled-digest-send/22-PATTERNS.md, section "internal/settings/settings.go" and the "No Analog Found" row for slot-math calendar functions
    - internal/notifier/notifier.go lines 148-166 (staleReleaseDate — the existing clock-free, table-testable split this file's tests mirror)
  </read_first>
  <files>internal/settings/slot.go, internal/settings/slot_test.go</files>
  <behavior>
    - MostRecentSlot(2026-06-10 09:30 America/New_York, CadenceDaily, loc) returns 2026-06-10 00:05 America/New_York.
    - MostRecentSlot(2026-06-10 00:04 America/New_York, CadenceDaily, loc) returns 2026-06-09 00:05 America/New_York (strictly before the slot means the slot has not arrived).
    - MostRecentSlot(2026-06-10 00:05:00 America/New_York, CadenceDaily, loc) returns 2026-06-10 00:05 (at the instant counts as arrived, not before).
    - A UTC-valued `now` still yields a slot in `loc`: MostRecentSlot(2026-06-10 03:00 UTC, CadenceDaily, NY) returns 2026-06-09 00:05 America/New_York, because 03:00 UTC is 23:00 on 2026-06-09 in New York.
    - Weekly: MostRecentSlot(2026-06-10 09:30 NY /a Wednesday/, CadenceWeekly, loc) returns 2026-06-05 00:05 (the preceding Friday).
    - Weekly on a Friday before the slot: MostRecentSlot(2026-06-12 00:04 NY, CadenceWeekly, loc) returns 2026-06-05 00:05, not 2026-06-12.
    - Weekly on a Friday at/after the slot: MostRecentSlot(2026-06-12 00:05 NY, CadenceWeekly, loc) returns 2026-06-12 00:05.
    - Spring-forward day 2026-03-08 (a 23-hour local day): stepping a clock from 2026-03-08 00:00 NY to 2026-03-09 00:00 NY in 5-minute increments yields exactly one distinct daily slot value for 2026-03-08, and that value's UTC offset is -05:00 (EST).
    - Fall-back day 2026-11-01 (a 25-hour local day): the same 5-minute sweep yields exactly one distinct daily slot value for 2026-11-01, and that value's UTC offset is -04:00 (EDT).
    - The same two sweeps over 2027-03-14 and 2027-11-07 yield exactly one distinct daily slot value each.
    - Weekly across the same four transition dates: a 5-minute sweep over the containing Friday-to-Friday week yields exactly one distinct weekly slot value per week.
    - GraceFor(CadenceDaily) == 12*time.Hour; GraceFor(CadenceWeekly) == 48*time.Hour.
  </behavior>
  <action>
Create `internal/settings/slot.go` in package `settings`.

Export `const ZoneName = "America/New_York"` with a one-line comment citing D-01 (the zone is fixed and documented; an operator-configurable picker is out of scope per REQUIREMENTS.md). Export `const GraceDaily = 12 * time.Hour` and `const GraceWeekly = 48 * time.Hour`, each with a one-line why-comment citing D-12 — daily so a brief outage still delivers promptly, weekly so a day-long outage does not produce a surprise mid-afternoon digest. Keep the slot's hour and minute as unexported constants `slotHour = 0` and `slotMinute = 5` with a comment citing D-02 (00:05 sits outside the 02:00 local transition window on both spring-forward and fall-back), and `weeklySlotWeekday = time.Friday` citing D-03 and D-25 (Friday 00:05 America/New_York, covering through Thursday night).

Export `func GraceFor(cadence Cadence) time.Duration` returning GraceWeekly for CadenceWeekly and GraceDaily otherwise.

Export `func MostRecentSlot(now time.Time, cadence Cadence, loc *time.Location) time.Time` implementing D-11's calendar math. Convert `now` into `loc` first (`local := now.In(loc)`). For the daily case, build `candidate := time.Date(local.Year(), local.Month(), local.Day(), slotHour, slotMinute, 0, 0, loc)`; if `local.Before(candidate)`, step back one calendar day with `prev := local.AddDate(0, 0, -1)` and rebuild `candidate` from `prev`'s year/month/day through `time.Date` again. For the weekly case, compute `daysBack := (int(local.Weekday()) - int(weeklySlotWeekday) + 7) % 7`, step to that calendar date with `AddDate(0, 0, -daysBack)`, build the candidate through `time.Date` on that date, and if `local.Before(candidate)` step back a further 7 calendar days with `AddDate(0, 0, -7)` and rebuild through `time.Date`. Switch on `cadence` with `CadenceWeekly` as the only named case and daily as the default, commenting that the `digest_cadence` CHECK constraint is what guarantees no third value reaches here.

Two rules the file's comments must state, because they are the whole point of D-11: never reach a slot by adding a fixed duration (`+24h`, `+168h`) to a timestamp, and always rebuild the candidate through `time.Date(..., loc)` after any `AddDate` step. Both exist because a DST day is 23 or 25 hours long, so duration arithmetic lands at 23:05 or 01:05 local instead of 00:05.

Write `internal/settings/slot_test.go` as external package `settings_test`, loading the zone once per test with `time.LoadLocation(settings.ZoneName)` and skipping nothing (the host and CI both have zoneinfo; the Alpine case is proved in plan 22-04, not here). Cover every case in the behavior block above. Implement the DST sweeps as a helper that walks a `time.Time` forward in 5-minute steps between two bounds, collects `MostRecentSlot` results into a set keyed by the returned instant, and asserts the expected cardinality — this helper is the same instrument plan 22-04 reuses at the scheduler level, so keep it obvious.
  </action>
  <acceptance_criteria>
    - `internal/settings/slot.go` exports `ZoneName`, `MostRecentSlot`, `GraceFor`, `GraceDaily`, `GraceWeekly`.
    - `grep -n 'covering through Thursday night' internal/settings/slot.go` returns at least one match — D-25 requires the weekly fire time to be stated in that exact operator-facing wording wherever the fire time is documented, and this file is where it is documented, because REQUIREMENTS.md makes "the fixed time is documented" the condition for keeping a configurable picker out of scope.
    - `grep -n 'time.Date(' internal/settings/slot.go` returns at least two matches (the daily and weekly candidate constructions).
    - The daily sweep test over 2026-03-08 asserts exactly 1 distinct slot and an offset of -05:00; the sweep over 2026-11-01 asserts exactly 1 distinct slot and an offset of -04:00.
    - `go test ./internal/settings/ -run 'TestMostRecentSlot|TestGraceFor' -count=1 -v` reports PASS for every named subtest in the behavior block.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/settings/ -run 'TestMostRecentSlot|TestGraceFor' -count=1 -v</automated>
    <fails_when>non-zero exit, or "FAIL" in the output, or "no tests to run" in the output</fails_when>
    <automated>go vet ./internal/settings/</automated>
    <fails_when>non-zero exit, or any vet diagnostic printed</fails_when>
  </verify>
  <done>Slot math is committed with table tests covering both cadences and all four 2026/2027 DST transition dates, all green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-anchor on enable/cadence-change, injectable clock and location, and the boot-time zone fail-fast</name>
  <precondition>`internal/db/sqlc/querier.go` declares `AckDigestBatch` and `internal/db/sqlc/models.go`'s `NotificationSetting` carries `DigestLastSlotAt` — i.e. Task 1's `sqlc generate` output is committed.</precondition>
  <read_first>
    - internal/settings/settings.go in full (Service, NewService, Get, Update, toSettings, Settings, Store)
    - internal/settings/slot.go (Task 2's output — the exact exported names being called)
    - internal/db/sqlc/notification_settings.sql.go (Task 1's regenerated param struct field names)
    - internal/httpserver/settings.go lines 20-50 (SettingsStore seam and the settingsResponse DTO that must not change)
    - cmd/server/main.go lines 1-60 (import block, shutdownTimeout/pollDrainTimeout constants) and lines 250-260 (the settingsStore construction site)
    - internal/notifier/notifier.go lines 107-117 (New's "required, not an Option" comment — the precedent this task cites for making the location a positional parameter)
    - .planning/phases/22-scheduled-digest-send/22-CONTEXT.md decisions D-14, D-15, D-23
  </read_first>
  <files>internal/settings/settings.go, internal/settings/settings_test.go, internal/httpserver/settings_test.go, internal/notifier/notifier_test.go, cmd/server/main.go</files>
  <behavior>
    - Given digest mode off and digest_last_slot_at NULL, Update{DigestEnabled: true, DigestCadence: daily} writes digest_last_slot_at = MostRecentSlot(clock(), daily, loc) and leaves digest_last_sent_at NULL.
    - Given digest mode already on with cadence daily and a known digest_last_slot_at, Update{true, weekly} overwrites digest_last_slot_at with MostRecentSlot(clock(), weekly, loc).
    - Given digest mode already on with cadence daily and a known digest_last_slot_at, Update{true, daily} leaves digest_last_slot_at byte-identical.
    - Given digest mode on, Update{false, daily} leaves digest_last_slot_at byte-identical (turning off never re-anchors).
    - Given digest mode off, Update{false, weekly} leaves digest_last_slot_at byte-identical.
    - Service.Get exposes the column as Settings.DigestLastSlotAt (*time.Time, nil when SQL NULL).
    - The PUT /settings/notifications response body for any of the above carries exactly the four existing keys and no slot key.
  </behavior>
  <action>
In `internal/settings/settings.go`: add a `DigestLastSlotAt *time.Time` field to `Settings` and populate it in `toSettings` with the same `row.<col>.Valid` unwrap idiom already used for `DigestLastSentAt`. Do not touch `internal/httpserver/settings.go` — `toSettingsResponse` is an explicit field-by-field mapper, so the wire contract stays identical by construction, and D-13 keeps the slot record off the operator-facing surface.

Change `NewService` to `func NewService(q sqlc.Querier, loc *time.Location, opts ...Option) *Service`, adding `loc *time.Location` and `now func() time.Time` fields to `Service`. Make the location a required positional parameter rather than an option, and comment it citing the identical reasoning `notifier.New` already records for `settingsReader`: a forgotten option would silently ship a service scheduling against the wrong zone, which is the exact silent failure D-23 exists to prevent. Add `type Option func(*Service)` and `func WithClock(now func() time.Time) Option` for test injection (D-15); default `now` to `time.Now` inside `NewService`.

Change `Service.Update` to compute `slot := MostRecentSlot(s.now(), cadence, s.loc)` after `ParseCadence` succeeds and pass `&slot` as the re-anchor parameter of `UpdateNotificationSettingsParams` under whatever field name Task 1's generated code produced. The SQL's CASE decides whether the value is applied; `Update` always supplies it. Add a 2-line comment citing D-14 and stating that the decision lives in the CASE, not here.

Update all ten `settings.NewService(` call sites to pass a location: in `cmd/server/main.go` pass the resolved `loc` (below); in `internal/settings/settings_test.go`, `internal/httpserver/settings_test.go` and `internal/notifier/notifier_test.go` load it with `time.LoadLocation(settings.ZoneName)` and fail the test on error.

In `cmd/server/main.go`: add `_ "time/tzdata"` to the import block with a one-line comment citing D-23 and DGST-07 (the Alpine runtime image ships no zoneinfo, so the tzdata the scheduler resolves against must be embedded in the binary). Immediately before the `settingsStore` construction, resolve the zone once and fail fast: call `time.LoadLocation(settings.ZoneName)` and on error return a wrapped error from `run()` naming the zone, so the process exits non-zero. On success emit exactly one Info record with the message string `digest zone resolved` carrying `slog.String("zone", loc.String())` and `slog.String("offset", time.Now().In(loc).Format("-07:00"))`. That message string is a literal contract: plan 22-04's CI boot step greps the running container's log for it, so do not reword it there without changing it here. There is no alternate branch, no default location and no recovery path — a zone that will not load is a boot failure.

Extend `internal/settings/settings_test.go` with real-Postgres tests over `testutil`'s pool covering every row of the behavior block, injecting a fixed clock via `WithClock` so the expected slot is computable in the assertion. Read `digest_last_slot_at` back through `Service.Get`'s new `DigestLastSlotAt` field rather than a raw SQL probe. Add one assertion in `internal/httpserver/settings_test.go` that the PUT response body's decoded JSON object has exactly four keys.
  </action>
  <acceptance_criteria>
    - `settings.NewService` takes a `*time.Location` as its second parameter and `settings.WithClock` exists.
    - `settings.Settings` carries `DigestLastSlotAt *time.Time`.
    - `internal/httpserver/settings.go` is unmodified by this task (`git diff --exit-code -- internal/httpserver/settings.go` is clean relative to the task's starting commit).
    - `grep -n 'digest zone resolved' cmd/server/main.go` returns exactly one match.
    - `grep -c 'time/tzdata' cmd/server/main.go` returns at least 1.
    - The four "leaves digest_last_slot_at byte-identical" behavior rows each have a named test asserting equality against the value read before the update.
    - The httpserver PUT response test asserts a decoded key count of exactly 4.
  </acceptance_criteria>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./...</automated>
    <fails_when>non-zero exit, or any compiler/vet diagnostic printed</fails_when>
    <automated>make db-up &amp;&amp; go test ./internal/settings/ -count=1 -v</automated>
    <fails_when>non-zero exit, or "FAIL" in the output</fails_when>
    <automated>go test ./internal/httpserver/ -run 'TestSettings|TestUpdateSettings' -count=1 -v</automated>
    <fails_when>non-zero exit, or "FAIL" in the output, or "no tests to run" in the output</fails_when>
    <automated>go vet ./... &amp;&amp; golangci-lint run</automated>
    <fails_when>non-zero exit, or any linter finding printed</fails_when>
    <automated>make test &amp;&amp; make coverage-gate</automated>
    <fails_when>non-zero exit, or the coverage gate reports a total below 80</fails_when>
  </verify>
  <done>Enabling digest mode or changing cadence re-anchors the slot record inside one UPDATE; every other update path leaves it untouched; the process refuses to boot when the digest zone will not resolve and logs one `digest zone resolved` line when it does.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols created by this plan (new in this phase — not pre-existing codebase surface):

| Symbol | Kind | Location |
|---|---|---|
| `digest_last_slot_at` | DB column (nullable timestamptz) | `notification_settings`, migration 000009 |
| `sqlc.NotificationSetting.DigestLastSlotAt` | generated struct field | `internal/db/sqlc/models.go` |
| `sqlc.Querier.AckDigestBatch` | generated method | `internal/db/sqlc/querier.go` |
| `sqlc.AckDigestBatchParams` | generated struct | `internal/db/sqlc/notification_settings.sql.go` |
| `UpdateNotificationSettingsParams`'s re-anchor field | generated struct field | `internal/db/sqlc/notification_settings.sql.go` |
| `settings.ZoneName` | exported const | `internal/settings/slot.go` |
| `settings.MostRecentSlot` | exported func | `internal/settings/slot.go` |
| `settings.GraceFor` | exported func | `internal/settings/slot.go` |
| `settings.GraceDaily` / `settings.GraceWeekly` | exported consts | `internal/settings/slot.go` |
| `settings.Option` / `settings.WithClock` | exported type + func | `internal/settings/settings.go` |
| `settings.Settings.DigestLastSlotAt` | exported struct field | `internal/settings/settings.go` |
| `"digest zone resolved"` | log-message literal (contract read by CI) | `cmd/server/main.go` |

No new env vars, no new CLI flags, no new Go module. `golang.org/x/text` stays indirect until plan 22-03.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator SPA → PUT /settings/notifications → Postgres | operator-controlled cadence/enabled values cross into scheduling state |
| container runtime → Go `time.LoadLocation` | the image's zoneinfo (or its absence) determines every computed fire instant |
| app clock → Postgres clock | two independent clocks; the slot value must come from exactly one of them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Spoofing | `cmd/server` zone resolution | medium | mitigate | `time.LoadLocation(settings.ZoneName)` failure returns an error from `run()`; no branch supplies an alternate location, so a wrong-zone schedule cannot masquerade as a correct one. Proven end-to-end against the shipped image in plan 22-04. |
| T-22-02 | Tampering | `UpdateNotificationSettings` re-anchor CASE | medium | mitigate | The slot value is computed in Go from the injected clock and written inside the same statement as the enable/cadence change; the CASE reads pre-UPDATE column values, so a concurrent PUT cannot interleave a stale re-anchor. `WHERE id = 1` plus the table's `CHECK (id = 1)` keeps it a singleton write. |
| T-22-03 | Tampering | slot value source | medium | mitigate | `digest_last_slot_at` stores the Go-computed slot instant, never `now()`, so app/DB clock skew cannot make a handled slot look unhandled (D-13). |
| T-22-04 | Repudiation | `AckDigestBatch` watermark write | medium | mitigate | `digest_last_sent_at` is written only through `COALESCE(nullable_param, digest_last_sent_at)`; a caller with nothing sent passes nil and the operator-facing timestamp stays put. |
| T-22-05 | Denial of Service | migration 000009 against a populated table | low | accept | A nullable `ADD COLUMN` with no DEFAULT is a catalog-only change in Postgres 11+ — no table rewrite, no long lock. The table is a single row regardless. |
| T-22-06 | Elevation of Privilege | HTTP settings contract | low | mitigate | `updateSettingsRequest` decodes with `DisallowUnknownFields` and carries only the two client-settable fields; the new column is server-owned and absent from both request and response DTOs, so no client can set it. |
| T-22-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no package from any package manager. No `go get`, no new `require` line, no `package.json` change — `go.mod` and `go.sum` are untouched. RESEARCH.md's Package Legitimacy Audit is therefore not required for this plan and no legitimacy checkpoint is inserted. |
</threat_model>

<verification>
- `make sqlc-check` clean (local-only gate; CI has no counterpart and will not catch sqlc drift).
- `go vet ./...` and `golangci-lint run` clean.
- `make test` green with `make db-up` running; `make coverage-gate` at or above the 80% backend floor.
- `git diff --exit-code main...HEAD -- internal/poller web/` is clean: this phase touches neither the poller nor the frontend.
- No `git commit --no-verify` anywhere in this plan's commits.
</verification>

<success_criteria>
- Migration 000009 exists as an additive nullable column with a paired down file, and a from-scratch migration run lands on version 9.
- `settings.MostRecentSlot` returns exactly one distinct daily slot per calendar date across all four 2026/2027 DST transition dates, for both cadences.
- Enabling digest mode or changing cadence re-anchors `digest_last_slot_at`; every other update leaves it untouched.
- The process exits non-zero when `America/New_York` will not resolve, and logs exactly one `digest zone resolved` Info line when it does.
- The `/settings/notifications` wire contract is byte-identical to before this plan.
</success_criteria>

<output>
Create `.planning/phases/22-scheduled-digest-send/22-01-SUMMARY.md` when done. Record the generated sqlc param field names verbatim — plan 22-02 constructs `AckDigestBatchParams` by name.
</output>
