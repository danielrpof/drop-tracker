---
phase: 22-scheduled-digest-send
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - internal/notifier/format.go
  - internal/notifier/format_test.go
  - internal/notifier/digest_format.go
  - internal/notifier/digest_format_test.go
  - go.mod
  - go.sum
autonomous: true
requirements: [DGST-08, DGST-10]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Discord markdown metacharacters in a community-editable artist name or title are backslash-escaped before they reach the digest Description: a title containing `[Deluxe]` renders as escaped literal text inside the masked-link label and does not terminate or retarget the link."
    - "DGST-08 encoding edge: the digest's length rule is code points, not bytes — each title is capped at 100 runes by truncateRunes, which cuts on a rune boundary, so a multi-byte character is never split into an invalid UTF-8 sequence, and a 100-rune title of multi-byte characters is not cut short by a byte-length rule."
    - "Escaping is applied to the artist name and the title only. URLs are never escaped and never built from Title or ArtistName — every href still comes from external_id through url.PathEscape via the existing format.go helpers (T-05-06)."
    - "DGST-10 ordering: within each event-type group, lines sort by the watched-artist key using golang.org/x/text/collate with collate.IgnoreCase, so a lowercase-stylized or accented name such as `Ñengo Flow` sorts among the N's rather than after Z."
    - "DGST-10 ordering, ties: when two events compare equal on the collated artist key, the order is broken by title and then by event id, so the output order is total, specified and stable regardless of ListUnnotified's arrival order."
    - "Group order is fixed at New Releases, then Guest Features, then Deluxe Changes, independent of detection order (DGST-10)."
    - "DGST-10 empty edge: a type group with zero sendable events has its heading omitted entirely — no empty heading, no placeholder line."
    - "DGST-10 adjacency: two sendable events for the same watched artist inside the same type group render as two separate lines under that group, never merged into one line."
    - "DGST-08 adjacency: two events describing the same release detected from both MusicBrainz and Deezer render as two separate lines and are acked separately — duplicate lines are not merged (D-26)."
    - "The artist key is watched_artist_name, falling back to artist_name only when the column is NULL or empty (D-20)."
    - "A guest_feature line names both the watched artist and the host credit in the form `Watched on Host — Title`, because artist_name on those rows is the host's primary credit."
    - "A deluxe_change line carries the track-count suffix produced by tracksFieldValue, appended after the closing parenthesis of the link; when tracksFieldValue returns the empty string the suffix is omitted entirely and no empty parentheses appear."
    - "golang.org/x/text is a direct dependency in go.mod with no version change and no new module added to go.sum."
    - "The per-event-type URL selection is a single shared helper called by both formatEmbed and the digest builder; formatEmbed's own output is byte-identical to before this plan."
  artifacts:
    - path: "internal/notifier/digest_format.go"
      provides: "Escaping, collation sort, grouping, host credit and track-count suffix for the digest Description"
      contains: "collate"
    - path: "internal/notifier/digest_format_test.go"
      provides: "Table tests for escaping, ordering, grouping, empty groups and the adjacency cases"
    - path: "internal/notifier/format.go"
      provides: "The extracted per-event-type URL helper shared by formatEmbed and the digest builder"
  key_links:
    - from: "internal/notifier/digest_format.go"
      to: "internal/notifier/format.go"
      via: "the digest builder calls the shared event URL helper and truncateRunes rather than duplicating them"
      pattern: "truncateRunes"
    - from: "internal/notifier/digest_format.go"
      to: "go.mod"
      via: "collate.New(language.Und, collate.IgnoreCase) requires golang.org/x/text as a direct dependency"
      pattern: "golang.org/x/text"
  prohibitions:
    - statement: "MUST NOT let community-editable artist or title text change the shape or target of anything in the digest message — the operator must be able to trust that a link labelled with an artist's name goes to that artist's catalogue page and that no title can inject formatting into a heading (D-21)."
      category: safety
      status: resolved
      verification: judgment
      flagged: true
    - statement: "MUST NOT silently merge or drop an event because it looks like a duplicate of another — one event, one line, one ack; merging is explicitly deferred as unverified (D-26)."
      category: transparency
      status: resolved
      verification: judgment
      flagged: true
---

<objective>
Make the digest body carry every locked decision from CONTEXT.md's message-layout
section: escaped and rune-capped community text, collated sorting by watched
artist, the guest-feature host credit, the deluxe track-count suffix, and a
shared URL helper instead of a duplicated switch.

Purpose: DGST-10 requires grouping *for readability* — grouped, deterministically
ordered and safely rendered — and DGST-08 requires all three types in one message.
Plan 22-02 delivered the structure; this plan delivers the content rules.
Output: a complete `digest_format.go`, an extracted URL helper in `format.go`, and
`golang.org/x/text` promoted from indirect to direct.

This plan closes threat T-22-07, which plan 22-02 deliberately left open. The
phase must not be marked complete until it is closed here.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-scheduled-digest-send/22-CONTEXT.md
@.planning/phases/22-scheduled-digest-send/22-PATTERNS.md
@.planning/phases/22-scheduled-digest-send/22-02-SUMMARY.md
@internal/notifier/format.go
@internal/notifier/digest_format.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Escape and cap community-editable text, and extract the per-event-type URL helper</name>
  <precondition>`internal/notifier/digest_format.go` exists and exports `buildDigestEmbed` with the signature recorded in 22-02-SUMMARY.md.</precondition>
  <read_first>
    - internal/notifier/format.go in full — `formatEmbed`'s switch (lines 58-73), the three `musicBrainz*URL` helpers and `newReleaseURL` (lines 155-184) with their T-05-06 comment about never interpolating Title or ArtistName into a URL, and `truncateRunes` (lines 199-209) with its T-05-02 rune-boundary rationale
    - internal/notifier/digest_format.go (plan 22-02's builder — the local switch this task replaces)
    - internal/notifier/format_test.go — the table-test conventions this package uses for pure transforms
    - .planning/phases/22-scheduled-digest-send/22-CONTEXT.md decisions D-05, D-07, D-21
    - .planning/phases/22-scheduled-digest-send/22-PATTERNS.md, "No Analog Found" row for the markdown-escaping helper — there is no existing sanitizer in this codebase, because formatEmbed only ever wrote into structured Fields and Title, never into markdown-parsed Description text
  </read_first>
  <files>internal/notifier/format.go, internal/notifier/format_test.go, internal/notifier/digest_format.go, internal/notifier/digest_format_test.go</files>
  <behavior>
    - escapeMarkdown leaves an ordinary name such as `Bad Bunny` unchanged.
    - escapeMarkdown of a title containing square brackets returns that title with a backslash before each bracket character.
    - escapeMarkdown of a title containing round brackets returns that title with a backslash before each parenthesis.
    - escapeMarkdown of a title containing an asterisk returns it with a backslash before the asterisk.
    - escapeMarkdown of a title containing an underscore returns it with a backslash before the underscore.
    - escapeMarkdown of a title containing a single backslash returns it with that backslash doubled, and the backslash rule is applied first so no other escape is double-escaped.
    - escapeMarkdown also escapes tilde, backtick, pipe, greater-than and hash.
    - escapeMarkdown of the empty string returns the empty string.
    - A 150-rune title is capped to 100 runes before escaping is applied, and the cap counts runes: a 100-rune title made entirely of multi-byte characters survives uncapped.
    - eventURL(ev) for a new_release row with source musicbrainz returns the MusicBrainz release-group URL; with source deezer returns the Deezer album URL; for guest_feature returns the MusicBrainz recording URL; for deluxe_change returns the MusicBrainz release URL; for an unrecognised event type returns the empty string.
    - formatEmbed's output for each of the three event types is unchanged by the extraction.
  </behavior>
  <action>
In `internal/notifier/digest_format.go`, add `func escapeMarkdown(s string) string`. Backslash-escape this exact set of Discord markdown metacharacters, one backslash before each occurrence: backslash, asterisk, underscore, tilde, backtick, pipe, greater-than, hash, open and close square bracket, open and close round bracket. Escape the backslash character first, before any other replacement, so an escape inserted by a later rule is never re-escaped by the backslash rule; a `strings.NewReplacer` with all pairs in one pass, or an explicit backslash-first ordering, both satisfy this — a naive sequence of `strings.ReplaceAll` calls with the backslash rule anywhere but first does not. Give it a 3-line comment citing D-21 and naming the reason: titles routinely carry bracketed and parenthesised suffixes that terminate or retarget a masked link, and emphasis characters bleed formatting into the bold headings. Record `T-22-07` in the comment as the threat this closes.

Add `const digestTitleLimit = 100` with a one-line comment citing D-21 (Description is capped at 4096 characters and every line's URL counts toward it, so per-line length is a budget, not just cosmetics). Apply `truncateRunes(title, digestTitleLimit)` **before** `escapeMarkdown`, so the cap counts the operator-visible characters rather than the inserted backslashes, and reuse `truncateRunes` rather than writing a second truncator — it already cuts on a rune boundary for the reason recorded at T-05-02.

In `internal/notifier/format.go`, extract the per-event-type URL selection out of `formatEmbed`'s switch into `func eventURL(ev sqlc.Event) string` returning `newReleaseURL(ev.Source, ev.ExternalID)` for `eventTypeNewRelease`, `musicBrainzRecordingURL(ev.ExternalID)` for `eventTypeGuestFeature`, `musicBrainzReleaseURL(ev.ExternalID)` for `eventTypeDeluxeChange`, and the empty string otherwise. Rewrite `formatNewRelease`, `formatGuestFeature` and `formatDeluxeChange` to set their `URL` field from `eventURL(ev)` so exactly one switch decides the mapping. Do not change `formatEmbed`'s behaviour, its default branch, its colors, its emoji prefixes or its Fields — the extraction is behaviour-preserving, and `format_test.go` must still pass unmodified except for any test you add for `eventURL` itself. Carry the existing T-05-06 comment's point forward: the URL is always built from `external_id` through `url.PathEscape` and never from community-editable text.

In `internal/notifier/digest_format.go`, replace plan 22-02's local URL switch with a call to `eventURL(ev)`, and route the artist name and title through `escapeMarkdown` (title through the cap first) at the single point where the `- [<artist> — <title>](<url>)` line is assembled.

Write `internal/notifier/digest_format_test.go` in package `notifier` (whitebox — `escapeMarkdown` and `eventURL` are unexported, matching the convention `musicbrainz_test.go` and `normalize_test.go` already use for unexported functions in this repo). Cover every row of the behavior block as a table test. Include the five literals D-21 names explicitly as cases. Add one test asserting that the built line for a title containing square brackets contains a backslash immediately before the opening bracket and that the substring between the line's `](` and its closing parenthesis is exactly the expected URL — that is the assertion that proves the link target cannot be retargeted by title text.
  </action>
  <acceptance_criteria>
    - `internal/notifier/digest_format.go` defines `escapeMarkdown` and `digestTitleLimit = 100`.
    - `internal/notifier/format.go` defines `eventURL` and each of `formatNewRelease`, `formatGuestFeature`, `formatDeluxeChange` sets `URL:` from `eventURL(ev)`.
    - `grep -c 'musicBrainzRecordingURL(' internal/notifier/format.go` returns 2 — the definition and exactly one call site, inside `eventURL`.
    - `grep -c 'musicBrainzReleaseURL(' internal/notifier/format.go` returns 2, on the same reasoning.
    - `grep -n 'escapeMarkdown' internal/notifier/digest_format.go` shows it applied to both the artist value and the title value on the line-assembly path.
    - The escaping table test covers all eleven metacharacters plus the empty-string case.
    - `go test ./internal/notifier/ -run 'TestFormatEmbed|TestFormatNewRelease|TestFormatGuestFeature|TestFormatDeluxeChange' -count=1 -v` passes with no edit to those existing test bodies.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/notifier/ -run 'TestEscapeMarkdown|TestEventURL|TestDigestLine' -count=1 -v</automated>
    <fails_when>non-zero exit, or "FAIL" in the output, or "no tests to run" in the output</fails_when>
    <automated>go test ./internal/notifier/ -run 'TestFormat' -count=1 -v</automated>
    <fails_when>non-zero exit, or "FAIL" in the output — the URL extraction must be behaviour-preserving for the existing embed formatters</fails_when>
    <automated>go build ./... &amp;&amp; go vet ./...</automated>
    <fails_when>non-zero exit, or any compiler/vet diagnostic printed</fails_when>
  </verify>
  <done>Community-editable artist and title text is rune-capped and backslash-escaped before it reaches the digest Description, one shared helper decides every event URL, and `formatEmbed`'s output is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Group by type then collated artist, with the guest host credit and the deluxe track-count suffix</name>
  <read_first>
    - internal/notifier/digest_format.go (Task 1's state — the builder being completed)
    - internal/notifier/format.go lines 125-140 (`tracksFieldValue` and its three documented shapes, including the empty-string case)
    - internal/db/sqlc/models.go `Event` struct — `WatchedArtistName *string`, `ArtistName string`, `PreviousTrackCount *int32`, `TrackCount *int32`, `ID int64`
    - internal/db/migrations/000007_backfill_events_watched_artist_name.up.sql — why the `watched_artist_name` fallback is a guard rather than a normal path
    - go.mod line 21 (`golang.org/x/text v0.39.0 // indirect`)
    - .planning/phases/22-scheduled-digest-send/22-CONTEXT.md decisions D-04, D-06, D-20, D-22, D-26 and the `<specifics>` block's Description shape
  </read_first>
  <files>internal/notifier/digest_format.go, internal/notifier/digest_format_test.go, go.mod, go.sum</files>
  <behavior>
    - Three new_release events whose watched artists are `Zion & Lennox`, `Ñengo Flow` and `bad bunny` render in the order `bad bunny`, `Ñengo Flow`, `Zion & Lennox` — case-insensitive and accent-aware, not byte order.
    - Two events with the identical watched artist and different titles render in title order.
    - Two events with the identical watched artist and identical title render in ascending event-id order.
    - Shuffling the input slice does not change the rendered Description.
    - A guest_feature event with watched artist `Rauw Alejandro` and artist_name `Drake` renders a line whose label is `Rauw Alejandro on Drake — <title>`.
    - A guest_feature event is grouped and sorted under its watched artist, not under artist_name.
    - A deluxe_change event with previous 12 and current 15 renders a line ending in the track suffix ` (12 → 15 tracks)` after the link's closing parenthesis.
    - A deluxe_change event with both counts NULL renders a line with no trailing parenthesised suffix and no empty `()`.
    - An event whose watched_artist_name is NULL falls back to artist_name for both the label and the sort key.
    - An event whose watched_artist_name is an empty string falls back to artist_name.
    - Groups render in the fixed order New Releases, Guest Features, Deluxe Changes regardless of input order.
    - A group with zero events emits neither its heading nor a blank line for it.
    - Two events for the same release, one with source musicbrainz and one with source deezer, render as two separate lines — the builder never deduplicates.
    - Two events for the same watched artist within one group render as two separate lines under one heading — never merged.
  </behavior>
  <action>
Promote `golang.org/x/text` from the indirect block to a direct requirement in `go.mod` by importing it and running `go mod tidy`. The version stays `v0.39.0` and no new module enters `go.sum` — it is already a transitive dependency of `golang.org/x/time`/the existing tree, so this is a `// indirect` comment removal, not an install. Do not run `go get` for it.

In `internal/notifier/digest_format.go`, add `func artistKey(ev sqlc.Event) string` returning `*ev.WatchedArtistName` when the pointer is non-nil and its value is non-empty, else `ev.ArtistName`. Comment it with D-20: keying a guest_feature line on `artist_name` would file it under the host credit — an artist the operator does not watch — and hide which watched artist is actually on the track; migration 000007 backfilled every existing row, so the fallback is a guard rather than a normal path.

Add `func lineLabel(ev sqlc.Event) string` producing the escaped, capped label: for `eventTypeGuestFeature` it is `escapeMarkdown(artistKey(ev)) + " on " + escapeMarkdown(ev.ArtistName) + " — " + escapedCappedTitle(ev.Title)`; for the other two types it is `escapeMarkdown(artistKey(ev)) + " — " + escapedCappedTitle(ev.Title)`. Use the em dash U+2014 as the separator, matching the `<specifics>` shape in CONTEXT.md. Escape the host credit too — it is the same community-editable `artist_name` column.

Sort each group with `sort.SliceStable` over a comparator built from one `collate.Collator` created per `buildDigestEmbed` call as `collate.New(language.Und, collate.IgnoreCase)`. Compare `artistKey` values with the collator's string comparison; on equal, compare `ev.Title` (collated as well, for consistency); on equal again, compare `ev.ID` ascending. Because `ev.ID` is unique this ordering is total, so the rendered output is fully determined by the input set and never by its arrival order. Comment the collator with D-22: a byte sort puts lowercase-stylized and accented names such as `Ñengo Flow` after Z. Create the collator once per call rather than as a package-level value — `collate.Collator` is not safe for concurrent use, and two schedulers or a scheduler plus a test could otherwise share one.

Append the deluxe suffix: for `eventTypeDeluxeChange`, call the existing `tracksFieldValue(ev.PreviousTrackCount, ev.TrackCount)` and, when it returns a non-empty string, append `" (" + that + ")"` after the line's closing parenthesis. When it returns the empty string, append nothing — never a bare pair of parentheses (D-26). The suffix text comes from integers, not community text, so it is not escaped.

Keep the fixed group order and the omit-empty-heading rule from plan 22-02 intact, and add no deduplication anywhere: D-26 records that duplicate lines for one release seen from two sources stay two lines, two acks, because how common that is has not been measured. If a future phase wants merging it needs a decision, not an implicit one here.

Extend `internal/notifier/digest_format_test.go` with a table covering every row of the behavior block. The shuffle-invariance case should build one input slice, render it, shuffle it with a fixed seed, render again, and assert the two Descriptions are equal strings. The source-duplicate case should assert the rendered Description contains two lines, not one.
  </action>
  <acceptance_criteria>
    - `go.mod` lists `golang.org/x/text v0.39.0` outside the indirect block, at the same version as before.
    - `git diff -- go.sum` shows no added or removed module lines for this plan.
    - `grep -n 'collate.New(language.Und, collate.IgnoreCase)' internal/notifier/digest_format.go` returns exactly one match, inside `buildDigestEmbed` rather than at package scope.
    - `internal/notifier/digest_format.go` defines `artistKey` and `lineLabel`.
    - `grep -c 'tracksFieldValue' internal/notifier/digest_format.go` returns at least 1 — the existing helper is reused, not reimplemented.
    - The ordering test asserts the exact sequence `bad bunny`, `Ñengo Flow`, `Zion & Lennox`.
    - The shuffle-invariance test asserts equality of two rendered Descriptions.
    - The empty-track-count test asserts the rendered line's last two characters are not `()`.
    - `go test ./internal/notifier/ -count=1` passes in full.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/notifier/ -run 'TestBuildDigestEmbed|TestArtistKey|TestLineLabel' -count=1 -v</automated>
    <fails_when>non-zero exit, or "FAIL" in the output, or "no tests to run" in the output</fails_when>
    <automated>make db-up &amp;&amp; go test ./internal/notifier/ -count=1</automated>
    <fails_when>non-zero exit, or "FAIL" in the output</fails_when>
    <automated>go vet ./... &amp;&amp; golangci-lint run</automated>
    <fails_when>non-zero exit, or any linter finding printed</fails_when>
    <automated>make test &amp;&amp; make coverage-gate</automated>
    <fails_when>non-zero exit, or the coverage gate reports a total below 80</fails_when>
    <automated>make sqlc-check</automated>
    <fails_when>non-zero exit, or the output reports a diff between committed and regenerated sqlc output</fails_when>
  </verify>
  <done>The digest Description groups by the three fixed headings, sorts each group by collated watched artist with a total tie-break, names the guest host credit, carries the track-count suffix only when known, and renders identically for any input ordering.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols created by this plan (new in this phase — not pre-existing codebase surface):

| Symbol | Kind | Location |
|---|---|---|
| `escapeMarkdown` | unexported func | `internal/notifier/digest_format.go` |
| `digestTitleLimit` | unexported const (100) | `internal/notifier/digest_format.go` |
| `artistKey` | unexported func | `internal/notifier/digest_format.go` |
| `lineLabel` | unexported func | `internal/notifier/digest_format.go` |
| `eventURL` | unexported func | `internal/notifier/format.go` (extracted from `formatEmbed`'s switch) |
| `golang.org/x/text` as a direct require | go.mod dependency line | `go.mod` |

No new env vars, no new CLI flags, no new Go module, no new package-manager install.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MusicBrainz / Deezer catalogue text → Discord embed Description | community-editable artist names and titles cross into markdown-parsed text that Discord renders as links and emphasis |
| `events.external_id` → link href | the only id-namespace input a URL is built from |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-07 | Tampering | digest Description line assembly | high | mitigate | `escapeMarkdown` backslash-escapes the eleven Discord markdown metacharacters in the artist name, the host credit and the title before interpolation, with the backslash rule applied first so no escape is double-escaped. A title carrying a bracketed or parenthesised suffix can no longer terminate the masked link's label or retarget its href. Closes the threat plan 22-02 left open. |
| T-22-14 | Spoofing | link href construction | high | mitigate | Every href comes from `eventURL(ev)`, which builds it from `external_id` through `url.PathEscape` — `Title` and `ArtistName` are never interpolated into a URL (carried forward from T-05-06). The extraction into one helper reduces this from three call sites to one. |
| T-22-15 | Denial of Service | Description length budget | medium | accept | Each title is capped at 100 runes, bounding per-line growth, but the whole-Description 4096-character ceiling is Phase 23's chunking (D-19: 21, 22 and 23 ship in one release precisely so an oversize digest can never reach production unchunked). Accepted here on the strength of that release coupling. |
| T-22-16 | Tampering | emphasis bleeding into headings | medium | mitigate | Asterisk, underscore, tilde and backtick are in the escaped set, so an unbalanced emphasis character in a title cannot reformat the bold group headings that follow it. |
| T-22-17 | Repudiation | duplicate-line merging | low | accept | Two events describing one release from two sources render as two lines and ack separately. Not merging is the deliberate choice (D-26) and is the transparent failure mode: an operator sees both detections rather than silently losing one. |
| T-22-SC | Tampering | npm/pip/cargo installs | medium | mitigate | This plan runs no install: `golang.org/x/text` is already present at `v0.39.0` in `go.mod`'s indirect block and already pinned in `go.sum`. Promotion to direct is a `go mod tidy` comment change. The acceptance criterion is that `go.sum` gains and loses no module line, which is the mechanical check that no new package entered the tree. No package-legitimacy checkpoint is inserted because no package is being adopted. |
</threat_model>

<verification>
- `go vet ./...` and `golangci-lint run` clean.
- `make test` green with `make db-up` running; `make coverage-gate` at or above the 80% backend floor.
- `make sqlc-check` clean.
- `git diff -- go.sum` shows no module additions or removals.
- `git diff --exit-code main...HEAD -- internal/poller web/ internal/discord` clean.
- No `git commit --no-verify` anywhere in this plan's commits.
</verification>

<success_criteria>
- A digest containing all three event types renders three bold headings in the fixed order, each group sorted by collated watched artist with a total tie-break.
- A title containing `[Deluxe]`, `(Remix)`, `*`, `_` or `\` renders escaped and cannot alter the link's target or the heading's formatting.
- Guest-feature lines name both the watched artist and the host credit; deluxe lines carry the track suffix only when it is known.
- `golang.org/x/text` is direct at an unchanged version with no `go.sum` churn.
</success_criteria>

<output>
Create `.planning/phases/22-scheduled-digest-send/22-03-SUMMARY.md` when done. Record whether `buildDigestEmbed`'s signature changed — plan 22-04 drives the same builder through the scheduler.
</output>
